__pycache__/
.runtime/
.venv/
.runtime/
node_modules/
frontend/dist/
.DS_Store
# ontology/ 为用户真实数据目录：旧文件存储树已退出 git 跟踪（20260921），
# 本地保留作迁移备份；在线权威数据在 data/workbench.sqlite3（见架构边界 §13）
/ontology/
.idea/

# 工作台数据库与备份（运行时产物，不入 git）
data/
# 存储加密根密钥（库外 0600 文件，绝不入 git；含备份目录里的副本）
keys/
wb-root.key
*.key
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# Task and integration worktrees live beneath the main repository.
/worktree/

# Design Review 插件的扫描报告产物（可由 design.qa.yaml 重新生成，不入库）
.design-qa/

# Codex/zcode 共享交接与各工具本地配置（20260924 用户指令：退出 git 跟踪，仅本机保留）
/.collaboration/
/.zcode/
/.codex/
