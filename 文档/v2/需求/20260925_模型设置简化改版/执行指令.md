# 执行指令 — 模型设置简化改版（model_setting 需求变更）

- 版本：v1（2026-09-25，zcode 交付）
- 接收者：zcode（实施与自测）；Codex 独立验收
- 本指令自包含，不依赖聊天记录。执行前逐节阅读。

## 0. 指令性质与停止点（先读）

- 本指令授权的实施范围 = **在已登记的 model_setting worktree 内开发、自测、分支提交、交验收**。收到「开发/实施本需求」类用户指令即可开工，**无需重建 worktree、无需重复请示环境**。
- **不包含**的授权（须用户明确指令后才做）：集成/合并到 main、更新主工作台（18765）、清理或删除 worktree 与其数据、推送远程。验收通过后停在「待用户授权集成」。
- 用户明确指令优先于本文件；与 AGENTS.md 冲突时以 AGENTS.md 为准并报告。

## 1. 路径与环境

| 项 | 值 |
|---|---|
| 主仓库（协调位置、交接命令执行地） | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_ontology_workspace` |
| **开发 cwd（所有构建/测试/启动在此执行）** | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_ontology_workspace/worktree/model_setting` |
| 分支 / 基线 | `model_setting`，基线 HEAD cdb5939（开工前 `git status`/`git log` 核对，有他人新提交先核对再继续） |
| 端口 | 18971（`WIZ_WORKBENCH_PORT=18971`）；禁止 8765；不抢占/停止他人服务 |
| 数据根 | `WIZ_WORKBENCH_ROOT=<工作树根>`；库 + 根密钥 + blobs 自含（**含真实数据副本，不可自动丢弃**） |
| Python / 依赖 | system Python 3.9；无新增依赖；前端沿用工作树内 node_modules |
| 共享上下文 | `.collaboration/` 仅在主仓库：开工前 `python3 .collaboration/context.py read --actor zcode`（主仓库根执行）；交付前 `record` 写交接 |

## 2. 需求文件与必读顺序

需求四件套位于**主仓库** `文档/v2/需求/20260925_模型设置简化改版/`（worktree 内以绝对路径只读引用，**不要复制进 worktree**）：

1. 主仓库 `AGENTS.md`（协作与工程约定）
2. `需求说明.md`（R 系列已确认需求、兼容约束、验收标准、非目标）
3. `交互原型_v1.html`（内容 v1.1，交互与文案基准）
4. `开发计划.md`（§1 方案、§2 接口现状与目标形态、§3 任务表、§5 走查清单）
5. 改接口前：主仓库 `文档/接口文档/04-编排与LLM接口.md`、`05-接口清单与规范差距.md`、`README.md`

## 3. 遵循原型

- 交互结构、字段、文案以原型 v1.1 为准；原型为纯前端静态演示（内置示意数据、不发真实请求），其「模拟」行为在实现中替换为真实接口调用。
- 视觉风格按工作台既有控件（EditorLayout/AppSelect/确认弹窗等）落地，不要求像素复刻；原型标注「建议方案·待确认」的默认模型交互**已经用户确认**，按需求说明 R4 实施。
- 原型未覆盖的细节以需求说明为准；两者都不覆盖时先在交接中提出，不自行发散。

## 4. 实施任务与顺序

按 `开发计划.md` §3 任务表执行：**T1（契约冻结+接口文档先行）→ T2 ∥ T3（目录收敛、域层适配）→ T4 ∥ T5（设置页单层化、消费端一致性）→ T6（组合回归与验收材料）**。

要点重申（细节以开发计划为准）：

1. **接口文档先改后码**：T1 冻结 §2 七点口径并修订 04/05/README（或登记「零端点变更」说明），之后代码才动；契约变更文档与代码同 commit。
2. 存储层**零新迁移**（分支已含 0006）；密钥体系与 AAD 不动；`server.py` 白名单表如无新端点不改。
3. T4 重写 `LlmProviders.vue` 为单层三列列表 + 添加/编辑弹窗；删除不再引用的 v4 前端模块（ProviderTemplatePicker/ProviderModelEditor/providerDraft/providerFetch/modelDraft/modelManagementView，删前核对无引用）；前端请求一律走 `app/http.ts` + `tools/llm.ts`，页面禁止自写 fetch。
4. T5 消费端（NodeConfig/FlowEditor/BuildScopePage）只做语义对齐（无启停过滤、默认无档位提示、旧引用兼容），不重构。
5. 每个任务完成即跑其「验收标准与验证命令」列，再进入下游。

## 5. 范围边界与非目标

- 非目标清单见 `需求说明.md` §5（高级配置、计价、Auto、查看文档、/v1/models 拉取 UI、启停/档位/超时温度 UI、Anthropic 模板展示等）——一律不做。
- 只改本需求任务表「负责文件」所列文件；`App.vue`、接口文档、`server.py` 白名单等热点如需变动，按任务表归属执行，不越权改他人 owner 文件。
- 不动 main、不动其他 worktree、不动 `ontology/` 真实数据、不改加密体系、不改既有迁移链。

## 6. 真实数据保护

- 18971 实例的库是 **main 真实库快照 + 根密钥副本**：允许只读浏览核对存量形态；**一切增删改走查与测试一律用临时隔离根**（`WIZ_WORKBENCH_ROOT=<临时目录>` + 独立端口 + 测试账号），用完即删。
- 自动化测试一律设 `WIZ_WORKBENCH_ROOT`/`WIZ_WORKBENCH_PORT` 隔离，绝不对真实库与 `ontology/` 写入。
- API Key 只写不读回：任何响应、日志、测试快照不得出现密钥明文。
- worktree 数据目录（含根密钥）不入 git、不打包外传、不清理。

## 7. 兼容与保存规则

- 契约红线：校验失败不得 200+空数据；revision 冲突必须 409+`currentRevision`；字段增删改名、状态码、路径、校验规则变化必须先登记接口文档；白名单即 `server.py` 表键。
- 协议镜像：本轮不涉及 `model_format.py`/`modelFormat.ts`；若实施中确需触及，两端同步改并登记。
- 存量兼容：旧模型配置经 0006 迁移后的形态在新 UI 正常显示与编辑；消费端旧引用（providerId/modelId）不悬空；默认悬空读为「未设置」+警告。
- 同模板多存量供应商取第一个复用，不合并（需求 §3）。

## 8. 验收步骤（自测通过标准）

1. 门禁全过（退出码 0）：`python3 tests/run.py`（quick/http/unit）；`cd frontend && npm run typecheck && npm run lint && npm run build`。
2. 新增/调整语义均有测试断言：末模型级联删除、默认置空读法、Key 留空沿用/覆盖、目录下拉数据来源、弹窗校验与保存编排次序。
3. 迁移演练：临时根从 main 形态（0005）升级到 0006 + 新代码可用。
4. 浏览器走查：开发计划 §5 十项，在**隔离实例 + 测试账号**完成并逐项记录（截图/文字证据），不在 18971 真实副本上做写操作。
5. 接口文档（04/05/README 变更记录）与实现、白名单一致。

## 9. 最终交付要求

- 分支提交：按开发计划 §4 划分 commit；信息首行 `类型(范围): 简述`，正文列改动点与验证方式；未验证内容不得写成已验证。
- 交付说明必须含：实际分支与 **HEAD SHA**、隔离验收实例 URL（如启动）、门禁与走查证据摘要、已知限制。
- 更新主仓库 `workbench-tasks/model_setting.json`（headSha/status/updatedAt/notes），并经 `python3 .collaboration/context.py record`（主仓库根，stdin JSON）写交接，status 用 `implemented`。
- 完成后**停在待 Codex 独立验收**：不合并 main、不重启主工作台（18765）、不清理 worktree/分支/数据、不推送远程。
- 18971 常驻实例的代码更新：如需让该实例运行新代码，先报告（重启会影响他人正在使用的登录态），经用户同意后操作；默认不动。

## 10. 附：独立分支开发与串行集成（自包含摘要）

- 本任务环境（已登记，无需重建）：worktree `worktree/model_setting/`、分支 `model_setting`、端口 18971、数据自含含真实副本。开发、修复、复验持续复用同一环境。
- 标准提示词与授权边界（用户口令 → 授权范围）：

| 用户提示词模板 | 默认接收者 | 授权与停止点 |
|---|---|---|
| 为【需求】创建worktree…先不要开发。 | zcode | 只建分支/工作树并登记；本需求已建，不再适用 |
| 在【需求】已登记的worktree中，按【执行指令】开发、自测并提交，交给Codex验收，不合并main。 | zcode | 本文件；实施+分支提交，停在待验收 |
| 验收【需求/分支】的最新提交，不合并main。 | Codex | 独立核对、记录 SHA、输出结论；不改业务代码 |
| 在原worktree修复【需求】的验收问题，自测并提交，不合并main。 | zcode | 同一工作树继续修复，交新 SHA 等复验 |
| 将【需求/分支】验收通过的提交集成并合并到main…成功后清理对应开发环境。 | Codex | 集成/合并/清理需此明确指令；含临时集成分支与自动清理 |
| 将已合并的main构建并更新主工作台… | Codex | 主环境构建/重启另行授权 |

- 验收通过 ≠ 可集成：停在「待用户授权集成」。需求冲突报用户决定，普通代码冲突执行者自行修复并测试。
- 清理（未来集成授权内）：含真实数据副本的 worktree 处置必须经用户确认，不 `--force` 绕过保护。
