# GLM 执行指令：无表对象与关联属性聚合

以下内容可直接发送给 GLM/ZCode。生成本指令不表示已授权自动修改实际本体发布版本或猜测项目参数。

---

请在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` 实施以下设计，先阅读 AGENTS.md：

1. `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/无表对象与关联聚合设计方案_v1_20260915.md`
2. `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/无表对象与关联聚合开发计划_v1_20260915.md`
3. 业务基线：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/创智园二期_储能系统容量映射填写示例_20260915.md`

## 本轮必须实现

- 实例识别新增“项目登记”，可登记系统编号、显示名称，不要求数据库表。
- 链接映射新增“按条件选择成员”，使用已发布本体链接，起点是登记实例，终点为已有数据库设备对象。
- 属性取值新增“登记信息”和“关联聚合 SUM”；设备容量仍复用原数据库直接字段映射。
- 新增受限的只读预览：从保存的项目配置解析成员及容量，返回求和、成员数、缺失情况，不写业务数据库。
- 完成真实页面操作和定向回归，不能只增加 JSON 配置却不支持取值。

## 具体分工

主负责人先检查当前修改并冻结设计第 4 节数据契约，特别是 empty 的编码、稳定 ID、旧类型兼容。然后最多安排 3 个并行 agent：

- A：bindingModel.ts 与 ObjectSources.vue、新登记实例交互。
- B：project_validation.py、project_mapping.py、projects.py 引用检查和金样。
- C：新的受限查询模块、project_routes.py、server.py 预览接口。

A 完成适配接口后，再处理 LinkMappings.vue 和 PropertySources.vue，可按不同组件并行。共享文件单一所有者，不能多 agent 抢写。主负责人集成 project/api.ts、相关摘要和旧演示过滤，并完成最终验收。

## 约束

1. 原数据库对象校验不能放松，只有明确 registered 分支不要求表；旧对象无 identity 仍按数据库解释。
2. 旧字符串属性、field/database/redis/computed、旧链接和 unknown 扩展必须零丢失。
3. 本轮只支持一跳成员关系、MySQL 身份表直接数值字段、SUM。其他聚合和数据源明确显示不支持，不隐式执行不完整结果。
4. 默认必须填写成员过滤条件。不能认为项目连接中的所有记录都属于项目；创智园二期 park 编号未知，禁止猜测。
5. 表／字段只来自已配置来源和服务端目录；值参数化。新接口不接收 SQL，不把用户输入拼成 SQL。
6. 查询不持全局写锁，使用 vault 密钥，保留超时、行数上限、输入输出限制和脱敏错误。
7. 求和按设备主键去重，冲突主键报错，null／空集合不静默变为 0；来源单位只允许明确确认，不从格式化后缀自动推断。
8. 不新增本体“虚拟对象”分类、不强制增加计算契约、不重构全项目、不新增控制下发能力。
9. 所有写测试用临时 WIZ_WORKBENCH_ROOT 和独立端口；不改真实 ontology/。不得自动发布本体、升级项目引用或初始化真实系统数据。
10. 复用 Vue、AppSelect、form-save/form-guard、app/http 与现有 API 分层；新增路由进入白名单表。

## 验收和交付

依据设计 A1—A12 验收；容量 200+300=500、过滤其他项目、缺失/重复、保存重开、409、旧格式兼容均须验证。改校验同步补金样，frontend build 必须通过。没有真实测试库时记录真实执行未验证，不把 fixture 通过称作真实数据库已跑通。

交付实施记录：`文档/交付物/无表对象与关联聚合实施记录_20260915.md`。列出代码变更、协议例子、测试证据、支持范围、未完成项以及真实项目需确认的园区字段和值。

先输出精简文件分工，再开始实施，按阶段报告。不重复询问已明确的常规功能；遇到缺业务参数只暂停真实项目填充，继续完成通用功能与隔离测试。
