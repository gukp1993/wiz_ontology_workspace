# 给 GLM 的开发指令

请直接在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` 实施项目映射与数据连接功能，以下是已确认的开发范围，不要重做整套工作台。

先读：

1. 根目录 AGENTS.md。
2. `文档/项目映射与数据连接需求说明_20260914.md`：本次需求与验收依据。
3. `outputs/prototypes/项目映射交互原型.html`：交互参考，所有示例与测试结果都是模拟，不能当成生产实现。
4. `文档/通用储能本体工作台设计方案_v3.md`、`outputs/V3实施方案说明.md`、`outputs/会话记录与演示指南_20260911.md`：现有架构边界。

先检查当前代码，不重复已经完成的相关来源功能。在保留现有 Vue3 管理后台风格、AppSelect 和列表详情布局的基础上完成：

- 新增项目级数据连接配置页，首期实现 MySQL、Redis 的真实连接测试；测试当前表单，不强制先保存。处理认证失败、连接拒绝、超时与旧响应覆盖问题，错误信息脱敏。真实探测不能阻塞全局写锁。
- 读取数据库表／视图与字段元数据，供来源和链接下拉选择；连接密码存受保护位置，不进入项目 YAML、快照、日志或浏览器持久存储。
- 对象保留一个实例识别来源，可配置零到多个补充表及 Redis。补充表支持零或一条、零或多条匹配；单值取多条时必须明确最新／汇总规则。不把所有属性强制归到主表。
- 属性列表逐项配置，每个属性只保存一种有效来源：数据库字段、Redis 查询、函数结果。字段指定来源 ID；Redis 配置 GET／HGET、Key、结构化参数绑定和转换；函数引用现有实现和输出。切换方式保存后整体替换旧来源，取消不改变原值。
- 链接两端各自从本体对象的已配置表、该表的字段下拉选择，禁止手填。换表清空字段；保留两侧表与对象身份的关联路径，并按本体链接数量关系校验。
- 本轮真实执行只包含连接探测和元数据读取。属性取值、联表查询、Redis 业务 Key、函数和时间序列执行不在本轮；页面须如实标明配置与执行的区别。

数据与兼容要求：

- 不手改或删除 ontology/ 真实资产，不向真实草稿写测试数据，不改任何已发布版本。
- 本体与项目两条状态线、撤销线、草稿与发布线保持独立；基础目录只用于初始化，实际保存仍走完整修订及原子指针。
- 兼容现有 table／primary_key、字符串字段映射、computed、related_sources 和旧链接 column。使用内存适配或安全迁移，不要求用户重填。
- 稳定 ID 保持不变；若修改本体格式转换，前后端映射必须同步。演示引擎继续只接收直接字符串字段映射。
- 保留 POST 白名单、Origin 校验、请求上限、LOCK 写保护与 revision 乐观并发。新增依赖说明用途，选择维护中的最小驱动依赖。

验证与交付：

1. 按需求文档的验收项检查，特别覆盖连接测试、元数据读取、单来源切换、多行规则、链接下拉和旧版本兼容。
2. 使用独立临时数据目录和可控本地测试服务；无真实服务时区分适配器单元测试与未完成的真实集成验证，不能假称连通。
3. 前端必须执行 `cd frontend && npm run build`；检查浏览器中的主要操作和样式。必要时通过 `./start.sh restart` 重启供用户查看。
4. 输出修改说明、验证结果、未覆盖的环境依赖和当前工作台地址；更新本次需求文档的实施状态，不擅自宣布未实现的执行能力可用。

遇到会改变上述业务范围的问题先说明并讨论；普通实现选择自行完成，不重复询问已确认的设计。
