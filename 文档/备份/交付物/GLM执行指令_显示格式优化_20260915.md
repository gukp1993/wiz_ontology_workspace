# GLM 执行指令：显示格式优化

将下面内容直接发送给 GLM / ZCode。本文是供后续开发执行的指令，生成本文件本身不代表已执行开发。

---

请在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` 实施“显示格式优化”。先读本项目 AGENTS.md，然后严格按以下两份文档完成：

1. `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/显示格式优化需求_v1_20260915.md`
2. `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/显示格式优化开发计划_v1_20260915.md`

## 明确目标

- 属性“格式化方式”下拉仅保留“常用格式”“自然语言”，不提供新建 JavaScript 格式函数入口。
- 不是只删除一个选项：常用格式必须覆盖需求表中的文本模板／映射、数值、布尔、日期时间、数组、结构体、时间序列。
- 根据对象属性或共享属性的已有数据类型联动表单；时间序列要分别配置时间格式与观测值格式，不能误当单个数值处理。
- 保持管理后台风格与现有 AppSelect / EditorField、属性选填区折叠、form-save/form-guard 保存方式。
- 新配置保留默认自然语言偏好，但空配置打开不落盘、不自动请求；具体空规则行为按需求执行。
- 历史 code、旧样式、未知键、不同 min/max 小数位必须保留；旧代码可只读查看及兼容预览，不能伪装为自然语言或自动覆盖。

## 怎么干

1. 只检查相关文件和当前改动，不做全仓架构分析。不要重置、覆盖其他人修改；这里可能不是 Git 仓库，无 Git 时不要卡在 git 命令。
2. 主负责人先冻结配置示例和默认值，沿用 `mg:formatting` 与 `/api/format-preview`，尽量不改顶层协议。
3. 有并行 agent 能力时，启动最多三个互不抢文件的任务：
   - A 前端：PropertyFormatting.vue 及最小前端辅助；完成交互和兼容展示。
   - B 后端：formatting.py、formatter_runtime.cjs、必要的 model_routes.py；完成实际预览语义。
   - C 测试：独立测试文件与 fixture；依据需求写测试，不修改 A/B 负责的实现文件。
   主负责人处理集成和必要的 PropertyManager.vue 调整。不要为凑并发拆到 50 个 agent。
4. 不允许 A/B 各自发明一套配置字段。新增键按需求第 6 节，变更必须统一记录和同步消费者。
5. 模型请求复用已有服务端配置，不输出密钥、不修改用户模型账号；自动化测试使用 mock，不默认消耗真实模型额度。
6. 运行定向测试和前端 build；真实数据只允许只读检查。写测试一律设置 `WIZ_WORKBENCH_ROOT` 到临时目录，HTTP 测试再设置独立的 `WIZ_WORKBENCH_PORT`。
7. 检查保存重开、取消、共享引用、历史往返和异步旧响应问题。不要以截图或 vue-tsc 通过代替功能验收。
8. 完成实施记录，对 A1—A14 逐项写“通过／未通过／未验证”及证据，不把未验证项写成已完成。

## 严格禁止发散

不要改项目映射／采样规则库，不新增菜单或三步向导，不引入 UI 框架，不做曲线图，不做单位换算，不增加新的模型配置页面，不重写保存协调器，不批量改 ontology/，不修改发布快照，不删除历史兼容数据。不因本任务主动开展其他架构优化。

若遇到与需求冲突的现有实现，先完成不受影响的部分，清楚列出冲突和建议；不要自行扩大产品范围。常规技术细节按现有项目约定实现，不反复询问用户已经确认的两项模式。

## 最终交付

- 可用的实现与前端构建产物。
- `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/交付物/显示格式优化实施记录_20260915.md`。
- 简洁汇报：改动内容、验证结果、尚未完成事项、工作台查看路径。只有后端改动需生效时才使用现有 start.sh 重启，遵守脚本对自身 PID 的管理方式。

先给出精简的文件分工，再开始实施；按阶段汇报，不要只停在计划。
