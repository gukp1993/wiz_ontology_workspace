# 05 · 接口清单与规范差距

---

## 1. 全量接口速查表（57 个）

图例：**锁**＝是否持有全局写锁（`LOCK`）；**探测**＝真实网络/执行操作，绝不持锁。

### 1.0 认证与账户（4，均为免登录）

| # | 方法 | 路径 | 说明 | 锁 | 成功码 |
| --- | --- | --- | --- | --- | --- |
| 0-1 | GET | `/api/auth-state` | 当前登录态（未登录 `{"user": null}`） | — | 200 |
| 0-2 | POST | `/api/auth-login` | 登录（失败 401 同文案 + 200ms 延迟） | — | 200 |
| 0-3 | POST | `/api/auth-register` | 开放注册（重名 409） | ✅ | 201 |
| 0-4 | POST | `/api/auth-logout` | 退出（无会话幂等 200） | — | 200 |

> 除上述 4 个外，**全部 `/api/*` 接口要求登录**；未登录返回 401 `UNAUTHENTICATED`。
> 所有业务数据按账号隔离：跨账号 id 按不存在处理（404/空列表）。契约见 `06-认证与账户接口.md`。

### 1.1 本体区（23）

| # | 方法 | 路径 | 说明 | 锁 | 成功码 |
| --- | --- | --- | --- | --- | --- |
| 1 | GET | `/api/ontologies` | 本体清单 | — | 200 |
| 2 | POST | `/api/ontologies` | 创建本体 | ✅ | 201 |
| 3 | GET | `/api/state` | 读取本体草稿 | — | 200 |
| 4 | POST | `/api/save` | 保存本体草稿 | ✅ | 200 |
| 5 | POST | `/api/publish-check` | 发布变更分类预检 | — | 200 |
| 6 | POST | `/api/publish` | 发布本体版本（支持 `requestId` 幂等） | ✅ | 200 |
| 7 | POST | `/api/restore` | 从快照恢复 | ✅ | 200 |
| 8 | GET | `/api/versions` | 版本清单 | — | 200 |
| 9 | GET | `/api/version-state` | 指定版本状态 | — | 200 |
| 10 | GET | `/api/releases` | 可恢复快照清单（**返回裸数组**） | — | 200 |
| 11 | GET | `/api/source-reference` | 来源参考节点 | — | 200 |
| 12 | GET | `/api/model-definitions` | 定义索引查询（分页） | — | 200 |
| 13 | GET | `/api/storage-status` | 存储健康状态 | — | 200 |
| 14 | POST | `/api/validate` | 本体配置校验 | — | 200 |
| 15 | POST | `/api/workflow-check` | 发布就绪度 | — | 200 |
| 16 | POST | `/api/preview` | 演示执行预览 | — | 200 |
| 17 | POST | `/api/demo-objects` | 演示对象列表 | — | 200 |
| 18 | POST | `/api/function-preview` | 计算函数试算 | — | 200 |
| 19 | POST | `/api/action-preview` | 动作试运行 | — | 200 |
| 20 | POST | `/api/value-type-check` | 值类型取值校验 | — | 200 |
| 21 | POST | `/api/format-preview` | 显示格式预览 | — | 200 |
| 22 | POST | `/api/explorer` | 实例探索快照 | — | 200 |
| 23 | POST | `/api/export` | 导出模型 ZIP（**二进制**） | — | 200 |

### 1.2 项目区（17）

| # | 方法 | 路径 | 说明 | 锁 | 成功码 |
| --- | --- | --- | --- | --- | --- |
| 24 | GET | `/api/projects` | 项目清单 | — | 200 |
| 25 | POST | `/api/projects` | 创建项目 | ✅ | 201 |
| 26 | GET | `/api/project-state` | 读取项目草稿 | — | 200 |
| 27 | POST | `/api/project-save` | 保存项目草稿 | ✅ | 200 |
| 28 | POST | `/api/project-validate` | 项目配置校验 | — | 200 |
| 29 | POST | `/api/project-publish` | 发布项目 | ✅ | 200 |
| 30 | POST | `/api/project-upgrade-check` | 本体版本升级预检 | — | 200 |
| 31 | GET | `/api/project-releases` | 项目发布版本 | — | 200 |
| 32 | GET | `/api/project-config` | 旧项目配置（**兼容/前端未用**） | — | 200 |
| 33 | POST | `/api/connection-test` | 连接连通性测试 | 🚫 探测 | 200 |
| 34 | POST | `/api/connection-catalog` | 读取表结构（临时凭据） | 🚫 探测 | 200 |
| 35 | POST | `/api/catalog-refresh` | 刷新并持久化目录缓存 | 🚫 探测 | 200 |
| 36 | POST | `/api/connection-secret` | 连接密码存取 | ✅ 短锁 | 200 |
| 37 | GET | `/api/api-credentials` | 凭据清单（仅元数据） | — | 200 |
| 38 | POST | `/api/api-credential` | 登记/清除凭据 | ✅ 短锁 | 200 |
| 39 | POST | `/api/calc-eval` | 公式校验/试算 | — | 200 |
| 40 | POST | `/api/project-property-preview` | 属性取值预览 | 🚫 短锁核对 | 200 |

### 1.3 编排与 LLM（13）

| # | 方法 | 路径 | 说明 | 锁 | 成功码 |
| --- | --- | --- | --- | --- | --- |
| 41 | GET | `/api/flows` | 编排清单 | — | 200 |
| 42 | POST | `/api/flows` | 创建编排 | ✅ | 201 |
| 43 | GET | `/api/flow-state` | 读取编排草稿 | — | 200 |
| 44 | POST | `/api/flow-save` | 保存编排草稿 | ✅ | 200 |
| 45 | POST | `/api/flow-check` | 配置检查（只读） | — | 200 |
| 46 | POST | `/api/flow-copy` | 复制编排 | ✅ | 201 |
| 47 | POST | `/api/flow-delete` | 软删除编排 | ✅ | 200 |
| 48 | POST | `/api/flow-run` | 运行/测试编排 | 🚫 短锁核对 | 200 |
| 49 | GET | `/api/llm-providers` | 提供方清单 | — | 200 |
| 50 | POST | `/api/llm-provider-save` | 保存提供方 | ✅ 短锁 | 200 |
| 51 | POST | `/api/llm-provider-delete` | 删除提供方 | ✅ 短锁 | 200 |
| 52 | POST | `/api/llm-provider-test` | 连通性测试 | 🚫 探测 | 200 |
| 53 | POST | `/api/llm-provider-default` | 设为默认提供方（幂等） | ✅ 短锁 | 200 |

---

## 2. 与标准 HTTP/REST 规范的差距

按「修复收益 / 改动成本」排序。P0 建议本轮做，P1 下轮，P2 择机。

| # | 差距 | 现状 | 标准建议 | 优先级 |
| --- | --- | --- | --- | --- |
| G1 | ~~5xx 被吞成 400~~ **已解决**（2026-09-18 批次 B） | 原 `do_POST` 兜底 `except Exception → 400` 且回传 `str(exc)`；现未知异常 → **500 INTERNAL_ERROR** + 服务端堆栈日志，客户端只拿泛化消息 + requestId | — | ✅ |
| G2 | ~~错误无机器码~~ **已解决**（2026-09-18 批次 B） | 分派层全部错误响应与业务层内联 409 已带稳定 `code`（见 README §2.4 错误码表），`error` 文本兼容保留 | — | ✅ |
| G3 | **删除用 POST** | `POST /api/flow-delete`、`/api/llm-provider-delete` | `DELETE /api/flows/{id}`、`DELETE /api/llm-providers/{id}` | P1 |
| G4 | **更新用 POST** | `POST /api/save`、`/api/project-save`、`/api/flow-save` | `PUT /api/ontologies/{id}/draft`、`PUT /api/projects/{id}/draft`、`PUT /api/flows/{id}/draft` | P1 |
| G5 | **动作式路径** | `/api/publish`、`/api/validate`、`/api/flow-run`、`/api/publish-check` | 资源化：`POST /api/ontologies/{id}/releases`、`POST /api/projects/{id}/releases`、`POST /api/flows/{id}/runs`、`POST /api/ontologies/{id}/publish-check` | P1 |
| G6 | **无版本前缀** | `/api/xxx` | `/api/v1/xxx`（旧路径 308/404 或双轨并存） | P2 |
| G7 | **并发令牌在请求体** | `revision` 字段 | 标准做法：`If-Match: "r-xxx"` 头 + 409；可两者并存 | P2 |
| G8 | **响应体形态不一致** | `/api/releases` 返回裸数组；其余返回 `{items}` | 统一 `{ "items": [...] }` | P1 |
| G9 | ~~隐式语义~~ **已解决**（2026-09-18 批次 B） | 原「带任意 query 即过滤」废除；现仅 `ontology` 参数控制范围（空值 400、无效 404），无 `all=1` 需求 | — | ✅ |
| G10 | **创建无 Location** | 201 仅返回 `{id,name}` | 加 `Location: /api/v1/ontologies/{id}` | P2 |
| G11 | **业务失败状态码不统一** | 校验类 200 + 报告；发布/运行阻断 422；连接探测 200 + `ok:false` | 规则化：**纯查询 → 200 + 报告**；**写操作被业务规则阻断 → 422**；**外部依赖结果（探测）→ 200 + `ok:false`**（保留现状，合理） | P1 |
| G12 | **分页不统一** | 仅 `/api/model-definitions` 支持 `limit/cursor` | 统一 `{items, cursor, total?}`；`total` 可选 | P2 |
| G13 | **无追踪与日志**（部分解决，2026-09-18 批次 B） | 已有：每请求 `X-Request-Id` 响应头 + 400/500 错误日志带 requestId 关联 + 默认访问日志。未做：结构化字段（method/path/status/latencyMs）与中间件化 | 结构化访问日志 | 🟡 |
| G14 | **无 OPTIONS / HEAD** | 只实现 `do_GET` / `do_POST` | 补 `do_HEAD`（静态资源）、`do_OPTIONS` | P2 |
| G15 | ~~GET 路由耦合~~ **已解决**（2026-09-18 批次 B） | do_GET 不再做统一工作区定位；本体/项目接口在自身边界校验，编排/LLM/存储接口与本体无关；未知端点稳定 404 | — | ✅ |
| G16 | **无幂等键覆盖** | 仅 `/api/publish` 支持 `requestId` | 所有写接口支持 `Idempotency-Key` 或 `requestId` | P2 |
| G17 | **字段命名混用** | 接口层 camelCase，状态内部 `object_bindings`、`rule_ref` 等 snake_case | 接口层保持 camelCase；内部历史字段在本体/项目状态版本升级时统一（谨慎，涉数据迁移） | P2 |
| G18 | **无健康检查端点** | `/api/storage-status` 部分承担 | 增加 `GET /api/health`（存活）与 `GET /api/ready`（依赖就绪） | P2 |

> **务实说明**：本系统是单机本地工作台（仅绑定 127.0.0.1、单用户、无网关），G6/G7/G10 这类「对外 API 治理」收益有限；**G1、G2、G9、G13 属于健壮性与可观测性缺陷，应优先修**，G3–G5 建议在下次大规模重构时随路由层改造一并完成。

---

## 3. 已知设计问题（实现层）

### 3.1 `/api/restore` 引用了未定义函数（**缺陷，需修复**）

`workbench/model_routes.py` 中 `post_restore` 调用 `_server_current(identifier)`，该函数在模块中**不存在**（当前模块只有 `current()`）。触发路径：`POST /api/restore` 写恢复前备份时会抛 `NameError`，经 `do_POST` 兜底 → 返回 400，恢复失败。

**建议修复**：改为 `current(identifier)`，并在 `tests/` 补一条恢复用例（当前恢复路径无自动化覆盖）。

### 3.2 GET 分派的工作区副作用

`server.py.do_GET` 对所有 `/api/*` 先执行 `workspaces.describe(query.get('ontology', ['storage'])[0])`，导致：

- 编排/LLM/存储状态接口与本体工作区耦合；
- 传入非法 `ontology` 时，与业务无关的只读接口也会 404/400。

**建议**：把工作区定位下沉到本体/项目路由内部（G15）。

### 3.3 路由表用 lambda 包装

`POST_ROUTES` 中 4 个 `project-*` 与 2 个 `connection-*` 端点用 `lambda` 包装同一个处理函数并传路径字符串，函数内再 `if path == ...` 分支。可读性差、易漏分支。

**建议**：改为显式注册的 `functools.partial(handler, path=...)` 或拆成具名函数。

### 3.4 导出接口的二进制特判

`/api/export` 在 `POST_ROUTES` 中值为 `None`，靠 `do_POST` 内 `if path == '/api/export'` 特判。表格语义被破坏（值 `None` 却代表"已注册"）。

**建议**：注册真实 handler，返回 `(bytes, content_type)` 二元组，由 `respond` 按类型分派。

---

## 4. 幽灵接口清单（后端存在、前端未调用）

| 接口 | 状态 | 建议 |
| --- | --- | --- |
| `GET /api/project-config` | 历史兼容，仅 `storage` + `chuangzhi` 硬编码 | 确认无调用后下轮删除 |
| `GET /api/model-definitions` | 已实现（定义索引投影），前端未接 | 保留（后续大本体性能优化要用），文档中标注"未接线" |
| `GET /api/storage-status` | 已实现，前端未接 | 保留，建议接入状态栏或并入 `/api/health` |

---

## 5. 演进路线（建议）

**P0（不影响调用方，纯健壮性）**
1. 修 `_server_current` 缺陷 + 补恢复用例；
2. `do_POST` 兜底分支拆为「已知业务异常 → 400」与「未知异常 → 500 + 日志」，禁止回传原始异常文本；
3. 错误响应增加 `code` 字段（保留 `error` 字符串兼容）；
4. `GET /api/projects` 改为显式过滤参数（保留旧行为一版）；
5. 增加 `X-Request-Id` + 结构化访问日志。

**P1（路由层规范化，双轨过渡）**
1. 新路由按资源注册：`/api/v1/ontologies/{id}/draft`（GET/PUT）、`/releases`（GET/POST）、`/api/v1/projects/{id}/draft`（GET/PUT）、`/releases`、`/api/v1/flows/{id}`（GET/PUT/DELETE）等；
2. 旧路由保留并标记 `Deprecated`，前端按区逐步切换，切换一个删一个；
3. `/api/releases` 响应体统一为 `{items}`（前端同步改）；
4. 拆开 GET 工作区副作用。

**P2（治理增强）**
1. `If-Match` / `Location` / `OPTIONS` / `Idempotency-Key`；
2. `GET /api/health`、`GET /api/ready`；
3. 全量接口契约测试（请求/响应 schema 快照），纳入 CI。
