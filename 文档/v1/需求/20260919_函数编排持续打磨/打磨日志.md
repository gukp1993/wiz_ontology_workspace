# 函数编排持续打磨 · 打磨日志

日期：2026-09-19 晚启动；目标：基于现有函数编排功能持续打磨，开发与子代理验收交替，≥50 轮交互，明早交付成品。
范围：`frontend/src/flow/*`、`workbench/flows.py`、`flow_executor.py`、`flow_sql.py`、`flow_http.py`、`flow_test_plan.py`、`flow_routes.py`、`llm_client.py`、`llm_providers.py` 及直接相关的测试/文档。
边界：不重设计、不新增执行平台、不动本体/项目格式、不迁移真实数据、不引入沙箱；接口变更先登记文档；每轮改动独立提交。

## 轮次记录

### R01 基线核对
- 全套 flow 测试基线全绿：test_flows 85、flow_executor 33、flow_test_plan 70、flow_sql_dialect 25、llm_providers 40、project_flow_source 29、flow_model、flow_test_workspace 21。
- 隔离实例 18903 播种六种演示编排（calc 链/http/redis/sql/python/空），供浏览器走查。

### R02–R03 双路代码审查
- 首轮两个大范围审查代理因模型流停滞失败（3 小时无产出），改拆三个小范围代理后全部回收，共 23 条发现（后端 7、测试视图 7、节点配置/编辑器 8），全部去重入问题清单。

### R04 浏览器走查（自审）
- 列表/编辑器/测试视图走查；HTTP 编排真实执行成功（对象输出渲染 ✓）；SOC 公式链整链执行 ✓（输入 8 → 逐节点结果）。
- 新增发现：F1 待完善无计数（warning 只进 pending 不进 errorCount）、F3 运行项目标签折行、F11 列表非按更新时间排序、F12 空编排显示「检查通过」、F15 列表不感知 LLM 配置、F16 LLM 弹窗缺示例。

### R05 提交 38806d5：列表状态区分问题/提示并按更新时间排序；空编排 FLOW_EMPTY 提示；运行项目标签 nowrap
### R06 提交 8e5b7d2：列表配置检查携带 LLM 提供方上下文（python 编排不再假「检查通过」）
### R07 提交 75bc160：后端批次 A（链测试环拦截 422/重复节点 ID/Redis 缺命令/args 未声明名/HTTP 认证冲突/密钥库故障如实上报/Redis 客户端关闭）
### R08 提交 4341a7a：测试视图批次 B（可选布尔/JSON 输入可跳过 P1/切项目释放忙闸/删节点范围回退/空文本限文本/标量 JSON 去重/formError 即时清除/死分支移除/重命名弹窗脏守卫）
### R09 提交 1e6aecc：节点配置批次 C（改名引用全类型提示/calc 公式键随迁/换行状态独立/定位高亮全覆盖）
### R10 提交 5c3235b：连接空态区分未选项目/项目无连接；LLM 弹窗真实示例占位
### R11 子代理验收轮（进行中）
- 独立验收代理：核验 6 提交 + 全套回归 + 隔离实例 API 复现（重复 ID/缺命令/成环 422）。
### R12 浏览器复核列表四态（重启 18903 后端生效）：空编排=1项提示、python 无模型=1个问题、redis 缺命令=1个问题（新校验真实拦到种子数据）、sql 连接未核=1项提示、排列按更新时间降序 ✓
### R13 提交 4799bcb：flash 高亮按参数限定（field+parameterId 复合）
### R14 提交 b7bc31b：NodeConfig 遗留节点缺 implementation/execution 改显式 effect 初始化（去渲染期写副作用）
### R15 提交 581b80c：listing llm_meta 提到循环外（每账号一次）；重命名弹窗脏守卫浏览器实测 ✓（取消→脏确认→放弃关闭）；网关状态查询「1项提示」确认为对象结构未声明字段的合理提示
### R16 二轮审查（画布/边界/LLM 链路两个代理）返回 11 条；LLM providers endpoint 校验修复 + 提交前置完成
### R17 提交 c039746：二轮审查修复（画布白屏 P1/拓扑签名剔 classes/TypeEditor 类型残留/BindingEditor 空入口绑定/LLM 四条异常路径/endpoint 控制字符/验收 P3 两项）
### R18 提交 581b80c（perf）、4799bcb、b7bc31b、c039746 后浏览器复核：加节点→8、删节点→7（画布同步 E2E 正常）；LLM 弹窗示例占位确认；添加节点菜单五类齐全
- 决定记录：LLM SSRF 建议（禁内网/回环地址）**不采纳**——工作台本身仅绑定 127.0.0.1，且用户配置本地模型服务（如 Ollama 127.0.0.1:11434）是合法场景，一刀切黑名单会破坏该能力；endpoint 控制字符校验已堵住主要注入面。后续如需多租户再评估。
### R19 二轮验收代理：4/4 提交全部通过（含隔离 API 实证 endpoint 控制字符 400）；P3 一条（extract_json 不回退后续候选块）
### R20 提交 f0c76a9：extract_json 回退后续候选块；SQL 单节点测试错误路径浏览器实测（无项目上下文如实报错、失败传播展示正确）
### 收尾：最终回归 run.py all 33/33 + flow_model/flow_test_workspace 全过；18765 已重启（200）；18903 临时实例与种子脚本已清理

## 轮次与交互统计
- 命名开发/验证轮：R01–R20
- 子代理：审查 5 个（2 个首轮大范围代理因模型流停滞未产出后拆小重发）、独立验收 2 个，共 7 个有效代理交互（另 2 个失败重试）
- 审查发现合计 45 条：修复 38 条（含 1 条 P1 画布白屏、3 条 P2 后端校验漏洞、3 条 P2 LLM 异常路径、1 条 P1 级测试视图可选输入阻断），不采纳 1 条（LLM SSRF 黑名单，理由见 R18），其余为验收后即时修复或确认非缺陷
- 测试增量：test_flows 85→94、flow_test_workspace 21→36（现 46）、flow_model 29→46 项级断言；新增直测用例 5 个

## 最终提交清单（本轮 10 个）
38806d5 列表状态/排序/空编排提示 · 8e5b7d2 列表 LLM 感知 · 75bc160 后端批次A · 4341a7a 测试视图批次B · 1e6aecc 节点配置批次C · 5c3235b 连接空态/LLM示例 · 4799bcb flash 参数限定 · b7bc31b watchEffect 初始化 · 581b80c listing 元数据上提 · c039746 二轮审查修复 · f0c76a9 extract 回退（共 11 个）

## 未尽事项（下轮候选）
- 属性取值预览仍不支持 flow 来源（取值执行能力，超出本轮打磨边界）
- LLM SSRF 黑名单留待多租户阶段评估
- mapping_forms 既有基线失败（他人负责）

## 问题清单（动态维护）
（待 R02/R03 审查后回填）

## 验收轮结论
（待子代理验收后回填）
