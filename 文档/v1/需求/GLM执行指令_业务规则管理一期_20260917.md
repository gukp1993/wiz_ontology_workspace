# GLM执行指令：业务规则管理一期

请在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` 实现业务规则管理一期。直接实施并验证，不再重新设计。严格控制范围，不能自行扩展。

## 一、先读取以下资料

1. 当前项目 AGENTS.md。
2. `文档/需求/业务规则管理_一期需求_v1_20260917.md`。
3. `文档/需求/业务规则管理_一期开发计划_v1_20260917.md`。
4. **必须打开并查看/操作** `文档/prototypes/业务规则管理交互原型_v1.html`，以此为页面结构及交互依据。不能只读需求后自行设计界面。

优先级：用户最新简版决定 → 本期需求 → 最新原型 → 旧文档。旧的“业务规则定义与原始储能规则整理”用于背景，不是本期表单规范。该旧文档有8字段和依赖设计，已被简化为4字段，不得恢复。

## 二、用户已确认范围，不许增减

1. 本体区“业务规则”菜单，列表只有：名称、引用对象、操作。
2. 名称搜索、新建、查看、编辑。查看只读；新建和编辑均为弹窗。
3. 表单只有：名称、业务定义、规则内容、输出结果，都是文本，四项必填。
4. 编辑底部只有取消、保存，允许顶部关闭。必填在保存时自动提示。
5. 规则列表可以查看被哪些对象类型引用，并跳到对象对应规则页签。
6. 对象建模在既有属性/链接/动作旁增加“规则”页签，可以多选添加已有规则、查看、移除引用。
7. 对象引用以稳定ID存储，一条规则可被多个对象引用；移除引用不删除规则。
8. 保存到本体真实草稿，复用现有保存和发布体系。

## 三、严禁恢复的内容

分类/标签、状态字段、业务定义列表摘要、规则之间的依赖或关联图、适用对象编辑字段、所需信息、参数表、补充配置、原始材料待确认事项、查看原文、项目衔接说明、预览规则按钮、检查填写按钮、撤销本页修改按钮。

不做执行引擎、SQL/Python规则编辑、项目规则绑定、LLM推理、批量导入、删除规则。不要恢复计算契约为必经步骤。业务正文可以保留自然语言引用，但不解析为规则依赖。

## 四、实现位置与协议

- 先检查当前文件，不覆盖其他开发者的动作功能或对象建模修改。
- `frontend/src/app/navigation.ts` 和 `App.vue` 做必要装配。
- 新建 `frontend/src/ontology/BusinessRuleLibrary.vue` 和轻量 `businessRuleModel.ts`；视需要复用规则详情/编辑弹窗，不引入UI库或重构状态管理。
- `ObjectWorkspace.vue` 增加规则页签；参考现有动作关联的代码组织，不照搬动作字段和业务语义。
- `workflow.businessRules[]` 存 `{id,name,description,content,output}`。
- `workflow.businessRuleAssociations[]` 存 `{objectTypeId,ruleId}`。
- 旧字段不存在按空数组读取，保留其他workflow字段。
- 不用历史rules.yaml，不用项目implementations，不重构JSON-LD，不增加独立草稿文件。
- 使用已有 `before-change/changed` 与 `commit-now` 保存机制；所有请求遵守现有app/http/API层，不新增裸fetch。
- 错误时保留表单，不假成功；沿用revision并发语义，不自行“重取后无条件覆盖”。
- 补上本体校验、校验定位和版本差异识别；无关后端模块不动。
- 新增规则和关联兼容；删已发布定义/关联破坏性；正文/定义/输出变化待确认；仅名称变化兼容。沿用现有分类机制，不加新审批流程。

## 五、实施步骤

按计划A—E实施：协议与持久化 → 规则列表/弹窗 → 对象引用 → 校验与版本 → 针对性验收。

原型是界面交互依据，但它仅在内存保存；正式实现必须真实持久化。不要把19条示例或示例引用自动灌入真实本体。需要验收数据时用临时数据根和独立端口。

默认按小范围任务执行，不要为提高并发硬拆大量agent。若自主采用并行，先固定数据协议，按互不重叠的文件拆分；App.vue、ObjectWorkspace.vue、导航、校验器由明确的唯一负责人修改。

## 六、验收与交付

逐条核对开发计划A1—A15，尤其：
- 表单只有四字段、列表只有三列、编辑是弹窗。
- 对象加引用后规则列表立即能反查；移除引用和重命名不破坏其他关联。
- 刷新保留、旧数据无损、发布快照含规则和引用。
- 保存失败/取消/关闭时不丢输入或静默修改数据。
- 未引入任何用户已删除的概念。

自动化测试必须隔离真实数据。前端执行 `npm run build`，浏览器实际检查列表、详情、编辑和对象引用链路；不能以类型检查通过代替交互验证。

完成后输出：
1. 简明结果与打开入口。
2. 改动文件列表。
3. 验收结果，未验证项明确标注。
4. 未完成项或限制（如有）。

不要以方案代替实施，不做超出一期范围的“顺手优化”。
