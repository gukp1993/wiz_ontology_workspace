# 可直接交给其他 harness 的执行指令

请在以下项目实施“规则与动作字段精简及Excel同步”，完成端到端验收，不要扩展范围。

项目根目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260920_规则动作字段精简与Excel模板同步`

## 必读顺序

1. 根目录AGENTS.md，运行`python3 .collaboration/context.py read --actor <实际执行者>`；身份按项目现有约定使用，记录接手信息。
2. 本目录`需求说明.md`，尤其§2字段、§4旧数据保护、§5Excel协议、§6验收。
3. 本目录`开发计划.md`与随附`本体模型填写模板_规则动作精简.xlsx`。
4. 本体相关接口文档与现有源码/测试。查看git status和近期交接，保护其他工具的在途修改。

本轮没有新增HTML原型：用户指定三份文档与模板，交互以需求§3和现有正式页面为准。不要依照历史统一维护改版原型重做布局。模板是准备交付的新文件，当前下载入口尚未替换。

## 必须完成的功能

- 规则：规则名称name、业务定义description必填；规则内容content选填；不再提供新output填写。
- 动作：动作名称name、业务定义description必填；effect文案改为预期效果并选填，存储键不改。
- 范围、例外、作用边界由业务定义承载，不新建字段。保持对象关联方式，不添加参数/条件/审批等。
- 旧规则output在读取、编辑、保存、发布、配置迁移中原样保留；有值时只读展示历史补充说明。不自动拼接、不清空、不批量迁移。
- 三字段表单、详情、列表、图谱/对象关联详情、前后端校验、Excel校验和发布检查同步，不只修改界面标签。
- 单条提交要求名称和业务定义；不要因其他历史不完整定义阻断无关草稿保存。发布检查全部定义；导入整批预检失败不写。
- Excel保留对象/属性/规则/动作四页，规则三列、动作预期效果新列。旧规则输出结果列保留为output；旧业务效果作为别名。新旧效果都非空且不同必须报冲突，绝不择一丢弃。选填列可省略，名称和业务定义表头必须存在。
- 解析仍按列名，不按位置；别名/旧output也拒绝Excel公式和非文本。不改变同名跳过/随机后缀策略、属性默认共享和关联仍由页面维护的边界。
- 将附带空模板作为上线模板，浅灰11pt简洁表头、冻结首行、保留属性下拉和时间序列联动。新解析器和新下载入口同一版本交付。

## 实施顺序

按开发计划A～F执行：先协议文档，再校验/保存，再表单，再Excel解析，最后切模板入口和综合验收。技术落点可按最新代码调整，但不能更改业务字段和兼容策略。

重点入口：`frontend/src/ontology/{businessRuleModel,BusinessRuleLibrary,ActionLibrary,excelImport,importPlan}`对应文件，`workbench/workflow.py`；检查`contracts.py`和转换层，确保不丢历史字段。搜索旧文案仅改本次定义，不能把计算函数/编排输出删掉。

没有新接口不要造新接口；接口/校验契约变更先更新文档及README记录。未知扩展透传，稳定ID不变。CAS冲突返回真实失败，失败保留表单，不擅自重试覆盖其他人内容。

## 禁止事项

不得手改真实ontology/、数据库或密钥；不得重写发布版本；不得静默转换旧动作；不得为此重做对象映射、编排或恢复任何延期项目；不得把Excel联动说成切换类型自动清空观测值（它只能约束新选择，导入仍需校验）。

## 验收与最终交付

1. 按需求R01～R11逐项记录通过或未通过证据，跑开发计划列出的相关回归及前端build。
2. 隔离数据根和独立端口启动；新建规则/动作只填两项，保存重开；缺定义报错；有旧output的规则编辑后读回保留；发布检查一致。
3. 浏览器下载新模板，填入最小规则/动作导入；旧模板也测试；分别验证别名冲突、选填空值、时间序列下拉和错误行无部分写入。
4. 旧字段随配置导出导入回环验证；未验证的Excel原生行为或浏览器行为如实报告，不称全量通过。
5. 更新共享交接，实施事实追加`开发计划.md`，只提交本任务文件，遵守仓库git提交规则。
6. 最终回复列出完成范围、测试证据、兼容处理、未完成项和commit。不得只交一个计划后停止，也不得将历史需求当本次新增任务。
