# 执行指令 — 模型供应商与模型管理改版

本指令自包含，可整段复制给任意 harness 执行，不依赖聊天记录。

## 0. 背景与必读顺序

- 项目根（主仓库/main 协调目录）：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`
- 必读顺序：①主仓库 `AGENTS.md`（协作/安全/架构边界强制约定）②本目录 `需求说明.md` v1 ③`开发计划.md` v1（协议设计与任务表 T0–T10）④`交互原型_v1.html`（交互基线，实施时在浏览器打开对照）⑤`文档/接口文档/README.md` + `04-编排与LLM接口.md`。
- 本需求把模型配置从「一条扁平配置」改为参照 ZCode 开源的「内置目录 → 供应商（模板继承）→ 模型规则（目录覆盖/手动）」三层结构。**协议以开发计划 §2 为准，冻结前不得自行发散**。

## 1. 标准提示词（照抄 AGENTS §0，缺一不可）

| 用户提示词 | 接收者 | 授权与停止点 |
| --- | --- | --- |
| 为【模型供应商与模型管理改版】创建 worktree，从最新已提交 main 建立独立分支与目录，登记端口和数据目录。先不要开发。 | zcode | 只建分支/工作树并登记；不装依赖、不启动服务、不开发 |
| 在【模型供应商与模型管理改版】已登记的 worktree 中，按【文档/需求/20260922_模型供应商与模型管理改版/执行指令.md】开发、自测并提交，交给 Codex 验收，不合并 main。 | zcode | 实施+自测+提交，交付 SHA 与 URL，停在待验收 |
| 验收【模型供应商与模型管理改版/分支】的最新提交，不合并 main。 | Codex | 独立验收，记录 SHA，输出结论与修复项 |
| 在原 worktree 修复【模型供应商与模型管理改版】的验收问题，自测并提交，不合并 main。 | zcode | 同一工作树修复，交付新 SHA 等复验 |
| 将【模型供应商与模型管理改版/分支】验收通过的提交集成并合并到 main，处理普通冲突并重验，成功后清理对应开发环境；不重启主工作台。 | Codex | 集成/合并/清理授权；不含其他需求、远程 push、真实库迁移、主服务更新 |
| 将已合并的 main 构建并更新主工作台；如需真实数据库迁移，先说明具体操作。 | Codex | 主环境构建/重启；真实库迁移须先说明 |

创建 worktree 与集成合并**分别需要用户明确指令**；「开始开发/继续/验收通过」不隐含这两项授权。

## 2. 环境要求（创建 worktree 后按 AGENTS「独立分支开发」§2–§3 执行）

- 工作树位于 `<主仓库>/worktree/<任务名>/`；分支 `codex/llm-provider-models`（用户命名优先）。
- `WIZ_WORKBENCH_ROOT` = 工作树根；数据库 = main 库快照（`python3 -m workbench.storage.transfer backup --output <worktree>/data/workbench.sqlite3`，**禁止直接 cp 在写库**）+ 复制 `<main>/keys/wb-root.key`（否则加密凭据全部不可解密）+ `data/ontology-build-blobs` 随迁。登记标注「含真实数据，不可自动丢弃」。
- 独立端口（禁 8765/18765），显式 `WIZ_WORKBENCH_PORT`/`WIZ_WORKBENCH_ROOT`；移除继承的 `WIZ_DATABASE_URL`。
- 测试用临时隔离根（空库），绝不动真实 `ontology/`、真实库与根密钥。

## 3. 实施任务（引用开发计划 §3 任务表，按批执行）

批0：与用户确认目录收录范围、`/api/llm-models-fetch`（P1）与 anthropic-messages 是否首版做 → 冻结契约并追加记录到开发计划。
批1（并行）：T1 目录文件+加载器 ‖ T2 存储/迁移 ‖ T3 接口文档（**先文档后代码**，README 变更记录登记破坏性变更：`isDefault`→`defaultSelection`）。
批2（并行）：T4 域层合并器 ‖ T5 llm_client 多协议（mock 单测先行）。
批3（并行）：T6 路由 ‖ T7 前端设置页。
批4（并行）：T8 前端消费方 ‖ T9 后端消费方。
批5（串行）：T10 组合回归 `python3 tests/run.py`（quick/http/unit）+ 迁移演练 + `cd frontend && npm run build && npm run typecheck` + 浏览器 8 场景（开发计划 §5）。

每批一个 commit（`类型(范围): 简述` + 正文列改动与验证方式）；共享文件 owner 唯一，跨任务变更走协调者。

## 4. 范围边界与红线

- **不做**：OAuth 账号体系、openai-responses 协议、map 表达式引擎、多模态能力交互、目录在线同步、流式输出；不迁移/修改真实 main 库（集成阶段先在隔离环境演练 Alembic 迁移，真实库操作另行向用户说明）。
- **必须保持**：密钥 AES-256-GCM + AAD、只写不读回（响应/日志/快照零密钥）；按 `owner_user_id` 隔离；POST 白名单 + Origin 校验；`llm-provider-test` 不持 LOCK；迁移保持 provider_id 稳定使密钥零重加密；旧编排节点（仅 providerId）兼容执行。
- 改 `storage/schema.py` 必须带 Alembic 迁移；改校验输出须重生成金样；改 `saveCoordinator` 无关本需求不得触碰。
- 前端新页面不得自写 fetch，一律 `tools/llm.ts` → `app/http.ts`。

## 5. 交付要求

1. 分支最终 commit SHA + 启动命令 + URL（含实际分支/commit/数据根输出）。
2. 验证证据：测试命令与结果、构建输出、迁移演练结果、浏览器 8 场景结果；失败项如实标注。
3. 更新共享上下文（`.collaboration/context.py record`，task 含 `llm-provider-models` 标识）；实施结论追加到本目录 `开发计划.md`。
4. 停在「待验收」，等待 Codex 独立验收；不合并 main、不重启主工作台。
