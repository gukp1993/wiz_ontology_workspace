# 给其他 Harness 的执行指令

项目：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`
需求：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260917_微软列表详情布局`

当前状态为原型评审。仅收到这份材料不代表用户授权改生产代码；用户明确要求实施后，按以下要求执行。

1. 先阅读项目 AGENTS.md，再阅读需求目录的 `需求说明.md`、`开发计划.md`，实际打开 `交互原型_v1.html`，体验搜索、分页、对象选择、详情返回、快速切换和编辑取消。
2. 核对当前工作台实现及已有修改，提供简短改动清单。按微软 List/details 的自适应列表与详情结构实施，不自由改成卡片墙、树形目录或纯图谱。
3. 本次只优化对象建模布局。复用 Vue、现有共享控件、对象详情、属性／链接业务组件、路由定位和保存机制。原型菜单不构成增删业务模块的授权。
4. 宽屏并列、窄屏单栏；搜索全量对象、分页、选中态、返回保留上下文，切换对象可搜索；确认用户对单页详情切换和收藏的最终决定后落实。
5. 不改本体数据协议，不自动迁移共享属性，不增加动作运行能力，不改数据连接与项目映射。禁止把示例类型导入真实 ontology 数据。
6. 所有表单继续通过现有 API 与保存协调机制；不复制原型内存状态为另一套生产状态。未知引用、旧深链及已有草稿必须保留。
7. 在临时数据和独立测试端口核验数量多、空列表、搜索无结果、末页、长名称、窄屏返回、编辑取消与保存失败。运行受影响测试和 `cd frontend && npm run build`。
8. 在开发计划末尾记录实际修改、验证结果与剩余限制。提供访问地址与体验路径，不以类型检查通过声称所有交互已验证。没有浏览器能力时明确说明视觉和点击验证尚未执行。

只更新本需求四份核心文件，不散建重复文档；不依赖历史聊天、特定模型或特定插件。用户的新决定应同步进四份材料，再按确认范围实施。

## 全菜单原型更新后的执行边界

当前 HTML 已补齐本体／项目主菜单、版本引用、更多工具，以及新建／切换本体和项目。交付物为全菜单体验方案；并不自动授权整体重写工作台。用户要求实施时，先确认采用的页面范围，再按需求说明的最新补充执行。

必须体验新建上下文与空状态、模拟发布及项目固定引用的完整路径。页面中的 SQL／Python、连接、模拟实例和发布不能被当成真实执行能力。辅助页只读并非要求删除正式产品已有编辑能力。

正式产品复用现有稳定 ID、共享属性、动作关联、规则、编排及发布协议。不得将原型演示使用的对象名称、数组序号或简单内存快照作为生产数据结构。不要因原型项目区域依赖当前项目而删除原有独立编排访问能力。不要把简单对象增删比较替代生产升级兼容分析。

## 最新入口布局要求

按照原型将“＋ 新建”放在侧栏顶部“当前本体／当前项目”标签右侧，切换框位于下一行。不要再将主入口放在侧栏底部，也不要只放进切换下拉。复用现有新建弹窗及保存逻辑，取消保留当前上下文，创建成功切换到新本体或项目。区分新建本体与新建对象。切换弹窗内可保留辅助新建入口。
