# 给开发 harness 的执行指令

请在以下项目实施“函数编排交互评审优化”，不要自行扩展范围。

项目根目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260918_函数编排交互评审优化`

## 先读再改

1. 项目根 `AGENTS.md`，查看 git status，保护已有修改。
2. 本需求 `需求说明.md`，尤其第2节取舍、第6节诊断、第7节执行、第9节验收。
3. **打开并操作 `交互原型_v1.html`，正式实现必须遵守其布局、操作层级、模式提示和问题/结果面板方案。不能只读文字再自己设计另一套页面。**
4. 本需求 `开发计划.md`。
5. 源码 `frontend/src/flow/`、App.vue 保存/检查接入、`workbench/flows.py`；既有 `20260918_函数编排_节点函数化与执行` 文档只作为执行语义参考。

两份外部评审已经过筛选；本需求是最终方案。原型只演示交互，不连接服务。正式版本必须用现有保存协调器、请求层、Cytoscape、绑定协议与执行 API；不能把原型里的样例数组、模拟执行和模拟检查搬到生产逻辑。

## 必须完成的任务

按此顺序落地，不以“先换个样式”作为全部交付：

1. 给 f-editor 增加专注布局：全局导航在该页面收起、紧凑头部、工具栏独立行；无选择不占详情，桌面按需三栏，窄屏抽屉/覆盖详情。保证 1366×768 可用，不能用整体缩放解决。
2. 修复画布视口：状态徽标/检查/选择变化不自动 fit；保留用户缩放平移，明确全图/100%/聚焦。整理坐标可撤销。不能只扩大容器而保留反复缩小的根因。
3. 检查结果移到独立面板，数量可点击；节点问题标记；增加兼容的结构化 diagnostics 用于节点→页签→参数→字段定位。不能按中文错误字符串匹配。
4. 检查新鲜度覆盖主动检查、保存回包、编辑、撤销重做、项目/编排切换；晚到旧结果不覆盖新状态。只移动画布不变待重检。
5. 普通查看、建立绑定、选择测试节点三模式明确且互斥；查看节点与勾选测试范围分离。连线仍写 binding，再派生边。范围测试展示缺少上游并显式补选，不静默执行额外节点。
6. 节点详情用输入/实现/输出/高级分区；保留全部五种节点功能与稳定引用。测试此节点只有一个固定入口，列表去掉重复按钮，删除归更多菜单。边界输入输出仍可编辑和定位。
7. 代码等宽/换行/展开编辑；重建骨架覆盖前确认，可撤销。项目连接/提供方失败与未配置分开；配置入口可点击并保留草稿。Python LLM 推演标识不能隐藏。
8. 运行全部、测试单节点、测试所选范围各有唯一入口。固定输入只读，必填布尔不默认为false，对象/数组提示正确。无输入可省空表单，但真实写操作仍在同一执行摘要中确认。
9. 本次结果独立面板，标范围/快照/耗时；接口未返回时只显示全局执行中和节点等待结果，不能伪造并行逐节点进度；编辑后标上次结果；失败定位保留。
10. 完成弹窗 Esc、Tab 焦点、返回焦点、脏参数关闭保护；提示不挤压画布；统一现有样式变量与图标，不新增另一套 UI 框架。

## 严格边界

- 不改本体/项目建模功能，不改业务 schema，不迁移真实数据，不添加执行引擎/沙箱/运行历史/调度/流式接口。
- 不删除现有 SQL、Python、计算、Redis、HTTP 能力。不能把读取评审当成授权执行真实外部调用。
- `ontology/` 与 vault 不手改、不清理、不提交。所有自动化测试在临时 WIZ_WORKBENCH_ROOT 和独立端口；外部调用用替身。
- 保存、revision/409、撤销重做继续走既有机制。不要绕过安全白名单、Origin校验、锁和密钥边界。
- 已有格式与未知扩展零丢失；新增诊断是 API 附加信息，旧 errors/warnings/items/status 兼容。
- 不依赖某个模型、插件或固定并发数。需要分工时先固定定位协议，不让多人同时改 FlowEditor/App。
- 原型里没有的字段不代表可以删除；不要引入新业务必填项。实现发现冲突先按代码事实修订开发计划并说明；涉及业务取舍时提问，不擅自扩展。

## 验收与最后回复

逐项完成需求 A01–A15，按开发计划执行定向测试、npm run build、隔离浏览器验收。只跑类型检查不算完成。记录真实运行命令和结果，未验证项明示，不虚报。

最终将实际文件改动、最终诊断协议、验收证据与剩余问题追加至本目录 `开发计划.md`；交互偏差需同步更新原型及说明。按 AGENTS.md 提交 git，只提交本次相关文件。回复提供：已完成内容、测试结果、未完成项、commit ID、可访问页面地址。不要只说“已优化”。
