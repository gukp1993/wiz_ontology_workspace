# 本体建设维护改版 · 执行指令 v1（延期范围已更新）

日期：2026-09-20。状态：保留范围待评审，未授权本轮实施。

项目根：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`
需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260920_本体建设维护与版本改版/`

## 当前范围调整（2026-09-20，当前唯一实施范围）

用户明确：**“管理本体”“本体校验与发布”两个模块的改版需求延期**，恢复时间未定，须用户另行明确恢复。本次是延期，不删除现有工作台功能、不回退已有实现、不操作真实数据。

| 状态 | 范围 |
|---|---|
| 延期 | 管理本体入口、改名、归档/恢复使用、受限空本体删除（原型01～04）。 |
| 延期 | 发布页分区、历史内容查看、版本比较、恢复为草稿、检查点、发布影响确认与语义变更分类（原型05～10）。 |
| 随之延期 | 工作概览中由上述能力派生的新增状态、入口（原型14）；相关API、schema迁移、恢复记录、元数据、发布审核改动。 |
| 保留待评审 | 共享属性保存前影响确认（11）；规则库安全删除（12）；对象/属性/链接/规则/动作操作语义和依赖提示一致性（13）。 |

保留项只作用于当前草稿维护，不借此修改版本分类、发布校验或本体管理。共享修改影响确认是保存前说明，不等于新增发布审核。本次延期不构成对保留项的实施授权，仍按用户后续交付实施的指令执行。

当前需求正文与计划仅列保留项11～13；旧完整方案由Git历史追溯，不作为执行依据。

## 给其他 harness 的指令

用户后续明确交给你实施时，只实施本目录当前保留范围，不执行原完整版中的延期部分。若尚未批准实施，只阅读核对。

### 必读顺序

1. 根目录AGENTS.md；运行共享上下文read，核对当前任务与用户最新决定，查看git status。
2. 本目录[需求说明](需求说明.md)，顶部“当前范围调整”优先；重点§3共享修改、§4操作语义、§5失败处理、§6边界与§7验收。
3. [交互原型](交互原型_v1.html)，重点11共享修改、12规则删除、13一致性；“延期”页仅供参考。
4. [开发计划](开发计划.md)，仅按P0～P4当前任务顺序执行。
5. 文档/接口文档的README、01、02、05及相关契约。接口变化先文档后实现；新增接口登记白名单。

### 具体任务

1. 重新核对当前代码，其他工具可能已经完成规则删除或图谱编辑跳转；已有实现满足要求就复用，不重写。
2. 统一当前草稿领域依赖检查：对象/属性/共享定义/链接/规则/动作，覆盖当前可达入口与保留底层命令，前后端边界一致。
3. 明确“移除对象属性”和“转为私有”：前者删除对象上的引用属性，后者保留稳定ID与生效内容解除共享。链接删除作用两端，动作/规则移除关联不删除定义。
4. 规则库安全删除：有依赖列出业务名称与定位入口，无依赖确认后删除。对象删除仍采用阻断式，不新增一键级联。
5. 共享属性编辑沿用当前表单；类型/业务定义等高影响变更展示旧值、新值、受影响对象，确认后才保存当前草稿。普通名称/展示修改不新增重型确认。基线或引用变化使确认失效。直接比较编辑前后草稿，不建设版本比较服务。
6. 使用既有保存、CAS、撤销与表单守卫；保持所有已发布版本和项目引用不变。

### 不得实施

- 管理本体入口、名称维护、归档/恢复使用、受限空本体删除。
- 发布页重组、历史版本查看/比较、恢复版本、恢复前检查点、恢复操作记录、发布影响确认、语义变更分类改造。
- 上述延期能力派生的概览状态、API、数据库迁移或占位入口。
- 不禁用或删除正式工作台原有发布、版本等功能；本次只是新需求延期。
- 不恢复图谱已移除按钮；保留最新详情跳转现有表单方式。不新增顶级菜单。
- 不做MCP/口径生成、项目映射、函数编排、动作参数/提交条件、Excel关系导入、多人协作。
- 不把原型改动标注、虚线框、延期提示或模拟数据复制到正式产品。

### 数据保护与验证

不改真实ontology/、SQLite、凭据；测试隔离WIZ_WORKBENCH_ROOT和数据库URL。HTTP用既有唯一层，未知字段与稳定ID完整保留，不能在静态配置保存阶段执行业务操作。

验收需求§7中A12～A14，并执行限本期的A01/A16：共享影响确认/取消/冲突失效；列表与图谱对被契约引用的私有属性、被接口引用的共享定义、被对象引用的规则动作给出一致判断；移除与转私有含义正确；批量一项失败整批不变。测试原有Excel、配置迁移和发布不回归，不要求实现延期验收项。

改前端必须build，运行相关测试和浏览器交互验收。原型遵循现有风格，弹窗影响说明与对象定位清晰；正式编辑器复用现有整页表单。

### 最终交付

在开发计划追加实际变更、测试/浏览器证据、未验证项与commit；不另建重复报告。record共享上下文，按主题只提交自己的文件。遇到需要延期功能才能完成的依赖，先说明并寻找基于当前草稿的最小方案，不擅自恢复延期需求。
