# 独立验收执行指令：本体与项目辅助填写正式实现（2026-09-22）

> 本指令自包含，可直接交给独立验收方（Codex 或其他 harness）执行，不依赖任何聊天记录。
> 开发方（zcode）已完成实现、自测与两轮内部子代理验收循环；本指令定义外部独立验收，不授予合并、部署或清理权限。

## 0. 你的角色与停止点

你是独立验收方，只做只读核对与验证，输出验收结论。不修改仓库任何文件（验收产物一律放 /tmp 你的专属目录）；不合并 main、不推送、不重启主工作台（18765）、不触碰 18881/18882/18971 等其他实例；不执行真实数据库迁移；发现缺陷只取证并出具修复清单（修复由开发方执行后再次送验）。验收通过停在「待用户授权集成」。

## 1. 被验对象与交付标识

- 主仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`；**被验代码所在 worktree（你的 cwd）**：
  `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/assist-fill-production`
- 分支：`codex/assist-fill-production`，base `039a4f0`。
- **代码交付 SHA：`fe1179f`**（最后一处产品/测试代码变更）。其后至当前 HEAD（写作时为 `d99178a`）仅允许纯文档/协作提交——用 `git log --format='%h %s' fe1179f..HEAD` 逐一核对，若出现代码文件变更即为缺陷。HEAD 可能因纯文档/协作提交继续前进，属正常。
- 交付范围声明：`git diff --stat 039a4f0..HEAD` 应仅含 assist 前后端（workbench/assist_* 五模块、server.py 白名单 +2、frontend/src/assist/*、9 个宿主组件接入）、测试（10 个新套件 + mapping_forms 修复）、接口文档（04 §5/05 速查表/README）、开发计划落款、协作记录。无其他业务文件。
- 接口契约：`文档/接口文档/04-编排与LLM接口.md` §5（含 assist-context 请求字段 `ontologyId` 的登记行）；05 速查表 53a/53b（总数 99）。

## 2. 验收环境（已就绪；服务若停止允许仅重启 18951）

| 项 | 值 |
|---|---|
| 隔离实例 | http://127.0.0.1:18951（分支最新构建；数据为**合成验收数据，可丢弃**） |
| 重启命令 | `cd <worktree> && WIZ_WORKBENCH_PORT=18951 WIZ_WORKBENCH_ROOT="$PWD/.runtime/assist-data" ./start.sh start` |
| 账号 A（主用） | `assist_dev` / `AssistDev#2026`——拥有全部合成验收数据；默认模型=本地模型桩（18913，响应快、零成本） |
| 账号 B（隔离与真实模型抽查） | `admin` / `admin`——默认模型=**真实 minimax**（另有 GLM-5.3-Flash；llm-provider-test 已连通）。**真实模型生成有成本与延迟：全程 ≤4 次生成调用** |
| 合成数据（归 assist_dev） | 本体「辅助验收本体」id `b809bb77-7ff6-413a-b629-3024c0b3aa3b` v1.6.0：对象 储能簇(mg:cluster)/储能设备(mg:device)；属性 簇编号/额定容量(capacity,double)/SOC采样值(soc_series,timeSeries)/业务标识(biz_id)/设备名称；链接 所属设备 many-to-one；规则 设备SOC平均规则；动作 停止充放电。项目「辅助验收项目」id `9a2cb7d65bc3`：MySQL conn_mysql（目录 m_cluster/m_device）、Redis conn_redis、实例识别(m_cluster/id)、capacity=database、soc_series=database 时序、biz_id=redis、device=登记实例 DEV-001、链接映射 l_c2d(cluster_code↔id)、动作绑定 cluster+action_stop；编排「容量换算编排」id `9da15a57ebc7`（runtime 输入 ratio + fixed 输入 base，标量数值输出） |
| revision 取证 | 本体 `GET /api/state?ontology=b809bb77-…`；项目 `GET /api/project-state?project=9a2cb7d65bc3`（带 Cookie） |

## 3. 必读材料（按序）

1. AGENTS.md（worktree 内副本；重点：接口文档红线、验收边界）
2. `文档/需求/20260920_本体与项目辅助填写/开发计划_正式实现.md` 全文（§3 生产交互与生命周期、§4 协议与安全校验、§6 完成门槛、§8/§9 交付与验收循环记录是你的清单来源）
3. 同目录 需求说明.md §1～8、执行指令_正式实现.md
4. `文档/接口文档/04-编排与LLM接口.md` §5（两接口全量契约与上限）
5. 共享上下文：`python3 .collaboration/context.py read --actor <你的执行者名>`（开发方交接 entry 000165/000169 等）

## 4. 只读边界与禁止项

- 不修改仓库任何文件；脚本/日志/截图/profile 放 `/tmp/<你的专属目录>/`。
- 不合并、不推送、不创建 worktree；不启停/触碰 18765、18881、18882、18971 及其他任何实例。
- **允许在 18951 上做的事**：用两个现有账号登录操作 UI（含采纳与演示保存，会写入合成数据——允许）；如需补齐场景数据可在 18951 上自建合成本体/项目/编排；服务停止时按 §2 命令重启。
- **真实模型限次**：admin 侧 assist-generate 调用全程 ≤4 次；其余生成用 assist_dev（模型桩）。不得把模型桩验证说成真实 LLM 验证，反之亦然。
- 测试端口说明：`tests/test_assist_api.py` 自管端口 18911/18912；若被占用如实报告，不强杀他人进程（你的浏览器自起端口建议 ≥19370）。
- 遵守共享上下文：开工 `read`、交付 `record`（只记录实际结果；口令类敏感值不写入）。

## 5. 判定标准

- 每项三态：**通过 / 不通过 / 未覆盖**；未覆盖不得计为通过。
- P0=数据误写/越权跨账号/采纳触发自动保存/凭据或密钥出网/未经授权的真实执行；P1=场景链路断开/建议错挂字段/禁选失效/契约违约；P2=文案布局。
- 浏览器被策略/环境阻塞时不得绕过：相应项标「未覆盖」，总判定最高「部分覆盖」。

## 6. 必做验证

### V1 交付完整性与文档一致
1. `git diff --stat 039a4f0..HEAD` 范围审查（对照 §1 范围声明）；`git log fe1179f..HEAD` 无代码变更。
2. 三方登记一致：server.py POST 白名单（`/api/assist-context`、`/api/assist-generate`）= 04 §5 = 05 速查表 53a/53b；04 §5.1 请求表含 `ontologyId` 行（可选、省略默认 storage、不可见/属他人 404）；README 变更记录含对应行。
3. 开发计划 §8（环境/字段映射/协议冻结）、§9（交付记录/测试清单/§9.7 验收循环）落款完整，无占位命令。

### V2 后端测试复跑（cwd=worktree）
```bash
python3 tests/test_assist_context.py   # 14 项
python3 tests/test_assist_schema.py    # 57 项
python3 tests/test_assist_api.py       # 10 组（自管端口 18911/桩 18912；被占则报告跳过）
```
另抽契约：篡改 contextToken → 409 CONTEXT_STALE；生成请求 draft 与取上下文时不一致 → 409；跨账号项目/本体 id → 404；白名单外 draft 字段 → 400。

### V3 前端全量
```bash
for f in tests/*.test.mjs; do node --import ./tests/ts_hooks.mjs "$f" || echo "FAIL $f"; done   # 46 套件应全过
cd frontend && npm run typecheck && npm run build
```

### V4 浏览器 11 场景链路（核心；assist_dev 登录，真实点击）
自起 headless Chrome（端口 ≥19370、profile 在你的 /tmp 目录），CDP `Input.dispatchMouseEvent/insertText/dispatchKeyEvent` 真实事件，禁 `el.click()`；原生 `<select>` 弹层在 headless 无法键序驱动时允许 change/字符前缀路径完成**预置**，但须标注「非弹层路径」（环境限制非缺陷）。console 订阅三通道，先探针自证再清零。

统一链路（每场景）：入口「✦ 辅助填写」→ 面板标题正确 → 生成 → 建议卡（ready 可勾/pending-blocked 禁选有原因）→ **只勾部分建议** → 点「填入所选建议」→ 出现「已填入表单，尚未保存」→ **等 ≥2.5s 后取 revision 断言零变化（采纳不触发自动保存，覆盖 900ms 窗口）** → 点表单原「保存」→ revision 前进。每场景记录：操作序列、面板标题、建议文本、checked/disabled、revision 前后值。

| # | 场景与导航 | 要点 |
|---|---|---|
| S1 | O1 对象：对象建模 → 编辑「储能簇」 | 建议 label/comment；采纳一项仅该字段变 |
| S2 | O2 属性：属性页签 → 编辑「额定容量」 | label/comment/dataType；obsType 仅时序出现 |
| S3 | O2b 类型原子组：编辑「SOC采样值」 | dataType→时序必须配套 obsType；离开时序观测类型清空 |
| S4 | O3 链接：链接页签 → 编辑「所属设备」 | from/to/cardinality 候选来自图内对象 |
| S5 | O4 规则：业务规则库 → 编辑「设备SOC平均规则」 | name/description/content；无 output 字段 |
| S6 | O5 动作：动作定义 → 编辑「停止充放电」 | name/description/effect；无参数字段 |
| S7 | P1 实例识别：实例识别 → 储能簇「修改/配置」编辑态 | connection/table/primaryKey 建议；切「储能设备」为登记实例模式（无表/主键建议） |
| S8 | P2 database：属性取值 → 「额定容量」 | connection/table/valueField 候选来自目录；note 采纳 |
| S9 | P3 redis：属性取值 → 「业务标识」 | command/key/hashField 建议；未配置目标标题含「未配置」 |
| S10 | P4 flow：属性取值 → 任一属性切「函数编排」卡片 | flow/output/inputs 建议；**表单保存被既有业务校验正确拦截（如输入未绑定）判通过但如实记录**；如需完整保存可在本体自建数值属性后重试 |
| S11 | P5 链接映射：链接映射 → 编辑既有映射 | sourceId/field/targetSourceId/targetField 建议；targetId 请求为 `cluster.l_c2d` 格式 |
| S12 | P6 动作绑定：动作绑定 → 「停止充放电」「修改接口」 | method/path/parameters 建议；**请求体与建议渲染 0 处 auth/credential/apiKey/password** |

### V5 生命周期与保护
- V5.1 缺信息：intent 含「缺信息」→ 问题卡 → 未答建议 pending 禁选 → 作答后重算 ready。
- V5.2 检查/解释：检查输出 issues（可定位字段）；解释为纯文本且无采纳按钮。
- V5.3 撤销保护：采纳后 undo 生效（revision 不变）；手改字段后 undo 禁用。
- V5.4 失效与重取：采纳后手改 → 失效横幅 + 采纳禁用 → 重新生成/重取上下文恢复。
- V5.5 CONTEXT_STALE：手改后直接生成 → 面板提示重新获取上下文 → 重取后恢复。
- V5.6 取消编辑：表单「取消编辑」回落已保存值、旧建议作废。
- V5.7 无模型态：模型设置删除默认提供方后（**用 assist_dev 删除自己的桩、验证后重建恢复**，或说明跳过）生成 → 引导前往模型设置，输入保留。

### V6 安全红线
- V6.1 凭据零出网：全程监听（CDP Network 或代理日志） assist-* 请求体与响应，0 处 auth/credential/apiKey/password/Authorization（P6 重点；auth 区 UI 不受辅助影响）。
- V6.2 跨账号隔离：admin 登录看不到 assist_dev 的本体/项目/模型配置，反之亦然；admin 直接请求 assist_dev 资源 id → 404。
- V6.3 XSS：intent 与问题回答注入 `<img src=x onerror=…>`、`<svg onload=…>`、`"><img onerror=alert(1)>` → 纯文本渲染、0 注入节点、标志变量未定义、无原生弹窗。
- V6.4 本体工作区贯通（ontologyId）：assist_dev 的 O1 辅助请求体含 `ontologyId=b809bb77…`；admin 请求 assist_dev 工作区 → 404；省略 ontologyId → 200 落默认工作区。

### V7 真实模型抽查（admin，≤4 次）
至少 1 次 O1 对象定义生成：返回结构合法建议（ready/pending 由后端裁定），meta.model=MiniMax-M3；如实区分「真实模型验证（抽查级）」与「桩验证（全量）」。

### V8 视口
1440×900 与 768×1024 两档：面板可达、底部按钮可见（768 允许遮罩形态）。

## 7. 记录要求

每条检查记录：操作序列（控件级）、观察文本原文、checked/disabled、revision 前后差异、三态判定。禁止只写「测试通过」。脚本/日志/截图留 /tmp 并在报告列绝对路径。

## 8. 已登记既有项（再次观测标「既有登记」，不算新问题；可反对但需理由）

1. 检查页签「定位到字段」无实现（DEF-04，P2 待办）。
2. 登录过渡态偶发一次性 `TypeError … reading 'status'`（疑存量全局 401 过渡问题，非本分支引入）。
3. headless 原生 `<select>` 弹层无法键序驱动（环境限制，允许 change 路径预置并标注）。
4. 真实模型完整浏览器采纳链未做（抽查为准）；GLM-5.3-Flash 未单独抽查。
5. P4 在种子数据下「保存」会被既有业务校验正确拦截（编排输入未绑定/候选为空，fail-closed 正确）。
6. 18951 数据为合成数据可丢弃；本轮验收写入亦属演示数据。
7. 更早登记于开发计划 §9.5/§9.7 的未覆盖项与 P2 清单。

## 9. 交付格式

报告含：① 被验对象核对（HEAD/分支/diff 范围）；② V1–V8 逐项三态与证据；③ console 统计（探针自证；采不到写「未知」）；④ P0/P1/P2 清单（区分新发现与既有登记）；⑤ 总判定：**通过 / 不通过 / 部分覆盖**（任一 P0/P1 即不通过）；⑥ 不通过时的修复清单（编号/级别/复现/预期实际/源码位置/建议修法）。
完成后：报告写 `/tmp/<你的目录>/report.md`，并可选将报告以 `独立验收报告_正式实现_<日期>.md` 提交至需求目录（纯文档 commit）；按 AGENTS 用 `context.py record` 写交接（status=verified/blocked；引用被验 HEAD 与报告路径）。
停止点：验收通过停在「待用户授权集成」，等待用户明确合并指令；你不执行合并、部署、清理或主工作台更新。

## 10. 禁止声明

- 不得引用开发方自测/内部验收循环（开发计划 §9、/tmp/kg_t11_browser*、/tmp/kg_accept_*）替代独立取证。
- 不得在未真实点击链路的情况下声称浏览器验证通过；不得把模型桩验证说成真实 LLM 验证。
- 不得把「部分覆盖」写成「通过」；未覆盖项不得写成已验证。
