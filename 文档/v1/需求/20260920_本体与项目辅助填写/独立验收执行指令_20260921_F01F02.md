# 独立验收执行指令：本体与项目辅助填写原型 F01/F02 整改（2026-09-21）

> 本指令自包含，可直接交给独立验收方（Codex 或其他 harness）执行，不依赖任何聊天记录。
> 开发方（zcode）已完成整改与自测；本指令只定义验收，不授予合并、清理或主工作台更新权限。

## 0. 你的角色与停止点

你是独立验收方，只做只读核对与测试，输出验收结论。不改业务文件、不合并 main、不推送、不重启任何服务、不清理任何环境。验收通过停在「待用户授权集成」；不通过则列修复项，由开发方继续修复后复验。开发方自测与并行复验结论（提交说明、需求说明 §9、共享上下文条目 000142、/tmp/kg_f01f02_* 证据）仅供参考，不能替代你的独立取证。

## 1. 项目与被验对象

- 主仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`（所有命令以仓库根为工作目录）。
- 被验提交：`4c2dd9e`（`fix(assist): 整改原型常量输入按位置解析与归属文案（F01/F02）`，父提交 `ed8ea32`）。其后另有 `42baa9a` 为纯交接文档提交（session_context + entries），不属于被验修复内容。HEAD 可能已前进，**以文件内容哈希为准**。
- 被验文件：`文档/需求/20260920_本体与项目辅助填写/交互原型_v1.html`
- 开发方声称的整改后基线：**2078 行，SHA256 `4fd7539fd00fcf3984bd1bd07b9b8bbb2288fd39e97f13a792ebaf962eafb735`**
- 整改前基线（父提交）：`ed8ea32` 同路径文件 2068 行 / `de5649554f0bdbdcefe62a4bfc7925631fb8a631c9057376d509b5d1861473b3`
- 范围声明：本轮修复仅改该 HTML 内联 JS（+45/−35）与 `需求说明.md` 追加第六轮记录（+22）；不改 frontend/、workbench/、接口文档、数据库、真实 ontology 数据。

开工核对（全部在仓库根执行）：

```bash
git log --oneline -6
git status --short
wc -l "文档/需求/20260920_本体与项目辅助填写/交互原型_v1.html"
shasum -a 256 "文档/需求/20260920_本体与项目辅助填写/交互原型_v1.html"
git diff --stat ed8ea32..4c2dd9e
```

判定：
- 文件哈希必须等于 `4fd7539f…`。若不等（例如后续又有新提交改了该文件），报告实际值并按当前内容重新定位，不得按本指令旧描述臆断。
- `ed8ea32..4c2dd9e` 的 diff 范围必须仅含 `交互原型_v1.html` 与 `需求说明.md` 两个文件。
- 工作树中 untracked 的 `文档/需求/本体工作台体验审查报告.md` 与本轮无关，不要动它、也不要计入验收。
- 原型文件 SHA256 在你开工与收尾各核对一次，必须一致；不一致须在报告中说明取证时段。

## 2. 必读材料（按序）

1. `AGENTS.md`（重点：共享上下文交接机制、原型交付例外、验收边界）
2. `文档/需求/20260920_本体与项目辅助填写/需求说明.md`（正文 + §9 各轮记录；第六轮为开发方自测记录，不是验收结论）
3. 同目录 `执行指令.md`（原始需求与 A01–A17 验收清单）
4. 同目录 `交互原型_v1.html`（被验对象本体）
5. 共享上下文：`python3 .collaboration/context.py read --actor codex`（本轮交接条目 `.collaboration/entries/000142-d01bd6003c09.json`）

## 3. 只读边界与禁止项

- 不修改仓库任何文件；你的脚本、日志、截图、profile 一律放 `/tmp/<你的专属目录>/`。不 stash/reset/checkout 覆盖任何内容。
- 不合并 main、不推送、不创建 worktree。
- 不启动、不停止、不重启 18765 或其他任何既有服务。浏览器验收直接 `file://` 打开离线 HTML，不需要任何服务。
- 不连接真实 LLM/数据库/Redis。原型无网络能力——若在验收中发现任何真实网络请求即为 P0。
- 自起 headless Chrome 用独立端口（建议 ≥19330）与独立 profile，用完关闭并确认无残留进程。
- 遵守共享上下文机制：开工前 `read --actor codex`；交付前 `record`（status 取 verified/blocked/in_progress 之一，只记录实际结果，不写未验证的通过结论）。

## 4. 判定标准

- 每个检查项三态：**通过 / 不通过 / 未覆盖**。不得把未覆盖写成通过。
- P0=数据误写/越界执行/跨目标覆盖/真实网络或密钥行为；P1=核心链路断开或错填（归属猜错、禁选失效、采纳连带写入未勾字段等）；P2=文案/布局/风格。
- §8 所列既有 P2 再次观测时标注「既有登记」，不算本轮新问题；你可以反对既有登记，但需给出理由。
- 浏览器被策略/环境阻塞时**不得绕过**：相应项标「未覆盖」，总判定最高只能给「部分覆盖」，不得写「通过」。

## 5. 必做验证

### V1 交付完整性

§1 的核对命令 + 逐块审查 `git diff ed8ea32..4c2dd9e -- "文档/需求/20260920_本体与项目辅助填写/交互原型_v1.html"`。重点审三处：新增 `parseConstPair()`、常量建议生成块（`whyAmb`/`constPendingWhy`/`constCard`）、问题卡与占位文案改动；确认没有夹带其他逻辑改动。`需求说明.md` 的 diff 应为纯追加（不应删改既有记录行）。

### V2 静态检查

```bash
F="文档/需求/20260920_本体与项目辅助填写/交互原型_v1.html"
for p in 'fetch(' 'XMLHttpRequest' 'WebSocket' 'EventSource' 'sendBeacon' '<script src' '@import' '<link' 'localStorage' 'sessionStorage' 'indexedDB' 'document.cookie' 'alert(' 'prompt(' 'confirm(' "|| 'SOC'" 'parts[1]'; do printf '%s => ' "$p"; grep -c -F -- "$p" "$F" || true; done
grep -n 'https\?://' "$F"
grep -n '标注归属' "$F" || echo "标注归属: 0"
```

判定：17 项计数全 0；`标注归属` 0 命中；http(s) 仅 3 处假域名/占位文案（约 1064/1698/1704 行，`.example` 假域名或提示文字，以当前文件为准），且无 `src=`/`href=` 指向外部资源。用 python 提取唯一内联 `<script>` 内容后 `node --check` 必须通过。

### V3 解析纯函数独立复验

从 HTML 提取 `parseConstPair` 原文（自己写提取脚本；开发方 `/tmp/kg_f01f02_20260921/parse_unit.mjs` 可参考，但**必须自己跑、自己断言**，不得直接引用其结果）。逐例断言（state 语义：two=两个位置；one=单个无归属标识；many=多分隔符；spacey=片段内含空白；kv=含 =／＝；empty=空输入）：

| 输入 | 期望 |
|---|---|
| `my_model` | one，mn=my_model |
| `my_model /` | two，mn=my_model，an='' |
| `/ SOC` | two，mn=''，an=SOC |
| `my_model / SOC` | two，mn=my_model，an=SOC |
| `/` | two，两侧空 |
| `a / b / c` | many（count=3，seps=2） |
| `a // b` | many 且 consec=true（连续分隔符不合并） |
| 空串/纯空白 | empty |
| `my_model SOC` | one（不按空格拆分） |
| `my_model ／ SOC`（U+FF0F） | two |
| `my_model ，SOC`（U+FF0C） | two |
| `a , b ,` | many |
| `my model / SOC` | spacey |
| `a=b`、`a＝b`、`a=b / c` | kv |
| `a / / b` | many（不合并，count=3） |
| `  /  `（仅分隔符+空白） | two，两侧空 |
| Tab/全角空格(U+3000)/换行包裹 `/ SOC` | two，mn=''，an=SOC |
| `   my_model   /   SOC   ` | two，mn=my_model，an=SOC |

### V4 真实浏览器 F01 解析矩阵（必做，不得用内部函数调用替代）

操作路径（每条用例重载页面后从头走，避免状态串场）：左栏「项目区」`[data-space="prj"]` → 「对象映射」→ 标签「属性取值」`[data-ptab="props"]` → 「SOC采样值」行「配置」`[data-ps="ps_series"]` → 「✦ 辅助填写」`[data-assist="ps:ps_series"]` → `#p-intent` 输入「采样规则」（或点第一个示例 chip）→ 点「生成建议」`#gen-btn` → 等 ≥1.3s → 问题卡自由输入框 `[data-free="consts"]` 输入用例文本 → 按 Tab → 等 ≥0.9s → 读卡。

逐用例记录每张建议卡：label、建议值/待补充文本**原文**、checkbox 的 checked 与 disabled。期望：

| 输入 | model_name 卡 | attr_name 卡 |
|---|---|---|
| `my_model` | pending+disabled | pending+disabled，文案含「不补造示例值 SOC；采纳 model_name 不会连带填入本项」 |
| `my_model /` | 可建议「固定值 my_model」 | pending+disabled |
| `/ SOC` | pending+disabled | 可建议「固定值 SOC」 |
| `my_model / SOC` | 可建议 my_model | 可建议 SOC |
| `/` | pending+disabled | pending+disabled |
| `a / b / c` | pending+disabled，说明含「3 个片段」 | 同左 |
| `a // b` | pending+disabled，说明体现连续分隔符不合并 | 同左 |
| 三个空格 | pending+disabled | pending+disabled |
| 点「暂不确定」radio（`input[name="consts"][value="__unsure__"]`） | pending+disabled | pending+disabled |
| `my_model SOC` | pending+disabled（未按空格拆） | pending+disabled |
| `my_model ／ SOC`、`my_model ，SOC` | 可建议 | 可建议 |
| 含 `=`／`＝` 的输入 | pending+disabled，说明含「不解析键值对归属」且整串不作为有效常量 | 同左 |

**反例必查**：输入 `/ SOC` 后对全 DOM 文本搜索「固定值 SOC」，必须只出现在 attr_name 卡，绝不能出现在 model_name 卡。

### V5 采纳范围与原值/无需变更（真实点击 + 快照取证）

快照证据允许 `Runtime.evaluate` 读 `JSON.stringify({b:BUFFERS['ps:ps_series'],s:SAVED.prj.propSrcs.find(function(x){return x.id==='ps_series';})})`，但点击/输入必须真实（CDP `Input.dispatchMouseEvent`/`Input.insertText`/`Input.dispatchKeyEvent` 或等价真实事件；不得用 `el.click()` 冒充）。

- **V5.1 `/ SOC`**：注意 attr_name 卡**默认勾选**——保留其勾选、取消其余默认勾选卡（说明/编排/model_id），点「填入所选建议」`#adopt-btn`。要求：BUFFERS 变化字段**恰为** `bind.attr_name`、`bindv.attr_name`（允许内部 `__filledTip` 标志）；SAVED 逐字段不变；表单 DOM 显示 attr_name=固定值/SOC、model_name 仍「待绑定」。若保留全部默认勾选直接采纳，会连带写入说明/编排/model_id——这是既有设计（「空字段的有效建议默认选中」，见 §8-3），按既有登记记录，不算本轮缺陷；但你须实测确认连带写入仅限默认勾选项，且 pending 卡在任何操作下都不写入。
- **V5.2 `my_model /`**：采纳后变化恰为 `bind.model_name`、`bindv.model_name`。先在表单把 attr_name 预置为「固定值」+`SOC`（真实操作，编辑后等 ≥1.2s 再生成），确认 attr_name 及其值保持不变。
- **V5.3 已有属性绑定**：表单把 model_name 来源设「当前对象属性」、来源项「簇编号」→ 生成并输入 `my_model / SOC` → model_name 卡「原值」显示「属性 簇编号」（删除线样式、非占位斜体）、`checked=false`、`disabled=false`；只采纳 attr_name 后 model_name 绑定（attr/cluster_code）未被覆盖。
- **V5.4 已有 const SOC**：表单把 attr_name 设为固定值 `SOC` → 输入 `my_model / SOC` → 不得出现 attr_name 待替换卡，应出现 note「attr_name 已按你提供的常量配置（SOC）：无需变更。」；model_name 卡照常生成可采纳。
- **V5.5 暂不确定禁选**：两卡 pending+disabled；枚举真实点击面板全部可点击控件后，`bind.model_name`/`bindv.model_name`/`bind.attr_name`/`bindv.attr_name` 全程为空。

### V6 缓冲区/保存/取消

- **V6.1** 采纳只写 BUFFERS：采纳前后 SAVED 逐字段 diff 为空。
- **V6.2** 取消编辑 `[data-act="cancel"]`：表单回落 SAVED（绑定表全「待绑定」）、面板提示原文「已取消编辑并恢复已保存数据，旧建议作废。」、旧建议卡清空。
- **V6.3** 演示保存才更新 SAVED：仅采纳后直接点「演示保存」会被必填校验拦住（flowId 必填，toast「存在必填项未完成，无法演示保存」）；真实选择编排「通用SOC采样查询」（headless 下原生 select 弹层无法键序驱动时，允许用字符前缀 type-ahead 或 change 事件路径完成，报告标注「非弹层路径」即可，这是环境限制不是缺陷）→ 再「演示保存」→ SAVED 出现 `flowId='f_soc'`、`bind.attr_name='const'`、`bindv.attr_name='SOC'`，行状态转「已配置」。随后 `Page.reload` 复位为初始（纯内存演示）。
- **V6.4** 未勾选字段零改动：V5.1 采纳后 `model_name`/`model_id`/`start_time`/`end_time` 在 BUFFERS 与 SAVED 中变更路径均为空。

### V7 F02 文案检查

- 单 token/无归属场景 pending 文案必须含「不补造示例值 SOC；采纳 model_name 不会连带填入本项」。
- 全文件 grep「标注归属」必须 0 命中（V2 已含）。
- 输入含 `=`／`＝`：提示改用「model_name / attr_name」格式，且无任何卡含「固定值 」+该输入串。
- 文案改动不得破坏 V4/V5 的禁选与按字段采纳（文案与行为一致）。

### V8 保留项回归（抽测）

1. 暂不确定禁选（V5.5 已覆盖，不重复）。
2. **F05**：`ps_series` 表单无 conn 字段，仅选编排即可演示保存成功，无「必须选择数据连接」类报错；行状态 待配置→已配置。
3. **生成中关面板作废**：`#dm-model` 选「生成延迟」→ 生成 → 3s 内点 `#p-close` → 等 ≥4s → 重开面板：无结果卡、提示原文「面板已关闭时取消了生成；迟到结果将被忽略。」
4. **切目标作废**：切到「额定容量」`[data-ps="ps_cap"]` 再切回 → 提示含「已切换辅助目标，上一目标的结果与生成已丢弃」且不串场。
5. **手动编辑后撤销禁用**：采纳 → 改表单任一字段 → `#undo-btn` disabled 且出现原因文案（「采纳后你已手动编辑表单：直接撤销会覆盖新修改。……」）。
6. **类型联动**：本体区 对象建模 → SOC 属性 `[data-prop="p_c_soc"]` → dataType 切「数值」→ 观测值类型字段消失且缓冲区 obsType 清空；切回「时间序列」→ 下拉恢复。
7. **D4 Redis**：SOC实时值 `[data-ps="ps_rt"]` 辅助填写输入「GET m_storage_cluster_phase-{id}-soc」→ 生成 → 「参数 {id} 来源」建议「实例主键（pk）」。
8. **R01 不回归**：model_name 预置「固定值」+`storage_cluster`（等 ≥1.2s）→ 生成 → 原值显示「固定值 storage_cluster」（不得是「未选/待绑定」）、默认不勾。

### V9 XSS 与 console

- **XSS**：`#p-intent` 与 `[data-free="consts"]` 分别输入 `<img src=x onerror="window.__x1=1">`、`my_model / <script>window.__x2=1</script>`、`/ <svg onload="window.__x3=1">`。注意：问题回答提交（Tab）后自由输入框会随问题卡移除，重放前先点「清空补充回答并重新提问」。判定：均纯文本渲染、img/svg 注入节点 0、`window.__x1/2/3` 全 undefined、无原生弹窗（订阅 `Page.javascriptDialogOpening`）。
- **console**：订阅 `Runtime.consoleAPICalled` / `Runtime.exceptionThrown` / `Log.entryAdded`，覆盖全部用例。先做注入探针（console.error + 未捕获 throw）确认采集有效，再清零正式跑；无法采集的写「未知」，不得写 0。开发方自测值为 error/warning/未捕获/Log 全 0，以你实测为准。

## 6. 记录要求

每条检查记录：操作序列（到控件级）、观察文本原文、checked/disabled、BUFFERS/SAVED 前后字段差异、三态判定。**禁止只写「测试通过」。**脚本、日志、截图全部留 /tmp 并在报告列绝对路径。

## 7. 环境与方法学说明（不构成放行/否决依据）

- 表单改动经软去抖写入缓冲区：编辑表单字段后必须等 ≥1.2s 再生成；自由输入+Tab 后等 ≥0.9s。不等会读到旧值，造成假失败。
- headless Chrome 原生 `<select>` 弹层无法用键序驱动（多次实测，弹出层不渲染且阻塞）：预置绑定等 select 操作允许走 change 事件/字符前缀路径，报告标注即可。有真实浏览器时可抽查弹层路径。
- 真机多浏览器矩阵、完整无障碍走查不在本轮必做（历史各轮均未覆盖，如实标注）。
- A15 三档视口本轮开发方未重测（本轮未改布局，上一轮已在 de564955 三档实测通过）；如需抽测可选 1440×900 一档。

## 8. 已登记 P2（既有，不作为本轮新问题；可反对但需理由）

1. 「已填入建议，尚未保存」dirty-pill 文案分支不可达（恒显示「未保存（编辑缓冲区）」）。
2. 常量自由输入 Tab 提交后问题卡收起，改答需先点「清空补充回答并重新提问」（既有设计，有恢复路径）。
3. 默认勾选策略：直接点采纳会连带写入说明/编排/model_id 等默认勾选项；「只补该常量对」需手动取消其余勾选（需求口径确认项）。
4. `ab:a_stop` 的 `srcLabel`/`powOld` 未转义拼接，依赖渲染处 `esc()` 兜底（实测不可利用，防御纵深建议）。
5. 更早各轮 P2（第四/五轮，见需求说明 §9）：old==neu 幂等卡、说明快照残留、data-locate 指向、同屏 DOM id 重复等。

## 9. 交付格式

报告含：① 被验对象核对结果（哈希/行数/diff 范围）；② V1–V9 逐项三态与证据；③ console 统计（或「未知」）；④ P0/P1/P2 清单（区分新发现与既有登记）；⑤ 总判定：**通过 / 不通过 / 部分覆盖**；⑥ 不通过时的修复项清单（按优先级）。
完成后按 AGENTS 用 `python3 .collaboration/context.py record` 写交接（`--actor codex`；status=verified 或 blocked；引用被验哈希与报告路径）。
停止点：验收通过停在「待用户授权集成」，等待用户明确合并指令；你不执行合并、清理或主工作台更新。

## 10. 禁止声明

- 不得引用开发方自测（需求说明 §9、提交说明、/tmp/kg_f01f02_* 证据）替代独立取证。
- 不得在未真实点击链路的情况下声称「浏览器验证通过」。
- 不得把「部分覆盖」写成「通过」，不得把未覆盖项写成已验证。
