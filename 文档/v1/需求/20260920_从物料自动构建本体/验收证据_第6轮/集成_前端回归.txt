通过：物料排除传字符串清单修订；合并/撤销/差异裁决/编辑/决定传候选 token；重新生成省略 revision；409 透出 currentRevision 供刷新。
通过：① VALUE_TYPES 与后端枚举逐字一致且都有中文标签
通过：② 读取平铺字段：timeSeries 必须读出平铺 valueType（当前缺陷项）
通过：③ 缺观测值类型读出空串（不崩、不臆造 double）
通过：④ 旧嵌套形态只读兼容：仍能读出 type/valueType
通过：⑤ 保存载荷平铺：timeSeries 带 valueType，绝不输出嵌套对象
通过：⑥ 非 timeSeries 不残留 valueType；未确定类型不发送 dataType
通过：⑦ 评审页回填：平铺 timeSeries+double 读进表单（旧实现读不到观测值类型）
通过：⑧ 评审页回填：嵌套历史数据仍可打开编辑器（只读兼容）
通过：⑨ 评审页保存：timeSeries 发平铺 {dataType:'timeSeries', valueType}，不含嵌套对象
通过：⑩ 评审页保存：普通类型只发 {dataType}，不残留 valueType
通过：⑪ 评审页校验：时间序列未选观测值类型时拒绝保存并提示
通过：⑯ 评审页校验（P2-1）：已有数据类型清回「未确定」被拦截且不发请求
通过：⑰ 评审页校验（P2-1）：原本无数据类型的候选仍允许保持未确定
通过：⑫ 回链：目标本体与当前不同 → 先切本体（带未保存守卫）再进对象建模
通过：⑬ 回链：目标就是当前本体 → 直接进对象建模，不重载
通过：⑭ 回链：id 缺失 → 退回原行为并给出可读提示
通过：⑮ 评审页不再自带类型表、不再发送嵌套载荷；任务行回链带本体 id
通过：⑱ 保存页加载（P2-3）：已交付任务跳过预检请求，预检区保持空
通过：⑲ 保存页加载（P2-3）：未交付任务照常预检（行为不变）
全部通过：19 项。
通过：① 客户端挂载 App 成功，setup 完整执行（无 flowState TDZ 初始化异常）
通过：② 依赖 flowState 的检查结论新鲜度 watch 在客户端真实生效
通过：③ 初始化链可用：加载本体/项目/校验仍正常，不因顺序调整回归

app_client_mount：3/3 通过
