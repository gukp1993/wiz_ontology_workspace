# 给其他 Harness 的执行指令：动作接口映射一期

在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` 实现本任务。先读当前AGENTS.md，再读：

1. `文档/需求/20260917_动作接口映射/01_需求.md`
2. `文档/需求/20260917_动作接口映射/02_开发计划.md`
3. **打开并实际查看** `文档/prototypes/动作接口映射交互原型_v1.html`，按该原型实现列表与弹窗，不自行重新设计。

这是实施任务，不停在规划。需求文档优先于原型中的假数据；旧的api/flow动作映射方案只用于兼容，不得重新加入函数编排选择。

## 固定范围

- 复用对象映射已有ActionBindings页签，不新建菜单或接口库。
- 本体动作名称/业务定义只读，来源是项目固定引用的发布版本。
- HTTP接口：方式、完整地址、条件展示的请求体格式、可选说明。
- 参数表：参数名、位置、取值来源、来源值、固定值类型。
- 可选折叠认证：无认证/Bearer/API Key，受保护项目凭据引用。
- 请求示意只展示文本，不调用接口，不查实时设备值。
- 弹窗保存/取消/关闭，真实项目持久化及发布配置校验。

## 实施前必须注意

当前已有 `frontend/src/project/ActionBindings.vue`、`ProjectBinding.vue` 动作绑定入口，`bindings.actionBindings` 和后端 `_check_action_bindings`。在这些基础上改，不另起炉灶。

当前本体新版动作只有name/description/effect，没有输入参数。原型的“目标设备/目标功率”等动作inputs只是示例：正式版只能从真实旧动作输入列出；无inputs时禁用该来源，其他实例主键/对象属性/固定值仍可使用。禁止为实现本需求给本体增加inputs表单。

现有secrets.py管理数据库连接密码，不可假装已有API凭据库。先核对最新代码，若无则按需求补最小、项目隔离的API凭据登记与元数据列举，不增加全局凭据管理产品。不得保存或打印真实密钥。

## 技术约束

- 按开发计划的implementation.schemaVersion=2扩展现有kind=api，保留path字段与旧数据。若有协议冲突先记录并处理兼容，不覆盖。
- 旧api、flow和未知字段无损；旧flow只读保留，不自动转换或删除。
- 前端统一用AppSelect与现有form-save/form-guard/Saver；先检查现有机制，避免绕过持久化。
- 后端只做配置存储、校验及受保护凭据管理，没有设备执行API。不放松dbdrivers固定只读边界。
- 项目发布、revision、引用版本、密钥保护全部沿用现有边界。
- 项目校验改动同步金样用例并回归；别把其他harness正在开发的内容覆盖。
- 不写本体草稿、不导入示例、不改真实ontology数据文件。

## 按P0—P5实施与验收

按开发计划完成H01—H12。自动化用临时WIZ_WORKBENCH_ROOT和独立端口，不能用真实数据执行测试。前端必须 `npm run build`，并实际浏览器检查来源联动、请求示意、保存回显及取消。

不要为“完整性”新增返回映射、成功判定、重试、超时、测试请求、设备控制、权限审批页面、函数编排集成、原始JSON编辑器、复杂嵌套参数或独立接口注册中心。

原型的“填入停止充放电示例”、模拟凭据、模拟属性、刷新恢复数据只用于演示，不进入正式页面。

如拆分并行工作，先固定协议，按不冲突文件分工；共享文件指定唯一写入负责人，不强求并发数量。

完成后提供：
1. 实现结果及入口。
2. 改动文件。
3. H01—H12验收结果与实际执行命令。
4. 旧格式兼容、API凭据处理方式。
5. 未完成项与明确限制；不得将“配置校验通过”说成“设备控制可用”。
