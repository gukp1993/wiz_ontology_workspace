# 系统全方位深度测试 · S1–S3 修订后独立复验指令（交 Codex）

> 用途：可直接复制给 Codex 或其它执行者，独立复验「继续验证」轮（S1–S3 修订与针对性复跑）。
>
> **本版已更新到第二轮（含对抗加固）**。完整、最新的验收清单以 [独立验收执行指令_20260921_F01F05.md](独立验收执行指令_20260921_F01F05.md) 为准。
>
> **第二轮**：除 M01–M10 外，新增 §4A 的 F01–F05 整改核验项（对应
> [S1S3独立复验记录_20260921.md](S1S3独立复验记录_20260921.md) 提出的问题与
> [缺陷清单.md](缺陷清单.md) 的「复验整改记录」）。**执行时以本版为准**；
> 若只复验 S1–S3 而不看 F 项，会漏掉判定器最关键的一批加固。
> 本指令自包含，不依赖聊天历史；**只做复验与报告，不改业务代码、不动原测试与金样、不合并 main、不触碰本任务以外环境**。

## 1. 被验对象与基线

| 项目 | 值（已实测核对，请自行复核） |
|---|---|
| 主协调仓库 | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` |
| **复验 cwd（唯一）** | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test` |
| 分支 | `codex/test` |
| 复验 HEAD | 以 `git log --oneline -1` 实际结果为准。本批次起点 `6b4665e`（S1–S3 修订主体），其后依次 `d1ce6e0`（交接）、`10d565b`（覆盖核对补强）、`1157e87`（本指令）、以及紧随其后的交接提交。 |
| 允许的 HEAD 差异 | 本指令交付**之后**只应再出现文档/交接类提交（仅触及 `文档/` 与 `.collaboration/`）。若新增提交触及 `tests/deep_*` 或业务文件，请按下方「先报告」处理，不要直接复验。 |
| 被验业务 SHA | `d6c73c29a19d97ad8da22ad3d8a8f3c0615e3b40`（`git diff d6c73c2..HEAD -- workbench frontend` 应为空） |
| 本轮改动范围 | 仅 `tests/deep_*.py` 与本需求 `文档/需求/20260921_系统全方位深度测试/*.md` 及共享交接；见 §3 |
| **不得改动** | 业务代码、既有非 deep 回归测试、`tests/fixtures/validation_golden.json`、依赖声明/锁文件、真实数据；**不得修改 `验收记录.md`**（真实浏览器/HTTP 复验如需就地补记，另存您自己的报告文件） |
| 原独立验收（只读参考，勿修改） | `文档/需求/20260921_系统全方位深度测试/验收记录.md`（S1/S2/S3 即出自此文件 §§4–5） |

先执行：`git log --oneline -6`、`git status --short`、`git diff --name-status d6c73c2..HEAD`、`git worktree list`，确认 HEAD 与改动范围；如与本表不符（例如出现新的 `tests/deep_*` 或业务提交），先报告再决定是否继续。

## 2. 只读边界与资源约束

- **不得**合并/推送 main、不得创建修复分支、不得重启主工作台 18765、不得触碰 18931 与 `test-data*`（前轮实例与数据，未授权处置）。
- 如需重跑真实 HTTP 链路：**自起自关**新隔离实例，端口自行核对空闲（禁 18765/8765），数据根建在 `/tmp` 或本树 `.runtime/` 下**新目录**；启动前 `env -u WIZ_DATABASE_URL`，并实测进程 `WIZ_WORKBENCH_ROOT`/`WIZ_WORKBENCH_PORT` 生效、SQLite 落在隔离根。
- 仅用合成账号/数据；不复制真实 `ontology/`、`vault/`、密钥；不连真实 MySQL/Redis/模型。
- 复验完成后停止自起实例（按 PID+cwd 双确认）、删除自建合成数据根，保留脱敏证据；**未明确归属的目录一律不动**。
- 脚本支持 `Q02_BASE` / `Q03_BASE` / `Q02_USER` / `Q03_USER` / `DEEP_RUN_ID` 环境变量覆盖目标与运行号；**不要指向 18931 或真实根**。

## 3. 被验改动清单（本轮，相对 `42987ce`）

新增：`tests/deep_verdicts.py`、`tests/deep_verdicts_test.py`、`tests/deep_reverify_scenarios.py`、`tests/deep_reverify_r02_a01.py`、`tests/deep_results_index.py`。
修改：`tests/deep_project_chain.py`（P8 段）、`tests/deep_ontology_o3o4o5.py`（O3-04 / O4-06 段）、`tests/deep_project_client.py` 与 `tests/deep_ontology_client.py`（记录器改写为按 runId 写独立目录）。
文档：`测试报告.md`、`缺陷清单.md`、`测试计划.md`（继续验证区）、`结果索引.md`（新增）。

复核点：`git diff 42987ce..HEAD --stat` 是否只含上述文件；有无夹带业务改动。

## 4. 必做复验（M01–M10）

### M01 · 判定函数是否正确拒绝「假通过」
运行 `.runtime/venv/bin/python tests/deep_verdicts_test.py`，应通过、退出码 0（**历史轮次数量为 64**；当前实际为 107/107）
（第一轮为 23 项，第二轮整改后并入 F01–F03 反例与配置自检共 49 项）。
同时运行 `.runtime/venv/bin/python tests/deep_results_index_test.py`，应通过（**历史轮次为 17/17**；当前实际为 51/51）。
**并自行加反例**（不要只跑既有用例）：构造至少 3 种输入确认分类正确 ——
5xx 不得记 product_pass；无关 4xx（如 401/404/409）不得记 product_pass；前置失败必须 blocked；
网络失败/空响应不得记 product_pass；「命中期望状态码但诊断与目标无关」必须 test_error。
判定口径以 `tests/deep_verdicts.py` 模块文档的 R1–R7 与源码为准（`测试报告.md` §3 是覆盖矩阵，不含 R1–R7 定义），
若有分歧按最保守者记录。

### M02 · 索引是否可核算（S1 核心）
运行 `.runtime/venv/bin/python tests/deep_results_index.py`，应重算成功并给出：
14 个有效批次、被替代 **94** 行、断言记录 **187**、唯一场景 **187**、缺陷根因 **9**，
分类 `product_pass=178 known_defect_reproduced=3 new_defect_reproduced=2 static_check_pass=1 blocked=1 info=2`。
再运行 `--write-doc` 确认 `结果索引.md` 可复现（除生成时间外内容一致）。
**抽查任意 5 行**：比对 `结果索引.md` 的行号/分类与 `.runtime/test-evidence/` 原始 JSONL 实际内容。

### M03 · 替代关系与覆盖是否真实（S1 关键，可证伪）
- 索引生成器内置机器核对：替代关系与根因观察引用按 `文件#行(用例)` 解析，caseId 不符即报错。
  **请自行制造一次反例**（例如临时把某条引用行号改错后再次运行，确认生成器报错；随后还原），
  以证明该核对不是摆设。
- 核对 §2.1 替代关系与 §2.2 覆盖表：被替代批次中的业务场景是否确已被有效批次覆盖；
  8 条 `*-crash` 行是否确为工具异常中断（打开原始行核对堆栈，确认不是产品缺陷被伪装）；
  info 说明行是否被单列而非当作业务覆盖；`O7-00` 这类「本身即说明行」是否如实列出。

### M04 · A01 判定与前置（S2 关键）
复核 `deep_ontology_o3o4o5.py` O3-04 段与 `deep_reverify_scenarios.py::scenario_a01`：
前置是否要求「合法对象+合法规则/动作+关联」且显式成功（失败即 blocked）；非法记录是**追加**还是替换（应追加，避免悬空）；
被接受时是否记 `known_defect_reproduced` 且核对发布快照原值保留；**未被接受时是否会错误地固定 fail**（应校验拒绝理由是否针对 content/effect）。

### M05 · R02 判定与根因计数（S2 关键）
复核 `deep_project_chain.py` P8 段与 `scenario_r02`：四条前置（创建/flow-save 含输出未绑定诊断/独立 flow-check pending/项目保存）是否都显式校验成功；
「project-validate 未拦截」是否记 `known_defect_reproduced`（不得记 pass）；
「project-publish 成功」判定是否同时检查 **版本零新增** 与诊断命中；
**负对照**（引用不存在编排）是否被正确拦截；两条观察是否只计 1 个根因。
自行构造一种反例：把某前置（如项目保存）响应改成失败，确认该用例变 blocked 而非给出产品结论。

### M06 · O4-06 不可互相替代
确认 O4-06 为 `static_check_pass`（白名单无写入口），O4-06b 为 `not_tested`（破坏路径未实测），
报告中不得把前者当作「发布不可变性已验证」。

### M07 · 真实 HTTP 复跑（定向，非全面重测）
在自起的隔离实例上运行 `tests/deep_reverify_r02_a01.py`（`DEEP_RUN_ID` 用您自己的编号），
应复现：前置全成功；A01 与 R02 两条观察均为 `known_defect_reproduced`；负对照通过。
**预期与上一轮一致：在当前 `d6c73c2` 未修业务基线上，A01/R02 仍应归为 known，不追求业务全绿。**
若某个前置失败，应记 blocked 并停止该用例，不得沿用旧状态继续发布。

### M08 · 修订后脚本可运行
在隔离实例上跑 `tests/deep_ontology_o3o4o5.py` 与 `tests/deep_project_chain.py` 全链路，
确认修订未破坏既有断言（仅有预期的 O4-05b/A01/R02 等已知项与 O4-07a 阻塞）。

### M09 · 文档数字一致且无旧口径残留（S1/S3）
核对 `测试报告.md`、`缺陷清单.md`、`结果索引.md`、`测试计划.md` 的统计与缺陷口径是否互一致；
确认以下修正确实生效：
- Q03 记 71 pass + 2 known + 1 info（不再写「73 通过」）；Q05 有效批次 59 条（不再写「约 75 条」）；Q02 按批次 41 条。
- 旧数字只允许出现在「原表述 → 修订」对照行中。
- D-Q02-01 标题为「新发布本体无法通过现有历史快照入口恢复」，且明确**发布/版本列表/版本内容读取正常**、旧迁移 ZIP 仍可能恢复；不得读成发布整体失效或数据丢失。
- R02 为 **P1** 且写明保持理由与降级条件；R01 未被写成整页瘫痪、也未声称相关订阅检查正常；A02 未被用来宣布全图谱校验通过；Q01 为「回归完成，存在已知失败 mapping_forms」；跨账号结论含 D3 例外。
- D1/D3 未被写成越权成立，也未被通过改文档来消除测试失败。

### M10 · 范围与未测边界是否如实
确认报告仍显式保留未测项：视觉/宽度/缩放/截图/画布/纯键盘/断网、MySQL 路径、TTL 30min 等待、
发布不可变性破坏路径；Q06 性能**取消未恢复**且取消来源标注为未独立核实；`mapping_forms` 失败保留未修。

## 4A. F01–F05 整改核验（第二轮新增，必做）

对应独立复验记录 §3 的 F 项。**要求自行构造反例验证，不要只跑仓库自带用例**。

| 项 | 要核验的声称 | 反例（请自行构造并确认结果） |
|---|---|---|
| F01 | 拒绝分支同样受「版本零新增」约束；版本不可读 → test_error；拒绝却新增版本 → 独立缺陷 | 422 + 诊断命中 + 版本 1→2 → 必须是 new_defect_reproduced；before/after 传 None → 必须 test_error；3→3 → product_pass（对照）；不传 require_no_version_increase 的拒绝仍 product_pass（确认门是可选的） |
| F01 | `_count_versions`（`deep_reverify_scenarios.py`）读版本列表失败返回 None，不默认 0 | 用假 API 注入 HTTP 500 / 200 但 `items` 非数组 → 返回 None；`items=[]` → 返回 0 |
| F02 | `classify_validate_observation` 必须先过传输/状态码/响应结构 | validate 返回 **500 但文案完全正确** → 必须 test_error（绝不是 product_pass）；`errors` 非数组、响应非对象、status=None → test_error；200 空 errors → 缺陷复现；200 有目标诊断 → product_pass |
| F02 | R02 负对照的 validate 与 publish **两段都须满足期望** | 读 `scenario_r02` 第 7 段，确认只有两段都 product_pass 才记通过；不一致时记 test_error |
| F03 | 诊断只取约定诊断字段，不搜索整份响应 | `{"errors":["属性A 未绑定"]}` 必须取到文案；`{"error":"名称重复","state":{"kind":"flow"}}` 与 `{"error":"x","payload":{"type":"flow"}}` **不得**取到 "flow"；无诊断字段时不得回退搜索整份响应；内层 `kind/type/status` 值不得当诊断（真实 409 冲突载荷 `conflicts[].kind='flow'` 反例） |
| F03 | 反向偏差：`errors[].name/field/section` 承载定位时不得漏收 | `{"errors":[{"name":"activePower","message":"未绑定"}]}` 配两组诊断须 product_pass |
| F03 | 不提供诊断依据不得自动放行 | 空 terms 且未声明 `diagnostic_not_required` → test_error；显式声明 → product_pass（对照） |
| F04 | 一拆多必须全部子用例覆盖 | `O4-07` 只保留 O4-07a 或只保留 O4-07b → **必须报错**；a+b → 通过；等价编号（`CASE_ID_ALTERNATIVES`）任选其一 → 通过 |
| F04 | 残余豁免通道已封 | 业务场景名含 crash 但 kind 非工具错误 → 报错；有效批次仅 info 说明行顶替业务行 → 报错；被替代批次自身是说明行（如 O7-00）须在 §2.2 列出 |
| F05 | 历史执行记录已加失效标记 | 读 `测试计划.md`：历史区开头有显著失效提示并链接最新索引，旧数字仍在（未删除冒充修复） |
| 文档 | 四份文档数字一致且无旧口径残留 | 187 / 178 / 3 / 2 / 1 / 1 / 2 / 94 / 9 / 14 在各文档一致；旧数字只出现在对照行或失效历史区；单测数量按轮次标注（第一轮 23 → 第二轮 49） |

另外请**自行核对**：结果索引能否用 `tests/deep_results_index.py` 重算出一致数字（14 批次 / 被替代 94 行 / 187 记录 / 根因 9），
并与 `结果索引.md` 逐项对照；抽查任意若干行与 `.runtime/test-evidence/` 原始 JSONL 一致。

## 5. 判定与输出要求

- 结论用「通过 / 有条件通过 / 不通过」并在每项后给证据（命令、退出码、关键输出、文件行号）。
- **不得**把「测试工具跑通」当成「业务场景通过」；本任务只验测试交付的判定与证据质量，不要求业务缺陷先修复。
- 若发现生成器核对可被绕过、替代关系与实际不符、判定仍会把 5xx/无关 4xx/前置失败记成通过，
  或文档数字与索引不一致 → 判不通过并列出最小复现步骤。
- 输出格式：逐项 M01–M10 结论 + 缺陷/偏差清单（含复现命令）+ 仍存限制 + 明确「是否可交回用户」。
- 停止点：复验完成后即止 —— **不合并 main、不改业务代码、不清理非本任务环境**。
  复验结论写入您自己的报告或共享上下文记录；不要修改 `验收记录.md` 与实施者的交接条目。

## 6. 已知限制（复验时不应视为新问题）

- 本轮为**针对性复验**，不是全面重测：未重跑 kill -9 注入、未做全端点扫描、未做全量密钥字节扫描。
- 本轮未做浏览器复测（沿用既有 dist 产物，未重新构建）。
- `mapping_forms` 失败为基线既有，保留未修。
- 前轮 18931 实例仍在运行且未被本轮写入；前轮 `test-data*` 与 `.runtime/accept-evidence` 均保留未动。
