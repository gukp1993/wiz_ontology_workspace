# 系统全方位深度测试 · 继续验证与交付修订指令

请在已授权的 `codex/test` 工作树中，处理独立验收记录 S1–S3：修订测试判定、整理可追溯证据、完成必要复验，再交回 Codex。**本任务是完善测试交付，不是修复业务功能。业务缺陷仍存在，不妨碍测试报告经核实后交付。**

## 1. 工作位置与基线

- 主协调仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。
- **唯一执行 cwd**：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test`。
- 分支：`codex/test`；不新建 worktree，不切换到 main。
- 被验业务基线：`d6c73c29a19d97ad8da22ad3d8a8f3c0615e3b40`。
- 测试成果：`4d4e497`；独立验收：`53ddab0`。本指令另有文档提交，开始时记录实际 HEAD，不能机械要求 HEAD 等于53ddab0。
- 本需求目录：`文档/需求/20260921_系统全方位深度测试/`，以下文档均相对此执行cwd。

先执行共享上下文 `python3 .collaboration/context.py read --actor <你的登记身份>`，保存返回ticket；检查 `git status --short`、`git log`、`git worktree list`、`git diff --name-status d6c73c2..HEAD`。读 AGENTS.md、本指令、验收记录.md、测试报告.md、缺陷清单.md、测试计划.md、原执行指令.md及相关接口契约。业务文件有额外变化时记录实际差异，先确定是否改变测试基线；不得自动合并其他修复分支。

允许修改本任务的 `tests/deep_*`、新增本任务专属测试判定/汇总测试、测试报告/缺陷清单/测试计划实际记录及自己的共享交接。**保留验收记录.md原文、旧执行记录和原始证据**，不替验收者改结论。禁止修改业务代码、已有非deep回归测试、金样、正式产品需求、依赖声明/锁文件或真实数据。

## 2. 任务拆分和归属

先把以下任务登记到现有测试计划的“继续验证”区；补充实际owner、命令、结果及证据，不预写完成。无需新建开发计划。

| ID | 目标与交付 | 依赖/输入 | 文件归属 | 验证要求 | 并行安排 |
|---|---|---|---|---|---|
| V00 | 冻结基线、登记环境和证据协议 | 本指令、独立验收 | 协调者：测试计划 | cwd/HEAD/业务差异/端口数据根核实 | 串行 |
| V01 | 修正A01/R02/O4-06判定及结果分类 | V00、接口契约 | 测试owner：对应deep脚本及专属判定测试 | 正确阻断、缺陷复现、无关错误三类不会混淆 | 可与V02审计并行 |
| V02 | 盘点历史证据，定义有效批次与替代关系 | V00、原JSONL | 证据owner：独立清单；原日志只读 | 汇总可重算，无法归属项标未确认 | 可与V01并行，只交建议 |
| V03 | 用新环境复跑必要场景 | V01、V02稳定结果协议 | 协调者：新run目录与结果索引 | HTTP真实链路，前置成功，保存准确结果 | 启停/写入串行 |
| V04 | 统一统计、缺陷范围与分级 | V03 | 文档owner：三份报告/计划 | S1–S3逐项有回应，数字一致 | 串行 |
| V05 | 自检、提交、交接与清理本人临时环境 | V04 | 协调者：Git与context | 精确SHA/结果/限制，交回独立复验 | 串行 |

能用多agent时只并行V01和V02，同一文件同一时刻一个owner；协议调整由协调者同步。工具不支持多agent则顺序完成，如实报告。Git、环境启停、依赖准备、汇总文档统一单人串行；多agent不授权额外工作树或改变范围。

## 3. V01：修正判定，不能为了绿灯放宽条件

### R02：项目引用输出未绑定编排

检查 `deep_project_chain.py` P8：

1. 创建/读取/保存编排以及project-save都须明确成功；编排自身诊断确实含输出未绑定；项目引用正确flowId/outputId。前置失败标test_error或blocked，停止该用例，不拿旧状态继续发布。
2. 坏编排仍成功发布，且版本可读回：标 `known_defect_reproduced`，不是产品通过。
3. 正确拒绝：必须命中该依赖错误的协议诊断、状态码，并验证没有新增发布版本。当前协议没有明确到错误字段时先记录预期依据，不凭自创错误码判业务失败。
4. 500、无关400/422、401、网络失败及空响应不能标pass。
5. P8.3“没有拦截坏编排”与P8.4“仍发布成功”分别记录观察，不重复计算成两个独立根因，也不算产品通过。

### A01：非法规则/动作字段

检查 `deep_ontology_o3o4o5.py` O3-04：

- 合法对象与规则/动作关联作为前置；追加非法字段，避免替换定义造成悬空引用。
- 非文本值被接受、发布快照保留原值：标已知缺陷复现。
- 拒绝结果必须确实针对字段类型，并检查未形成非法发布；不能因为 `repro=false` 固定判fail。
- 身份、revision、其他引用错误或500只能记测试错误/独立异常，不能当成类型校验正确。

### O4-06和测试状态

“路由白名单没有直接改写入口”可记静态检查通过；“发布不可变性未实测”仍单列未验证，不能替代为动态验证通过。

统一结果至少区分：product_pass、known_defect_reproduced、new_defect_reproduced、test_error、blocked、not_tested、info。可兼容旧枚举，但报告必须明确映射。**测试工具运行完成与业务场景通过是两回事。**

为判定逻辑增加小型测试：正确阻断/实际缺陷/500/无关4xx/前置失败分别输入，验证输出分类。模拟响应只证明分类逻辑，不冒充真实HTTP业务修复证据。随后在当前未修业务基线上复现A01/R02，预计仍应归为known，不追求业务全绿。

## 4. V02–V04：证据、报告与缺陷定性

### S1：统计可追溯

- Q03原记录：project 49 pass+1 info+1 known，flow 23 pass；原“73通过”含info，必须纠正。修订后的分类再以实际结果重算，不把此旧数字直接当新结论。
- Q02原始日志追加了多轮，O4-07和O4-07a有替代关系。不能只取最后一条或只按case去重，必须说明哪个完整执行批次有效、哪些记录被替代以及原因。
- Q05历史crash与后续成功重跑并存。列清重跑覆盖，无法确认的记录标证据不足；不要把“约75条”“全部符合契约”保留为最终计数。

每次新跑写独立 `runId` 目录，不追加覆盖旧JSONL。结果索引至少含：runId、时间、业务SHA、脚本SHA（未提交运行时记录脚本哈希/工作区差异）、caseId、状态、证据路径/行号、替代记录及原因。统计明确按断言、场景还是缺陷根因；不同单位不相加。有效结果索引的脱敏摘要入库，原大日志留.runtime。

历史记录可追溯就沿用；缺哪项重跑哪项，不重做全套。如果原日志不存在，如实标缺失；不要依据报告倒造日志。最终三份文档数字一致，不能把已知缺陷、说明、未测算入产品通过。

### S3：具体文案与分级

- D-Q02-01：改为“新发布本体无法通过现有历史快照入口恢复”。明确发布/版本列表/版本内容读取正常；ZIP恢复工件与SQLite发布快照不同；旧迁移工件可能可恢复。不能写发布整体失败或数据丢失，不自动恢复已延期的版本管理改版需求。
- R01：保留启动错误事实，生产界面仍能操作。不要推广为整页瘫痪，也不声称全部检查订阅正常。
- R02：相对历史P1降为P2必须有影响与绕行依据；没有依据保持原P1，注明已知基线。
- A02：旧NodeEditModal未接入生产路径，不能用旧入口声称当前UI可绕过；也不能据此宣布全部图谱校验通过。
- D1/D3：文档与行为不同待裁定，未证明越权，不改安全策略或接口文档来消除测试失败。
- Q01列“回归完成，存在已知失败mapping_forms”，避免“通过（带失败）”。

Q06完整性能测试保持取消；本次不恢复压测。能找到原工具会话取消消息则补时间/消息定位，找不到标授权来源未独立核实，不要求用户重新回答。原UI视觉/画布/键盘等未测继续显式保留；本轮不以完成“全方位”标题为由扩展成全面重测。

## 5. 独立环境与验证边界

- 原18931/test-data是前次测试环境，先观察归属，不写入、不停止、不复用其账号。
- Codex的18941/accept-data已清理，**不代表现在仍空闲**。选新空闲端口并实际bind/lsof核对；不得用18765或8765。
- 在本工作树 `.runtime/` 下建立本轮唯一数据根和证据目录；不要覆盖既有目录。登记PID/cwd/URL/端口/数据根/Python/业务SHA/测试SHA到测试计划，并在公共Git目录 `workbench-tasks/` 按现有格式加锁登记资源。
- 所有命令显式cwd。设 `WIZ_WORKBENCH_ROOT=<本轮根>`、`WIZ_WORKBENCH_PORT=<核实端口>`，清除或隔离 `WIZ_DATABASE_URL` 及其他存储覆盖项，检查有效SQLite位置。仅合成账号/数据，不复制真实ontology、vault或密钥。
- 可以复用本树已准备依赖，不升级其他环境；新增依赖非本轮必要范围。若需浏览器复测，先确认dist来源；必要时本树build，避免与其他agent同时构建。新旧产物SHA要有记录，不能换修复分支掩盖失败。
- 新run隔离浏览器会话，避免同host不同端口Cookie相互覆盖；不向真实DB/Redis/业务接口发请求。
- 只需重跑受改判定影响的场景及证据缺口。已有mapping_forms失败保留，不改它；不要求其通过才能提交本轮测试交付。

## 6. 交付与停止点

1. 更新现有测试报告.md、缺陷清单.md、测试计划.md实际记录，追加本轮版本与S1/S2/S3处理表；不覆盖原独立验收记录。
2. 提交修订的deep脚本、判定测试、脱敏结果索引。记录实际执行命令、退出码、结果及限制。先检查 `git diff --check` 和 `git status`，只暂存本轮文件，不用全仓库git add。
3. 最终回复前通过 `.collaboration/context.py record`（stdin JSON）写 ticket/task/status/summary及验证/下一步，提交自己的entry与生成摘要，不手改session_context。身份用实际登记actor，不冒称独立验收者已通过。
4. 按PID/cwd/数据根确认并停止本轮临时服务，删除明确本轮新建且可丢弃的合成数据库；保留报告和脱敏证据。其他任务环境、18931、原日志及未知归属数据保留，不强删。无法确认归属则报告保留原因。
5. 最终报告：分支、业务SHA、成果commit、URL/已停止状态、清理/保留路径、S1–S3逐项结果、测试分类统计、仍存在的产品缺陷及未测项。

**停止在“修订与自测完成，交回Codex独立复验”，不自行把验收记录改成通过。** 不合并main、不push、不重启主工作台，不创建新修复worktree。

流程边界：本任务复用已授权环境，无需再次申请创建；如以后用户明确要求集成，仍由Codex依据精确已验SHA串行协调集成队列，普通冲突自行处理并组合重验，产品冲突交用户决定。验收通过不隐含合并授权。main脏不stash/reset，main更新/真实数据库迁移另按明确授权；回退用revert，数据另处理。集成成功后才按AGENTS清理对应工作树/已合并分支，本次不执行该阶段。
