# 仓库架构整理与代码规范 · 开发计划

## 1. 任务与环境登记

| 项 | 值 |
| --- | --- |
| 任务ID | repo-cleanup-20260921 |
| 执行者 | zcode（用户明确指定本工具执行） |
| 分支 | codex/repo-cleanup |
| base SHA | b4a56a9（创建时最新已提交 main） |
| 工作树 | /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/repo-cleanup |
| 预留端口 | 18941（本任务纯文档/配置/规范，未启动服务；如需隔离实例再用） |
| 数据根 | 工作树内 .runtime/（未初始化；不触碰真实 ontology/ 与 data/、keys/） |
| 运行命令 | python3 tests/run.py（回归验证用，隔离临时根由测试自建） |
| 共享文件归属 | AGENTS.md、README.md、.gitignore 本任务独占修改（main 侧他人仅 session_context.md 未提交渲染，不触碰） |
| 当前状态 | in_progress |

## 2. 需求（用户 2026-09-21 指令）

1. 文件夹太多太乱，整理，没用的删除。
2. 文件太多太臃肿，优化精简。
3. 代码规范化，参照 GitHub 上的 Python 与 Vue 代码规范。

## 3. 盘点结论（实施前实证）

- tracked 文件分布：ontology/ 991（其中 drafts 956、releases 27、projects 5、workspaces 2、catalogs 1）＞ 文档/ 294 ＞ .collaboration/ 196 ＞ frontend/ 151 ＞ tests/ 118 ＞ workbench/ 88 ＞ .idea/ 5。
- `ontology/models/storage/`（演示基线静态资源）**磁盘上不存在、git 未跟踪**：演示链路本地已处于优雅降级态（demo_available=False、版本种子守卫返回 None），untrack 全树对运行时零影响；transfer 导入有 is_dir 守卫。
- 文档/ 备份/ = 2026-09-15 目录迁移时留存的 0914–0916 旧文档树快照（59 文件，绝大多数在现行结构中无副本），命名误导；AGENTS.md 引用的 3 个根级文档（设计方案_v3、迁移清单、架构优化实施说明）与 8 个 交付物/*.md 实际都只存在于 备份/ 中——**当前 main 上这些引用已断链**。
- 文档/imp/ 仅 1 文件，与 备份/ 中同名文件重复。
- .idea/ 5 个 IDE 配置文件被 git 跟踪。

## 4. 实施方案

### A1 文档/ 重组（移动为主，不删历史内容）

- `备份/` → `历史归档_20260916前/`（git mv，内容原样）。
- 从其中**移动**（非复制）AGENTS 仍引用的文件归位：设计方案_v3、迁移清单_20260915、架构优化实施说明_20260915 → 文档/ 根；8 个交付物 md（V3实施方案说明、会话记录与演示指南、通用属性来源配置×2、时间序列、工作台整体交互迭代×2、项目属性取值规则）→ 文档/交付物/。
- 根级 10 个散落 md 分组：评审/审查类 6 个 → `评审与纪要/`；概念/说明类 4 个 → `概念与说明/`。
- 删除 `imp/`（重复副本，git 历史可找回）；`导出/`（用户导出/导入测试物料）保留不动。
- 需求/ 32 个历史需求目录按既有归档规则不动。

### A2 ontology/ 退出 git 跟踪（本地文件保留）

- `git rm -r --cached ontology/`：991 个迁移输入/备份文件退出版本库，**磁盘文件全部保留**（含 vault/）。
- .gitignore：`ontology/vault/` 规则改为 `/ontology/`（整目录）。
- .idea/ 退出跟踪（git rm --cached）+ .gitignore 增加 `.idea/`。
- 效果：仓库 tracked 文件 1758 → 约 765；合并后 main 工作树磁盘数据不变。
- 风险与边界：真实用户数据零删除；新克隆机无旧文件树，transfer import 已有目录守卫；tests 中引用 `REPO/ontology/models/storage` 的用例本就处理缺失（test_project_api_roundtrip 文档注明）。

### A3 文档引用同步

- AGENTS.md：目录节（文档/ 新结构、ontology/ 新状态）、存储规则 §2 与 §13 补注「文件树已退出 git 跟踪（20260921），本地保留作迁移备份」；参考文档节路径经归位后自然恢复有效。
- README.md 目录表同步。

### B 代码规范（参照业界标准）

- **Python**：PEP 8（github.com/python/peps）+ Google Python Style Guide（github.com/google/styleguide）为基准，工具用 Ruff（github.com/astral-sh/ruff）。`ruff.toml`：target py39、line-length 120（明确登记的偏离：E501 不启用避免大范围重排）、select 默认核心集（E4/E7/E9/F）。机械修复违反项；`requirements-dev.txt` 登记开发依赖。
- **Vue**：Vue 官方风格指南（vuejs.org/style-guide，规则 A–E 分级）+ eslint-plugin-vue（github.com/vuejs/eslint-plugin-vue）flat/recommended；TypeScript 侧沿用既有 vue-tsc 严格检查。新增 eslint.config.js 与 npm script `lint`，机械修复可安全项。
- 规范要点写入 AGENTS.md（前端约定/后端约定增补「代码规范」小节），不重写既有代码风格（如中文注释、单行紧凑写法），只约束新增/修改代码。

## 5. 验证

- 后端：python3 tests/run.py quick + unit（全量 all 视耗时补跑）；金样回放不变（无业务逻辑变更）。
- 前端：npm run build（vue-tsc 类型检查）通过；eslint 0 error。
- ruff check workbench tests 0 error。
- grep 复核：AGENTS/README 无断链文档引用；代码无对已 untrack 路径的新增依赖。

## 6. 交付与停止点

- 提交到 codex/repo-cleanup，交付 SHA 与文件清单，交 Codex 独立验收；**不合并 main**（含 ontology untrack 等决定请用户在验收/集成时确认）。
- 主工作台 18765 不重启不更新（本任务无运行时行为变更）。

## 7. 整改记录（2026-09-21，验收 B 项不通过后）

**验收结论**：独立验收记录对 HEAD `6f61f6c` 判定不通过——B 项「ontology/.idea 退出跟踪」实际未发生（索引中仍 991+5 个文件），A/C/D 通过。

**根因（如实记录）**：
1. 实施时 `git rm -r --cached ontology/ .idea/` 曾正确完成并暂存，但随后按主题拆分提交前执行了 `git reset -q`，把删除暂存整体撤销（索引从 HEAD 恢复，文件回到已跟踪态）；后续 `git add -u` 只暂存"已跟踪且有变更"的文件，删除操作无法经它恢复。`.gitignore` 新规则只对未跟踪文件生效，因此提交树里 991+5 个文件始终保留。
2. 拆分提交时 `git add -u` 把全部 lint 机械修复带进了 d7f79ce（其提交信息只声称退跟踪），ec0c0aa 仅剩 eslint.config.js——提交信息与内容严重错位（验收"附带交付质量问题"所指）。
3. 自验在 reset 之前做过 `git diff --cached` 断言，reset 后未复测 `git ls-files`，导致核心未交付未被自验发现。

**整改（提交即本节所在分支新提交）**：
- 单独提交补做 `git rm -r --cached ontology/ .idea/`（996 文件出索引，磁盘保留），提交后实测：ontology tracked=0、.idea tracked=0、tracked 总数 1860→864、磁盘 ontology 仍 991、data/keys diff=0。
- 数字更正：基线 tracked 实测 1852（此前提交信息所写 1758 有误）；855 的来源是计划期按 1758 估算，正确值 = 当前 HEAD tracked − 996。
- 验收指令期望值同步修正：`交付物/*.md ≥12` → 8（基线交付物顶层本就是 4 个子目录 0 份 md，8 份全系本轮归位）；`git ls-files | wc -l ≈855` → ≈864。
- 历史提交不改写（本地未合并分支允许，但验收以最终树为准）；错误数字以本节与本整改提交信息为准。
