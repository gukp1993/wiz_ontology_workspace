# 自动化构建续会 6 纪要评审（meeting-47）

日期：2026-09-21。

评审对象：[meeting-47-export.md](/Users/gukepeng/Downloads/meeting-47-export.md)（本体自动化构建 · 续会 6，会议状态 awaiting_confirmation，导出时间 14:14，用户未发言）。

对照依据：

- [自动化构建续会5终版纪要评审_20260921.md](自动化构建续会5终版纪要评审_20260921.md) — 本次 会议的材料即该评审（材料版本 4b6c07b5a4d8，31 段）
- [从物料自动构建本体：需求说明](需求/20260920_从物料自动构建本体/需求说明.md) 与已合并实现 main ab27442（`workbench/ontology_build/`）

本文归档对续会 6 纪要的评审意见，不修改既有需求，不构成实施授权。会议中的 AI 共识不视为正式产品决定；文中建议均待用户确认。

## 一、整体判断

**这是系列会议的转机轮：材料换成终版评审后，四个角色正面接收了全部核心发现（差异核对、R10 断言、确认机制圈定、三表汇总），并收敛出一套质量较高的落地方案（源码事实表 + 确认记录表 + 回放验证），"清单静默丢项"第一次有了结构性防复发手段（前置完成条件 + 三态状态值）。本评审顺手闭合了会议留下的三条源码验证（R02 晋升输出、R10 幂等、候选路由），事实表七条代表条目的源码结论至此全部可得。遗留问题在纪要导出侧：truncated 第四次复发、结构化区第四次全空。**

另需点明一个现实：会议设计的核对流程已经比核对本身复杂一个数量级（详见第五节）。在本项目侧，用户可直接指令完成事实表，不需要等会议侧角色开工。

## 二、上轮评审核心发现的接收核对

| 终版评审提出 | 续会 6 回应 | 判定 |
|---|---|---|
| 差异核对：三类归类（文档化/试点收窄/增量需求） | 完整采纳并升级为流程：先"源码事实表"再归类再拍板（发言 1、4）；palantir 精准复述"试点收窄是验证范围的物料组合，不是能力分期——前者试点通过即可放量，后者还有一整期开发要等"（发言 3） | 接收到位，理解准确 |
| 第二类措辞约束 | 产品评审员钉死句式标准："不允许'代码适配器为二期首项'这类能力分期句式，只允许'一期试点用 DDL+业务说明验证'这类验证范围句式"（发言 33、37），并延伸到放量触发条件（"验证通过即可用"，不得写"等二期开发完成"） | 超出评审要求的自觉强化 |
| R10 断言矛盾补记 | W11 划清边界（文档修订不进事实表，但代码断言结果可作证据）；产品评审员立为"差异清单提交拍板前的硬性前置完成条件"，归属暂空、闭环判定不空；最终以三态状态值（未认领/已认领/已完成）+ 粗版交付附"文档侧待办状态：未认领"防第三次丢失（发言 25–27、41–43） | 处理方式成熟，是本系列第一次对"静默丢项"建立结构性防复发 |
| 业务方确认机制适用范围（连续两轮遗漏） | 与 R10 并列为前置条件②，"已连续两轮遗漏"被明确点出（发言 25） | 接收 |
| 哈希口径与确认失效绑定 | 显著深化：palantir 给裁决判据（确认对象是语义结构还是文档原文，发言 19）；储能fdev 补"拍板后附失效量级回放"（发言 20）；W11 识别依赖环（拍板→回放→归一化规则→拍板）、palantir 解环——朴素归一化已是完整等价关系，raw_bytes 失效数为上界、朴素归一化为下界，白名单只影响区间内部（发言 22–23） | 收敛质量高，解环的数学 reasoning 正确 |
| 三份待确认表汇总 | palantir 提映射（发言 15）、储能fdev 升级为数据血缘式映射（来源表+来源行+关系类型，允许一对多，发言 16）、"升级为增量"须显式保留旧决定原文（发言 17，"升级不等于推翻"） | 完整采纳并工程化 |
| 导出工具结构化区缺陷 | 进后续问题第 5 条，建议向工具方反馈（发言内容有对应讨论） | 接收，但本轮导出自己又复现（见第五节） |

诚实度方面值得肯定：产品评审员开场即声明"我没有实际打开过仓库，以上对源码的引用均来自本评审材料第五节，作为讨论输入处理"（发言 1）——证据层级标注正确；储能fdev 确立"以 ab27442 实际代码为准、出入单独标注，不反推材料对错"（发言 6）。

## 三、会议新增方案的质量评价

**粗版事实表**（发言 6–18、31–34）：只读核对、基线 commit + 核对时间绑定、正反判例（falsification，"搜到即判伪"的可复现动作）、来源层级标记（代码本体 vs 注释）、基线漂移单独重跑——设计扎实，边界自觉（"事实表阶段不做设计"被划线，palantir 的 Reference 建模落点只记附注）。七条代表条目三类各至少一条。

**确认记录表**（发言 28–29、44）：palantir 对"清单静默丢项"的根因诊断准确——确认散落在纪要、表格、评审意见三处、各自独立失效；最小落法（确认对象定位+版本基线为主键、追加式更新、旧记录置失效不删除）是合理的最小治理，且预留了与候选生命周期状态模型的同构映射。

**候选路由核对**（发言 47–54）：palantir 的"按物料语义承载能力路由"假设（文档→对象/属性，DDL/代码→链接/动作）是本轮最有价值的新设计视角；三层证据方案（生成侧 protocol.py / 对齐侧 alignment.py / 确认记录分布）配了两条边界——判定标准钉为可观察行为差异（防"有个 if 就算路由"的假阳性）、结论可基于一至两层独立成立且确认层缺失如实标注（防为凑三层翻日志拼凑、防范围膨胀）。中性措辞要求（"现状描述 + 增量方向"，不写"缺陷"）保持了全表基调一致。处理得当。

**方案叙事**（发言 35–37、55）："能力已建（ab27442 全量物料线+五类候选+确认/晋升协议）、治理待补（哈希口径、确认失效、留痕）、试点验证后放量"——定位正确，且放量触发条件被钉在验证域句式上，防止开头叙事对、后面又滑回分期表述。

**物料质量分层**（发言 45–46）：储能fdev 的业务洞察真实（文档侧口径模糊、术语不一致），分物料设通过率 + W11 补"表述不一致已走两侧保留人工确认，不会静默产出错误候选，只会推高确认量"——与现行实现对齐。

## 四、本侧直接闭合：事实表三条待验证项的源码结论

会议把 R02 晋升输出、候选路由、R10 断言列为"待源码核对"。本评审在 main 当前代码（d10c412，含 ab27442 全部业务代码）上直接验证，**事实表七条代表条目的源码结论至此全部可得**：

| 事实表条目 | 源码结论 | 证据 |
|---|---|---|
| R02 晋升输出不含 candidateId | **满足**。`ontology_adapter.assemble` 为每个候选分配类型前缀的全新正式 id（`_new_id(ID_PREFIX[type])`，与 candidateId 不同源），`build_node` 以正式 id 组装 @graph；`ontology`/`workflow` 核心结构不含 candidateId | `ontology_adapter.py:140-148`；candidateId 仅出现在 precheck 阻断清单的错误定位（`delivery.py:115,133`，任务侧反馈） |
| R02 映射留痕位置 | 候选→正式 id 映射存于资产 `summary.candidateMap`（与 taskId/batchId 同层的运维元数据），**不进核心 schema**——两层方案已落地 | `delivery.py:295-303` |
| R10 重试不重复创建资产 | **满足且实现完整**：同 requestId + 内容指纹（本体名+选定集合）一致→返回原结果（replayed）；同 requestId 不同内容→409 REVISION_CONFLICT；已交付且不同 requestId→409 ALREADY_DELIVERED（一任务一交付）；回执检查先于 stale/token 校验；路由层 `write_tx()` 事务原子创建 | `delivery.py:232-307`、`ontology_build_routes.py:702-713` |
| protocol 五类候选 | 已实现（前轮已核） | `protocol.py:69` |
| alignment 同名保留 | 已实现（前轮已核） | `alignment.py:131` |
| "代码适配器为二期首项"（第二类样本） | `parsers/code.py` 已存在（前轮已核）→ 归类为试点范围收窄表述 | `workbench/ontology_build/parsers/` |
| 候选路由核对 | **生成端五类全量、无显式路由**。protocol.py 仅有常量与合法性校验；pipeline.py 的 `ctype not in CANDIDATE_TYPES` 是非法类型过滤；alignment.py 的 materialId 仅用于证据组展示，无候选类型排除/降级。按会议钉的判定标准（可观察行为差异）：**无路由雏形**，palantir 的语义承载路由归第三类纯增量需求。注：候选由 LLM 按证据生成（llm.py），可能存在事实上的软分布，但代码无强制路径 | `protocol.py`、`pipeline.py:337,420`、`alignment.py:284-299` |
| 确认失效机制/哈希口径（第三类） | 未见实现（前轮已核）——纯增量需求属实 | 全模块 grep 无 stripped_dump/raw_bytes/失效机制 |

两点精确化：(1) R02 断言的准确措辞应为"核心 schema（@graph + workflow 定义）序列化输出不含 candidateId；映射留存于资产 summary.candidateMap（任务回溯元数据）"——断言写得太宽（"晋升输出不含"）会把合法的 summary 留痕误判为违规；(2) "重新生成时 candidateId 不复用"这半条本轮未验证（候选 id 生成方式未核），事实表批量阶段补验。

**R10 的实现结论改变了会议待确认建议第 6 条的性质**：断言挂回 G12 测试集不是"补缺口"而是"固化既有正确行为"，工作量极小，无实现风险。

## 五、新问题

1. **truncated 第四次复发**：发言 68 orchestrator 标注 truncated，纪要"五、后续问题"截断于"6. 储"（附录摘要同处截断，同源）。会议基本信息里"均为 done，无 interrupted/truncated"对对话部分属实（1–67 无截断），但纪要本体截断——导出整体状态与声明仍不完全一致。丢失内容估计仅半条（储能fdev 的物料质量抽样相关），损失小，状态问题真实。
2. **结构化区第四次全空**：顶部"待确认建议（无）""行动项（无）"仍与附录矛盾（附录有待确认建议 8 条、明确认领与交付口径）。
3. **会议流程过度精细化的苗头**：为核对一份差异清单，会议设计了映射血缘、三态状态、正反判例、画像四件套、基线漂移协议……核对方案比核对本身复杂一个数量级。公平地说，会议角色的约束是"没有实际打开过仓库"（发言 1 自认），只能设计流程交 W11 执行；但在本项目现实里，事实表核心条目已可由本侧直接闭合（第四节），**用户不必等"授权 W11 产出粗版"——直接指令本侧出完整事实表 + 收尾文档修订即可**。确认记录表值得保留，作为收尾文档的一张真实运行的表落地。
4. **W11 仍是会议系统内角色**（glm-5.3-flash）：其"提交代码快照""run_check"是会议侧工作流，与本项目 main 无关。角色们对此已有自觉（待确认建议第 6 条明确"动仓库文件需单独授权走 worktree 流程"），本评审予以确认。

## 六、建议

对会议 8 条待确认建议（附录第四节），本侧处置意见：

1. **第 1、2、4 条（差异清单、两条前置补记、汇总表）**：可立即执行，本侧可直接产出——差异清单的源码结论已齐（第四节），汇总表按血缘映射规则合并三份表，两条前置补记在收尾文档中完成。
2. **第 3 条（授权 W11 粗版事实表）**：不需要——事实表由本侧直接完成更可靠（会议侧角色无法访问仓库）。
3. **第 5 条（哈希口径裁决+回放）**：按会议方案执行——判据（语义结构 vs 文档原文）正确，回放上下界方案成立；裁决权在用户。
4. **第 6 条（断言挂回 G12 测试集）**：值得做且工作量极小（R02/R10 行为已正确，写断言固化即可）；按流程走 worktree 授权。
5. **第 7 条（确认记录表）**：建议采纳，作为收尾文档的拍板输出格式直接落地使用。
6. **第 8 条（增量需求立项）**：候选路由（语义承载能力路由）、哈希口径机制、确认失效、业务方留痕、缺口标注、来源枚举——如用户采纳，按需求归档规则立 `文档/需求/` 新条目并走 worktree 开发。

## 七、结论

续会 6 是这个系列第一次"讨论质量与事实对齐"的会议：核心发现被完整接收，落地方案（事实表、确认记录表、回放验证）设计成熟，防丢项机制结构化。本侧已顺手闭合三条待验证项，事实表全部条目源码结论可得，收尾文档的障碍只剩文档动作本身（汇总表、两条补记、叙事改写）。用户现在拍板的有效输入是：本评审第四节事实表 + 会议 8 条建议的处置意见。所有事项最终决定权在用户。

本轮仅评审并归档意见并做只读源码核对，未修改既有需求、代码或用户数据。
