# 原型对齐验收记录（2026-09-15）

执行依据：`文档/交付物/GLM执行指令_严格按原型修正交互_20260915.md`（T00～T08）。
交互基准：`文档/交互基准/本体工作台整体迭代交互原型_v1.html`（SHA-256 42f621…4286）。
当前地址：**http://127.0.0.1:18765/**（8765 为其他服务，未占用）。`./start.sh restart` 已完成。

## 1. T00～T08 完成情况与实际改动文件

| 任务 | 内容 | 改动文件 | 状态 |
| --- | --- | --- | --- |
| T00 | 表单守卫契约、离开保护、form-save 提交助手、Saver.clearFailed | `app/formGuard.ts`（新）、`app/saveCoordinator.ts`、`App.vue` | ✅ |
| T01 | 深色侧栏（#142a40/#dce6f2）与原型比例（174px 侧栏、157px 目录+16px、22px/20px/10px/16px、14px 正文、21px 页标题、11px 10px 表格、7px 输入圆角；850px/620px 响应式） | `style.css`（就地修改既有规则，无叠加覆盖层）、`app/navigation.ts`（p-upgrade 视图） | ✅ |
| T02 | 对象浏览/对象定义/链接定义：编辑态整体替换主内容、prompt 与占位创建全删、链接双向清单 | `ontology/ObjectWorkspace.vue` | ✅ |
| T03 | 无目录独立属性表单、共享库列表+“为对象添加属性”挑选态、新建不预插空记录 | `ontology/PropertyManager.vue`、`SharedLibrary.vue` | ✅ |
| T04 | 实例识别只读摘要+独立表单、补充来源折叠、链接映射两端登记表下拉 | `project/ProjectBinding.vue`、`ObjectSources.vue`、`LinkMappings.vue` | ✅ |
| T05 | 属性取值三步向导（选择来源/定位记录/映射结果） | `project/PropertySources.vue` | ✅ |
| T06 | 契约与实现：列表→独立编辑表单、本地草稿+form-save | `ontology/FunctionManager.vue`、`project/ImplementationManager.vue` | ✅ |
| T07 概览/参数/版本 | 项目概览三步清单、空参数可维护、版本引用独立页（先比较后确认） | `ontology/OntologyHome.vue`、`project/ProjectHome.vue`、`project/ProjectVersion.vue`（新） | ✅ |
| T07 连接 | 连接列表态+独立配置表单、真实测试状态机、vault 流程 | `project/ConnectionManager.vue` | ✅ |
| T08 | 两区发布页三区块（问题清单/发布/历史）、“去处理”定位、hasDirty 发布拦截 | `ontology/OntologyRelease.vue`、`project/ProjectValidation.vue` | ✅ |

并行实施：集成人（T00/T01）先冻结契约与样式，随后 5 个负责人按文件所有权并行（本体页/项目映射/契约实现/概览发布/连接），无同文件冲突。

## 2. D01～D10 差异消除

| 编号 | 消除方式 | 证据 |
| --- | --- | --- |
| D01 深色侧栏 | style.css 就地改 | V01 截图（1280×800 目视确认） |
| D02 属性编辑多重目录 | 编辑态整体替换主内容（`ui.editor?editorView():pageView()` 结构） | V03 交互验证：点新增属性后目录/详情消失，仅"← 返回对象"+表单 |
| D03 小弹窗/prompt/先创建 | 全部改一步进入完整表单，保存前零写入 | 行为验证：新建属性填一半取消，服务端 revision 不变、无残留 |
| D04 无保存取消/自动保存 | 本地草稿+底部保存/取消；打字不触发 touch；保存走 form-save 一次落盘 | 行为验证：保存后 API revision 变化、顶栏"已保存" |
| D05 共享库丢上下文 | ObjectWorkspace 内挑选态“为「对象名」添加属性”，复制/引用两动作 | 源码级（SharedLibrary 挑选态在 ObjectWorkspace 编辑态内） |
| D06 链接仅起点 | 链接页签展示起点∪终点，一行一个三元组 | 源码级（linkRows 过滤 domain∪range） |
| D07 实例识别默认大表单 | 默认只读摘要（连接/身份表/主键+修改），补充来源折叠，说明文案已换 | V07 快照：摘要+折叠+“识别有哪些对象实例，不限制属性可以从哪里获取。” |
| D08 概览重复列表/版本折叠 | 项目概览只留当前项目三步清单；版本比较移独立页 p-upgrade | V06/V14 快照 |
| D09 空参数禁用 | 维护参数无禁用条件，可新增首项 | V06 快照：“还没有参数；也可以先不配置”+可点维护参数 |
| D10 契约/共享旧布局 | 均改列表→独立表单 | V05 快照：契约列表+编辑态（返回契约列表+紧凑输入输出） |

## 3. 交互与保存验证（隔离实例 18900 + 临时数据目录，未碰真实 ontology/）

已实测通过：
1. 新建属性→填一半→取消：revision 不变、列表无残留、无空记录（7.2-1）。
2. 编辑属性→保存→API 读回："循环寿命"已持久化；编辑器关闭、顶栏"已保存"、无二次保存（7.2-2）。
3. 编辑中切菜单：统一对话框“继续编辑/放弃本次修改并离开”；继续编辑停留编辑器；放弃后草稿不落库（7.2-3）。
4. 集成期发现并修复：组件端“放弃后守卫注销未生效”导致残留 dirty 守卫持续拦截导航/刷新——App 在放弃路径统一收口移除被放弃守卫；修复后回归通过（放弃→pill 熄灭→后续导航无弹框）。
5. 实例识别：启用对象→表单（连接→表→主键目录联动）→保存→摘要态显示（7.2-11 部分）。
6. 三步向导（Redis GET）：来源卡→来源选择→Key 模板+{id} 参数绑定（预览展开 `GET m_storage_cluster-123-soc`，标注不读取 Redis）→类型核对→保存→API 读回完整 redis 配置（params.id=primary）（7.2-8）。
7. 向导中断恢复：取消向导→去登记 Redis 补充来源→返回属性取值重开，上下文恢复（7.2 相关）。
8. V12 连接列表态：名称/引擎/地址/测试状态/凭据状态/测试/配置/删除 + 折叠目录。
9. V14 版本页：当前引用只读、双下拉、“下拉选择只改变比较对象”、同版本禁用比较、先比较后确认。
10. V13 项目发布页三区块齐全（检查/发布/历史+属性使用范围只读块）。
11. 构建门禁：全量 `vue-tsc --noEmit` 零错误 + `npm run build` 通过。

未完成/未验证项（如实声明）：
- **原型页同尺寸截图对照未做**（file:// 原型浏览器访问受限，指令 §7.1 允许的替代路径）：实页截图仅 V01/V02 级别（.runtime/ui-alignment/），V03～V14 以 DOM 快照/行为断言验收，“同尺寸视觉验收”仍列为未完成项。
- 链接映射双向/换表清字段（V10）与函数来源向导路径未逐项实测（源码级核对，Agent-B 自测通过）。
- 保存失败/409 表单不关闭路径未实测（form-save 失败回滚逻辑源码实现，未注入故障）。
- 页内（切对象/页签）离开确认使用原生 confirm、App 级用统一对话框，两套并存（Agent-B 报告声明）。
- 跨本体版本比较受真实 API 限制（upgrade-check 仅同本体两版本），页面如实说明边界。

## 4. 固定例外落实（§3.1）

- 顶部无二次保存：表单保存即落盘；发布统一进“校验与发布”页。✅
- 无示例数据/硬编码；无“已执行验证通过”假状态（文案均为“配置校验通过（未执行业务数据验证）”）。✅
- 契约保留多输入多输出与 object/property/base 引用；未截断签名。✅
- 原生 select 未回归（AppSelect 保留）。✅
- **项目属性使用范围排除：未实现**（ProjectValidation 只读范围清单+“排除功能暂未实现”说明块；无假开关、未放松校验）——已知功能缺口，单列。⚠️

## 5. 剩余问题与建议

1. 守卫收口为 App 级兜底；组件端注销未生效的根因（疑似 v-if 卸载时序与 watch 竞态）建议后续专项定位。
2. 从对象页进入契约时的对象预选 focus 参数（App 一行接线）未做，契约表单内适用对象仍为多选，无协议限制。
3. 实页与原型的逐页同尺寸视觉对照（1440×900 / 1280×800 / 850px / 620px）建议在原型可访问环境补做。
