# 本体生成控输出：最终执行指令 v3

日期：2026-09-22；任务ID：`auto-build-output-v3`。本文为用户交给实施harness的唯一派发入口。技术规格和唯一任务表在同目录[整合方案与并行开发计划v3](本体生成控输出_整合方案与并行开发计划_v3.md)。本轮仅交付文档，未实施预算/codec/试验代码。

## 1. 可直接复制的总指令

> 在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/auto_build`（`codex/auto_build`）开发“本体生成控输出v3”。以同目录《本体生成控输出_整合方案与并行开发计划_v3.md》为唯一技术基线，按其D00–D19任务表实施，替代旧两组v1及试点v2的执行口径。
>
> 先读取共享上下文、核对实际HEAD、R1–R5修复提交及在途修改归属，冻结内部/HTTP契约和文件owner，再用多个子agent按依赖与可用槽位滚动并行。无写入冲突的独立任务尽量并行；共享热点、Git操作、依赖安装、构建与启停由协调者串行处理。不要给多个agent同一份文件的不同片段同时写。
>
> 完成共用预算/规划/单次调用/codec/恢复执行核心、在线checkpoint与实验SQLite两种存储适配器、独立A/B/C/D试验，以及在线后端和前端接线。两条路径共用核心，禁止实验目录复制一套调度器。先通过假模型和恢复契约，再做分支隔离联调；真实小样本效果独立评估，缺模型或额度不足不阻塞其他可验证开发。
>
> 输出上限、调度覆盖、语义质量和总Token成本分别报告。compact不达收益/质量门槛时保留原协议搭配自适应分批，不自动调参或换模型掩盖结果。所有开关缺省关闭，只在临时隔离实例开启；不自动切换18890含真实数据的服务，不更新18765。
>
> 执行开发—测试—修复循环，完成相关回归、前端构建和隔离浏览器验收后提交本任务改动，交付实际分支/SHA、URL/ROOT、测试和试验报告、默认关闭状态及未验证限制，交Codex独立验收。停在待验收，不合并main，不push，不迁移真实库，不清理含真实数据的工作树。不要只提交方案或只完成独立CLI就声称整项完成。

## 2. 环境、授权及停止点（自包含）


主仓库（协调位置）：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。
实际开发 cwd（已授权并登记）：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/auto_build`，分支 `codex/auto_build`。不得回主目录修改业务代码。

| 登记项 | 本轮实际核对/接手要求 |
|---|---|
| worktree创建基线 | main `578acd7d47e2e457757e088110f2582ae261cbb0` |
| 本轮核对的接手参考 | `8d38f63`（含修复提交`22a60e5`）；本次文档提交后HEAD会再前进，实施前另记实际SHA |
| 基线与在途交接 | R1–R5已由原执行者提交`22a60e5`，其自测声明不等于Codex验收。接手先核对修复差异与当前status/owner，复用已实现机制、不重复整改；仍有他人未提交交接时保留，不代为提交或覆盖 |
| 端口 | 登记18890，本轮未重新验证服务/PID/端口归属；使用前检查，不抢占服务。main保留18765，禁用8765 |
| 数据根 | 工作树根；`data/workbench.sqlite3`、`keys/wb-root.key`、`data/ontology-build-blobs`，含真实数据与密钥副本，不可自动丢弃 |
| 环境 | 登记为system Python 3.9、工作树自己的node_modules；依赖/Python实际路径接手复核，变化时用独立环境 |
| 全仓登记 | 用 `git rev-parse --git-common-dir` 解析公共目录中的 `workbench-tasks/auto_build.json`；串行/加公共锁登记owner、范围版本、端口和状态，不写密钥 |
| 负责人 | 用户指定的实施harness为协调者，派发时填写真实owner；Codex独立验收；获集成授权后Codex串行集成 |

标准提示词与停止点：

1. **仅创建环境**：“为【需求】创建worktree，从最新已提交main建立独立分支与目录，登记端口和数据目录。先不要开发。”收到明确创建指令才执行；新树必须在主仓库`worktree/<任务名>`，分支默认`codex/`，main脏内容不复制。先看status/worktree/main/remote，无remote不pull。新开发或临时集成树需用backup CLI生成main库一致快照，复制根密钥与运行资产，数据根在新树内，登记不可自动丢弃；不得cp正在写入的SQLite库。创建指令本身不授权安装、启动、开发。
2. **开发**：“在【需求】已登记worktree中，按【执行指令】开发、自测并提交，交Codex验收，不合并main。”本需求默认复用auto_build，不需要新建树；确认文件owner交接后开发，停在待验收。新开树仍需明确授权。
3. **验收**：“验收【分支】最新提交，不合并main。”记录精确SHA，只读评审/隔离测试；通过停在待用户授权集成。
4. **修复**：“在原worktree修复验收问题，自测并提交，不合并main。”复用原树，交新SHA复验。
5. **集成**：“将【需求/分支】验收通过提交集成并合并main，处理普通冲突并重验，成功后清理对应开发环境；不重启主工作台。”只有此类明确授权才进入串行集成队列；允许在主仓库worktree下建临时集成树，合入精确已验收SHA，做组合重验。普通实现冲突自行解决、需求取舍请用户决定。main必须干净且仍为记录基线才ff-only更新；main前进则重新组合受影响验证，不自动清场。
6. **主服务更新**：“将已合并main构建并更新主工作台；如需真实数据库迁移先说明具体操作。”代码合并不等于主服务更新；不隐含真实库迁移授权。

分支启停一律从实际cwd执行，显式指定 `WIZ_WORKBENCH_PORT=18890 WIZ_WORKBENCH_ROOT=<实际工作树>`，清除或改正继承的`WIZ_DATABASE_URL`，先核对PID/cwd，不能误停其他任务。优先分支后端托管本树dist，未经核对的vite代理可能指main。浏览器使用独立会话。自动化测试必须临时隔离根、合成数据、独立端口，不使用该真实副本。

回退用`git revert <sha>`，代码回退不恢复数据库。集成成功保存交接后默认清理对应已合并开发/临时树、本人服务/worker、专属依赖与明确可丢弃测试数据；含真实数据副本、未提交/未合并内容、其他执行者仍写入或归属不明时保留，报告具体原因并等数据处置决定，禁止force绕过。不得删除整个worktree父目录。最终报告分支/SHA/URL/验证、main合并提交、清理路径/端口、保留原因、主服务是否更新。每阶段通过context.py read/record交接，提交只含本人文件与脚本生成摘要。


## 3. 接手与需求优先级

从上述auto_build目录运行：

```bash
python3 .collaboration/context.py read --actor codex
git status --short
git branch --show-current
git rev-parse HEAD
git worktree list
git remote -v
git rev-parse --git-common-dir
```

zcode用actor zcode；其他harness按脚本支持的真实角色填写并在task注明执行主体，不冒用身份。主目录另读共享上下文只作协调。初始接手参考8d38f63并不锁定最终HEAD；记录实际值与任务文件内容，不能把文档中的旧SHA写成当前已验证SHA。

必读顺序：当前AGENTS → 最新共享上下文 → 本指令 → 整合计划v3§0–12 → 接口08及README → 当前实现和相关测试。旧v1/v2只供历史追溯。R1–R5已提交、待独立验收，不能把其自测结论当本任务基线已验收，也不能照旧复现描述重复改代码。

本期包含分支默认关闭的在线接线，覆盖v2“仅独立试点”的停止点；不含主服务更新。本期不引入整套GraphRAG/LightRAG/Neo4j/KAG依赖、不整体重构解析器，不宣称100MB单JSON已无损解析，不新增自动发布或实时流式传输。

## 4. 冻结共同契约与参数

D00先完成08/README/必要05和batch_contracts.py，再允许依赖任务写实现。至少冻结：Profile、Target/selector、Job、Attempt/usage、CallResult/errorCode、Codec、Persistence、HTTP摘要和错误时机；不得先并行让各agent自行定义同名字段。

| 项目 | 统一口径 |
|---|---|
| 输出请求cap | min(默认32000，已核实输出能力)，不凭模型名猜能力 |
| 规划比例/首批/输入预留 | 0.50 / 最多2语义单元 / max(2048,ceil(0.02×C)) |
| 调批 | 首版只缩不自动扩大；正文约3K为软装箱目标，不能把推理空间吞掉 |
| 调用与重试 | 每次物理请求一个Attempt；同job首调+网络/格式修复共享3次总额，格式修复最多1次 |
| 在线预算 | 4096目标、512job、1024attempt、3600活跃秒、1MiB checkpoint；完成写入预留必须测 |
| 单个实验运行 | 每样本/臂/重复64attempt、1800秒、并发1、已知completion十万软限 |
| 整个真实campaign | 合计128attempt、7200活跃秒、已知completion二十万软限；跨续跑/换结果目录不重置 |
| 版本 | 文档v3、checkpoint schema2、codec legacy-v1/compact-v1是三个不同维度 |
| 默认启用 | adaptive关；codec legacy-v1；实验B/D可显式选择compact评估G2；在线隔离服务启用compact需先过G2 |

默认阈值是工程试点边界，不是已压测或节省承诺。HTTP同步422应发生在任何task/run/candidate状态变更前，CAS409保留；异步规划超限写运行失败和原因，不伪造HTTP422。

## 5. 多agent派发步骤

唯一任务表在整合计划§8，不另造第二份任务编号；D00–D19共20项，每项有依赖、交付文件、测试和角色。协调者派发时填真实owner与状态（待派发/实施中/待联调/验证通过/受阻），状态不能由“代码已写”直接变“验收通过”。

推荐以协调者＋3子agent起步，实际槽更多时增加独立任务，不新建额外worktree：

1. **W0，串行冻结**：C完成D00；明确现有文件归属和能力来源，写协议与任务表。无确认的热点归属只读，不stash/reset/代为提交。
2. **W1，滚动派发基础模块**：D01配置、D03语义单元、D05调用可先并行；空槽继续D06 codec、D07状态机、D11合成材料、D15前端mock。允许已冻结协议的mock，不算真实联调完成。
3. **W2–W3，依赖就绪即派**：D01→D02预算；D02+D03→D04规划；D07→D08在线存储与D12试验存储；D07+D08→D10恢复；D11→D13指标。D17可先写黑盒场景。
4. **W4，共享执行收口**：D04/D05/D06/D07完成后C做D09。严格先持久化started再请求；成功候选、用量、叶状态同事务；写失败不得推进内存done。
5. **W5，实验与在线接线**：D14 CLI和D16在线接线在各自文件上并行；均依赖真实模块与假模型机制门。G1仅含共享核心和两存储适配器的假模型/恢复契约，不要求HTTP/浏览器先通过，后者接线后在G3验收。D15连接真实API，禁止以mock截图作联调证据。
6. **W6，测试和效果**：D17组合故障/HTTP/浏览器；D18真实A/D冒烟及预算允许的完整矩阵。真实样本缺失/模型不可用明确列未测，其他任务继续；不能把缺失项标通过。
7. **W7，串行收口**：D19组合回归、构建、提交与交接。发现失败按文件owner派回修复，再跑受影响测试；最后全套一致性验证，不让多个agent同时构建或提交。

子agent任务模板：

> 负责【Dxx】；cwd为auto_build；以v3已冻结契约【版本/提交】实施。唯一允许写入【表中明确文件列表】，禁止写共享文件或其他owner文件。依赖【列表】未交付时仅做独立部分与标注mock。完成运行【对应命令】，回报修改路径、结果/证据、限制、依赖变更；不操作Git、环境启停或真实数据。协议变化先交协调者统一冻结，不自行扩展。

C独占pipeline/llm/protocol/routes/App.vue/tests/run.py与契约文档；D独占storage/ontology_build.py和runner.py；B独占llm_client.py。其他任务尽量新建模块。不同角色不能同时按行号分写同一热点；转移归属必须交接后旧owner停止写。

## 6. 不可省略的正确性要求

- 不把输入字节、候选数、正文token和供应商completion混为一谈；reasoning若已含completion不重复加。
- 不解析截断半JSON当成功，不补括号掩盖缺项；compact coverage缺目标则整叶不提交，最多有限修复。模型自报processed不代替金样。
- 成功子叶保留；失败续跑只补未成功叶。失败预算不重置；不可分原子目标明确blocked，不能无限“继续”。
- 首500事实/候选不能静默裁切后成功；候选全量读取分页，500产品容量超限明确阻塞并保留中间结果。
- 老任务、未知版本、auto与abstract重跑语义分清；参数/事实/模型/codec指纹变化不能复用旧计划。未知版本拒绝恢复，不能删候选兜底。
- 短证据别名只能引用本次允许材料；程序不得补supported、单位、关系基数和owner，不能仅凭名称合并不同主体。
- 实验与在线适配器跑同一组恢复契约。旧lease晚结果不得写回，取消不复活；外部请求崩溃计费未知明确显示unknown，不承诺供应商恰好一次。
- 只返回有界安全摘要，原prompt/response、reasoning、key不进日志/接口；业务JSON格式保持原协议。

## 7. 验证与效果判定

执行整合计划§8每项P(ID)测试，随后§9既有回归清单；新增脚本实际创建后才能运行/报告，不允许“找不到文件所以跳过通过”。各HTTP测试由C登记tests/run.py分组。在线验收用临时ROOT、合成数据库和动态空闲端口，不拿工作树真实数据副本做故障注入。

组合后串行执行：

```bash
python3 tests/run.py all
# frontend为cwd：
npm run typecheck
npm run build
```

前端验证使用隔离浏览器会话、分支自己构建dist和测试实例：旧计划、新计划、生成中渐进计数、截断拆分、失败/受阻、重试、unknown用量、终态评审和未完成交付门禁。记录实际URL/端口/PID/cwd，不预填18890为本次测试地址。

效果试验：先原8KB A/D各一次，再按campaign余量跑三类样本A/B/C/D×3冷跑，暖恢复单列；A取实际交接基线，B只换codec，C只换调度，D组合。记录hash、模型/配置、解析缺口、全部attempt用量、质量差异、时延及未完成项。预算不足不擅自扩额；少于3次标探索性，不宣称统计稳定。

G1机制通过后可以完成默认关闭的在线开发；G2决定compact是否值得启用。关键金样100%、真实召回/精确率均不比A降超过2个百分点；B正文token中位数少20%才记compact收益门通过。D总token增加超过10%或usage未知需明确成本取舍，不自动开compact。G2未达标可交legacy+预算恢复能力及实验性compact，但明确效果未通过；不得冒充整项所有门槛都通过。

## 8. 最终交付

提交前再次read共享上下文，核对需求和他人更新，record本任务实际结果，由脚本生成摘要。git status/diff后仅提交本任务文件，不纳入业务数据、key、dist、依赖或他人新交接。commit写实际功能与已跑验证，不写未经验证结论。

必须交付：接手与最终SHA、分支、v3文档版本、20项任务实际owner/结果、实际URL/ROOT/profile/codec、测试命令与结果、浏览器证据、真实试验报告或未测原因、默认开关状态、剩余容量/解析限制、保留数据目录、main未合并及主服务未更新状态。实现记录追加整合计划，避免再生成重复需求文件。

停止点为交Codex独立验收。开发者自测不替代独立验收；未经用户明确指令不集成main、不启动主环境更新、不删除真实数据副本。
