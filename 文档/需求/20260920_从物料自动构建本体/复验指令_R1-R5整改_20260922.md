# 复验指令：R1–R5 五项正确性缺陷整改（2026-09-22）

接收者：Codex（独立验收人）。本指令自包含，不依赖聊天记录。

## 0. 授权与停止点（AGENTS「独立分支开发与串行集成」）

- 本轮只做**复验**：核对并记录实际验收 SHA；运行测试与只读评审；输出结论与修复项。
- **不合并 main、不改业务代码、不重启任何服务、不清理环境**。
- 复验通过 ≠ 集成授权：通过后状态为「待用户明确授权集成」，集成需用户另发指令。
- 发现问题按既往惯例写验收报告；实施方（zcode）在原 worktree 修复后交你复验，旧结论不覆盖新提交。

## 1. 环境与对象

| 项 | 值 |
|---|---|
| 工作树（唯一工作目录） | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/auto_build` |
| 分支 / 验收对象 | `codex/auto_build`，**fix 提交 `22a60e5`**（其上另有 `8d38f63` docs 归档、`ebd794f` 证据固化，均不影响代码语义） |
| 基线 | `805e1d9`（你的审核意见提交所在） |
| 审核依据 | `文档/需求/20260920_从物料自动构建本体/五阶段方案审核意见_Codex_20260922.md`（R1–R5 + 第 4 节论证修正） |
| Python | 系统 python3 3.9（无 venv）；测试一律 `WIZ_WORKBENCH_ROOT=<临时目录>` 隔离 |

## 2. 必读顺序

1. 你的审核意见（R1–R5 各条「验收」小节即本次复验标准）
2. `git show 22a60e5 --stat` 与逐文件 diff（6 代码文件 + 4 新测试 + 08 分册 + README）
3. `文档/需求/20260920_从物料自动构建本体/验收证据_R1-R5整改/README.md`（复现脚本用法与终验输出）
4. 接口文档变更：`文档/接口文档/08-…md` §1.6/§5/§8.1 与 README 变更记录 2026-09-22 行

## 3. 复验步骤

1. **核对实际 SHA**：`git -C <worktree> rev-parse --short HEAD` 与 `git log --oneline -4`；记录实际验收 SHA。
2. **复现脚本复核（核心）**：在 worktree 下逐个运行（脚本已固化在 `验收证据_R1-R5整改/`，也可用 /tmp 原件）：
   ```
   WIZ_WORKBENCH_ROOT=/tmp/vr1 python3 文档/需求/20260920_从物料自动构建本体/验收证据_R1-R5整改/repro_r1.py
   （r2/r3/r4/r5_final 同法，临时根各换一个目录）
   ```
   预期：五个脚本全部输出「✅ 未复现」。如需复核"修复前确实失败"，可 `git stash` 或在临时 clone 中检出 `805e1d9` 重跑（脚本在 805e1d9 上应全部复现失败）。
3. **回归复核**：
   ```
   python3 tests/test_ontology_build.py                     # 预期 250/250（零既有断言修改）
   python3 tests/run.py all                                 # 预期 60/60（含 4 个新套件）
   ```
   新套件单独跑：`tests/test_ontology_build_{batch_accounting,key_namespace,conflict_gate,fact_identity}.py`（36/30/29/21）。
4. **只读评审要点（对照你的验收标准逐条核对 diff）**：
   - R1：`alignment.namespace_batch` 是否"仅实际冲突键加前缀 + 先建映射再重写 ownerKey/sourceRef/targetRef"；`llm._normalize_key_ref` 四字段统一规范化；`test_ontology_build` 是否真的零断言修改（`git diff 805e1d9..22a60e5 -- tests/test_ontology_build.py` 应为空）。
   - R2：二次 verify 撤销自动 include（`AUTO_INCLUDE_REVOKED`，reviewed=true/exclude 不动）；交付端 `_delivery_allowed` 门禁口径 = include ∧ 未合并 ∧（reviewed ∨（无 conflicts ∧ supported））；模型注入 decision/直写库路径同样被拦。
   - R3：`snippet_digest` 事实身份（snippet+kind+locator+data 语义键）单一入口；`plan.duplicateOf` 持久化；duplicates=全池实际剔除数；5 万条冒烟 <5s。
   - R4：拆批三态；部分成功父批不进 done、成功半候选落库、失败事实 id 上抛；`_flush_tx` 同键去重幂等。
   - R5：无前缀游标；每批完成即独立落库；落库失败计 failed 不静默；调度收尾核对 done∪failed 覆盖 pending；`_batch_wait_timeout` 已删除。
5. **接口文档一致性**：08 §1.6（AUTO_INCLUDE_REVOKED/重算时机）、§5（三条新语义）、§8.1（选定集合口径 + conflictExcluded/conflictExcludedItems）与实现是否一致；README 变更记录是否登记。
6. **数据保护核查（只读确认）**：确认本复验全程使用临时根；未写入 `ontology/`、`data/`、未触碰 18765/18890 服务。

## 4. 已知未修项（不应计为本次缺陷，属遗留/另行需求）

- 拆批**两子批之间**同名 key 仍回落 `_dedupe_keys` 首个保留（彻底解决需拆批处给子批独立前缀）。
- `_flush_batch` 部分成功分支未累计 `rejectedRefs`（统计略低估）。
- `store.all_candidates` 500 行上限（单批候选受 MAX_CANDIDATES_PER_BATCH=500 约束，实际风险有限）。
- 审核意见 D1–D3 与「布尔/短标量过滤」：方案层改进，待用户拍板另立需求，不在本次范围。

## 5. 交付要求

- 验收报告写入 `文档/需求/20260920_从物料自动构建本体/`（命名沿用惯例，如 `验收报告_R1-R5复验_20260922.md`），逐条给出 R1–R5 结论（通过/不通过+证据）。
- 通过 `python3 .collaboration/context.py read --actor codex` → 实施前/验收前再读 → 交付前 `record`（status: verified 或 blocked，记录实际验收 SHA 与证据）。
- 结论口径：已实施不等于已验收；复验通过停在「待用户授权集成」。
