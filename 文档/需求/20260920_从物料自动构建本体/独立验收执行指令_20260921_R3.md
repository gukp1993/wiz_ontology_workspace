# 从物料自动构建本体 · 第 4 轮独立复验执行指令（2026-09-21）

本文件可独立交给 Codex 或其他验收 harness 执行，不依赖聊天记录。第 3 轮验收报告
（[验收报告.md](验收报告.md) 附录 B，基线 `43dddc8`）复现 4 项 P1（R3-01～R3-04）与 1 项 P2（B.4），
开发方已整改并提交。本指令定义复验范围、步骤、判定标准与交付格式。

## 1. 被验对象与工作树

| 项 | 值 |
|---|---|
| 分支 | `codex/ontology-build` |
| 被验 HEAD | `f1b90c6`（整改完成时点，须实测核对 `git rev-parse HEAD`） |
| 整改提交 | `25333c3`（R3-01～R3-04 后端）、`7835e05`（R3-04 前端 + B.4 回链）、`f1b90c6`（已交付页误报修复） |
| 第 3 轮被验基线 | `43dddc8`（对照用：`git diff 862da5b..f1b90c6` 即全部整改内容，18 个文件） |
| 工作树 | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2-ontology-build` |
| Python 环境 | 该树 `.venv/bin/python`（已含依赖，不要 pip install） |
| 前端构建 | `cd frontend && npm run build`（node_modules 已就位） |

开工先做三件核对：`git rev-parse HEAD` 等于 `f1b90c6`；`git status` 无未提交业务代码
（允许存在 `.collaboration/entries/*` 与 `session_context.md`，那是交接设施产物）；
`git diff --stat 862da5b..f1b90c6` 的文件清单与上面 18 文件一致（范围收敛证据）。

## 2. 只读边界与禁止项（强制）

1. **不修改任何业务代码/测试/接口文档**；本轮验收只读 + 运行测试 + 起停隔离实例。
2. **不合并 main、不推送、不清理工作树、不动 18871 的数据根**。
3. **不重启主工作台 18765**；结束验收时 18765 必须仍存活（可 `curl -s -m 3 -o /dev/null -w '%{http_code}' http://127.0.0.1:18765/` 确认为 200）。
4. **禁止 `pkill -f workbench.server` 等按命令行模式杀进程**——主工作台 18765 与本任务实例
   命令行同构，第 3 轮整改期间曾因此误杀主工作台（已恢复）。需要重启 18871 时，
   只允许按 `.runtime/server.pid` 里的 PID 精确 stop/start（见 §4）。
5. 隔离测试一律 `WIZ_WORKBENCH_ROOT=<临时目录>` + 该根下 `WIZ_DATABASE_URL` + 动态端口；
   临时产物放 `/tmp`，结束自行清理；绝不写真实 `ontology/`、真实库、`data/`。
6. 共享上下文以 `--actor codex` 读取/记录（脚本仅支持 codex|zcode）。

## 3. 复验矩阵与必做命令

以下命令全部在该工作树根执行。括号内是开发方实测值（2026-09-21，HEAD `f1b90c6`），
你的实测结果应一致或更好；任何一项不一致即记录为待解释差异。

### 3.1 单命令复验脚本（先跑这个）

```bash
.venv/bin/python '文档/需求/20260920_从物料自动构建本体/验收证据_第三轮/复验_修复后回归.py'
```

这是把报告 B.5 三份**缺陷复现脚本**转成的「断言正确行为」回归（21 项断言），
逐项对应 R3-01～R3-04（开发方实测 **21/21**）。判定：

- R3-01：旧 worker 晚写入被拒、未污染 stage、未把新尝试改成终态、新 worker 登记保留、
  两次 submit 执行权不同；
- R3-02：连续第 3 批仍保护（保护数=1、defer、revived=true）、用户显式重新纳入后解除；
- R3-03：合并别名贯通属性 domain 与动作关联；无别名时属性/动作宿主解析不到**被阻断**
  （不再静默丢关联）；
- R3-04：平铺 payload 被接受；旧嵌套 payload 被拒且提示含「平铺」与允许枚举；
  非法观测值（number）被拒；枚举 7 项与本体协议一致。

### 3.2 同目录三份原始复现脚本（反例有效性抽查，可选但建议）

`验收证据_第三轮/取消重试并发.py`、`排除继承与合并引用.py`、`时间序列编辑协议.py`
是**缺陷复现脚本，不是通过标准**。修复后它们的输出会变化（第一个会因「race did not
reproduce」断言失败退出；第二个会在动作关联阻断点抛 AdapterError；第三个仍打印
REPRODUCED——因为后端对嵌套 payload 的拒绝本身就证明协议收紧生效，前端已不再发这种
payload）。这些行为变化是修复生效的证据；不要把它们按旧预期判为回归，以 3.1 的
复验脚本为准。

### 3.3 自动化回归（全量）

```bash
.venv/bin/python tests/run.py all            # 开发方实测 44/44
.venv/bin/python tests/test_ontology_build.py   # 实测 162/162（含 R3-04 新增 2 断言：
    # 「非法观测值类型在保存时被拒并列出允许枚举」「被拒的非法值没有写进候选」，
    # 以及 D02 兜底：库内残留旧枚举值仍被预检拦截）
.venv/bin/python tests/test_ontology_build_runner_isolation.py        # 34/34
.venv/bin/python tests/test_ontology_build_exclusion_inheritance.py   # 17/17
.venv/bin/python tests/test_ontology_build_merge_refs.py              # 42/42（含真实交付端到端）
node --import ./tests/ts_hooks.mjs tests/ontology_build_review_edit.test.mjs   # 15/15
node --import ./tests/ts_hooks.mjs tests/ontology_build_frontend.test.mjs      # 既有，须通过
cd frontend && npx vue-tsc --noEmit -p tsconfig.json && npm run build          # 0 错误 + 产出 dist
```

已知环境噪音（不计入本需求失败）：`tests/test_ontology_build.py` 的未登录 401 断言
偶发 `ConnectionResetError`（隔离实例并发窗口，复跑即过）；前端 `mapping_forms.test.mjs`
为 main 既有失败，与本需求无关。除这两处外，任何失败都要定位并记录。

### 3.4 抽查整改 diff 与文档一致性

- `git show 25333c3 -- workbench/ontology_build/runner.py`：确认执行权按 worker token +
  线程本地绑定、`submit()` 先轮换 lease、`finally` 只清自己的 token（R3-01 修复主体）。
- `git show 25333c3 -- workbench/ontology_build/pipeline.py`：确认 `_manual_exclusion_keys`
  跨全部批次回放（新覆盖旧），仅 `include+reviewed` 解除（R3-02）。
- `git show 25333c3 -- workbench/ontology_build/ontology_adapter.py workbench/ontology_build/delivery.py`：
  确认别名表（传递解析 + 环保护）传入 `assemble()`，非空宿主解析不到抛 `AdapterError`
  （R3-03）；`文档/接口文档/08-从物料自动构建本体接口.md` §7 已登记 R3-04 字段口径。

## 4. 浏览器点击级复验（可选；若做，按本节环境）

隔离实例 **已在运行整改后代码**：`http://127.0.0.1:18871`（PID 见该树
`.runtime/server.pid`，当前 45790，启动于 `f1b90c6` 的 dist 构建之后）。
本地假 LLM：`127.0.0.1:18899`（OpenAI 兼容，已配置为账号默认提供方）。
账号：`tester` / `tester123`。已有任务「验收设备本体任务」**已交付**（本体
`25d3cad2-b7ca-40f1-a8bd-56273f040be5`），可直接复验 B.4 回链与已交付页；
如需复验 R3-04 编辑链路，新建任务→上传任意小材料（可用 `/tmp/wiz-e2e/files/` 三份
合成物料，若已被清理就按需求 §4.1 自造）→扫描→范围确认生成→评审页操作。

必验点（与报告 B.3/B.4 一一对应）：

1. **R3-04**：评审页选一个属性 → 数据类型改「时间序列」→ 观测值下拉应出现 7 项
   （文本/数值/小数/整数/是或否/日期/日期时间）→ 选一项保存 → 无错误提示，
   刷新后仍保持（服务端已落库平铺 `dataType`+`valueType`）。
2. **B.4**：任务列表对已交付任务点「查看已创建本体」→ 应切换到该本体并进入对象建模
   （URL 出现 `?ontology=<id>#objects`），对象/属性/链接可见；**不是**停留在之前选中的本体。
3. **已交付页（f1b90c6 修复）**：重进已交付任务的「保存新本体」页 → 不出现红字
   「交付预检失败」，显示「已创建本体 …」只读区（`role=alert` 为空）。
4. 顺带记录 console error/warning 数量与任何布局/文案问题（G15 仍不在本轮范围）。

如需重启 18871（仅在它未运行或代码不符时）：

```bash
cd /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2-ontology-build
kill "$(cat .runtime/server.pid)"   # 只按 PID 精确停，禁止 pkill
sleep 2
env -u http_proxy -u https_proxy -u all_proxy nohup .venv/bin/python -m workbench.server \
  > /tmp/wiz_r4_server.log 2>&1 &
sleep 5; cat /tmp/wiz_r4_server.log
```

## 5. 判定标准

- **通过**：§3.1 复验脚本 21/21；§3.3 各套件通过数不低于开发方实测值；
  §3.4 三处 diff 与文档一致；若做 §4 则四个必验点全部符合。
- **不通过**：任一 R3-0x 行为未达 §3.1 判定、新增回归失败且非上述已知噪音、
  或发现整改引入的新 P0/P1。
- 与第 1～3 轮相同，以下维持「未验收」标注、不因本轮通过而解除：真实供应商语义质量
  与多领域样本（G16/T25）、OCR、规模限额校准、768 窄屏与键盘可达性（G15/T24）、
  T12 服务重启恢复、T20 写点故障注入、与最新 main 的组合重验（集成阶段做）。

## 6. 交付格式

按第 3 轮报告的格式输出复验记录：

1. 结论（通过/不通过）+ 被验 HEAD 实测值 + `git status` 终态；
2. §3 每条命令的实测结果（通过数/失败项原文）；
3. R3-01～R3-04 + B.4 逐项判定与证据（可引用复验脚本断言名）；
4. 若发现问题：编号 + 位置（文件:行）+ 复现方式 + 建议；
5. 未验证项清单（照抄 §5 末段或注明豁免理由）；
6. 以 `--actor codex` 写共享上下文 record；验收记录可追加到
   [开发计划.md](开发计划.md) §11 或另立 `验收报告_第4轮_20260921.md`。

验收通过后停在「待用户授权集成」；是否合并 main、是否更新主工作台由用户另行明确指令。
