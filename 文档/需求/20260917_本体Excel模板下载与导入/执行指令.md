# 交给其他 harness 的执行指令

请在下面的项目中实施「本体 Excel 模板下载与导入」。任务是完成可运行功能，并按要求验证和提交；不要仅生成方案，也不要扩展到项目映射或自动关系推断。

## 项目与必读资料

项目根目录：

`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

按以下顺序阅读：

1. 根目录 `AGENTS.md`（以及改动目录下如有的更细指令）。
2. `文档/需求/20260917_本体Excel模板下载与导入/需求说明.md`。
3. `文档/需求/20260917_本体Excel模板下载与导入/交互原型_v1.html`，实际打开操作，不能只读标题。
4. `文档/需求/20260917_本体Excel模板下载与导入/开发计划.md`。
5. 模板 `文档/需求/20260917_本体Excel导入模板/本体模型填写模板_v2.xlsx`，检查四表表头和属性页联动。

这些文件在同一个项目中。不得依赖聊天历史、特定模型或插件。如果路径变化先定位同名当前文件，再报告；不要自行编造另一版需求。

## 必须遵守的产品范围

1. 本体「工作概览」顶部增加下载模板和导入Excel入口；没有选择本体时可下载，但不能导入。不新增一级菜单。
2. **模板就是给定 xlsx**，仅四个Sheet及业务表头。复制为前端静态运行资产，不能添加编号、关联码、样例、说明页或新增Sheet。保留下拉、联动及隐藏辅助区域。
3. 导入四类：对象类型、共享属性、业务规则、动作。所有导入属性为共享属性。
4. 两个策略严格命名为 **跳过同名内容**（默认）与 **保留并自动重命名**。解释文案按需求；不能新增覆盖/合并策略。
5. 同名按当前本体、同一类别比较；批内重复同样处理。随机后缀格式 `_六位数字`，防碰撞，预览后的名字和稳定ID不得在确认时改变。
6. 模板没有关联信息，因此**不自动建立对象与属性、规则、动作的关联，也不导入对象间链接**。导入成功后提示去对象建模维护关联。
7. 上传、策略、检查预览和确认位于同一面板，**严格照原型的入口、层级、按钮文案和状态设计实现**，不要换成三步向导或另起大型管理页。
8. 任何待导入行错误阻断整批保存；跳过重复项不是错误。预览给出Sheet、真实Excel行号、字段及具体原因。读文件失败不能用硬编码示例伪装成功。
9. 导入只保存本体草稿，不自动发布，不执行规则/动作，不调用LLM，不修改项目数据与发布快照。

## 实施顺序

先执行 `git status` 查看他人修改。只编辑本任务文件，不还原或提交他人修改。

按开发计划 T1—T6 实施：

- T1核对现有创建格式和表单保存链路。现状入口包含 `OntologyHome.vue`、`App.vue`、`app/formGuard.ts`、`ontology/propertyModel.ts`、`businessRuleModel.ts`、`ActionLibrary.vue`、`modelFormat.ts`。
- T2复制模板至 `frontend/public/templates/ontology-import-v1.xlsx` 并完成真实下载。
- T3用单个受维护的浏览器端XLSX解析依赖懒加载读取，锁定上游来源和版本；解析/校验/导入计划拆成纯逻辑，复用现有枚举与构造器。只取业务列，忽略模板隐藏H列，不执行单元格公式。
- T4创建导入面板并按原型接入。原型里的「正常／有问题／版本变化」等演示按钮仅用于原型，正式页面不能保留。原型不会解析真实Excel，正式功能必须解析所选文件真实内容。
- T5经 `form-save.submitForm('ontology', mutate)` 一次合并、一次持久化，沿用快照/撤销/序列化/409机制。保存成功才展示成功；发生冲突必须重新读取和检查，不强制覆盖。结果不明先按预览固定ID核对服务端，不重新随机生成第二批内容。
- T6完成隔离测试、构建与浏览器验收，把实施记录追加到开发计划，再提交本任务改动。

不要增建Excel上传服务器、后台任务系统、导入批次数据库、后端持久化协议、关系映射页或Excel在线编辑器。若遇到现有框架确实无法实现的边界，说明具体阻点和最小补充方案，不自行大范围重构。

## 数据和安全边界

- `ontology/` 全部为真实用户资产；禁止手改、清空、重建或写测试数据。测试使用临时 `WIZ_WORKBENCH_ROOT` 和独立端口，不能向18765真实服务写入。
- 保存仍走已有API、LOCK及revision；不得放松127.0.0.1绑定、Origin校验、POST白名单或2MB请求上限。
- 文件≤1MiB、四表非空业务行合计≤1000；解析后最终保存请求也必须符合现有2MB限制；超限明确报错。
- 页面请求复用 `app/http` 与 `ontology/api.ts`；不要新增页面裸fetch与重复错误解析。
- 现有项目/本体两条状态线保持分离；未知字段、已有定义/关联全部保留。不得把导入内容作为完整本体替换。
- 默认不改 `saveCoordinator.ts`；如必须改，先加有意义的回归用例，避免引入自动覆盖409或网络重复提交。

## 验收与最终答复

按需求 A01—A14 逐项给证据，重点完成：

1. 模板真实下载，哈希一致，四表下拉联动保留。
2. 四类真实Excel导入、刷新可见，属性进入共享库、无自动关系。
3. 两种重名策略、批内重复、随机碰撞和预览/结果一致。
4. 隐藏枚举区、空表空行、时序观测值错误、业务公式、超限文件、错误表头和损坏文件。
5. 取消/只预览/全跳过零修订，单批一次保存；409与保存结果不明不会重复导入或覆盖较新数据。
6. `cd frontend && npm run build` 和变更相关测试通过；真实浏览器操作验证，不只截图原型。

测试文件和临时数据不得混入真实 ontology；不要做无关目录清理。按 AGENTS.md 仅提交本任务文件，commit写清功能和实际验证，不能把未运行的检查写成通过。

最终答复包含：实现范围、入口、关键文件、测试结果、已知限制、commit编号；同步回填 `开发计划.md` 的实施记录。未完成项明确写出，不用「基本完成」掩盖。
