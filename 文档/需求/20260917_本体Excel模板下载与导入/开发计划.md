# 开发计划：本体 Excel 模板下载与导入

版本 v1，2026-09-17。实施范围以[需求说明.md](需求说明.md)为准，界面按[交互原型_v1.html](交互原型_v1.html)。本文件在实施后追加实际结果，不新建一串重复实施报告。

## 1. 现状核对（本次只读检查）

| 当前入口 | 已有能力 / 注意点 |
| --- | --- |
| `frontend/src/ontology/OntologyHome.vue` | 本体工作概览；当前只读展示。本期在顶部挂载导入面板和模板下载。 |
| `frontend/src/App.vue` | 本体空态、表单守卫和 `provide('form-save')`。`submitForm` 先 flush，再快照/撤销、mutate、commitNow；失败回滚。 |
| `frontend/src/app/formGuard.ts` | `FormGuardAPI`、`FormSaveAPI` 已有，不需要重建保存框架。 |
| `frontend/src/app/saveCoordinator.ts` | 串行保存与 409 冲突；当前已不会自动续发冲突。文档旧段落有历史描述，实施以最新代码行为为准。 |
| `frontend/src/ontology/api.ts` | `loadStateRaw`、本体编码请求、导出等；没有 Excel 导入接口。 |
| `frontend/src/ontology/SharedLibrary.vue` | 已有粘贴导入小功能，只覆盖属性；不能冒充四表 Excel 导入。无需清除该功能。 |
| `frontend/src/ontology/editorModel.ts` | 当前数据类型：文本、数值、是／否、时间、数组、结构体，属性页面追加时间序列；观测值排除数组/结构体。 |
| `frontend/src/ontology/propertyModel.ts` | `makeProperty`、`setPropertyDataType` 等，复用共享属性构造与时序表示。 |
| `frontend/src/ontology/modelFormat.ts` | JSON-LD 与 API schema 转换；不要绕过 `definitionOrder` 等规则。 |
| `frontend/src/ontology/businessRuleModel.ts` | 规则 `name/description/content/output`，关联单独存储。 |
| `frontend/src/ontology/ActionLibrary.vue` | 新动作 `name/description/effect/definitionVersion:2/status:'experimental'`。 |
| `workbench/model_routes.py::post_save` | 现有 `/api/save` 做 revision 校验、全局锁、写草稿；`validate` 返回问题不一定拒绝保存，因此导入必须先校验待导入内容。 |
| `workbench/server.py` | 仅本机、Origin校验、JSON请求、2MB上限；不为了导入放松。 |

## 2. 推荐实现：浏览器解析，复用现有草稿保存

本期不新增上传服务、解析后台任务、导入数据库或后端 Excel 依赖。

1. 将已确认模板复制到 `frontend/public/templates/ontology-import-v1.xlsx`。这里是运行资产，不是用户数据。开发/生产均通过静态路径下载，用 Vite BASE_URL 生成地址，不用硬编码文件系统路径。
2. 新增一个浏览器端 XLSX 读取依赖，懒加载。实施者核验正式上游分发渠道、安全状态和许可，锁定明确版本，提交锁文件；不要随手使用过时 CDN 或在页面运行时从外网加载脚本。
3. 解析只取业务列的原始值、公式标记、Excel行号；识别隐藏列和合并区域，不计算公式。格式、下拉验证不视为数据。单独封装解析模块，不能让 UI 到处感知库的数据结构。
4. 纯函数处理标准化、字段校验、同名决策与导入计划。计划在浏览器内存中，不写 localStorage、用户草稿或临时用户文件。
5. 确认后复用 `formSave.submitForm('ontology', mutate)`，批量追加全部新增定义。保存请求仍走既有编码与 `/api/save`。

该方案控制改动面；不需要更改本体持久化协议、项目映射、后端写入路径。若所选 XLSX 库无法可靠识别本模板的隐藏列/公式等必要信息，应在解析模块内换合适依赖，不能悄悄降低验收标准。

## 3. 建议代码拆分

| 文件 | 职责 |
| --- | --- |
| `frontend/src/ontology/OntologyImport.vue`（新增） | 上传、策略、检查、预览、确认、结果；复用现有对话框/按钮/错误样式、表单守卫。 |
| `frontend/src/ontology/excelImport.ts`（新增） | 懒加载解析依赖，读四表、表头/行号/隐藏区域/原始类型与文件校验。 |
| `frontend/src/ontology/importPlan.ts`（新增） | 类型映射、必填校验、重名计划、固定名字/ID、JSON-LD/工作流定义构造；无网络、无Vue依赖。 |
| `frontend/src/ontology/OntologyHome.vue` | 入口与面板挂载，成功后刷新概览统计/校验，不改业务计算。 |
| `frontend/src/App.vue` | 无本体下载入口，必要时只为导入暴露当前 revision/读取结果不明后的状态同步能力。不得重写整个 App。 |
| `frontend/src/ontology/api.ts` | 如需结果核对，复用 `loadStateRaw`。不要另写 fetch。 |
| `frontend/public/templates/ontology-import-v1.xlsx` | 已确认模板字节副本，随前端构建发布。 |
| `tests/ontology_import.test.mjs`（建议新增） | 解析/计划/同名/字段映射核心回归，用现有 TS hooks 模式。 |

若当前分支已有同职责模块优先复用，不机械创建上述文件。禁止为了这项功能重组目录或拆分无关页面。

## 4. 内部数据契约（不新增本体字段）

建议结构，用于导入面板内部：

```ts
type ImportPolicy = 'skip' | 'rename';
type ImportKind = 'object' | 'sharedProperty' | 'businessRule' | 'action';
type ImportIssue = { sheet: string; row: number; column: string; message: string };
type ImportDecision = {
  kind: ImportKind; sheet: string; row: number;
  originalName: string; finalName: string;
  disposition: 'create' | 'skip' | 'rename' | 'error';
  reason: string;
  definition?: unknown; // 待追加的完整节点/工作流定义，含预先生成的稳定 ID
};
type ImportPlan = {
  ontologyId: string;
  baseRevision: string;
  baseContentFingerprint: string;
  fileFingerprint: string;
  policy: ImportPolicy;
  decisions: ImportDecision[];
  issues: ImportIssue[];
};
```

- fingerprint 仅用于判定预览过期，不是服务端授权凭证。结构可用现有编辑代数或稳定快照摘要实现，不引入跨后端认证协议。
- ID 使用已有生成方式（对象/属性 mg 稳定 ID、规则/动作按现有库构造器）。随机后缀和 ID 是两回事，Excel 不要求用户填写它们。
- 对象追加 `owl:Class`；属性用共享类型构造器，再设数据类型和自然语言格式。规则追加 `workflow.businessRules`；动作追加 `workflow.actions` 的 V2 结构。
- 不修改 `actionAssociations/businessRuleAssociations`，不生成属性引用、不生成链接。
- 已有数据用既有状态深拷贝/快照方式保护，不能只根据四表重建本体、丢失其它内容。

## 5. 保存与并发的实施要点

1. 打开面板前按现有导航/表单保护处理未保存修改，确保本体区保存队列进入可提交状态。
2. 检查完成记录本体ID、基线和计划；面板打开期间发生草稿变化则失效。切换本体经守卫，不把旧面板挂在新本体下。
3. 确认前再次检查基线和最终编码请求大小；进入 `submitForm` 后，在同步 mutate 入口再确认本体与状态未变，避免 flush 期间的变化被遗漏。不要在 mutate 中执行异步读取。
4. 若需要让 mutate 可以有理由地中止，局部扩展既有表单提交机制并补该分支测试，不能让异常逃逸破坏工作区状态。优先通过现有组件状态锁与基线检查实现。
5. 409必须重新读取和预览，不能通过「换 revision 强制重提交」掩盖过期的重名判断。无需修改所有普通表单的冲突策略。
6. 网络结果不明时，面板保留本次计划及 ID；禁用再次确认，先只读加载最新本体并逐项核对。所有计划新增 ID及字段一致则同步最新状态并显示成功；全不存在则使旧计划失效并重检；部分匹配或被随后修改则要求核对。
7. 核对时不能靠名称或成功数量猜测，不能因为观察到其他用户新增同名项就认定本次成功。后缀绝不因网络重试反复生成。
8. 不承诺跨浏览器重启的批次幂等。关闭浏览器后重新上传是新操作，会重新遵守用户选择的重名策略；本期不新增批次持久化。

## 6. 实施任务

| 顺序 | 任务 | 输出与完成条件 |
| --- | --- | --- |
| T1 | 核对当前分支、模板、表单构造和保存链路 | 确认实际路径，记录依赖选型与必要偏差。禁止改真实数据。 |
| T2 | 加模板静态资产和下载入口 | 下载字节与原件一致；无本体时也可下载，联动保留。 |
| T3 | XLSX解析及导入计划 | 四类字段、空行、隐藏辅助、类型、安全和重名处理完整；通过纯函数测试。 |
| T4 | 导入面板 | 按原型完成文件/策略/预览/问题/结果，取消零写入。 |
| T5 | 接入一次保存与失败处理 | 成功刷新可见；409过期；断网结果核对；固定计划ID；无关联自动生成。 |
| T6 | 隔离环境验收和交付 | 前端构建，必要回归与真实浏览器流程，报告 A01—A14 对应证据。 |

可以在字段/计划接口确认后，将静态下载、纯解析模块、面板样式安排给不同实施者；不能让多个人同时改 App、锁文件或同一保存协调器。无需为小功能拆成几十个并发任务。

## 7. 验证计划

- **模板检查**：四表名称、准确表头、1000行下拉、观测值 IF 联动、隐藏辅助列；下载文件哈希与原件相同。
- **解析测试**：空模板，单Sheet有数据，隐藏H列，打乱业务列顺序，缺表/缺表头/重复表头，业务公式及缓存，损坏文件，合并数据单元格，空格行，超行数，额外有数据列。枚举验证不等于业务单元格公式。
- **计划测试**：现有/批内同名，跨类同名，不同本体独立，trim/大小写，跳过缺其他字段的重复行，新行缺字段阻断；注入确定性随机函数测试碰撞、名称占位和超限，不依赖偶然随机结果。
- **序列化测试**：普通/时间序列/数组/结构体、显示格式自然语言、规则/动作与现有结构一致，decode/encode 往返，definitionOrder 无遗漏。
- **保存集成**：单批一次保存、刷新读回、取消和预览零修订、错误整批零写入；旧字段与旧关联保留；两客户端409；模拟服务端已写但响应丢失，不重复导入。
- **前端验收**：运行 `cd frontend && npm run build`；用真实页面选择文件操作，不用只有 mock 的单元测试冒充端到端。
- 自动化后端/浏览器写入均用 `WIZ_WORKBENCH_ROOT=<临时目录>`、`WIZ_WORKBENCH_PORT=<隔离端口>`，不要向18765真实草稿写测试数据。
- 若修改 `saveCoordinator.ts`，遵守AGENTS要求先补保存队列用例；否则不为了本功能重写或全量扩展其测试。

## 8. 实施记录（由执行者回填）

方案交付验证：四份文档已生成；原型通过 jsdom 的模板链接、默认跳过、自动重命名、预览失效、填写问题、空文件、全跳过、重复点击、成功统计与取消检查。此项是原型 DOM 交互检查，不是工作台功能或真实 Excel 导入验收。项目运行代码、用户数据未修改。

- 实际修改文件：`frontend/src/ontology/excelImport.ts`（新增，解析层）、`frontend/src/ontology/importPlan.ts`（新增，计划层）、
  `frontend/src/ontology/OntologyImport.vue`（新增，导入面板）、`frontend/src/ontology/OntologyHome.vue`（概览入口）、
  `frontend/src/App.vue`（无本体空态下载入口）、`frontend/public/templates/ontology-import-v1.xlsx`（模板字节副本）、
  `frontend/package.json`/`package-lock.json`（XLSX 依赖）、`tests/ontology_import.test.mjs`（新增 16 项回归）。
- XLSX依赖版本与上游来源：`@e965/xlsx@0.20.3`（SheetJS 社区维护分支，Apache-2.0，registry.npmjs.org，锁定于 package-lock）。
  选型原因：官方 npm `xlsx` 停更于 2023（0.18.5），`exceljs@4.4.0` 无法解析本模板（模板 workbook/worksheet XML 使用
  `x:` 命名空间前缀，exceljs 的 SAX 解析返回 undefined）。`@e965/xlsx` 实测可读四表、隐藏列（!cols[].hidden）、
  公式单元格（f 字段）与错误值（t:'e'）。懒加载经动态 `import()`（Vite 分包），测试经 `setXlsxModule` 注入同一实现。
- 与设计偏差及原因：① 解析层校验 ZIP EOCD 记录：`@e965/xlsx` 会把纯文本当 CSV 解析成功，为满足"损坏文件报错"
  在 readWorkbook 增加 ZIP 结构预检；② 导入面板的确认前二次确认复用 `appConfirm`（工作台统一确认弹窗），
  原型无此步骤但符合现有交互约定；③ 无本体空态的下载入口放在"创建第一个本体"卡片头部（现有布局内，不新增选择器）。
  无产品范围变化。
- 构建/测试/浏览器验收结果：`tests/ontology_import.test.mjs` 16/16（空模板/单表/隐藏H列/真实模板/哈希一致/类型映射/
  枚举校验/同名两策略/碰撞重试/trim与大小写/跳过不阻断/额外列与额外Sheet/公式与错误值/文件级校验/表头校验/超限与verify）；
  全部既有套件通过（object_workspace 8、global_interaction 7、save_queue 22、ontology_lazy_load 11、http_layer 12）；
  `npm run build` 通过。隔离实例（临时 WIZ_WORKBENCH_ROOT + 18767）浏览器验收：A01 下载哈希一致
  （e87da7cd…与原件相同）且概览/无本体空态均有入口；A02 四类各一行导入后对象/共享属性/规则/动作库均可见、
  刷新仍在；A03 单Sheet导入"导入 1 项"；A04/A05 同名跳过与自动重命名（储能簇_492337/_549105）保存后名称与预览一致、
  原储能簇不变、策略切换后旧预览失效；A06 四类错误逐行定位、整批禁用导入；A07 隐藏H列未误导入、公式单元格阻断；
  A08 时序（timeSeries+valueType）与自然语言格式原文保存；A09 检查/取消/全跳过零修订（revision 前后一致）；
  A13 损坏文件（非ZIP）报"这不是有效的 .xlsx 文件"。
- A01—A14未通过项：A11（409/结果未知的并发场景）未在浏览器演练——冲突处置复用 submitForm 既有路径
  （409 提示"版本冲突：服务端已有更新的草稿"，计划作废需重检），结果未知的固定 ID 核对逻辑已有实现与单元行为，
  但未构造真实 409/断网会话；A14 的"上传—检查—导入—刷新"已完成，唯 409/断网分支见前述限制。
- 提交编号：见 git log（本文件回填后与代码同批提交）。
