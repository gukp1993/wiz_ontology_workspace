# 可直接交给其他 harness 的执行指令

请在下面的项目中实现“项目映射：说明与配置 v2.1”。严格遵循给定原型和需求，不重新设计产品交互。

项目根：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_项目映射双模式协同`

本指令供用户交付给执行者使用；当前 Codex 任务只产出文档，没有替执行者实施正式功能。

## 一、先读后做

1. 项目根 AGENTS.md；运行 `.collaboration/context.py read`，使用当前 harness 的真实 actor；检查 Git 状态，保护其他人的修改。
2. 本目录 `需求说明.md`，重点 §9 数据协议、§10 保存/发布/消费矩阵和 §11 验收。
3. **打开 `交互原型_v2.html` 并实际操作两对象、四页签、说明、折叠、保存取消、反向链接。最新 v2 的说明区只有一个 textarea；不要按 v1 或旧版 v2 截图开发。**
4. `开发计划.md`，再读 `文档/接口文档/` 相关分册并核对源码。

## 二、这次必须交付什么

1. 对象、属性、链接、动作四类项目说明可编辑、保存、刷新读取，并与原配置一起进入项目发布快照/导出。
2. 页面按原型重整；说明在前、默认展开可折叠，具体配置在下方默认折叠，展示摘要。
3. 说明区域仅 textarea（提示用 placeholder）。没有变量插入、说明专属连接选择、连接标签、字数工具栏。
4. 清单统一名称、项目说明、编辑。没有普通/严格/组合使用标签、模式开关、编号大卡片、流程条或理解示例。
5. 原字段、Redis、编排、实例登记、链接来源选择、动作参数和认证凭据等功能继续复用，不能因为原型省略而删除。
6. 说明通过现有项目状态接口被消费方获取；已有不支持说明的执行入口必须显式拒绝，不能忽略说明直接执行旧配置。

## 三、按这个顺序实施

### T0–T1：基线、协议和接口文档

记录原有失败与并行修改。按需求 §9 采用最小 mappingDescriptions 扩展；若发现已有等价字段，说明并复用，不能保存两份原文。先更新接口文档的通用模型、项目接口、清单及 README 变更记录，再改实现。

对象/属性/链接/动作均使用稳定 ID；共享属性在不同对象上的说明独立；链接两端共用一份说明；同一动作在不同对象上的说明独立。不要使用显示名称做主键。

### T2：后端完整回环

实现结构/长度检查、旧字段缺省兼容、旧客户端省略整块时保留、新客户端完整块显式清除语义、失效引用、快照与升级预检。旧请求合并必须在同一写锁/CAS 边界内。

已启用映射缺少必要身份/字段/参数时，原发布阻断不变。只填说明不创建空映射，说明可以保存，但不算可执行配置。发布行为严格按需求 §10 表格。

### T3：四类页面

按原型实现 ObjectSources、PropertySources、LinkMappings、ActionBindings 及必要父级接线。抽取一个最小说明编辑组件即可，不引入新 UI/路由/状态大库，不全站重写样式。

说明和配置一次保存；复用 formGuard、form-save、撤销、revision/409。取消不写；折叠不标记业务变更；保存失败保留输入；未知结构只读保留。

### T4：消费边界

复用当前 state/发布快照输出说明上下文；不新建聊天或 LLM 执行系统。实际预览/演示入口若遇到目标或依赖说明且不能解释，调用数据源前返回明确不支持。没有说明的旧行为保持。

动作继续只配置，不新增执行或测试按钮，不发送设备命令。不要将簇级控制自动替换成设备级控制。

### T5：验证与交付

执行需求 V01–V11、D01–D12。完成 frontend 的 npm run build、相关后端和前端套件、隔离浏览器验收（1440/1024/768px）。不要把 DOM 或类型检查当作视觉/执行验收。

## 四、禁止扩大范围

- 本次不实现大模型自主查询、自动生成并强制维护 SQL/编排、模型语义审查或设备动作执行；不声称说明已经能够自动取值。
- 不把本体业务定义替换成项目说明，不改变本体字段/数据类型/引用版本。
- 不新增说明专属连接或参数表单，不因文字描述就开放任意 SQL/Python。
- 不自动覆盖已配置字段、丢失原文、修改发布快照或放松原校验；冲突不能假装已解决。
- 不回退 SQLite 存储为文件；若无需 SQL schema 改动，不新建数据库表或迁移框架。
- 不修改无关文件和其他 harness 的在途工作，不机械启动大量并发代理。

## 五、数据保护与验收重点

测试使用独立 WIZ_WORKBENCH_ROOT、端口和 WIZ_DATABASE_URL；不改真实 ontology/、data/ 或用户数据库，不输出密钥。

至少验证：四类文本刷新回环；共享属性对象隔离；链接反向共用；动作对象隔离；旧请求省略保留与显式清空区别；409 与失败保留输入；发布快照不可变；升级失效说明可定位；纯文本转义及上限；仅说明不产生空绑定；未支持说明的消费者在调用前被阻断。

若遇到仓库现状与需求冲突，记录具体冲突，提出最小调整；不要擅自换一种交互。技术字段命名如需调整，同步契约与计划，不增加用户填写项目。

## 六、交付给用户

1. 在 `开发计划.md` §7 追加实际文件、命令结果、验收项、截图与未测项，不另开重复报告。
2. 通过共享上下文脚本记录实际实现与证据；implemented 与 verified 分开，不替其他执行者声称验收完成。
3. 查看 Git 状态，仅提交本次文件；commit 写清功能和真实验证。
4. 回复访问地址、主要改变、验收结果、限制和 commit。明确这次完成说明管理及上下文读取，尚未实现按说明自动取值或执行动作。
