# 执行指令 · 项目映射双模式协同 v1

项目根：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_项目映射双模式协同`

**当前是原型评审稿。只有用户明确要求实施后，才执行下面的开发任务；交付本指令不等于授权改产品或真实数据。**

## 必读

1. AGENTS.md，执行共享上下文 read，检查 Git 状态，不覆盖其他 harness 的修改。
2. 本目录需求说明.md。
3. 打开交互原型_v1.html，操作实例识别、三个属性、添加/移除结构化配置、文本、连接、保存取消和理解示例。必须遵循原型：自然语言说明在前且可折叠（默认展开），结构化配置在后；不重新设计互斥模式切换。
4. 开发计划.md 与文档/接口文档相关分册，再核对源码。

## 实施任务

1. 先落实开发计划阶段 A：冻结协议和执行/发布边界，先更新接口文档再改代码。未冻结部分提交具体设计，不凭空发散。
2. 阶段 B：复用现有对象与属性表单，增加可与结构化来源共存的自然语言配置、连接范围和稳定上下文引用。保留旧配置、保存队列、409 和未知结构零丢失。
3. 阶段 C：在用户授权的实施范围内实现实际解释与只读取值；普通配置不能只存成备注，组合配置不能忽略说明直接执行旧结构。
4. 阶段 D：执行相关回归、npm run build、隔离浏览器验收，记录实际证据。

## 禁止发散

- 不把普通/严格做成互斥的开关，不复制两个独立项目，不改本体建模。
- 不强制把自然语言转换为编排，不要求每个属性先建规则/声明函数参数。
- 不自动用自然语言覆盖结构化来源，不在结构化执行失败时静默换来源。
- 不借本需求增加动作写接口、任意 SQL/Python 执行、关联聚合模块或新 UI 大库。
- 不解除已有编排输出类型校验来实现未确认的中间记录集合转换。
- 原型“查看理解示例”是固定演示，不得在正式版硬编码成 AI 检查成功。
- 不删现有登记身份、补充来源、旧映射和已有功能；原型简化不等于正式范围收缩。

## 数据与验证

所有测试使用隔离 WIZ_WORKBENCH_ROOT、独立端口和隔离 WIZ_DATABASE_URL。用户数据、连接密钥和不可变快照不动，不在输出记录密钥。读操作不改草稿；文本编辑经统一保存路径，失败保留输入。

验收需求 P01–P11，加真实功能验证：三种配置持久化与读取；修改文本使旧检查失效；冲突/缺失/模型不可用提示；身份唯一；类型匹配；动态表字段允许范围；纯自然语言/组合规则不能被旧执行器静默忽略；发布兼容；409；原始说明与实际执行步骤可追溯。

## 最终交付

实施记录追加到开发计划.md，列文件、验证命令、结果、未测项、用户访问路径和提交。更新共享上下文，区分 implemented 与 verified。只提交自己负责的文件，不把其他执行者改动混进来。
