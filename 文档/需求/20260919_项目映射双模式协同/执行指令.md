# 执行指令 · 项目映射说明与配置 v2

项目根：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_项目映射双模式协同`

当前仅原型评审。收到用户明确实施要求后再开发，不把本指令当作授权。

## 必读顺序

1. AGENTS.md；运行共享上下文 read；查看 Git 状态，保护其他执行者的修改。
2. 本目录需求说明.md（v2）。
3. **打开交互原型_v2.html，切换两对象及对象/属性/链接/动作四页签，操作说明、配置折叠、保存取消和反向链接。必须按 v2 原型落地，v1 已被替代。**
4. 开发计划.md，相关接口文档和代码入口。

## 固定设计

- UI 围绕对象、属性、链接、动作；说明在前，具体配置在后。
- 说明默认展开且可折叠，具体配置默认折叠、显示摘要；复用现有配置表单。
- 不展示“普通模式／严格模式／组合使用／自然语言模式”标签、配置方式列、编号卡片、取值流程条或理解示例。
- 清单统一名称、项目说明、编辑入口；本体定义只读按需查看。
- 普通文字和精确配置可以共存，但不以机制名称要求用户选择。

## 具体任务

A. 冻结项目层四类说明及连接/上下文引用协议。链接两端一份映射；动作按项目+对象+动作唯一。先改接口文档再改代码。
B. 实现保存、读取和旧结构兼容，原有字段、函数编排、链接、接口参数/凭据、登记身份不丢失。
C. 改四类页面和统一说明编辑区，保留 formGuard、form-save、撤销、revision/409；折叠不写数据，取消不落库。
D. 在用户授权范围内实现只读解释取值；不能仅存备注或静默忽略说明。未授权/未实现执行时明确标注限制。
E. 按需求 V01–V11 验证，build、相关回归和隔离浏览器检查。

## 范围红线

- 不改本体业务定义、主导航或全站样式；不引入模式工作区。
- 不擅自把文字转换成必须维护的编排，不重建所有表单。
- 冲突和信息缺失不能猜测；文本变动使旧理解结果失效。
- 原型模拟目录和参数摘要不能当作正式取值实现；正式版必须复用完整的字段/参数表单。
- 动作只配置不执行，不做接口测试，不发送设备命令；簇级动作不能自动扩大到设备级。
- 不动真实 ontology/、data/、用户数据库和密钥；测试独立数据根、端口和 WIZ_DATABASE_URL。
- 保留未知结构、历史动作只读及失效映射提示；不让旧执行器忽略新增说明继续执行。

## 交付

在开发计划.md 追加实际修改、命令、验收结果和未测项；更新共享上下文，区分已实施/已验证；只提交本次文件。提供访问地址、行为变化、验证结果、限制与 commit。接口或执行边界未定时先提出明确方案，不自由发散。
