# 本体列表统一设计 · 开发计划

基准：[需求说明](需求说明.md) · [交互原型](交互原型_v1.html) · [执行指令](执行指令.md)

## 1. 当前事实及代码入口

已静态检查 2026-09-18 当前源码和用户截图；正式实现未修改。

| 文件 | 现状 | 处理范围 |
| --- | --- | --- |
| frontend/src/ontology/ObjectWorkspace.vue | 对象导航；属性/动作/规则表格；链接卡片 | 两种容器接入、四页签统一工具栏与表格、只读详情/返回定位 |
| frontend/src/ontology/SharedLibrary.vue | 自有条目样式，维护/引用/复制/删除及批量工具 | 全宽表格；保持现有共享引用/私有化语义 |
| frontend/src/ontology/BusinessRuleLibrary.vue | 名称、引用对象、操作表格 | 加已有业务定义摘要，统一样式；不增删除能力 |
| frontend/src/ontology/ActionLibrary.vue | EditorLayout 左列表/右详情 | 列表态换为全宽表格，编辑态继续复用已有字段/守卫 |
| frontend/src/shared/RowMenu.vue | 已有键盘可用菜单 | 复用，支持列表行“⋯”呈现与准确 aria-label；不破坏现有调用 |
| frontend/src/shared/AppSelect.vue | 自定义下拉 | 复用筛选，无原生 select 混搭 |
| frontend/src/shared/EditorLayout.vue | 多模块复用 | 优先不改公共默认结构，避免误伤其他页面 |
| frontend/src/ontology/PropertyManager.vue | 属性编辑与保存 | 原样接回，不重写业务表单 |
| frontend/src/ontology/BusinessRuleDialog.vue | 四字段规则查看/编辑 | 编辑继续复用；只读详情内容可提取复用，字段/校验不改 |
| frontend/src/ontology/propertyModel.ts、actionModel.ts、businessRuleModel.ts | 有效属性与引用模型 | 复用，不新增协议 |
| frontend/src/app/formGuard.ts、saveCoordinator.ts | 保存/离开保护 | 本期不重构，接入不得绕过 |
| frontend/src/style.css | 现有 ld-* 及全站样式 | 审查依赖后使用本体局部类；禁止全局改所有 table/button |

实施前重新读取共享上下文、Git 状态及最新文件；zcode 可能同时在处理其他需求，本表不是冻结代码快照。

## 2. 建议的最小公共层

复用优先，不为本期引入大型表格库或页面配置 DSL。

- `OntologyListToolbar`：搜索、筛选插槽、结果数、操作插槽；不访问 API、不保存模型。
- `OntologyListTable` 或等价样式/组合：语义 table、列插槽、空态、分页外壳。业务行值和操作由页面提供，禁止抽成巨型万能组件。
- `DefinitionPeek` 或等价小容器：只读详情面板，负责遮罩、关闭、焦点和窄屏；各类型内容用插槽复用。
- 已有 RowMenu/AppSelect 直接复用；通用视觉变量统一，不复制四套 CSS。

若现有组件已经能承担上述职责，允许复用而不新增同名文件。公共层只处理显示与事件，不获取 props.state 全量本体并自行增删记录。

## 3. 实施顺序

### T0：基线与映射确认

读取四份交付物，打开 HTML 交互，列出八处区域的现有入口、可执行操作和保留项。确认共享删除是私有化、业务规则无删除、动作历史转换仍存在。记录本次涉及文件与他人未提交修改。

产出：在本文末尾追加实际基线和范围，不新建一堆状态报告。

### T1：统一容器和视觉

建立最小工具栏、表格与只读详情外壳；先用对象属性页签验证视觉、名称/编辑区别、更多菜单和分页。确认未改 API/保存逻辑后复用到其他区域。

完成标准：至少一页真实数据接入；搜索与分页只是视图；原有表单保存/取消保持。

### T2：对象建模五处列表

对象导航调整样式并保留键盘/分页/收藏/画布入口；统一属性、链接、动作、规则。链接卡片改表格，真实方向不反转；关联移除仍调旧函数；悬空引用仍可见。

完成标准：U03–U06、U09 的对象页部分通过隔离验收。

### T3：三个资产库

共享属性、业务规则、动作定义接同一表格；动作列表态不再使用额外窄列；各编辑器继续复用。引用数/对象数分开统计；跨菜单定位和返回恢复工作。

完成标准：U07/U08 通过；原有低频能力未丢失，不出现空“更多”菜单。

### T4：响应式与状态验收

对齐 1440/1024/768px 页面；验证无结果/空/异常/历史/大量数据；确认菜单不被表格滚动容器裁切，详情焦点不跑到遮罩后。

### T5：回归、记录、交付

相关既有测试 + npm run build + 隔离页面验收。将完成项、未测项、截图位置及命令结果追加本文；更新共享上下文并只提交本次文件。

## 4. 可并行边界

先完成 T0/T1 统一接口，之后对象建模与三个资产库可以按文件分工。公共组件、style.css、App.vue/导航由同一负责人集成。不要让多个执行者同时改 ObjectWorkspace 或共享样式，不以并发数量为目标。

## 5. 测试计划

本次不要求全后端回归，因为不改协议或服务。若实施中发现确需后端变更，应先报告原因、更新需求/接口文档，再扩大验证范围。

1. 先确认测试实际入口，再选用既有对象工作区、共享引用、动作关联、业务规则、导航和保存守卫测试。
2. 新测试仅覆盖新增风险：分页前全量过滤、跨菜单返回、链接真实方向、更多菜单不改删除语义。不要写只断言 CSS 文本或重复模板结构的机械测试。
3. `cd frontend && npm run build` 必须通过。
4. 真实页面验收用临时数据根/隔离数据库/独立端口；同时处理 WIZ_DATABASE_URL，不能让临时根服务连到真实库。
5. 列表浏览/排序/分页期间检查不产生保存请求；编辑保存失败仍保留输入；取消不产生新定义。
6. 用隔离夹具验证删除/移除影响，不在用户储能本体上做破坏性验证。

## 6. 当前交付状态

- 已输出八处列表的统一容器、列字段、动作、状态、响应式与验收要求。
- 已提供独立 HTML 原型，业务编辑/真实保存不在原型中实现。
- 正式前后端、本体数据、项目数据均未因本方案修改。
- 原型检查结果由 Codex 在交付前追加；正式实施与验收结果由执行者后续追加，不把本节当成实施完成证明。

### 原型交付检查（2026-09-18，Codex）

- 使用本机既有 jsdom 对独立 HTML 做 24 项源码级 DOM 检查，全部通过：四个对象页签、三个资产库表格、20 项分页、跨页搜索、类型筛选、详情、引用定位、移除关联后库定义保留、空/失败/重试、新建对象入口与 Esc 关闭。
- 原型无外部脚本或样式资源；未调用工作台接口，未写业务数据。
- 浏览器工具的 URL 策略拒绝打开本地 HTML，未绕过该限制。**本轮没有完成浏览器视觉验收**，DOM 检查不代表 1440/1024/768px 布局已实测。交付后可由用户本地打开评审，正式实施阶段仍须按 U11/U13 验收。
- 本次仅文档和独立原型，无正式前端源码变更，因此未运行产品构建或业务回归。
