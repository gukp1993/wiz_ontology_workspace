# 函数编排配置与调试优化 · 执行指令 v1.1

以下指令可直接交给任意开发 harness。你负责在现有工作台实现本需求，不依赖其他聊天历史。此文档的生成不表示已实施；在用户将本指令交给你执行后开展代码工作。

## 工作目录与需求基准

项目根目录：
`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：
`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_函数编排配置与调试优化`

本次唯一基准：

1. [需求说明.md](需求说明.md) v1.1，含F01–F20。
2. [交互原型_v1.html](交互原型_v1.html)，已包含独立测试页和B–C–D连续片段。
3. [开发计划.md](开发计划.md) v1.1。
4. 本执行指令。

更早的函数编排文档只用于理解兼容逻辑，不能覆盖本次“不要把输出挤在画布下方”“只执行中间片段”的决定。

## 开工前必须做

- 阅读AGENTS.md，Codex 运行 `python3 .collaboration/context.py read --actor codex`，zcode 运行 `python3 .collaboration/context.py read --actor zcode`；其他 harness 沿用已配置的交接身份，脚本当前仅接受 codex/zcode，不自行传入任意名称。记录返回的 ticket。先核对git status，保护其他执行者修改。
- 按需求说明 → 原型 → 开发计划 → 接口文档 → 当前代码顺序阅读。
- **实际打开原型，分别体验节点连续配置、测试全部、单节点、测试片段B到D、逐节点结果、返回编排及属性绑定。严格遵循当前浅色风格和独立测试布局，不凭文字另做一套UI。**
- 若环境无法打开原型，读取HTML/CSS/JS核对结构，并如实记录视觉检查限制；不能声称已经视觉验收。

## 需要完成的工作

### 1. 先登记接口，再写执行代码

复用POST `/api/flow-run`。按需求说明§9，先更新接口文档04、相关01结构、05条目和README变更记录：

- 新增可选 `testMode: "isolated"`。
- 新增稳定ID输入覆盖 `inputOverrides: {nodeId: {inputId: value}}`。
- 新增可选节点实际输入预览 `inputs` 与 `inputsTruncated`。
- 明确错误码、边界输入规则与旧调用分支。

不要让前后端绕过接口文档约定。不要新增不必要的通用执行API。

### 2. 实现真正的中间片段测试

针对A→B→C→D→E，选择B/C/D：

- 手工输入B本来从A获取的数据，不执行A。
- B输出自动交给C，C输出自动交给D。
- E不执行。
- 不要求补齐范围外上游，不把A/E配置不完整作为片段阻断条件。
- 范围内真实依赖、实现、类型、连接必须检查；输入不足或类型错误必须在所有节点执行前拒绝。
- 处理同名参数、对象字段、多条边界输入、固定值和侧路来源。
- 不允许inputOverrides替换范围内上游输出或固定配置；不允许通过旧技术名覆盖入口绕开新规则。
- 失败沿所选下游传递：B失败则C/D都不执行；保留此前成功结果。

不能仅删除missingUpstreams提示或validate_chain调用就算完成；必须补齐输入解析、范围校验、结果及测试。

### 3. 按原型调整编辑与测试页面

- 节点配置连续显示输入/实现/输出，高级项折叠；保留所有现有节点配置能力。
- 来源选择合并分组，兼容与循环错误明确；继续用稳定ID，连线从绑定派生。
- 顶部显式显示运行项目；复用现有上下文切换，不静默重绑连接。
- 测试进入独立主工作区：左输入、右结果，不在画布下方开一个更高的结果面板代替。
- 整条、单节点、连续片段三种范围都能操作；起止按依赖图，不按数组/画布位置。
- 多路径不猜测，提示明确选节点并展示最终范围，复用已有多选。
- B/C/D分别查看实际输入、输出、错误、日志；范围外与依赖失败未执行清楚区分。
- 长结果在自己的区域滚动，1024×768首屏能看到结果区域。
- 返回保留选中节点、画布视口、输入与结果；项目/配置/范围/输入变化后旧结果明确标记；晚回包不污染新上下文。
- 从属性配置进入时打开正确编排，返回恢复原属性表单；测试值不写入属性来源。

### 4. 保留现有业务边界

本次不要实施：Python沙箱、Redis追加字段新模式、列表SUM/属性访问表达式、时间序列属性接入、调度/循环/运行历史/编排发布制度、顶层菜单重组。

原型的SOC流程是交互示例；其列表语法不是正式执行协议。用已有公式语法构造五节点测试链验证本次能力：A={x}、B={x}+2、C={x}*3、D={x}/2、E={x}*100。B输入10时，片段结果依次12、36、18，A/E执行计数为0。不得硬编码这条示例逻辑到产品。

当前Python仍为LLM推演，必须准确展示；现有SQL/Redis/HTTP可真实执行，保留当前写操作确认和凭据安全边界。不要把所有正式测试标成无副作用模拟。

## 建议代码入口与拆分

- 前端：flow/FlowEditor.vue、NodeConfig.vue、BindingEditor.vue、FlowCanvas.vue、BoundaryConfig.vue、flowModel.ts、api.ts，必要时提取FlowTestWorkspace及小型测试状态辅助。
- 后端：flow_routes.py、flow_executor.py、flows.py，必要时提取纯范围计划函数。
- 接线：App.vue、app/navigation.ts、project/PropertySources.vue；只改本次所需部分。
- 保存：复用现有flowSaver、before-change/changed及undo/redo，不复制保存器。

顺序：T0契约 → T1后端隔离执行与T2前端测试视图 → T3配置/导航 → T4联调 → T5验收。独立工作可并行，同一文件保持单一负责人，不为凑并发拆分。不得依赖特定模型、插件或固定并发数。

## 数据和兼容保护

- 不修改真实ontology、SQLite、发布快照、连接/模型密钥。
- 测试使用独立WIZ_WORKBENCH_ROOT、端口、WIZ_DATABASE_URL和账号；避免继承真实数据库环境变量。
- 所有API仍需登录，资产/项目/凭据按账号隔离。
- 旧编排及未知扩展字段保存无损；全图revision与409/per-flow约束不变。
- 旧testMode缺省调用语义兼容；空targets不变成全图。
- 执行期间不持全局LOCK；输入预览不能影响真实计算值或泄露凭据。
- 测试输入/结果放页面内存，不写入编排资产、localStorage或共享上下文。

## 验收与交付

1. 逐项完成需求F01–F20。特别记录B–D结果和A/E真实调用次数为0的证据，不能只截前端绿色状态。
2. 覆盖预检零执行、错误传递、边界类型、旧调用、账号隔离和晚回包。
3. 运行相关回归：flow_model.test.mjs、test_flows.py、test_flow_executor.py、test_flow_sql_dialect.py、test_project_flow_source.py及新增用例；按开发计划使用隔离测试入口。
4. 前端 `npm run build` 通过。
5. 隔离浏览器检查1440×900、1280×800、1024×768，并检查768宽；保留独立测试布局与返回状态截图/点击证据。不能用类型检查代替浏览器验收。
6. 实际文件、接口变更、命令结果、F01–F20、未测项、截图和提交号追加到开发计划.md“实施记录”。不要新建重复说明。
7. 通过共享上下文脚本记录实际进度；只提交本轮负责文件。提交信息准确区分已实现、已测试与未验证。
8. 最终回复访问地址、改动、验证、限制和commit。没有完成关键片段执行或视觉验收时必须明确，不得宣称全部完成。
