# 样式与交互统一 · 开发计划与验收基线

需求来源：用户口头指令（2026-09-21）——"帮我样式，交互统一，优化。保持现在工作台的风格。修改完毕之后，让子 agent 验收，验收不通过把验收意见反馈给主 agent 修改，直至验收通过。"

验收工具：用户选定 **Qoder 官方市场 Design Review 插件 v0.1.0**（`qoder-marketplace/design-review`），并明确"按照新安装的插件的规则"执行循环。第三方社区 skill `hallmark` **未安装、不使用**。

---

## 0. 授权与停止点（自包含）

| 阶段 | 是否已授权 | 说明 |
|---|---|---|
| 创建 worktree / 分支 | 已授权（用户明确指令） | `codex/ui_fix` @ 基线 `4714e8a`，目录 `worktree/ui_fix/` |
| 开发、自测、提交到分支 | 已授权 | 本轮实施者：zcode |
| 独立验收 | 已授权（用户指令要求子 agent 验收） | 验收不通过 → 回原 worktree 修复 → 复验，循环到通过 |
| **集成并合并 main** | **未授权** | 必须等用户明确要求；不得自行合并 |
| 重启主工作台（18765） | 未授权 | 另需指令 |
| 删除本 worktree / 隔离数据 | 未授权 | 数据根含真实数据快照，删除前必须用户确认数据处置 |

- 开发目录与主仓库不同：主仓库 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`，开发 cwd 必须显式为 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/ui_fix`。
- 隔离实例：`WIZ_WORKBENCH_PORT=18882`，`WIZ_WORKBENCH_ROOT=<worktree 自身目录>`（库 `data/workbench.sqlite3`、根密钥 `keys/wb-root.key`）。**该库是 main 库的 WAL 一致快照 + 根密钥副本，含真实数据，不可自动丢弃。**
- 18765（主工作台）、18881（ontology-build 任务）、18912 归属其他会话，不抢占、不重启、不为其释放端口而杀进程。

## 1. 范围与非目标

**范围**：前端样式与交互表现统一——色值收敛到 `:root` 令牌、重复控件收敛到共享件、交互态（hover/focus/disabled/loading/empty/confirm/aria）一致、死样式与漂移副本清理。

**非目标（明确不做）**：

1. **不改 `:root` 任何既有令牌的值**——这是"保持现在工作台的风格"的可审计硬约束。
2. 不改接口协议、不动 `modelFormat.ts` / `navigation.ts` 的行为语义。
3. 不改造 `ontology/legacyGraph/**`（登记例外 1）。
4. 不做 **`padding/margin/gap/width` 那类大批量** 像素→尺寸令牌迁移（登记例外 6：必然改变实际像素，属视觉改动）。
   **本条在实施中被部分突破，如实登记**：`border-radius` 中间值并档 32 处、`outline-offset` 3px→2px 两处、`box-shadow` 并到 `--shadow-*` 若干处、`.field-help` 与骨架宽度各一处——这些改的是**组件声明**（`:root` 令牌值一个没动），但确实改变了渲染像素，属视觉改动而非纯漂移收敛。全部前后实测值见 §9 的决议值对照附表（135 行），不在此逐条复述。剩余 62 处非令牌圆角与约 48 种 `.field-help` margin 组合仍按例外 6 保持 main 原值。
5. 不引入新视觉语言、新配色体系、动画或 Tailwind。

设计契约唯一来源：仓库根 `DESIGN.md`（含 `## 登记例外` 1–11）；扫描配置：`design.qa.yaml`。

## 2. 验收基线（通过判据）

验收 agent 用插件脚本从 worktree 根运行：

```bash
export DESIGN_PLUGIN_ROOT=/Users/gukepeng/.qoder-cn/plugins/cache/qoder-marketplace/design-review/0.1.0
node "$DESIGN_PLUGIN_ROOT/skills/design-md-review/scripts/audit-design-md.mjs" --config design.qa.yaml
node "$DESIGN_PLUGIN_ROOT/skills/component-library-alignment/scripts/audit-components.mjs" --config design.qa.yaml
node "$DESIGN_PLUGIN_ROOT/skills/design-debt-review/scripts/audit-design-debt.mjs" --config design.qa.yaml
```

**必须满足（A 组，硬判据）**：

| # | 判据 | 实测 |
|---|---|---|
| A1 | `audit-design-md.mjs` findings = 0 且 `ok: true` | 0 / true |
| A2 | `audit-components.mjs` findings = 0 | 0 |
| A3 | 硬编码色（`hard-coded-color`，排除 `legacyGraph/**` 与 `shared/graphStyle.ts` 后）目标 **0**，本轮实测 **1** | 1 —— 唯一一处是 `flow/FlowEditor.vue:723` `.flow-list` 的侧栏方向性阴影 `8px 0 20px #1b32531a`。本轮曾把它并入 `--shadow-2`，但那令牌是**居中浮层大阴影**（`0 16px 48px`），几何完全不同，合并属视觉回归，故按缺陷 D5 回退为 main 逐字节字面量。**口径：`hard-coded-color ≤ 1` 且该处必须逐字节等于 main**，不再要求 0；理由与实测见 §4 与 §8.1/D5 |
| A4 | `rgb()/rgba()/hsl()/hsla()` 函数色（同口径）= **0** | 0 |
| A5 | 原生 `<select>`（`legacyGraph/**` 之外）= **0**，一律 `AppSelect` | 0（26 个视图全量走查） |
| A6 | `:root` 令牌值与 **merge-base `07f8d8d`** 零差异（**不是** `git diff main`：main 在分支建立后又前进 3 个仅文档提交，见 §8.3 第 4 条） | 基线 37 个 / 分支 37 个，改值 **0** 处；集合变化仅 `− --ok-bright`、`+ --paper-float`，见 §9.1 |
| A7 | `cd frontend && npm run build`（含 vue-tsc 严格检查）与 `npm run lint` 通过 | 通过 |
| A8 | 隔离实例浏览器实测：两区全部视图渲染、无新增 console error、自动保存/撤销/离开保护/保存队列行为不变 | 见 §5 |
| A9 | 每条"保持风格"的改动都能追溯到 DESIGN.md 里**已存在的**令牌或组件；新增语义名必须同时出现在 `:root` 与 DESIGN.md | 新增 CSS 语义类 `.danger-btn`（由 `BuildTasksPage` 的 `.bt-danger-btn` 上收）与继承属性 `--app-select-gap`，均已登记在 DESIGN.md 组件表。**`CANVAS_WHITE` 不是本轮新增**——它是 `shared/graphStyle.ts` 里 main 既有的 TS 常量（图形渲染参数，例外 2/3），不是 CSS 令牌，早先本节把它写成"新增已登记"属口径错误，此处更正 |
| A10 | **特异性守恒**：全局基元交互态规则（`button`/`.primary`/`.danger-btn` 的 hover）必须写成 `:where(:not(:disabled))` 保持 (0,1,1)，**不得**写成 `:not(:disabled)`(0,2,1)；类级 hover 覆盖必须仍然生效。上收到 `style.css` 的规则若与组件同名 scoped 规则同特异性，**必须改为继承自定义属性或删除**，不得留死规则 | `grep -c 'where(:not(:disabled))' frontend/src/style.css` = 3、`grep -c 'button:not(:disabled):hover' frontend/src/style.css` = 0；`dist` 内 `.editor-field .app-select` = 0 命中。见 §8.1 D1/D6 与 §8.3 第 3 条 |

**不作为通过门槛（B 组，已登记的口径限制，不得据此判失败）**：

| # | 项 | 为什么不作门槛 |
|---|---|---|
| B1 | `px-magic-number` 计数 | DESIGN.md 例外 6：收进"既有档位"必然改变像素，属视觉改动，超出本轮范围 |
| B2 | `custom-shadow` 计数 | 例外 7：该正则匹配属性名不看值；main 与分支同为 19 处命中，除 `flow/FlowEditor.vue` `.flow-list` 的 1 处方向性阴影（main 既有字面值，本轮曾误并入 `--shadow-2` 后已回退）外全为 `var(--shadow-*)`、结构性内描边、选中环或 `none` |
| B3 | `tailwind-arbitrary-value` 计数 | 例外 7：本项目不用 Tailwind，main 与分支同为 4 处命中，是 JS 正则字面量/注释误报 |
| B4 | `inline-style` 计数 = 14 | 例外 4 已穷举登记这 14 处数据驱动绑定；扫描器无豁免机制，故 `audit-design-debt.mjs` 的 `ok` 会因 `major` 恒为 false。**判据是"命中集合恰好等于例外 4 清单且无新增"，不是布尔 `ok`** |
| B5 | `audit-design-debt.mjs` 的 `summary` 分类计数 | 该脚本 `sortFindings().slice(0,1000)`，而 `px-magic-number` 单类 2700+，截断后其他类别计数被低估。需要可信计数时按同源正则不截断统计并固定 `code.exclude` 口径 |
| B6 | `uiAlignment` 视觉回归 | 本机无 playwright，`design.qa.yaml` 已设 `runVisualComparison:false`；改以 §5 浏览器实测覆盖 |

**验收反馈格式要求**：每条不通过项必须给 `文件:行号` + 现象（observed）+ 期望（expected）+ 判据编号（A1–A10）。只给结论不给定位的反馈无法闭环。

## 3. 任务拆分与状态

| ID | 目标 | 依赖 | 状态 |
|---|---|---|---|
| T1 | 冻结设计契约 `DESIGN.md` + 扫描配置 `design.qa.yaml` | — | 完成 |
| T2 | Batch A：flow/project/shared 近重复语义色收敛到既有令牌 | T1 | 完成 |
| T3 | Batch B：`tools/*` 辅助页配色与静态内联样式收敛 | T1 | 完成 |
| T4 | Batch C：交互态一致性（hover/focus/disabled/loading/empty/confirm/aria）+ 原生 `select` 全部改 `AppSelect` | T1 | 完成 |
| T5 | Batch D：债务扫描复检、量化对照、例外登记 | T2–T4 | 完成 |
| T6 | 子 agent 独立验收循环（第一轮 4/4 不通过，结论见 §8） | T5 | 进行中：缺陷 D1–D7 已修并复验（§9），待按新 SHA 复验 |
| T7 | 集成合并 main | 用户指令 | **等待授权** |

## 4. 量化结果（main 与本分支同一口径）

**口径**：`/tmp/debt_measure.mjs` 复刻 `audit-design-debt.mjs` 的同源正则但**不做 `slice(0,1000)` 截断**，排除 `legacyGraph/**` 与 `shared/graphStyle.ts`；main 基线取 `git archive main` 解出的纯净树（`7c4679e`），两侧各扫描 **120 个文件**。

| 类别 | main | 本分支 | 判据 |
|---|---|---|---|
| 硬编码色 `#hex` | 155 | **1** | A3（见下注） |
| 函数色 `rgb/rgba/hsl` | 2 | **0** | A4 |
| 内联 `style=` | 59 | **14**（= 例外 4 清单） | B4 |
| 原生 `<select>`（legacyGraph 外） | 9 | **0** | A5 |
| 组件绕过类 findings（插件 + 同一 `design.qa.yaml`） | 9 | **0** | A2 |
| `box-shadow` 声明 | 19 | 19 | B2 |
| `px` 魔法数 | 2854 | 2744（不在本轮范围） | B1 |
| `tailwind-arbitrary-value` | 4 | 4 | B3 |
| 未被任何样式引用的死令牌 | 1（`--ok-bright`） | 0 | — |
| 非令牌 `border-radius` 字面值 | 62+ | 62（本轮未并档的存量，见 §1 非目标 4） | B1 |

> **A3 由 0 改为 1 的说明**：`flow/FlowEditor.vue` 的 `.flow-list{box-shadow:8px 0 20px #1b32531a}` 是 **main 原值**。首轮实施把它并进 `--shadow-2`（居中浮层阴影，与侧栏方向性投影几何语义不同），属把合法用法改坏；本轮已回退成逐字节等于 main，因此全站剩这 1 处一次性色值，按例外 7 登记为"保留 main 渲染"而非债务。此前本节写的"155→0"混用了不同口径（155 是 main 的同源正则计数，0 是排除图形参数后的计数），现统一为上表口径并把这 1 处如实写出。
> 早先版本还把"组件绕过 812→0"写进本表：812 来自插件**默认配置**（不排除 `graphStyle.ts`/`legacyGraph`），与提交进仓库的 `design.qa.yaml` 口径不同，同一口径的真实对比是 **9→0**。

`:root` 令牌值差异：**0 处改值**（main 与分支的 `--shadow-1/--shadow-2/--focus/--r-*` 等 37 个令牌逐字节相同）。令牌**集合**变化只有两处：①删除全站零引用死令牌 `--ok-bright:#47c4a3`；②新增 `--paper-float:#fffffff0`（收纳 main 里散落的 `#fffffff2` 一类画布浮层近透明白底）。所以两侧计数都是 37，不是"改了一个值"。

## 5. 已实测 / 未实测（诚实边界）

**已在 18882 隔离实例浏览器实测**（账号 `uiverify`，为该副本新建，非 main 账号）：

- 26 个视图全量走查（本体区 7 + 工具 8 + 项目区 6 + 编排 2 + 设置 2）：渲染正常、原生 `select` 计数 0、内联 `style` 计数与例外 4 一致。
- 本体区：对象建模（列表/图谱双页签）、共享属性、业务规则、动作定义、接口定义、计算契约、校验与发布（**实际发布成功 1.0.0**，顶栏保存态随之刷新）、从物料生成（入口页）、更多工具。
- 项目区：新建项目"UI验收项目"（引用 1.0.0）→ 项目概览 / 数据连接 / 对象映射 / 取值规则库 / 项目校验与发布 / 版本引用，均含内容渲染，无 console error。
- 编排：新建编排"UI验收编排" → 添加 SQL 节点 → 输入参数类型改为"是/否" → 测试调试视图的布尔 AppSelect 展开选项（（请选择）/是/否）并成功选中绑定；`FlowEditor` 检视栏拖拽、画布节点定位内联样式为例外 4。
- 空态：`.ont-empty-ico` 与 `.empty-state-ico` 同为 40×40 圆（`border-radius:50%`）。
- 行为安全网：自动保存、撤销/重做按钮态、离开保护、发布前 commit-now 均未见回归。

**未实测（限制，须如实标注）**：

1. `ontology/build/BuildReviewPage.vue` 的 7 处 `AppSelect` 转换**未做浏览器实测**——该页需要真实模型跑完一次物料构建才有数据，隔离副本未配置模型密钥。已通过 vue-tsc 严格检查 + 与其他 9 处已实测同构调用比对，但不等于实测。
2. 视口宽度受限于 831px（浏览器窗口不可调），`legacyGraph` 图谱画布在 `≤900px` 时因 `.lgh{height:auto}` 塌成 0 高——**main 同一文件、同一规则，属既有行为**（例外 1 范围），非本轮回归；强制给 `.lgh` 一个确定高度后画布恢复正常。
3. 未做多浏览器/多分辨率对比，未做视觉快照回归（B6）。

**console error 核对**：`#settings-models` 有一条 `Cannot read properties of null (reading 'status')`，来自 `App.vue` 的 `activeSaver.value.status`（全局视图下 `activeSaver` 为 null）。该行与 main **逐字节相同**，属既有缺陷，不在本轮样式范围内，另案处理。

## 6. 隔离副本内新增数据（披露）

`worktree/ui_fix/data/workbench.sqlite3` 是 main 库快照，本轮为验收在其内新建：账号 `uiverify`、本体"验收本体"（1 个对象"储能簇"，已发布 1.0.0）、项目"UI验收项目"、编排"UI验收编排"（1 个 SQL 节点）。**均只存在于该副本，不影响 main。** 是否保留 `uiverify` 与这些验收数据由用户决定；删除整个 worktree 需用户确认数据处置。

## 7. 验证命令

```bash
cd /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/ui_fix
cd frontend && npm run build && npm run lint && cd ..
WIZ_WORKBENCH_PORT=18882 WIZ_WORKBENCH_ROOT="$PWD" ./start.sh status
python3 tests/run.py quick        # 后端未改动，作为回归哨兵
```

---

## 8. 首轮独立验收结论（4 组只读子 agent，被验提交 `4d719ef`）

派发的四组均为**只读评审**，判据 A1–A9，结论 **4/4 不通过**。合并去重后的判定如下。

### 8.1 被验真实缺陷（全部已修，复验见 §9）

| # | 缺陷 | 位置 | 修复 |
|---|---|---|---|
| D1 | 全局基元 hover 写成 `button:not(:disabled):hover`，特异性 (0,2,1) **反吃**所有类级 hover（`nav button:hover` (0,1,2)、`.row-link.danger:hover` (0,2,0)、`.space-new:hover`、`.go-fix:hover`） | `style.css:21/23` | 改为 `:where(:not(:disabled))`，恢复 (0,1,1)：既屏蔽禁用态又不抬权重 |
| D2 | `.empty` 与 `.empty-state` 被错误"合并为同一条声明"并声称同值；main 实际 `.empty{padding:45px}` / `.empty-state{padding:44px 24px}`，合并会让约 12 处非 legacy 空态左右各少 21px | `style.css:129/130` | 回退为 main 各自的值；DESIGN.md 登记例外 8 说明三名并存且不合并的理由 |
| D3 | 两处空态文案 `#789` ≈ 3.4:1，承载信息却低于 AA | `tools/InstanceGraph.vue` `.graph-empty`、`tools/KnowledgeCanvas.vue` `.kc-empty` | 改 `--muted`（实测 5.34:1） |
| D4 | 上收时新增的全局 `.detail-heading button{font-size:13px}` 是**永不生效的死规则**（同名 scoped 规则特异性相同且注入更晚，实际生效 `padding:2px 8px;font-size:20px`） | `style.css` | 删除该全局规则；保留 `InstanceGraph.vue` 的 scoped 规则（`grep` 复核：全仓仅 1 处 `.detail-heading button`，即 scoped 那条） |
| D5 | `@media(max-width:1439px)` 下 `.flow-list` 的**方向性**侧栏阴影 `8px 0 20px #1b32531a` 被并进 `--shadow-2`（居中浮层大阴影，几何完全不同） | `flow/FlowEditor.vue:723` | 回退为 main 逐字节字面量。代价如实登记：全站硬编码色由 0 变 **1**（A3 见 §4 说明） |
| D6 | `AppSelect` 在 `.editor-field` 内 `margin-top` 与同容器原生控件不一致（5px vs 8px）；而全局 `.editor-field .app-select{margin-top:8px}` 因 scoped 同特异性 + 注入更晚**完全失效**（死规则） | `shared/AppSelect.vue`、`style.css:37-39` | 改用**继承自定义属性** `--app-select-gap`：容器设 8px，组件读 `var(--app-select-gap,5px)`。继承逐元素生效，不进特异性竞争 |
| D7 | 文档失准（详见 §8.3） | `DESIGN.md`、本文件 | 逐条改为可复测叙述与实测数 |

### 8.2 经核对**不成立**、已拒绝的验收结论

| 声称 | 实测 |
|---|---|
| `.btn:focus`、`.mini:hover`、`.tag:hover` 缺焦点/悬停态 | 三个选择器在本分支**与 main 中均不存在**，全仓 grep 0 命中 |
| "`--app-select-gap` 链路断裂 / `.editor-field .app-select` 死规则仍在" | 源码与 `frontend/dist` 双重 grep：该全局规则**已不存在**（0 命中）；引用行号 21/110/308/353/380 处无对应内容 |
| `.ws-tabs button.active` 的 `inset` 描边是本轮新增 | 该行与 main **逐字节相同** |
| `.form-grid .editor-field{margin:0}` 吞掉字段间距 | 真实规则是 `.form-grid .editor-field.full{grid-column:1/-1}`，只设栅格跨列 |
| "`.editor-field{margin:0}` 共 8 处" | 实测 **2** 处（`LoginView` 与 `.library-toolbar`，后者无命中是 main 遗留死规则），已登记例外 11 |
| "`--focus` 用了 17 处" | 实测 9 处 `outline` + 3 处选中环，另有 2 处 `outline:none` 已登记补偿 |

### 8.3 必须记录的**验收数据可信度问题**

1. **G2 伪造基线 SHA。** 其报告给出的"基线 `e54d9bd` + 提交信息"在仓库中**不存在**：`git reflog`/`git log` 只有 `8abd3a1 → 4d719ef`。该组所有依赖此 SHA 做的"与 main 对比"结论因此无法复核，本轮按**未证**处理，不作为修复依据。
2. **根因：评审对象与修复位置不同步。** 四组评审的都是**已提交的 `4d719ef`**，而我随后的修复只存在于**工作树**。同一缺陷因此被重复报告，部分"问题仍在"实际是对已被工作树修好的版本的误报。**后续派发必须在提示词里写死"检查 <新 SHA> 或当前工作树"**，并每组标注被验 SHA。
3. **明确拒绝的一条修改建议**：G3 建议把 `button:where(:not(:disabled)):hover` 改回 `button:not(:disabled):hover`。`:where()` 贡献 **0 特异性**，改成 `:not()` 会把基元规则抬到 (0,2,1)，**正好重新引入 D1 这个根缺陷**。该建议不予采纳，理由已在 `style.css:19-21` 写成注释固化，防止后续再次"修复"回去。
4. **G1 要求的 `git diff main --stat` 交叉核对**已并入 §9.2。执行时需注意：main 在分支建立后又前进了 3 个 **仅文档** 提交（`a57a5c1`/`73a2349`/`7c4679e`，不动 `frontend/`），因此 `git diff main` 会把 main 侧新增的内容（`交互原型_v2.html`、`AGENTS.md` 一行）**反向显示为本分支的删除**。正确的变更集是 `git diff <merge-base=07f8d8d> HEAD`。

## 9. 第二/三轮修复、复验证据与决议值对照附表

### 9.1 复验命令与实测结果（2026-09-22，工作树状态）

```bash
export DESIGN_PLUGIN_ROOT=/Users/gukepeng/.qoder-cn/plugins/cache/qoder-marketplace/design-review/0.1.0
cd /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/ui_fix
cd frontend && npm run build && npm run lint && cd ..
node "$DESIGN_PLUGIN_ROOT/skills/design-md-review/scripts/audit-design-md.mjs"      --config design.qa.yaml
node "$DESIGN_PLUGIN_ROOT/skills/component-library-alignment/scripts/audit-components.mjs" --config design.qa.yaml
node "$DESIGN_PLUGIN_ROOT/skills/design-debt-review/scripts/audit-design-debt.mjs"   --config design.qa.yaml
```

| 判据 | 实测 | 结论 |
|---|---|---|
| A1 `audit-design-md` | `findings=0`，`ok=true` | 通过 |
| A2 `audit-components` | `findings=0`，`ok=true`（120 文件） | 通过 |
| A3/A4 色值（不截断同源正则，排除 `legacyGraph/**` 与 `shared/graphStyle.ts`） | `hard-coded-color=1`（=D5 回退的 `.flow-list`）、`functional-color=0` | **A3 由 0 变 1，已按 §4 说明登记，不判失败**；A4 通过 |
| A5 原生 `<select>`（legacyGraph 外） | 0 | 通过 |
| A6 `:root` 令牌值 | 基线 37 个 / 分支 37 个；`CHANGED VALUES: {}`（**零改值**）；集合变化仅 `− --ok-bright:#47c4a3`、`+ --paper-float:#fffffff0` | 通过 |
| A7 构建与 lint | `npm run build` ✓ 3.76s（含 vue-tsc 严格检查）；`npm run lint` 退出码 0 | 通过 |
| B4/B5 `audit-design-debt` | `findings=1000`（`slice(0,1000)` 截断）、`ok=false`（14 处 `inline-style` 全为例外 4 清单） | 按 B4/B5 不作门槛 |
| 不截断债务计数 | `px-magic-number 2744` / `inline-style 14` / `custom-shadow 19` / `tailwind-arbitrary-value 4` / `hard-coded-color 1` / `functional-color 0` | 与 §4 表逐格一致 |
| dist 层断言（D4/D2/D5/D6 的物证） | `.editor-field .app-select` **0**；`entity-editor__title` **0**；`.detail-heading button` 仅 1 处且为 scoped；`padding:45px` **1**、`padding:44px 24px` **1**（两条并存=未合并）；`8px 0 20px #1b32531a` **1**（已回退）；`--app-select-gap` **2**；`where(:not(:disabled))` **3** | 通过 |

### 9.2 变更集交叉核对（G1 要求）

```bash
git rev-parse --short HEAD       # 本文件所在提交（docs(design)），其父即 fix(ui) 提交 76eeb15
git diff --stat 07f8d8d HEAD     # 分支自身变更集；07f8d8d = merge-base(main, HEAD)
```

被验对象必须是 **`76eeb15` 或其后继**，不是首轮的 `4d719ef`（`4d719ef` 上 D1–D6 仍然存在，在它上面复验只会重复首结论）。

分支自身改动 **45 文件 / +882 / −308**（提交 `fix(ui)` 后再加本次 `docs(design)`），其中 `frontend/src/**` 全部是样式与交互表现层，**未触碰** `modelFormat.ts`、`navigation.ts`、任何 `.py`、任何接口文档与迁移。`python3 tests/run.py quick` 仍为 2/3：`test_save_iteration.py` 因 worktree 无 `ontology/` 文件树（main 自 2026-09-21 起 git 忽略）而失败，属环境性，非代码回归。

### 9.3 附表 A：`--app-select-gap` 的决议值对照（工具看不到，必须手工登记）

D6 的修复**不体现在附表 B 里**，因为附表 B 的生成脚本只展开 `:root` 变量，而 `--app-select-gap` 定义在 `.editor-field` 上、靠继承传递：

| 场景 | main 决议值 | 本分支决议值 | 说明 |
|---|---|---|---|
| `.editor-field` 内的 `AppSelect` 上间距 | `5px`（组件 scoped `.app-select[data-v-*]` = (0,2,0) 胜过全局 `.editor-field input,…{margin-top:8px}` = (0,1,1)，也胜过本轮曾误写的 `.editor-field .app-select` 全局规则） | **`8px`**（容器设 `--app-select-gap:8px`，组件读 `var(--app-select-gap,5px)`） | 与同一 `.editor-field` 内 `input/select/textarea` 的 `margin-top:8px` 对齐。**有意的 5px→8px 视觉变化**，已登记 DESIGN.md 例外 11 |
| 其他容器内的 `AppSelect`（未设该属性） | `5px` | `5px`（回退到 `var(--app-select-gap,5px)` 的兜底值） | 无变化 |
| `app/LoginView.vue` `.login-form` | `5px` | `5px` | 该容器当前不使用 `AppSelect`，零渲染差异 |

### 9.4 附表 B：决议值前后对照（由脚本生成，非手抄）

生成命令（在 worktree 根执行）：`node /tmp/g3-verify/visual-delta.mjs 07f8d8d "$PWD"`。脚本对 `main` 与分支两份 `style.css` + 全部组件 `<style>` 展开 `:root` 变量、归一 `hex/white/单位`、维护 `@media` 上下文栈、按选择器出现次序配对重复选择器，因此下面每一行都是**实际渲染值**的差异，不是源码文本差异。共 **135 行 / 20 文件**。

**Token-level :root delta**
REMOVED token --ok-bright = #47c4a3
ADDED   token --paper-float = #fffffff0

**Resolved-value declarations that differ: 135**

### app/LoginView.vue
- `.login-card` → **box-shadow**: `0 1px 2px rgba(24,44,62,.06)` ⇒ `var(--shadow-1)`   (resolved: 0 1px 2px rgba(24,44,62,.06) ⇒ 0 1px 2px rgba(24,44,62,.08))

### flow/FlowCanvas.vue
- `.flow-canvas` → **background-color**: `var(--paper-2)` ⇒ `var(--bg)`   (resolved: f4f7fa ⇒ f6f8fa)
- `.test-pick` → **background**: `#fffffff2` ⇒ `var(--paper-float)`   (resolved: #fffffff2 ⇒ #fffffff0)

### flow/FlowEditor.vue
- `.menu` → **border**: `1px solid var(--line)` ⇒ `1px solid var(--line-2)`   (resolved: 1px solid dce4eb ⇒ 1px solid c9d5e0)
- `.menu` → **padding**: `5px` ⇒ `4px`   (resolved: 5px ⇒ 4px)
- `.menu` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)
- `.list-btn.boundary` → **border-left**: `3px solid var(--purple,#8464d8)` ⇒ `3px solid v-bind("FLOW_CATEGORY.boundary.border")`   (resolved: 3px solid 8464d8 ⇒ 3px solid v-bind("flow_category.boundary.border"))
- `.canvas-note` → **bottom**: `10px` ⇒ `14px`   (resolved: 10px ⇒ 14px)
- `.canvas-note` → **left**: `12px` ⇒ `16px`   (resolved: 12px ⇒ 16px)
- `.canvas-note` → **background**: `#ffffffeb` ⇒ `var(--paper-float)`   (resolved: #ffffffeb ⇒ #fffffff0)
- `.canvas-note` → **padding**: `5px 10px` ⇒ `5px 12px`   (resolved: 5px 10px ⇒ 5px 12px)
- `.canvas-note` → **border**: `undefined` ⇒ `1px solid var(--line)`   (resolved: (absent) ⇒ 1px solid dce4eb)
- `.viewer-json,.viewer-logs` → **border-radius**: `8px` ⇒ `var(--r-sm)`   (resolved: 8px ⇒ 6px)
- `.flow-modal` → **box-shadow**: `0 20px 60px #0f213a44` ⇒ `var(--shadow-2)`   (resolved: 0 20px 60px #0f213a44 ⇒ 0 16px 48px rgba(16,40,61,.18))

### flow/NodeConfig.vue
- `.field-flash` → **border-radius**: `4px` ⇒ `var(--r-sm)`   (resolved: 4px ⇒ 6px)
- `.field-flash` → **transition**: `background .6s ease` ⇒ `background .6s var(--ease)`   (resolved: background .6s ease ⇒ background .6s cubic-bezier(.2,.6,.3,1))
- `.field-flash` → **outline-offset**: `undefined` ⇒ `2px`   (resolved: (absent) ⇒ 2px)

### ontology/OntologyHome.vue
- `.check-loading .skeleton` → **border-radius**: `4px` ⇒ `var(--r-sm)`   (resolved: 4px ⇒ 6px)

### project/MappingDescription.vue
- `textarea` → **display**: `block` ⇒ `undefined`   (resolved: block ⇒ (absent))
- `textarea` → **width**: `100%` ⇒ `undefined`   (resolved: 100% ⇒ (absent))
- `textarea` → **resize**: `vertical` ⇒ `undefined`   (resolved: vertical ⇒ (absent))
- `textarea` → **border**: `1px solid #ccd8e6` ⇒ `undefined`   (resolved: 1px solid ccd8e6 ⇒ (absent))
- `textarea` → **border-radius**: `7px` ⇒ `undefined`   (resolved: 7px ⇒ (absent))
- `textarea` → **color**: `var(--ink)` ⇒ `undefined`   (resolved: 182c3e ⇒ (absent))
- `textarea` → **background**: `#fff` ⇒ `undefined`   (resolved: #fff ⇒ (absent))
- `.md-collapsed` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)

### project/PropertySources.vue
- `.ps-summary strong` → **color**: `#2c4a86` ⇒ `var(--blue-ink)`   (resolved: 2c4a86 ⇒ 234fa8)
- `.ps-summary p` → **color**: `#33517f` ⇒ `var(--ink-2)`   (resolved: 33517f ⇒ 3d5063)
- `.ps-impl-binding` → **color**: `#33517f` ⇒ `var(--ink-2)`   (resolved: 33517f ⇒ 3d5063)

### project/QueryRuleImplementation.vue
- `.sql-pre` → **white-space**: `pre-wrap` ⇒ `undefined`   (resolved: pre-wrap ⇒ (absent))
- `.sql-pre` → **font-family**: `ui-monospace,SFMono-Regular,Menlo,monospace` ⇒ `undefined`   (resolved: ui-monospace,sfmono-regular,menlo,monospace ⇒ (absent))
- `.sql-pre` → **font-size**: `13px` ⇒ `undefined`   (resolved: 13px ⇒ (absent))
- `.sql-pre` → **line-height**: `1.6` ⇒ `undefined`   (resolved: 1.6 ⇒ (absent))
- `.sql-pre` → **margin**: `0` ⇒ `undefined`   (resolved: 0 ⇒ (absent))
- `.sql-pre` → **background**: `var(--paper)` ⇒ `undefined`   (resolved: ffffff ⇒ (absent))
- `.sql-pre` → **border**: `1px solid var(--line)` ⇒ `undefined`   (resolved: 1px solid dce4eb ⇒ (absent))
- `.sql-pre` → **border-radius**: `6px` ⇒ `undefined`   (resolved: 6px ⇒ (absent))
- `.sql-pre` → **padding**: `10px 12px` ⇒ `undefined`   (resolved: 10px 12px ⇒ (absent))

### project/ReferenceNotice.vue
- `.reference-notice` → **border-radius**: `8px` ⇒ `var(--r-md)`   (resolved: 8px ⇒ 10px)
- `.reference-notice` → **color**: `#624b23` ⇒ `var(--warn)`   (resolved: 624b23 ⇒ 8a6116)
- `.reference-notice small` → **color**: `#77654b` ⇒ `var(--muted)`   (resolved: 77654b ⇒ 5b6d7e)

### settings/ConfigurationTransfer.vue
- `.ct-tabs button.active` → **border-color**: `var(--blue)` ⇒ `var(--blue-line)`   (resolved: 245cdf ⇒ b8cff8)
- `.ct-tabs button.active` → **color**: `var(--blue)` ⇒ `var(--blue-ink)`   (resolved: 245cdf ⇒ 234fa8)

### shared/AppSelect.vue
- `.app-select-search:focus-within` → **border-color**: `var(--blue-line)` ⇒ `var(--blue)`   (resolved: b8cff8 ⇒ 245cdf)
- `.app-select-search:focus-within` → **box-shadow**: `undefined` ⇒ `inset 0 0 0 1px var(--focus)`   (resolved: (absent) ⇒ inset 0 0 0 1px 245cdf)

### style.css
- `:root` → **--ok-bright**: `#47c4a3` ⇒ `undefined`   (resolved: 47c4a3 ⇒ (absent))
- `:root` → **--paper-float**: `undefined` ⇒ `#fffffff0`   (resolved: (absent) ⇒ #fffffff0)
- `.item.selected` → **color**: `var(--blue)` ⇒ `var(--blue-ink)`   (resolved: 245cdf ⇒ 234fa8)
- `.canvas-tools button.active` → **color**: `var(--blue)` ⇒ `var(--blue-ink)`   (resolved: 245cdf ⇒ 234fa8)
- `.editor-field` → **--app-select-gap**: `undefined` ⇒ `8px`   (resolved: (absent) ⇒ 8px)
- `.ld-filters` → **display**: `flex` ⇒ `undefined`   (resolved: flex ⇒ (absent))
- `.ld-filters` → **gap**: `6px` ⇒ `undefined`   (resolved: 6px ⇒ (absent))
- `.ld-filters` → **flex-wrap**: `wrap` ⇒ `undefined`   (resolved: wrap ⇒ (absent))
- `.row-menu-list button.danger` → **border-top**: `1px solid var(--line)` ⇒ `undefined`   (resolved: 1px solid dce4eb ⇒ (absent))
- `.row-menu-list button.danger` → **margin-top**: `4px` ⇒ `0`   (resolved: 4px ⇒ 0)
- `.row-menu-list button.danger` → **padding-top**: `10px` ⇒ `undefined`   (resolved: 10px ⇒ (absent))
- `.row-menu-list button.danger` → **border-radius**: `0 0 var(--r-sm) var(--r-sm)` ⇒ `undefined`   (resolved: 0 0 6px 6px ⇒ (absent))
- `.ont-table th` → **color**: `#576d82` ⇒ `var(--muted)`   (resolved: 576d82 ⇒ 5b6d7e)
- `.ont-table td` → **border-bottom**: `1px solid #e7edf3` ⇒ `1px solid var(--line)`   (resolved: 1px solid e7edf3 ⇒ 1px solid dce4eb)
- `.ont-table tbody tr:hover td` → **background**: `#f8faff` ⇒ `var(--paper-2)`   (resolved: f8faff ⇒ f4f7fa)
- `.ont-badge` → **border-radius**: `4px` ⇒ `var(--r-sm)`   (resolved: 4px ⇒ 6px)
- `.ont-empty-ico` → **border-radius**: `8px` ⇒ `50%`   (resolved: 8px ⇒ 50%)

### tools/InstanceCharts.vue
- `.charts text` → **fill**: `#455d76` ⇒ `var(--ink-2)`   (resolved: 455d76 ⇒ 3d5063)
- `.bar-track` → **border-radius**: `4px` ⇒ `var(--r-sm)`   (resolved: 4px ⇒ 6px)

### tools/InstanceGraph.vue
- `.instance-graph` → **color**: `#243b55` ⇒ `var(--ink)`   (resolved: 243b55 ⇒ 182c3e)
- `.graph-canvas:focus-visible` → **outline**: `2px solid #6798da` ⇒ `2px solid var(--focus)`   (resolved: 2px solid 6798da ⇒ 2px solid 245cdf)
- `.graph-empty` → **color**: `#789` ⇒ `var(--muted)`   (resolved: #789 ⇒ 5b6d7e)

### tools/InstanceMap.vue
- `.location text` → **fill**: `#496179` ⇒ `var(--ink-2)`   (resolved: 496179 ⇒ 3d5063)

### tools/InstanceTable.vue
- `.instance-table button` → **border**: `1px solid var(--line)` ⇒ `undefined`   (resolved: 1px solid dce4eb ⇒ (absent))
- `.instance-table button` → **background**: `var(--paper)` ⇒ `undefined`   (resolved: ffffff ⇒ (absent))
- `.instance-table button` → **cursor**: `pointer` ⇒ `undefined`   (resolved: pointer ⇒ (absent))
- `.it-modes button.active` → **border-color**: `#498ae4` ⇒ `var(--blue-line)`   (resolved: 498ae4 ⇒ b8cff8)
- `.it-modes button.active` → **color**: `#1c62bb` ⇒ `var(--blue-ink)`   (resolved: 1c62bb ⇒ 234fa8)
- `.it-muted,.it-hint,small` → **color**: `#708198` ⇒ `var(--muted)`   (resolved: 708198 ⇒ 5b6d7e)
- `.it-scroll` → **border-radius**: `8px` ⇒ `var(--r-md)`   (resolved: 8px ⇒ 10px)
- `.instance-table .it-link` → **color**: `#236bc7` ⇒ `var(--blue-ink)`   (resolved: 236bc7 ⇒ 234fa8)
- `.it-cards article,.it-detail,.it-compare` → **border-radius**: `9px` ⇒ `var(--r-md)`   (resolved: 9px ⇒ 10px)
- `dt` → **color**: `#708198` ⇒ `var(--muted)`   (resolved: 708198 ⇒ 5b6d7e)
- `.it-empty` → **border-radius**: `8px` ⇒ `var(--r-md)`   (resolved: 8px ⇒ 10px)
- `.it-empty` → **color**: `#708198` ⇒ `var(--muted)`   (resolved: 708198 ⇒ 5b6d7e)
- `.it-column-menu` → **box-shadow**: `0 8px 28px #23405d22` ⇒ `var(--shadow-2)`   (resolved: 0 8px 28px #23405d22 ⇒ 0 16px 48px rgba(16,40,61,.18))
- `.it-column-menu` → **border-radius**: `8px` ⇒ `var(--r-md)`   (resolved: 8px ⇒ 10px)
- `}input[type=checkbox]` → **accent-color**: `#2874d1` ⇒ `var(--blue)`   (resolved: 2874d1 ⇒ 245cdf)

### tools/KnowledgeCanvas.vue
- `.knowledge-canvas` → **color**: `#29415e` ⇒ `var(--ink)`   (resolved: 29415e ⇒ 182c3e)
- `.kc-toolbar button` → **border**: `1px solid var(--line)` ⇒ `undefined`   (resolved: 1px solid dce4eb ⇒ (absent))
- `.kc-toolbar button` → **background**: `var(--paper)` ⇒ `undefined`   (resolved: ffffff ⇒ (absent))
- `.kc-toolbar button` → **border-radius**: `6px` ⇒ `undefined`   (resolved: 6px ⇒ (absent))
- `.kc-toolbar button` → **padding**: `8px 11px` ⇒ `7px 10px`   (resolved: 8px 11px ⇒ 7px 10px)
- `.kc-toolbar button` → **color**: `var(--ink-2)` ⇒ `undefined`   (resolved: 3d5063 ⇒ (absent))
- `.kc-toolbar button` → **cursor**: `pointer` ⇒ `undefined`   (resolved: pointer ⇒ (absent))
- `.kc-toolbar button` → **font**: `inherit` ⇒ `undefined`   (resolved: inherit ⇒ (absent))
- `.kc-legend i` → **border**: `1px solid #adbbce` ⇒ `1px solid var(--line-2)`   (resolved: 1px solid adbbce ⇒ 1px solid c9d5e0)
- `.kc-legend i` → **border-radius**: `3px` ⇒ `var(--r-sm)`   (resolved: 3px ⇒ 6px)
- `.kc-solid,.kc-dashed` → **border-top**: `2px solid #8b9db6` ⇒ `2px solid v-bind('KNOWLEDGE_CANVAS.edgeLine')`   (resolved: 2px solid 8b9db6 ⇒ 2px solid v-bind('knowledge_canvas.edgeline'))
- `.kc-surface:focus-visible` → **outline**: `2px solid #6798da` ⇒ `2px solid var(--focus)`   (resolved: 2px solid 6798da ⇒ 2px solid 245cdf)
- `.kc-empty` → **color**: `#789` ⇒ `var(--muted)`   (resolved: #789 ⇒ 5b6d7e)

### tools/KnowledgeDetails.vue
- `.knowledge-details` → **color**: `#263950` ⇒ `var(--ink)`   (resolved: 263950 ⇒ 182c3e)
- `.kind-tag` → **border-radius**: `5px` ⇒ `var(--r-sm)`   (resolved: 5px ⇒ 6px)
- `.kind-tag` → **color**: `#3566a8` ⇒ `var(--blue-ink)`   (resolved: 3566a8 ⇒ 234fa8)
- `.explanation` → **border-left**: `3px solid #89aad6` ⇒ `3px solid var(--blue-line)`   (resolved: 3px solid 89aad6 ⇒ 3px solid b8cff8)
- `.explanation` → **color**: `#58718f` ⇒ `var(--muted)`   (resolved: 58718f ⇒ 5b6d7e)
- `.maintain` → **color**: `#2c64b1` ⇒ `var(--blue-ink)`   (resolved: 2c64b1 ⇒ 234fa8)
- `.edge-path button` → **border-radius**: `5px` ⇒ `var(--r-sm)`   (resolved: 5px ⇒ 6px)
- `.edge-path button` → **color**: `#315f9d` ⇒ `var(--blue-ink)`   (resolved: 315f9d ⇒ 234fa8)
- `.related` → **color**: `#587394` ⇒ `var(--muted)`   (resolved: 587394 ⇒ 5b6d7e)
- `.related strong` → **color**: `#2e5b96` ⇒ `var(--blue-ink)`   (resolved: 2e5b96 ⇒ 234fa8)
- `.property-row button` → **color**: `#2e67b1` ⇒ `var(--blue-ink)`   (resolved: 2e67b1 ⇒ 234fa8)
- `.raw pre` → **border-radius**: `5px` ⇒ `var(--r-sm)`   (resolved: 5px ⇒ 6px)
- `.no-selection h3` → **color**: `#425d7e` ⇒ `var(--ink-2)`   (resolved: 425d7e ⇒ 3d5063)
- `.empty-icon` → **color**: `#a0b5d0` ⇒ `var(--faint)`   (resolved: a0b5d0 ⇒ 8394a5)
- `button:focus-visible,summary:focus-visible` → **outline**: `2px solid #3478d6` ⇒ `2px solid var(--focus)`   (resolved: 2px solid 3478d6 ⇒ 2px solid 245cdf)
- `button:focus-visible,summary:focus-visible` → **outline-offset**: `3px` ⇒ `2px`   (resolved: 3px ⇒ 2px)

### tools/KnowledgeExplorer.vue
- `.ke-presets button` → **border-radius**: `8px` ⇒ `var(--r-md)`   (resolved: 8px ⇒ 10px)
- `.ke-presets button.active` → **border-color**: `#4162d6` ⇒ `var(--blue)`   (resolved: 4162d6 ⇒ 245cdf)
- `.ke-presets button.active` → **box-shadow**: `inset 0 0 0 1px #4162d6` ⇒ `inset 0 0 0 1px var(--blue)`   (resolved: inset 0 0 0 1px 4162d6 ⇒ inset 0 0 0 1px 245cdf)
- `.ke-presets button.active` → **color**: `undefined` ⇒ `var(--blue-ink)`   (resolved: (absent) ⇒ 234fa8)
- `.ke-options` → **border-radius**: `8px` ⇒ `var(--r-md)`   (resolved: 8px ⇒ 10px)
- `.ke-options input,.ke-search input[type=checkbox]` → **accent-color**: `#4162d6` ⇒ `var(--blue)`   (resolved: 4162d6 ⇒ 245cdf)
- `.ke-project` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)
- `.ke-warnings` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)

### tools/OntologyDiscover.vue
- `.resource-tabs button` → **border-radius**: `9px` ⇒ `var(--r-md)`   (resolved: 9px ⇒ 10px)
- `.resource-tabs strong span` → **color**: `#567cb3` ⇒ `var(--muted)`   (resolved: 567cb3 ⇒ 5b6d7e)
- `.resource-tabs button.active,.view-switch button.active` → **border-color**: `#377bdd` ⇒ `var(--blue-line)`   (resolved: 377bdd ⇒ b8cff8)
- `.resource-tabs button.active,.view-switch button.active` → **color**: `#1d60bd` ⇒ `var(--blue-ink)`   (resolved: 1d60bd ⇒ 234fa8)
- `.search-label input` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)
- `.resource-item` → **border-radius**: `9px` ⇒ `var(--r-md)`   (resolved: 9px ⇒ 10px)
- `.resource-item.selected` → **border-color**: `#4080d8` ⇒ `var(--blue)`   (resolved: 4080d8 ⇒ 245cdf)
- `.resource-item.selected` → **box-shadow**: `0 0 0 1px #4080d8` ⇒ `0 0 0 1px var(--blue)`   (resolved: 0 0 0 1px 4080d8 ⇒ 0 0 0 1px 245cdf)
- `.resource-symbol` → **color**: `#2a6aca` ⇒ `var(--blue-ink)`   (resolved: 2a6aca ⇒ 234fa8)
- `.resource-symbol` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)
- `.selected-marker` → **color**: `#3472c9` ⇒ `var(--blue-ink)`   (resolved: 3472c9 ⇒ 234fa8)
- `.text-link` → **color**: `#286cca` ⇒ `var(--blue-ink)`   (resolved: 286cca ⇒ 234fa8)
- `.related-row` → **color**: `#315a8b` ⇒ `var(--muted)`   (resolved: 315a8b ⇒ 5b6d7e)
- `.info-note` → **color**: `#4b6790` ⇒ `var(--ink-2)`   (resolved: 4b6790 ⇒ 3d5063)
- `.info-note` → **border-radius**: `7px` ⇒ `var(--r-sm)`   (resolved: 7px ⇒ 6px)
