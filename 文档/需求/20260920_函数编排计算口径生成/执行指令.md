# 给其他 harness 的执行指令：函数编排生成计算口径 v1

## 启动条件

本目录当前为待评审方案。**只有用户评审后明确委托实施本版，才执行以下任务。** 用户对完整Python脚本尚未确定；本版建议范围是Markdown说明＋结构化规则JSON（含节点原始实现），不是整段可执行脚本。若用户选择脚本方案，先更新协议与本目录四份文件，不自行扩展本指令。

## 目标与路径

项目根：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。
需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260920_函数编排计算口径生成/`。

为当前函数编排新增“生成计算口径”：从已保存编排生成可读说明与规范内容，供以后MCP返回给智能问数。本次不建设MCP、不执行数据查询、不携带连接或凭据。

## 必读顺序

1. 根AGENTS及适用子目录说明；运行 `.collaboration/context.py read --actor zcode`（使用当前工具在该协作设施支持的身份），查看git status，不覆盖他人改动。
2. 本目录需求说明、交互原型、开发计划、执行指令。
3. 当前接口文档README、01、04、05及保存/鉴权约定。
4. 当前FlowEditor/flowModel/api、flows/flow_routes/flow_executor相关字段与执行语义。

必须遵循原型的入口、结果页和状态交互。原型是模拟HTML：演示场景下拉、模拟修改、模拟数据、延时都不能写入正式功能；真实错误来自接口。不能把原型“已生成”视为执行验证通过。

## 具体任务

依次完成开发计划T0–T4：

1. 核对源字段，冻结自有格式 `flow-calculation-rule` v1、完整输入输出及各节点投影。先更新接口文档/README/05，再改路由白名单与代码。
2. 实现纯转换器，按真实绑定确定依赖；保留节点原SQL、公式、代码、LLM指令、行模式、执行限制及最终输出来源。缺业务口径写未声明，不能猜测。说明由同一JSON渲染。
3. 实现只读生成接口：flowId/revision和显式可选项目上下文，权威保存快照、CAS错误、权限隔离。生成不得调用网络、密钥读取、执行器或LLM。
4. 实现生成页：生成前保存、说明/步骤/规范三个页签、只读实现、复制、下载Markdown和JSON、缺项定位、旧修订提示、返回设计保持上下文。异步响应防串编排/项目。
5. 结构损坏、缺实现、敏感内容和未知影响字段阻断；普通说明缺项允许带提示输出（以用户确认版本为准）。不删源数据、不修正用户代码。
6. 统一真实执行声明：本期无standalone脚本；Python现有节点属于LLM求值；写Redis/HTTP未知效果不可误标只读；runtimeValidation为本次未执行。
7. A01–A16测试、构建与浏览器验证，记录实际结果和提交。

## 范围红线

- 不新增本体概念、属性类型、项目来源模式、编排发布机制、生产执行/调度服务。
- 不生成整体Python脚本，不将公式简单改写成Python从而改变语义。
- 不要求生成前配好真实数据库和模型密钥；资源是逻辑需求，连接由问数系统负责。
- 不在输出携带连接地址、端口、账号、密码、认证头、provider元数据、测试结果或画布信息；实现正文中无法安全保真的内容阻断而非盲删。
- 不全量序列化state，不因敏感过滤误删普通业务token字段。
- 不改用户真实ontology、SQLite与密钥。测试同时隔离DATA_ROOT、数据库地址和端口。
- 旧流程兼容、保存错误不假成功、409不覆盖、页面请求经现有api/http；非本功能的编排编辑/调试UI不重构。
- 原型中的示例口径不是默认业务规则，正式生成不得自动添加del_flag、SOC范围、容量单位或缺失策略。

## 验证与交付

测试必须覆盖：节点字段保真、稳定ID、类型嵌套、输出绑定、独立步骤、循环与悬挂引用、SQL动态模板、Redis行模式、LLM标识、逻辑来源、无真实连接可生成、敏感阻断、CAS、账号、旧响应、旧快照下载、复制失败和保存失败。验证生成全过程未发出业务网络请求、未修改源revision。

前端执行npm run build及相关回归，后端执行新增纯转换与隔离HTTP测试。浏览器检查三个页签、问题定位、导出内容和1024/1440布局。

最终将源字段清单、冻结接口、实施事实、A01–A16证据与未测项追加本开发计划；不要另造重复交付文件。通过共享上下文脚本记录，提交自己文件（不提交真实数据、dist或他人文件）。回复包含改动范围、验证结果、剩余问题、提交SHA。没有完成的内容如实标注，不宣称本期可直接执行脚本。
