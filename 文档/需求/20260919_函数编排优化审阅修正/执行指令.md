# 交给其他 harness 的执行指令

你负责修复本体工作台函数编排优化中的已确认缺陷。保留当前设计和唯一测试主入口，不能借此重新设计整套编排系统。

项目根目录：
`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

本次需求目录：
`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_函数编排优化审阅修正/`

## 必读顺序

1. 项目根 AGENTS.md；运行 `.collaboration/context.py read --actor zcode`（其他harness按仓库支持的actor流程）；查看 git status。
2. 本目录 `需求说明.md`，确认R01–R06与可保留方案。
3. 本目录 `交互原型_v1.html`，实际打开并操作多路径勾选、运行等待、返回恢复、项目变化、结果类型切换。
4. 本目录 `开发计划.md`，按T0–T4实施并完成A01–A14。
5. `文档/接口文档/04-编排与LLM接口.md`，必要时01及README；原需求 `文档/需求/20260919_函数编排配置与调试优化/` 用于理解兼容能力。

审阅固定基线3f0d120，当前代码可能继续变动。必须核对最新代码，已有修复先验证，不重做、不回退他人改动。旧原型多测试入口已被用户否定，以唯一“测试编排”主入口和节点快捷入口为准。

## 明确任务

1. 修复FlowTestWorkspace运行状态：第一次异步确认前防重入，确认和提交使用同一冻结快照，结束/取消/异常释放，视图切换无法绕过，晚回包不污染新上下文。延迟API测试证明一次用户操作最多一个请求。
2. 修复多路径复选区选择一个节点后消失；全图不能受片段连通性限制；禁用原因明确显示。不要把所有模式的校验全部放松。
3. 入口按稳定ID去重，各测试范围使用一致类型控件；显式空文本、0、false与未提供区分；非法布尔不静默转false。不擅自改变旧接口的null语义。
4. 主测试入口再次进入恢复范围、输入与结果；节点快捷入口有意切范围。缓存按编排/项目/声明隔离；结果名称、项目与类型元数据来源于提交快照。
5. 修复数值/文本列表空白，处理对象列表、混合列表、空列表；保留JSON及原有标量表现、左右独立滚动。
6. 修复bounded_preview：真实返回inputs的UTF-8 JSON≤64KiB、列表预览≤100项，保留实际计算值，不只设置截断标志。正常小值结构不变，截断结构先写接口文档再编码。

## 实施约束

- 遵循本次修正原型的交互状态和需求语义，沿用现有组件和风格；原型不是完整工作台，未展示的既有功能不得删除。
- 原型计时器、固定结果、示例项目只是演示；正式功能必须调用既有API，不能复制假运行。
- 前端集中在flow相关组件和纯辅助；后端限输入预览及必要的契约/测试。不要顺手做本体导入导出、菜单重构、SQLite迁移、权限改造。
- 不新增节点、SQL能力、Python沙箱、列表算法引擎、循环/调度、运行历史或通用幂等框架。
- 不手改或清空真实ontology/data/keys；测试使用隔离的WIZ_WORKBENCH_ROOT、WIZ_DATABASE_URL、WIZ_WORKBENCH_PORT。不要对真实SQL/HTTP/Redis执行写验证，不调用收费模型做本次确定性回归。
- 接口文档先于代码；保留auth/账号隔离、CAS、保存协调器、稳定ID、未知字段和旧targets兼容。网络执行不得持全局写锁。
- 当前用户任务授权实施本目录修复；发现其他业务范围问题先记录，不自行扩大。

## 验收和最终交付

按开发计划A01–A14逐项提供证据。至少包括真实Vue交互或组件行为测试（运行防重、多选、输入、恢复、旧结果）、前后端范围测试、真实字节数断言和完整原值断言。前端build必须通过；浏览器检查1440×900、1280×800、1024×768。不能仅以类型检查/源码grep判定完成。

把实施事实、验证命令、结果及未验证项追加到本目录开发计划，不另写重复报告。共享上下文record记录接手版本、实现与证据；git status检查后仅提交本任务文件，commit按AGENTS要求。最终报告：修复了哪些R项、哪些差异明确保留、测试证据、未完成项及提交号。没有证据的项目不能写“已验收”。
