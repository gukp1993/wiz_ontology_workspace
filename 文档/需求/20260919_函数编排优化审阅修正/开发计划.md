# 函数编排优化审阅修正 · 开发计划 v1

日期：2026-09-19；状态：待实施。依据：[需求说明.md](需求说明.md)、[修正原型](交互原型_v1.html)、[执行指令](执行指令.md)。

## 1. 基线与范围

本次独立审阅固定在 `3f0d120`，通过 git archive 隔离副本构建和测试，服务端口 18886；使用新建的临时账号、两项目和三个纯公式编排。没有接触真实业务数据库。执行修正时基于当前分支增量改动，不 reset 到审阅版本；先核对是否已有其他人修复。

审阅已确认的证据：

- `npm run build` 通过；flow_model 测试通过；test_flow_test_plan 51 项、test_flow_executor 33 项通过。
- 浏览器复现：B–D 8→10→30→15 成功；整条入口重复；重开测试范围重置；菱形图勾 B 后复选区消失；合法并列节点整条按钮禁用无原因。
- 对原组件 script setup 提取后用 TypeScript 转译、注入延迟 runFlow 桩，连续调用 run 两次：API 调用 2 次、running=false。此为逻辑探针，不是端到端浏览器网络断言。
- tableRows([1,2,3]) 得到 keys=[]；输出模板没有标量列表分支。
- bounded_preview({'text':'x' * 1048576})：truncated=true，但序列化 preview 仍为 1,048,588 字节。
- 快照只存项目 ID/签名等，显示项目名和节点名读取实时 props，此项为源码审阅，待修复时补动态交互回归。

## 2. 实施顺序

### T0 契约及测试基线

阅读 AGENTS、共享交接、当前接口文档04与本需求四件套；git status 确认其他任务文件。将 R06 精确字节预算和截断显示结构登记到接口文档04，涉及通用结构同步01，README记录变更。若没有新增端点，无需扩大路由白名单。R01–R05尽量保持 API 不变。不得依赖原需求页首“尚未实施”等历史状态判断当前代码。

### T1 先修运行和范围

入口：frontend/src/flow/FlowTestWorkspace.vue、FlowEditor.vue、flowModel.ts。

1. 补运行生命周期测试，覆盖确认未完成、请求未完成、取消、拒绝、返回视图。
2. 同步锁定 pending；冻结提交对象；finally 释放；防晚回包污染。
3. 分开全图和连续片段计划限制，保留拓扑和结构校验。
4. 多路径选择模式独立于已选数量；错误统一放按钮附近。
5. 主入口首次初始化、随后恢复；节点快捷入口显式覆盖范围。

完成后R01/R02及范围保留先验收，再扩展结果显示，不把所有修复堆到最终联调。

### T2 输入与结果状态

入口：同上；必要时提取一个小型测试输入控件，禁止为本需求引入新状态管理框架。

1. 入口去重；类型控件复用；使用明确 provided 状态区分空值。
2. 输入缓存按上下文与声明签名隔离，旧声明变更精确失效。
3. 结果元数据快照化，旧结果的名称/项目/范围保持原值；当前状态变化明确提示。
4. 结果按标量、对象、对象列表、其他列表分类，列表空态和 JSON 兜底。
5. 保留左右独立滚动、唯一主入口、现有节点类型和保存机制。

### T3 输入预览预算

入口：workbench/flow_test_plan.py 的 bounded_preview，相关 flow_executor 调用；无需改 SQLite 表。

先写失败用例检查实际 UTF-8 JSON 字节数，而非仅 truncated 标志。实现有界展示副本，考虑字符串转义、字段名和结构开销。可用预算遍历或安全摘要替代，不要求特定算法。断言执行器及下游收到原始完整对象，原对象未变。

### T4 联调、浏览器验收及交付

使用 WIZ_WORKBENCH_ROOT、WIZ_DATABASE_URL、WIZ_WORKBENCH_PORT 三项隔离。SQL/Redis/HTTP使用桩或测试服务，绝不连真实数据源执行写操作。前端改动必须 build。追加本文件“实施记录”，记录实际提交、命令、结果、浏览器尺寸与未验证项。

## 3. 必须覆盖的验收用例

| ID | 验收动作 | 通过标准 |
|---|---|---|
| A01 | 延迟请求中连续点击运行；异步确认未完成时连点 | 至多1个确认流程、1个run请求；按钮显示等待；取消/失败后能再运行 |
| A02 | 运行时返回设计再进入；切项目后旧请求回包 | 不能绕过当前请求等待态；响应不标为新项目结果、不串账号 |
| A03 | A→B→D / A→C→D，勾B再勾D再取消B | 始终可继续选择；当前集合和诊断明确；不会首次勾选即消失 |
| A04 | 独立A/B均从入口读取，整条测试 | 通过现有配置校验后可执行全部节点；片段B+D缺C仍拒绝 |
| A05 | A→B→C→D→E，只测B–D，B输入8 | 输出10/30/15；执行器调用A/E次数严格为0；非靠结果列表推测 |
| A06 | 整条、单节点、片段共用入口 | 稳定ID只渲染一次；bool用下拉；JSON有结构校验；错误有字段定位 |
| A07 | 0、false、显式空文本、未提供、非法布尔 | 前三者按类型合法；未提供明确处理；非法布尔不转false；旧接口兼容未被暗改 |
| A08 | B–D→返回设计→主入口；节点快捷入口 | 主入口保留B–D、输入和结果；快捷入口才切到指定节点 |
| A09 | 结果后改节点名/声明或切项目；延迟回包 | 展示提交时名称/项目与旧结果标志；新上下文不套用旧输入 |
| A10 | 输出0/false/null、对象、[1,2,3]、["a"]、[]、混合/嵌套列表 | 每种都可读、不空白；空列表明确；对象列表表格与JSON可切换 |
| A11 | 1MiB文本、中文、嵌套对象、101+列表、超长键、多字段合计超限 | inputs序列化≤65536字节；truncated正确；列表≤100项；执行原值不变 |
| A12 | 原全图CAS、旧targets、内部/固定覆盖拒绝、节点失败传播 | 旧语义保持；无意外调用范围外节点；失败和跳过清楚 |
| A13 | 1440×900、1280×800、1024×768，长输入/长表/长日志 | 结果首屏可见、左右独立滚动、无整页下翻找结果、按钮可达 |
| A14 | 返回设计、保存/撤销、重新打开既有编排 | 测试状态不写草稿，不破坏配置保存；未知扩展字段无损 |

建议测试入口：tests/flow_model.test.mjs、tests/test_flow_test_plan.py、tests/test_flow_executor.py，加一份真实 Vue 组件行为测试覆盖 A01/A03/A06/A08/A09/A10。可按已有测试设施实现，不指定新测试框架；不能仅匹配源码字符串。

常用命令（根目录）：

```sh
node --import ./tests/ts_hooks.mjs tests/flow_model.test.mjs
python3 tests/run.py --test tests/test_flow_test_plan.py
python3 tests/run.py --test tests/test_flow_executor.py
```

前端目录执行 `npm run build`。如果改到flows配置校验，再运行对应flows用例。测试全部通过后不无依据扩大为全仓重构或重复全量回归。

## 4. 交付与协作

先完成T0再实现；可独立处理后端预览和前端修复，但不要多人同时编辑FlowTestWorkspace/flowModel。无需追求并发数量。真实数据、密钥、生成dist、临时库不入库。提交前更新共享上下文，只提交自己负责文件。

## 5. 实施记录（执行者追加）

待填写：接手基线、实际文件、契约变化、验收A01–A14证据、遗留问题、提交号。不得把本次审阅的通过证据当作修复后验证。

### 交付物检查（Codex，2026-09-19）

修正原型经过 jsdom 18 项交互断言：运行防重复、等待状态、返回保留、菱形多选继续增减、并列全图、列表类型与空态、项目缓存隔离和旧结果提示。浏览器直接打开本地 HTML 被浏览器安全策略阻止，未绕过；因此本原型未完成浏览器视觉验收。此前针对正式实现隔离副本的浏览器实测不受影响。原型不是正式功能完成证据。
