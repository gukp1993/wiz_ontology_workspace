# 可直接交给其他 harness 的执行指令

你要在现有本体工作台中实施「本体图谱画布能力优化 v1」。完成编码、构建、隔离验收和Git提交，不停在分析或框架搭建；不要扩展本期业务范围。

## 路径与必读顺序

项目根：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_本体图谱画布能力优化`

依次读取：

1. 根目录 `AGENTS.md`；执行 `python3 .collaboration/context.py read --actor zcode`（其他harness按实际actor/脚本支持使用，不冒充另一方）；记录ticket与git status。
2. 本目录 `需求说明.md`，完整阅读行为细节及G01–G20。
3. 本目录 `交互原型_v1.html`，**必须实际打开并操作**搜索、框选拖动、整理/撤销、关联查看/退出、面板收起、最大化。
4. 本目录 `开发计划.md`，T1→T5。
5. 当前源码 `frontend/src/ontology/OntologyGraph.vue`、`ObjectWorkspace.vue`、`App.vue`、`app/navigation.ts`、`app/auth.ts`，以及业务规则/动作关联帮助函数和相关测试约定。
6. 参考项目只读：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder/frontend/src/views/EditorView.vue`、`PreviewView.vue`。重点借鉴applySearch/focusSearchAt、bindCyEvents、applyHop/restoreHopPositions、autoLayout/undoLayout、toggleInspector/showEdgeTip。**不要修改旧项目。**

## 执行范围

当前本体图谱由ObjectWorkspace中的OntologyGraph挂载，已有对象、共享属性、私有属性、规则、动作全量投影。不要改错成flow/FlowCanvas、tools/KnowledgeCanvas、InstanceGraph或已退役ObjectCanvas。

遵循原型的工作台风格、工具分组、画布优先布局、邻域状态条和可收起详情。原型中导航弹窗、静态示例数据、简化环形布局只是演示；正式版必须使用当前草稿真实数据、真实定义导航、账号隔离偏好和需求中的确定性布局。需求文字中的细节验收优先，不能因为原型未模拟持久化/性能/所有异常就省略。不得把整个HTML嵌入Vue页面，也不得只把截图做成静态页面。

## 具体任务（按顺序）

1. 提取真实投影与稳定内部ID：五类节点命名空间隔离；保留不同定义ID的同名同端点链接、自关联；引用边保留实际引用身份；悬空引用可见异常提示，不改原数据。添加G01–G04/G18对应测试。
2. 改为增量更新：名称/类型/说明/端点/同数替换即时同步；不因为更新自动fit、销毁画布或丢选择。清理已删目标。
3. 统一筛选/搜索/选择/邻域状态。搜索仅高亮、有结果列表与显式定位；Tab可离开。按方向BFS+诱导子图保留范围内所有边。邻域不改变全图持久化坐标；退出恢复。
4. 选择/移动工具、Shift多选框选、多节点拖动、Space平移、缩放/适应/定位、两种整理、重置位置、布局撤销重做。布局历史与业务撤销隔离；输入区快捷键不接管。
5. 通过prefGet/prefSet实现账号+本体隔离的本机视图记忆；切换/注销/卸载正确flush和清理；不迁入匿名旧缓存。画布操作不调用保存接口、不修改业务state/revision。
6. 实现可收起筛选/详情、拖宽、窄屏覆盖面板、画布最大化、ResizeObserver保持中心；定义页跳转复用现有表单，返回恢复图谱视图。
7. 在隔离实例完成G01–G20验证，记录证据；构建并提交自己文件。交付可用实现，不留占位按钮、TODO或模拟导航。

## 不能自行增加的内容

不要新增直接删节点、自由连线、批量修改、动作执行、规则计算、规则依赖推断、合并图谱、图片导出、坐标文件导入、离线包或新发布流程。无需引入图编辑器产品/路由库/状态库。不复制旧项目的“手动保存当前版本”逻辑。

“打开定义”可以让用户在现有页面编辑，但本轮不为画布建第二套业务表单。不能把“隐藏/筛选”实现成删除。字段、定义ID和链接方向必须来自真实模型，不能为图好看重写业务关系。

## 保护与兼容

- 用户真实本体、项目、连接、密钥、数据库不作为测试数据。禁止重建或清理真实ontology/、data/、keys/。
- 先看git status，不覆盖其他人的未提交内容。只在自己文件范围修改并提交。
- 测试同时设置 `WIZ_WORKBENCH_ROOT=<临时目录>`、`WIZ_DATABASE_URL=sqlite:///<临时目录>/workbench.sqlite3`、`WIZ_WORKBENCH_PORT=<空闲端口>`，不用真实18765写入。
- 无预期后端协议变更。如果确有必要，先登记接口文档，再实现；不要为前端个人视图状态加业务API。
- 实际业务编辑沿用saveCoordinator、before-change/changed和409行为；视图操作绝不调用该链路。
- 保留原hash别名和既有focus调用；新导航返回上下文可选且仅前端；稳定ID不因名字变化而改变。

## 验收与最终交付

必须完成 `cd frontend && npm run build`，新增图谱模型/控制器测试及涉及的既有导航/首页/懒加载回归。不要用build通过代替实际交互验收。

浏览器重点：搜索不隐藏、平行/自关联完整、边端点改动即时更新、多选拖动一次撤销、邻域退出不漂移、布局/业务撤销不串、账号/本体隔离、定义编辑往返保留视图；1440/1280/1024/768下不拥挤到无法操作。验证网络无画布保存请求、业务revision未改。性能记录500节点1200边的实际数据。

将结果追加到 `开发计划.md` §6：实现文件、G01–G20逐项结果、命令、截图、未覆盖/失败项；未测不得写通过。根据脚本规则完成共享上下文record，最后git commit，信息说明本轮功能和验证；不要提交运行数据库、密钥、dist、node_modules或测试临时数据。

最终回复：已完成能力、验证结论、任何未解决项、可复核截图/页面和commit。若需偏离原型以适配现有实现，必须说明原因和用户影响，不能静默变成另一套布局。
