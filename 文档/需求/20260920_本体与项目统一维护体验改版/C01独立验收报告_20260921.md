# C01 独立验收报告（2026-09-21）

**结论：通过。** C01 空白依赖引用漏检已修复，B01/B02及原五项定向回归通过。剩余一项P3文档准确性问题，不阻断本轮缺陷关闭。

被验业务提交：`1ec22a7`；开始与结束HEAD：`5e5ce5f`；分支：`codex/acceptance-fixes`。已确认业务提交之后无workbench/tests变化，上一轮9d0fa6f与113e224业务等价。唯一业务改动为project_validation.py；底层检查器、保存及发布路由未变，未降低既有断言。

## 运行结果

cwd：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/acceptance-fixes`。
表中Python命令均使用本树`.runtime/venv/bin/python`，执行前清除WIZ_WORKBENCH_ROOT、WIZ_WORKBENCH_PORT、WIZ_DATABASE_URL；测试自行建立隔离根和假账号。旧版测试cwd为/tmp解包副本。

| 项目 | 判定 | 实际命令/检查 | 关键结果 |
|---|---|---|---|
| C01及B01/B02 | 通过 | python tests/test_flow_dependency_context.py | 92通过/0失败 |
| 原四组反例 | 通过 | python /tmp/B01B02_独立验收_20260921/b01b02-independent.py | 四组均422、零版本；未知上下文errors=[]；原脚本42项通过，额外观察单列 |
| 独立边界扩展 | 通过 | python /tmp/C01_独立验收_20260921/independent.py | 空格/Tab/换行/混合×两维×可读空集合/读取失败，共16组发布均拒绝；6组未填写对照不误伤 |
| 同资产恢复 | 通过 | 同独立脚本 | 16组均在同一个编排、同一个项目修正ID后发布200，恰新增1版本 |
| 反例有效性 | 通过 | git archive 9d0fa6f解包；复制当前test_flow_dependency_context.py后运行 | 旧版67通过/25失败，失败项对应空白引用校验与发布漏检；退出1 |
| 金样 | 通过 | python tests/test_validation_split.py；对比113e224 JSON | 98样例/515断言；旧97样例深比较相等；新增57_c01_blank_provider_reference，生成/回放共用固定夹具 |
| 后端全量 | 通过 | python tests/run.py all | 40/40 |
| 原五项后端 | 通过 | python tests/codex_reacceptance_backend_20260920.py | 43/43 |
| 原五项前端 | 通过 | node --import ./tests/ts_hooks.mjs tests/app_client_mount.test.mjs；同命令运行codex_reacceptance_a02_20260920.mjs | 客户端挂载3/3，A02独立断言全部通过 |
| mapping_forms | 范围外既有失败 | 新旧两树同跑node --import ./tests/ts_hooks.mjs tests/mapping_forms.test.mjs | 同一expected /数据连接/断言失败，非本批引入 |

## 原四组发布反例

原脚本四组均已重跑。revision验证由新增独立矩阵与92项套件补齐：

| 引用 | 目录状态 | errors关键内容 | publish | releases | 草稿revision |
|---|---|---|---|---|---|
| 空白providerId | 可读空集合 | 提供方不存在／尚未配置 | 422 | [] | 不变 |
| 空白providerId | 读取异常 | LLM提供方目录读取失败 | 422 | [] | 不变 |
| 空白credentialId | 可读空集合 | 认证凭据不存在 | 422 | [] | 不变 |
| 空白credentialId | 读取异常 | API凭据目录读取失败 | 422 | [] | 不变 |

非空目录且不含该ID、正常ID、动作绑定、重复拒绝与异常信息不回显已由92项套件实测。独立矩阵不只检查errors：逐组断言422、发布数为零、revision不推进，并校验改正后的恢复路径。

## 剩余问题

**P3：文档“未声明时不读取目录”与实现不一致。**

位置：`workbench/project_validation.py:538`、`:543`；接口文档03:196、04:175；测试文件中“未填写”用例的说明。

执行独立脚本可复现：键缺失、None、空字符串共6组，错误均为空，但对应元数据读取mock的call_count均为1。实际行为是“仍读取并缓存；无关目录失败不阻断”，不是“不读取”。既有测试只断言不误伤，没有验证零调用。

建议将文档、实施记录、用例说明改成“未声明该依赖时，不以对应目录读取失败阻断校验”，不要声称已实现按需读取。若未来要求按需读取，再单独补缓存与读取次数测试。本轮判断：C01目标是堵住空白引用发布漏检，实际已闭环；此项是非阻断准确性问题，不能将其写成完全符合所有文档字面描述。

此前P3-1/C01关闭；P3-4缺少金样已补齐；P3-2本轮修复前计数67/25核实一致；P3-3提示不含flowId仍是可读性建议，已有属性/动作前缀定位，未扩大范围。

## 假设、限制与结束状态

本轮未做可选HTTP网络端到端、浏览器、真实业务数据源访问或重新构建前端。发布证据为正式路由函数加隔离SQLite，不冒充浏览器验收。另一ticket辅助填写不在范围。

18921/PID82961未重启、未写入、未发请求；该旧进程没有加载本次修复，不能用于判断线上效果。未访问主服务18765。只读约束下未修改被验树、未提交、未写共享上下文；交接由协调者补记。

结束git status --short为空，HEAD仍5e5ce5f。旧版/tmp解包副本已清理；报告、独立脚本、JSON结果和日志保留在本目录便于追溯。未合并main。
