# B01/B02 编排依赖上下文三态修复 · 独立验收执行指令（2026-09-21）

> 用途：交给任意其他 harness（或人工验收者）独立执行的验收指令，不依赖任何聊天记录。
> 本指令是 2026-09-21 补充修复轮（B01/B02）的**第二次独立验收**模板；执行者不得信任实施者或前一轮验收的自述结论，一切以亲自运行的命令输出为准。

## 0. 你的角色与授权边界

你是独立验收员，只做验证与报告，不做开发。硬约束：

- 被验环境（只读使用，所有命令的 cwd 都用它）：
  `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/acceptance-fixes`
  分支 `codex/acceptance-fixes`；**被验业务提交 `9d0fa6f`**（父 `6955e76`=修复指令与验收报告文档；`6516f51` 为其后的纯文档验收记录提交，可先核对 `git diff 9d0fa6f..HEAD -- workbench/ tests/` 为空）。
- 不得修改/新增/删除该工作树内任何文件；不得 `git commit/merge/reset/checkout/stash/worktree add`；不得合并 main。自己的脚本与解包一律放 `/tmp`。
- 已有登记实例 http://127.0.0.1:18921（PID 82961，数据根为该树 `.runtime/acceptance-data`）：**不得重启、停止或写入**。如需活服务做 HTTP 验证，自起自关：端口 ≥18931（先实际检查占用）、`WIZ_WORKBENCH_ROOT=$(mktemp -d)`、清空 `WIZ_DATABASE_URL` 等继承变量；只清理自己起的进程。
- 绝不读写真实业务数据（主仓库 `ontology/` 等）。不得为释放端口杀他人进程。
- 范围：只验收 B01/B02（R01/R02/R03/A01/A02 此前已验收，本批只查无回归）。`文档/需求/20260920_本体与项目统一维护体验改版/` 下 2026-09-21 14:00 相关的其他需求（本体与项目辅助填写原型）属另一 ticket，**不得纳入验收标准**。
- 遇到需用户拍板的岔路不要卡住：按指令文档自行做最合理判断，写入「假设与限制」。

## 1. 必读（按顺序，均在被验树内）

1. `文档/需求/20260920_本体与项目统一维护体验改版/补充修复指令_20260921.md` —— 绑定标准（含必测矩阵）。
2. `文档/需求/20260920_本体与项目统一维护体验改版/验收报告_20260921.md` —— B01/B02 缺陷定义与反例。
3. `文档/接口文档/03-项目区接口.md` §2.2、`04-编排与LLM接口.md` §2.2（三态协议表）、`README.md` 变更记录（应有 2026-09-21 行）。
4. `文档/需求/20260920_本体与项目统一维护体验改版/开发计划.md` §14.3（实施者自述，仅作对照）、§14.4（第一轮独立验收记录，含 4 项 P3，仅作对照）。

## 2. 被验改动与判定核心

`git diff 6955e76..9d0fa6f` 应只含：`workbench/project_validation.py`（唯一业务文件）、新增 `tests/test_flow_dependency_context.py`、接口文档 03/04/README、开发计划、共享上下文。任何既有测试/业务文件的语义放松都要标红。

核心语义（项目校验 `validate_project` → `_flow_check_context` / `_flow_struct_errors` 传给 `flows.check_flow` 的依赖上下文三态）：

- (a) **已知集合（含空列表 `[]`）→ 做引用存在性判定**。B01 根因是旧代码 `ctx.get('connections') or None` 把空集合降级为未知；修复后不得复发。
- (b) **未知（None）→ 跳过该项存在性检查**；`/api/flow-check` 编辑页此语义必须不变。
- (c) **目录读取失败（`_READ_FAILED` 哨兵）→ 仅当被检编排实际声明该维度依赖时输出定位到编排的阻断 error**（LLM：任一节点 `implementation.providerId` 非空；凭据：`kind 或 implementation.language == 'http'` 且 `credentialId` 非空）。文案含「读取失败」与依赖类别、不含异常原文；该维度以 None 传 `check_flow`（读取失败不得冒充「引用不存在」）；未声明该依赖的编排不受连带阻断（fail-closed 且无误伤）。
- 其余必须成立：同一 `validate_project` 缓存保留读取状态、多引用顺序无关；`flows.check_flow` 全局语义未改；账号未配置模型（`llm_meta==[]` 且编排未声明 providerId）仍跳过不误报；纯公式编排不受任何维度影响；错误不回显密钥；**不得通过删除结构校验或降低断言修复**。

## 3. 必做验证（全部亲跑，记录命令与关键输出）

前置一律：`cd <被验树>` 且 `env -u WIZ_WORKBENCH_ROOT -u WIZ_WORKBENCH_PORT -u WIZ_DATABASE_URL`。

1. **新测试**：`.runtime/venv/bin/python tests/test_flow_dependency_context.py` → 38 项全过、退出码 0。通读该文件确认断言真实覆盖 §2 各条与补充指令必测矩阵（B01 空连接 Redis/SQL、非空列表不存在ID、合法元数据对照、登记实例+纯公式合法、属性/动作两路径、B02 两维读取失败阻断、无依赖不误伤、顺序无关、注入串 `sk-INJECT-DO-NOT-ECHO-9f2e` 不回显、恢复后重校验/发布、发布路由 422 边界），无注水断言。
2. **反例有效性（关键）**：`mkdir /tmp/prefix_check && git archive 6955e76 | tar -x -C /tmp/prefix_check`，复制新测试进 `/tmp/prefix_check/tests/`，在 `/tmp/prefix_check` 用被验树 venv 运行 → 应有失败项且恰对应 B01（发布 200 v1）与 B02（provider/credential 读取异常发布 200 v1）。第一轮验收实测 24 过/14 败；实施者记录 23/15（P3-2 计数偏差）。**若旧实现全过则反例无效，判不通过。**
3. **全量回归**：`.runtime/venv/bin/python tests/run.py all` → 40/40（含 `test_validation_split` 金样 97 样例逐字节等价、`test_publish_guards`、`..._adversarial`、`test_business_rules`、`test_action_library`、`test_project_flow_source`）。
4. **原五项无回归**：`.runtime/venv/bin/python tests/codex_reacceptance_backend_20260920.py` → 43/43。
5. **前端连带**：逐个 `node --import ./tests/ts_hooks.mjs tests/<文件>` 跑 `tests/*.test.mjs` 与 `tests/codex_reacceptance_a02_20260920.mjs`。已知 `mapping_forms.test.mjs` 为基线既有失败（开发计划 §11.4 延期项 P02；需在 6955e76 副本复现同败后判范围外）。
6. **全局语义不变抽查**：`git diff 6955e76..9d0fa6f -- workbench/flows.py workbench/flow_http.py workbench/project_routes.py workbench/projects.py` 应为空；grep 确认 `_flow_check_context` 无 `or None` 降级、`_READ_FAILED`/`_flow_declares_provider`/`_flow_references_credential` 判据与文档一致（读代码，不轻信注释）。
7. **文档-代码一致性**：03/04/README 新契约逐条对照实现；确认变更记录日期与被验提交同日。
8. **可选 HTTP 端到端**：自起 18931+ 隔离实例：配置空连接项目 + 引用不存在 Redis 连接的编排属性来源 → `/api/project-validate` 出 error、`/api/project-publish` 422 零版本；改纯公式合法后发布 200。做完自关自清。

## 4. 已知项（须复核而非照抄）

第一轮独立验收（2026-09-21，子代理）结论为通过，P3 四项登记在 §14.4：P3-1 空白串 `providerId='  '`/`credentialId='   '` 且目录读取失败时的窄窗 fail-open（判据 strip 与 `flows.py:475`/`flow_http.py:127` 不 strip 不一致）；P3-2 计数 23/15 vs 24/14；P3-3 读取失败文案不含 flowId；P3-4 金样未新增空连接样例。**你的职责包含抽查这些结论是否属实**；若你认为 P3-1 应升级为阻断项，如实写明理由。

## 5. 交付格式（你的最终输出）

- 结论：**通过 / 不通过**（二选一）。
- 逐项表：编号 | 判定 | 实际运行的命令 | 关键输出摘录。
- 反例有效性单独结论（第 2 步）。
- 问题清单（如有）P1/P2/P3 分级 + file:line + 复现命令。
- 假设与限制：跳过项、自行判断、范围外（含 mapping_forms、14:00 另一 ticket）。
- 结束时报告：被验树 `git status` 干净、HEAD 未变、18921 未被写入、临时产物已清理。

不要执行 `.collaboration/context.py record`（写项目文件），交接由协调者补记；不要输出合并建议，合并由用户另行授权。
