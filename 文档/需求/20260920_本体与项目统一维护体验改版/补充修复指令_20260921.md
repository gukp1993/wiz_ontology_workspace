# 依赖上下文漏检 · 给实施harness的补充修复指令

在已经授权的原worktree修复本轮B01/B02，自测并提交，交回独立验收。不合并main、不重启主工作台、不扩大到延期功能。

## 工作目录与基线

- 主协调仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。
- 实际所有开发cwd：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/acceptance-fixes`。
- 分支`codex/acceptance-fixes`；本轮被验HEAD `1db8901`，业务提交`3f85cf2`。之后的报告交付提交是本任务文档，不要reset回旧SHA。
- 沿用登记端口18921、`.runtime/acceptance-data`、`.runtime/venv`及本树node_modules/dist。先核对运行进程、cwd、生效数据库路径，不覆盖已有验收数据；新增故障注入用独立临时根和假账号。

必读顺序：AGENTS.md → context脚本与最新交接 → 同目录验收报告_20260921.md → 开发计划§11.5/11.6/12/14 → 原验收修复执行指令.md → 接口文档03项目区/04编排与LLM及通用错误约定。

先记录pwd/branch/status/HEAD。只在本树修改；主目录和其他任务工作树不动。其他在途修改先核对，不stash/reset。用户已授权本树继续修复，无须再次创建。

## 任务与顺序

按开发计划§14任务表执行。两个问题集中在同一project_validation.py，共用适配逻辑，指定一名实现owner，串行修改；独立QA可同时编写只用公开调用的反例，禁止两人同时写同文件。接口文档与提交/构建由协调者串行处理。

1. 先保存反例：B01明确空连接列表；B02显式模型引用但元数据读取异常。确认原实现会零错误并发布成功。
2. 先定义最小语义并更新接口文档：已知空集合≠未知上下文≠读取失败。项目实际需要的依赖不可读不能放行；错误需定位依赖类别/编排且不泄露密钥。
3. B01：移除空列表被转换为None的语义丢失。项目校验具有确定连接集合时传原集合，包括[]。复核SQL/Redis及属性/动作两条调用路径。保持原编排局部编辑尚无项目上下文时的兼容提示，不全局更改check_flow含义。
4. B02：识别被检查编排实际依赖的模型/凭据，再加载相应上下文；已需要但读取失败必须形成阻断或契约规定的可重试失败。缺失和不可读取区分。不要对所有异常转None，不把连接/凭据服务错误假装成不存在；避免将原始异常文本回显密钥。
5. 同次validate_project的缓存必须保留状态语义。先检查不需LLM的编排，后检查需LLM编排时，不可因前者缓存使后者跳过读取；反序同样测试。纯公式编排在无关元数据读取故障时应继续合法。
6. 补自动化和正式发布边界测试。修正文档和记录，提交后停止在待验收。

## 必测矩阵

- 明确空项目连接 + Redis GET节点引用不存在ID：校验报错，发布拒绝，草稿/发布数不因拒绝而增加。
- 同场景SQL节点；非空连接列表中引用不存在ID；有效SQL/Redis元数据对照（不实际查询）。
- 登记实例 + 纯公式编排 + 空连接集合：合法通过，不要求数据库身份表。
- 属性取值和动作实现两类绑定均不能绕过。
- 显式providerId + 模型元数据读取异常：受控失败，不产生发布。
- HTTP节点实际引用credentialId + 凭据目录读取异常：受控失败；空集合时明确不存在；合法凭据元数据通过。
- 不需要模型/凭据的编排不被无关故障误伤；多个编排缓存的先后顺序不改变结论。
- 返回错误不泄露密钥或异常敏感细节；失败后恢复依赖重新校验可成功。
- 原R01/R02/R03/A01/A02全部回归，不能通过删除结构校验或降低断言修复。

复用已有测试并新增职责明确用例：test_project_flow_source、test_publish_guards、codex_reacceptance_backend_20260920；必要独立新测试。HTTP固定端口串行。Python命令使用本树`.runtime/venv/bin/python`，清除继承WIZ_DATABASE_URL/ROOT/PORT并确认脚本内部隔离；不可用真实项目故障注入。

跑test_validation_split核对金样；有变更先说明预期，不盲目生成。补跑属性/动作、发布幂等与目录并发回归。前端不必修改；若确需改前端才扩大到对应组件测试与build，不为布局优化动App。代码变更后按仓库规则完成必要构建/测试。

浏览器有能力时只用隔离上下文与假账号，在18921核对实际代码SHA，确认失败提示和合法恢复；不能用18765或默认代理。不能运行的项目明确标未测，不以源码/单元测试冒充浏览器证据。

## 交付与停止点

在开发计划§14续记实际实施，保留本报告历史发现。交付准确分支/SHA、B01/B02逐项反例前后证据、合法对照、测试命令和限制、实际URL/运行状态、数据根；按context脚本追加交接。只提交本任务文件，真实数据/密钥/dist/node_modules不入库。

本轮无合并授权。完成后“已提交待独立验收，未合并main、未更新主服务”。后续明确授权集成时，由协调者从最新main串行集成并组合重验；普通冲突核对双方语义，业务冲突另行确认。不得覆盖main脏文件或全文件ours/theirs。成功集成后才按AGENTS清理已登记任务环境；当前不得删除worktree。回退使用revert，代码回退不代替数据恢复。
