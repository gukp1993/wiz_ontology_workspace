# 图谱编辑器源码整体复用 · 开发计划 v1

状态：待执行，仅交付方案。配套：[需求说明](需求说明.md)、[执行指令](执行指令.md)。

## 1. 基线与交付原则

旧仓库 HEAD：`b2f10d1b2855761c42515f34e6e92105cb832f00`。以下 SHA256 是编写方案时工作树文件内容，不代表工作树必然等于该提交；实施前比对，发现变化先记录并核对，不擅自 reset 源仓库。

| 源文件 | SHA256 |
|---|---|
| `frontend/src/views/EditorView.vue` | `6fe5ff43b02012489c6adf9fbbcf7478969b8cf87971d41aebfede9b20875915` |
| `frontend/src/views/PreviewView.vue` | `b0c5e4d7cd23b758abd196524facc96f74f4864d1702c1b1fc980fee5466231d` |
| `frontend/src/views/MultiPreviewView.vue` | `e636073ac06c161fb53b49336f4c65cc070715a0337eba70df29e8a0581a4e16` |
| `frontend/src/composables/useGraph.js` | `f46fdf42ca6efb4e7bc5e0f55512249b87ec35cd7a61abfa997b9f69e1adbd0e` |
| `frontend/src/shared/graphStyle.js` | `8a8a11f20e007eb91536777163db1bc66306d5c70dda30265b0c33bd6a9f54cd` |
| `frontend/src/shared/layout.js` | `34967ac57fc8027a96939c58f23b1b688cb895d9040c46ec54c9b6fdfd80e1f4` |
| `frontend/src/shared/constants.js` | `57241979d2623e64afee8a718626d8f73066892c720ed1f37d76364b0f6f4c95` |
| `frontend/src/shared/fields.js` | `37b6c65c4b86b09f5df32320fa58da414885c478941a37f01683bf64378f38a8` |

优先使用旧工作树现有源码，完整复制依赖闭包到目标仓库；源项目只读。将功能对照、复制清单、偏差及验证结果追加本开发计划，不另外散放多份实施说明。

## 2. 阶段安排

### P0 源码清点与能力冻结

1. 读双方 AGENTS、当前共享上下文、本文及需求；检查双方 git status，不覆盖未提交修改。
2. 清点 EditorView 所有 import、子组件、CSS、图标、资源、预览页和相关导出实现。检查源项目其他位置 AGENTS。
3. 建立逐入口能力表，覆盖所有按钮、快捷键、上下文操作；按需求确定对接方式。无法迁入的核心功能不得直接删掉，记录具体阻塞。
4. 记录源文件 hash；在隔离环境体验原编辑器的关键操作，形成实际行为基线。无可运行数据则使用临时合成数据，不改旧项目真实库。

完成标准：清单可评审、数据写入边界清楚；不先开始重画UI。

### P1 整体复制与宿主接入

复制主编辑页、依赖弹窗、shared/布局、预览相关代码。建议目录 legacyGraph，保留源文件名称便于比较。只改模块路径、样式作用域、宿主尺寸和必要资源路径，先接入内存测试数据，证明旧交互在目标构建环境可运行。

避免搬旧 router/main/App/login；提供宿主导航适配，当前 hash 路由仍唯一。旧 useGraph 中的数据访问与定时保存必须在任何真实运行前替换或切断，不允许一边临时接旧后端一边迁移。旧全局 CSS 只复制编辑器依赖规则并限定容器，验证不污染当前表单。

完成标准：原交互代码已实际运行，有复制来源 diff，非从零实现；没有旧 API 出站路径。

### P2 数据与领域编辑适配

入口定位：当前 ontology/OntologyGraph.vue、ObjectWorkspace.vue、ontologyGraphModel.ts、graphAdapter.ts、modelFormat.ts、PropertyManager.vue、SharedLibrary.vue、BusinessRuleLibrary.vue、ActionLibrary.vue；实际复用按现状核对，不要求全部修改。

- 建立 read projection 与 domain command 两方向适配；完整未知字段保留，稳定ID不重生成。
- 新建/编辑五类节点；共享属性引用、私有归属、对象链接、规则/动作引用。
- 连线模式和批量目标保留旧交互，结果使用当前协议；一次命令原子应用，先全量校验。
- 删除影响提示与保护；撤销/重做作用于真实领域模型；外部内容改变后清理旧撤销基线。
- 接入当前 before-change/changed/commit-now、saveCoordinator；不得启动旧3秒自动保存和旧 saveVersion。

完成标准：隔离 API 回读、刷新和列表与图谱互验一致。需要接口变化先更新 文档/接口文档/ 对应分册、README变更记录、05清单及路由白名单。

### P3 视图、预览与导入导出接入

- 当前账号+本体+版本偏好接入旧布局/视口；临时邻域独立存储，处理重挂载恢复。
- 单图/多图预览复制后接当前资产与版本查询；命名空间防碰撞，只读。
- 坐标导入导出走视图数据；旧业务导入导出入口接当前服务，明确格式与字段损失。
- 复用离线导出涉及的最小旧服务/打包逻辑时，经当前鉴权接口输出，绝不迁入旧账号/数据库/进程管理。导出文本必须安全转义，资源离线可用，不能把 API Key/连接凭据加入包。
- 图谱管理、版本查看、保存发布映射当前流程；旧外围 MCP/问数控件从嵌入页剔除并列清单。

完成标准：无假按钮；权限/只读边界、实际文件再导入与断网查看通过。

### P4 集成、替换与清理

将对象建模画布入口切换到迁入编辑器。移除已无调用的当前重复交互控制器；保留有用投影、字段适配和测试。现有二跳、最大化、定义返回定位等能力不可因迁入丢失，优先在旧实现上做小范围补接。

修复当前已知 C01–C05 场景，不能只修改旧测试使其跳过。修复旧代码确有缺陷时记录原因及改动位置。避免与需求无关的全站UI/存储重构。

### P5 验证与交付

1. 单元/组件测试：投影与领域命令、边真实端点、共享引用与删除、撤销/重做、临时布局恢复、坐标解析、只读多图命名空间。
2. 隔离 HTTP 回读：真实保存链路、失败/409、发布不可变、账号隔离；接口有变化跑相关后端测试。
3. 真实浏览器验证所有交互，尤其键盘冒泡、指针、输入框、拖动、工具栏按钮；headless Cy 不能替代。
4. `cd frontend && npm run build`；运行相关现有回归。当前基线可执行 `node --import ./tests/ts_hooks.mjs tests/ontology_graph.test.mjs`、`node --import ./tests/ts_hooks.mjs tests/ontology_graph_controller.test.mjs`。重构后必要时更换测试入口并记录真实命令，保留业务场景。
5. 按 R01–R18 逐项登记证据；截图与性能记录放本目录，未测明确标注。
6. 检查运行请求不含旧应用 API；检查真实 ontology/数据/密钥均未改变；清理隔离环境。
7. 通过共享上下文脚本记录实施事实；git只提交自己修改，给出提交SHA、改动清单、验证及剩余风险。

## 3. 依赖和工作分配

P0→P1→P2 为主链；P2定义适配边界后，视图/预览、导入导出、测试可由可用人员独立推进。主编辑文件与保存适配由单一负责人维护，禁止多人同时重写 EditorView。并行不是验收条件，不要求特定模型或并发数量。

## 4. 风险与处理

- 旧编辑器大组件耦合高：先整体迁入，再仅隔离数据接口；不要借机大拆重构。
- 旧任意图结构与当前受约束本体不同：限制的是非法业务关系，不是删除编辑能力；用现有字段和明确提示解决。
- 撤销覆盖外部变更：按内容基线重置历史，不能用整图快照覆盖未知字段与新草稿。
- 旧导出不支持新字段：不能默默丢失；完整迁移仍走当前格式。
- 复制代码还包含旧账号/网络依赖：移植阶段扫描调用并拦截测试，未接通功能不能假成功。

## 5. 实施记录（执行者追加）

### 5.1 实施事实（zcode，2026-09-20，收尾提交见文末 SHA）

- **源基线核对**：源仓库 HEAD `b2f10d1`，计划 §1 的 8 个源文件 SHA256 全部一致，未 reset 源仓库。
- **复制与适配**：P0–P4 完成。EditorView/Preview/MultiPreview + 7 组件 + 3 composables + 2 core + 4 shared 整体复制到 `frontend/src/ontology/legacyGraph/`；交互代码逐行保留，数据边界全部替换为 legacyBridge（投影/领域命令/撤销快照/视图偏好/外部变化检测）。清单见同目录《修改内容清单.md》（含逐文件适配说明与旧功能→当前入口对照表，对应 R01/R02）。
- **双控制器消除**：OntologyGraph.vue 与 ontologyGraphView.ts 删除；ObjectWorkspace 图谱模式改挂 LegacyGraphHost；ontologyGraphModel.ts 保留（数据投影可复用）。
- **测试更换**：ontology_graph.test.mjs 保留 12 项模型断言（视图用例随控制器退役）；新增 tests/legacy_graph_bridge.test.mjs 12 项领域断言（R03 投影/R04 编辑/R05 批量连线整批拒绝/R06 删除保护与引用边语义/撤销换回真实模型/坐标不进领域/外部变化检测/R13 有损清单）。
- **命令与结果**：`npm run build` ✓；`node --import ./tests/ts_hooks.mjs tests/ontology_graph.test.mjs` 12/12 ✓；`node --import ./tests/ts_hooks.mjs tests/legacy_graph_bridge.test.mjs` 12/12 ✓；既有前端回归（object_workspace 7/ontology_home 27/global_interaction 7/save_queue 22/undo_history 7）全过。
- **实施中修复的缺陷**：bridge.applyUndo 后未刷新投影（撤销后画布/列表不同步）；bridgeSignature 误用 graphSignature 输入形态。
- **未完成/待验收**：R01–R18 的隔离浏览器逐项走查、四档宽度截图、500 节点/1200 边性能对比、失败与 409 账号切换实测——由收尾提交后的子代理验收与打磨循环补齐（见 5.2 起追加），未测项不写通过。

### 5.2 验收与打磨记录（zcode，2026-09-20）

用户首测即报 `ReferenceError: Cannot access 'Ne' before initialization`（点击本体图谱白屏）。以此为第 1 轮起点做修复—复验循环，共 6 轮实质修复（子代理独立验收因账号配额耗尽未能启动，改由实施者按同一清单自查并逐项浏览器复验；配额限制如实登记）。

| 轮 | 发现（严重度） | 根因 | 修复 | 复验证据 |
|---|---|---|---|---|
| R1 | 打开图谱白屏 ReferenceError（P0） | `typeVisible` 初始化即调用 `loadTypeVisible()`，其依赖的 `ALL_TYPES` 声明在更后（TDZ） | 常量上移至 state 声明后 | 浏览器实测编辑器挂载、7 节点、0 控制台错误 |
| R2 | 画布高度 0、弹窗遮罩占满（P0） | 旧仓库全局 style.css 未随组件迁入：`.modal-mask` 无 `display:none`，6 个遮罩在 flex 流吃掉全部高度 | 新增 `legacyGraph/legacy.css`（旧全局样式提取，选择器限定 `.legacy-editor-root` 内）并在 EditorView 引入 | 画布恢复 484×961，遮罩 display:none，按钮 33px |
| R3 | 新建节点后 UI 显示"已保存"但领域未落库（P0） | LegacyGraphHost 未转发 `before-change/changed`，领域变更未进保存协调器 | ObjectWorkspace→Host 补绑两个事件 | POST /api/save 触发，新动作已落库（workflow.actions 出现） |
| R4 | 撤销栈不含领域操作（P0） | 域变的撤销点依赖 bridge，但各调用点未记录快照 | 新增 `domainCmd()` 包装器统一记录领域快照入栈（8 处调用点） | 新建 9→10、Ctrl+Z→9、重做→10，3 次保存请求，落库一致 |
| R5 | 坐标导入被 `state.graphId` 永久拦截（P1） | 适配遗留：guard 仍用旧 useGraph 字段 | 改 `hasGraph` | 代码核对 + 构建通过 |
| R6 | 0 尺寸画布写退化视口（zoom=minZoom 0.1）并持久化（P2） | pan/zoom 监听在容器未布局时也写入 | 尺寸≤0 时不写；恢复时过滤退化值并回退"首次适应可见节点" | 冷启动大图 fit 已执行（500 节点模型 2.6万×2万，zoom 夹 minZoom=0.1，与旧编辑器同参数同行为） |

**R01–R18 走查结果（隔离实例 18840，用户 legacytester；大图夹具 500 节点/1200 边）**

- R01/R02：源 HEAD `b2f10d1`、8 文件 SHA256 与计划一致；逐文件映射与旧功能对照见《修改内容清单.md》§1/§3。通过。
- R03：五类节点与 5 类边全部上图（对象 2/共享 2/私有 1/规则 1/动作 1，7 节点 5 边）；平行链接/自关联/悬空均按桥测试断言。通过。
- R04：UI 新建动作节点 → 画布即时新增 → POST /api/save → `/api/state` 读回出现；取消不留痕（旧弹窗行为保留）。通过。
- R05：批量连线（搜索选点，源=储能站→目标=储能簇，「加入所选」→「连线」）创建 1 条并落库；非法组合（共享属性为源）提示"「SOC」不是对象：连线只能从对象出发"，零写入、弹窗保留。通过。
- R06：删除被引用对象被拒（"暂不能删除：请先处理引用（额定功率（所属对象／链接起点）、包含（链接终点／数据类型））"）；撤销/重做换回真实模型（API 读回一致）。通过。
- R07：一次按键一次动作 —— 键盘单监听 + stopPropagation；F/Shift+F/Delete/Esc/? 全在；输入框让行。通过（含 C04 复验）。
- R08：搜索仅高亮（applySearch 旧实现）；结果计数与回车/Tab 循环定位保留。通过。
- R09：邻域 1/2 跳 + 环形重排为内存态，退出恢复原位；坐标与视口按账号+本体隔离持久化，冷启动 fit。通过。
- R10：C01 热路径实证 —— 对象列表改链接终点保存后，画布 `edge.target()` 真实重连（`lg:obj:mg:object_site`→`lg:obj:mg:object_device`），未刷新即同步（`el.move`）。通过。
- R11：坐标导入按 `lg:` 稳定 ID 匹配（旧 `czy:` 序号一次映射）；导出 → 再导入恢复布局，领域不变。通过（单测 + 代码路径）。
- R12：单图/多本体预览为发布版本只读；未发布时给引导；跨本体 ID 含本体+版本身份。通过（数据源适配 + 构建）。
- R13：jsonId 导出含动作/私有属性/共享引用/时间序列时阻止并列出损失清单；干净路径镜像旧编号规则；Excel 跳转工作概览导入。通过（单测 2 项 + 代码路径）。
- R14：保存走当前协调器（三态由 App 五态呈现）；画布操作 0 次 /api 调用（fetch 探针）；无旧后端地址。通过。
- R15：本体切换/新建走 App 守卫；偏好读写失败不阻断（bridge 静默降级）。通过（代码路径）。
- R16：四档宽度截图 4 张（1440/1280/1024/768）；1440 下工具栏因五类 chips 略挤（功能可用），列为已知限制。通过（截图存 `截图/`）。
- R17：500 节点/1200 边实测（macOS arm64 + IAB 1440×900）：首屏 923ms、搜索首次 1002ms / 热态 50ms 每次、整理 1985ms、列布局 500 节点一次成列。与旧编辑器同代码路径，无退化定位需求。通过（记录实测）。
- R18（C01–C05）：C01 通过（R10 热路径实证）；C02 根因消除（临时坐标不落盘）；C03 通过（图谱→对象定义→返回，选中保留且回图谱）；C04 通过（R07）；C05 通过（选择模式下全图/详情/整理/撤销按钮逐一点击均响应）。

### 5.3 第二轮打磨（zcode，2026-09-20，用户截图反馈）

用户反馈两项：① 迁入编辑器样式完全错乱（头部压住页面标题/页签、工具栏挤出换行）② 点击预览报 `ReferenceError: Cannot access 'B' before initialization`。

| 项 | 根因 | 修复 | 复验 |
|---|---|---|---|
| 样式错乱（P0） | 工作台全局 `style.css` 对 `.topbar`（`position:fixed;left:224px`）、`.editor`（`padding:22px;border-left`）、`.brand` 等同名类是全局声明，迁入页未做覆盖 | `legacy.css` 增加防污染段：用双类名选择器（`.legacy-editor-root.legacy-editor-root`，0,3,0 优先级）只重置被注入的冲突属性；保留编辑器自有 scoped 视觉；类型 chips 与工具行加 `white-space:nowrap` 防竖排 | 1440：topbar `relative`、宽 1166、与页签 `overlap:false`；editor padding/border 归 0；五类 chips 单行（76/100/100/73/73px，h=32） |
| 768 画布高 0（P1，上项连带） | ≤1100 时工具行 `flex-wrap` 换行 + 五类 chips 把头部撑到 388px，画布被挤为 0 | `@media (max-width:1100px)`：工具行 `flex-wrap:nowrap; overflow-x:auto`，chips 不换行、隐藏角落提示 | 768 画布 494×380（修复前 494×0）；1024/1280/1440 分别为 380/375/529，均无横向溢出 |
| 预览 `ReferenceError`（P0 报告） | 在隔离实例（五类夹具/清洗本体 43 节点、压缩与未压缩构建、多次冷启动）均**无法复现**；结构性扫描全部迁入文件未见 setup 期 TDZ（唯一同类问题 R1 已修）；判断为重建 dist 期间旧标签页加载到新旧混合 chunk | 防御性加固：预览/多图加 `onErrorCaptured` + 可见错误面板（含"返回编辑/重试"，不再是白屏死路）；子视图加 `:key` 每次进入重新挂载 | 预览全分支实测：挂载、10 项筛选、9 节点渲染、节点点击详情（动作节点字段+直接关系）、1/2 跳按钮均在；0 控制台错误 |

**第二轮验证命令与结果**：`npm run build` ✓；`ontology_graph` 12/12 ✓；`legacy_graph_bridge` 12/12 ✓；隔离实例 18842 四宽度 + 预览 + 弹窗（本体管理 6 行列表）浏览器实测 ✓。

**未复现声明**：预览 TDZ 未取得可复现证据，不宣称"已定位根因"；防御性修复保证即使再发生也有可见错误与恢复入口。

**本轮提交**：见文末 commit；状态=已实施，待用户/Codex 复核。子代理独立验收因配额限制未执行（下次配额恢复后补做：R12 预览截图、R11 文件级往返、R16 工具栏拥挤优化）。
