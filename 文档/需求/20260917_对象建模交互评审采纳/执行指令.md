# 给其他 harness 的执行指令

可将本文件路径或全文交给执行者。任务是实施本目录需求，不能只提交分析或样式方案。

## 工作位置与必读顺序

项目根目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

1. 读取项目 `AGENTS.md`、`git status --short`，记录当前已有修改，不覆盖其他人的工作。
2. 完整读取 `文档/需求/20260917_对象建模交互评审采纳/需求说明.md`，它是本次范围与验收的唯一清单，逐条实施R1–R6、验证A1–A12。
3. 查看既有 `文档/需求/20260917_微软列表详情布局/交互原型_v1.html` 与当前工作台。遵循现有列表／详情、独立编辑页、后台风格，不重新设计整个页面。原型仅作布局基线，业务行为以本次需求为准；旧原型模拟数据／模拟发布不能当成正式实现。
4. 阅读下列代码入口，确认现状后修改。不要把注释、旧报告或本指令的历史文件行号当成真实现状。

原评审 `/Users/gukepeng/WorkBuddy/2026-09-17-21-33-19/outputs/object-workspace-review.html` 只作参考，不逐条照搬；无法访问外部评审文件不阻碍按本目录实施。本次没有新原型，不能擅自增设字段、页面或要求用户重新确认已明确条款。

## 实施任务与顺序

### 第一步：统一对象与链接编辑（R1）

主要入口：`frontend/src/ontology/ObjectWorkspace.vue`、`ObjectCanvas.vue`、`shared/EditorField.vue`；先阅读现有 `app/formGuard.ts` 和注入的 `form-save` 用法。

- 保留现有字段要求：对象名称＋业务定义必填；链接起点＋终点＋正向名称＋数量关系必填，其余保持现有规则。
- ObjectWorkspace承接列表和画布的同一套表单；画布通过少量语义明确的事件传递创建／编辑意图、稳定ID与预选端点，不再直接修改文字字段。
- 画布详情改为只读加“编辑定义”。节点创建、选点连线都进入共用表单，保存前不插入模型记录。只读预览不得绕过禁用。
- 记录表单返回上下文，保存／取消回到原列表页签或画布；画布不丢视口、选中项，改名不自动布局。可通过保留组件或捕获并还原视口实现，避免全局组件重构。
- 沿用form-save提交和form-guard离开保护，失败保留输入。检查表单提示，不出现必填缺失禁止保存却提示可以直接保存草稿的矛盾。
- 保留拖动布局自动保存、缩放平移不触发保存、快捷键、撤销重做与删除引用检查。

### 第二步：本体／项目按需加载（R2）

主要入口：`frontend/src/App.vue` 的onMounted、navigate、hashChanged、switchSpace、loadProject，以及 `app/navigation.ts`、`project/api.ts`。

- 本体冷启动不等待项目列表、完整项目状态、项目引用版本。首次进入需要项目数据的页面再加载。
- 加载必须覆盖菜单切换、显式hash深链、跨区入口及恢复记忆项目，不能只在一个按钮handler里加条件。
- 区分“尚未加载”“加载中”“失败”“成功但无项目”；未加载时不要抢先触发无项目重定向。
- 项目局部加载／错误／重试；等待中仍可去本体。去重请求，旧响应不能覆盖新项目或抢走用户当前页面。
- 检查快速切换项目、返回本体、保存未完成时切换，以及不依赖项目的函数编排入口。已加载项目继续使用原有flush与引用版本机制。
- 以最小改动落实；不引入路由／状态库，不重写保存器，不以shallowRef或接口裁剪替代正确的加载依赖。

### 第三步：完成界面精简（R3–R6）

- 对象详情的删除放入更多操作，复用确认与引用检查，支持键盘与焦点返回。
- 只限制对象／链接编辑内容宽度为约760px并自适应，按钮和提示靠近；不要缩窄画布和数据表。
- 去掉对象详情重复计数行；复用方式显示共享／私有。共享可点击查看正确来源定义，使用稳定引用、只读详情并处理失效引用。
- 空态短句加现有单一入口；动作页不堆叠共享维护和项目实现说明；不要删除动作关联或项目绑定功能。
- 保留画布已有工具分组；更新为“新建对象／自动布局／显示全部／直接关联”等可理解文案，检查启用状态与键盘使用。
- 不因左栏留白新增字段、第三行、底部新建按钮或视觉装饰。

## 实施边界

- 本期是前端交互改进，无业务schema、后端接口或真实数据迁移需求。发现确需超出范围时，先说明具体阻碍，不自行发散重构。
- 保留原有本体／项目独立草稿、revision、409、发布及引用机制。请求经过现有http/API层，不在页面新写fetch与错误解析。
- 禁止手改、清理、重建或测试写入真实 `ontology/`；不动不可变release、密钥vault、现有项目引用。
- 不改动无关动作接口映射、业务规则、SQL规则、计算函数、函数编排等功能。不要恢复计算契约作为本体基础建模步骤。
- 既有未提交修改属于用户或其他执行者；不要reset、批量覆盖或混入本次提交。无需追求高并发；独立模块可按工具能力处理，但App.vue与ObjectWorkspace.vue等共享文件应明确单一编辑责任。

## 验证计划

自动化验收仅使用 `WIZ_WORKBENCH_ROOT=<独立临时数据目录>`、`WIZ_WORKBENCH_PORT=<独立空闲端口>`。所需示例数据自行在临时目录建立，不能让浏览器测试指向真实18765写数据。

1. 为本次行为补充少量有意义的回归：画布取消不新增、表单失败不丢输入／不重复新增、项目延迟不阻塞本体、深链加载、旧响应不覆盖新项目。优先沿用仓库现有测试工具，不为颜色、间距写测试。
2. 用浏览器在隔离实例完成需求A1–A12，记录通过／失败／未验证。性能验证用延迟或阻断项目请求证明不阻塞本体，不编造“优化60秒”结论。
3. 验证至少1440、1024、768px及长名称；覆盖列表、画布详情、编辑表单、空态、更多菜单、共享详情。
4. 在frontend目录运行 `npm run build`（含类型检查）。运行与改动直接相关的已有测试；如果改了保存协调器必须先加对应行为测试并运行现有保存队列回归，但优先不改它。
5. 构建和相关检查通过后查看git diff与status，只提交本次文件。按AGENTS.md提交功能清晰的git commit，不提交数据、vault、runtime、dist和依赖目录。

浏览器或工具受限导致某项未验收时，明确指出，不用构建通过替代交互验收。不要求为本次纯前端改动重启后端或执行真实接口控制动作。

## 最终交付格式

1. 按R1–R6列出完成情况及主要修改文件，未完成项与原因单列。
2. 按A1–A12给验收结果与证据，附关键截图和测试命令结果。
3. 给出实际commit哈希及简短内容说明；不把未运行的检查写成通过。
4. 把简短实施记录追加到本文件末尾，包含日期、改动入口、验证、限制，不另生成多份重复方案。
5. 明确真实本体与项目数据未用于验收、未迁移，正式工作台是否已加载本次构建如实说明。

本指令只授权执行上述交互改动，不授权新增业务能力。不要停留在分析、另做一份方案或仅修CSS；完成行为、界面、验证后交付。
