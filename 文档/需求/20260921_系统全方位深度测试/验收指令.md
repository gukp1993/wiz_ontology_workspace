# 系统全方位深度测试 · 给独立验收 harness 的指令

你是一名独立验收负责人。请对另一名 harness（协调者）刚交付的"系统全方位深度测试"结果做独立验收：核对其**测试过程是否越界**、**缺陷是否真实可复现**、**报告分类是否成立**。你不修复业务代码，不重跑其全部工作，不合并任何东西。

## 1. 位置与基线

- 主协调仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。
- **验收 cwd：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test`**，分支 `codex/test`。
- 被验业务 SHA：`d6c73c2`（测试执行期间业务代码承诺零修改）。
- 测试记录提交：`4d4e497`（14 个 tests/deep_* 脚本 + 测试报告/缺陷清单/测试计划执行记录 + 子任务 entries）、`b8b4605`（协调者交接）。当前 HEAD 应为 `b8b4605`；先记录实际 HEAD，不 reset。
- 主工作台 18765、保留号 8765 不得触碰。18931（PID 56914，数据根 `.runtime/test-data`）是测试执行者保留的实例，**只读观察可以，不在其上写入或停止它**；你需要跑测试时自起专属实例：建议端口 18941、数据根 `$PWD/.runtime/accept-data`，启动前 `lsof` 实核空闲。

## 2. 开始前必须做

1. 读 AGENTS.md 与 `文档/需求/20260921_系统全方位深度测试/` 下执行指令、测试计划（含实际执行记录）、测试报告、缺陷清单；运行 `python3 .collaboration/context.py read --actor codex`，本任务 entries：000132（环境登记）、000133（Q05）、000134（Q03）、000135（协调交付）。
2. `git diff --stat d6c73c2..HEAD` 核对：提交内容应只有 tests/deep_*、本需求文档、`.collaboration/entries/00013[3-5]*`、session_context.md。**任何业务代码（workbench/、frontend/src/）、既有测试、金样、正式需求文档被改动 = 直接不通过项。**
3. 检查 git status：执行者声明工作树业务零修改、`.runtime/` 证据不入库。声明与实际不符要点名。

## 3. 逐项独立复现（核心验收动作）

以下每项给出**最短独立复现路径**与报告主张；复现一致记"实锤"，不一致记"报告失实"，无法复现说明环境与步骤。全部在你的专属实例/合成账号上做，不改任何仓库文件（新增你自己的验收脚本除外）。

- **D-Q02-01（P1 新发现）**：新建合成本体→save→publish 任一版本→`GET /api/releases` 应恒为 `[]`；核对 `workbench/versions.py::publish` 无 release-zip 写入、`model_routes.py` restore 只读附件表；前端 `OntologyRelease.vue` 恢复下拉取该接口。报告同时主张接口文档 02 §2.3/§3.3、05 速查表把恢复标为可用——核对文档原文。
- **A01（P1 基线已知）**：规则 `content={"x":1}`、动作 v2 `effect=["a"]` 经 HTTP validate→save→publish 应全通过且非法值进入发布快照（`/api/version-state` 读回）。注意：这是**已在主仓库验收记录登记**的基线缺口，复现的意义在于确认执行者没有夸大或掩盖。
- **R02（P2 基线已知）**：构造被项目 `kind:'flow'` 引用、自身输出绑定缺失的空壳编排→project-validate 不拦→project-publish 成功；核对根因陈述 `project_validation.py:512 _check_flow_binding` 不调 `flows.check_flow`（只读代码核对+至少一条 HTTP 实锤）。
- **R01（P1 基线已知）**：真实浏览器打开任一页面，console 应出现一次 flowState TDZ ReferenceError，页面功能正常。若你的浏览器能力受限，如实标"证据不足"，不得沿用对方截图。
- **D1（P2 待裁定）**：不带 Origin 头、带合法会话 Cookie 的 POST `/api/save` 应被放行进入业务（对照 `README.md` 接口规范"缺失或不符→403"）；带错误 Origin 应 403。裁定权在用户，你的任务是确认实测与文档矛盾的登记是否准确。
- **D2/D3（P2/P3）**：D2 需损坏库副本注入（可用 `.runtime/test-data-q05` 的既有证据链核对脚本，或自建一次性副本注入非法 JSON 后 GET /api/state 应 400+原始解析文本）。D3 用两个新假账号验证 connection-secret 跨账号 400、llm-provider-delete 他人 id 返回 200 cleared=true，且无越权效果。
- **Q04-01（P3 新发现）**：连续 新建→撤销→重做 后，顶栏撤销/重做按钮 title/aria-label 仍指向历史条目而实际操作的栈顶不同（toast 正确）。可用 DOM evaluate 复现。
- **A02 重新定性**：grep `frontend/src` 确认 `NodeEditModal` 无任何引用（死代码），报告"本树 UI 生产入口不可达、旧验收的图谱绕过路径不再成立"的改判是否有依据；改判不成立则列为报告缺陷。
- **mapping_forms**：`node --import ./tests/ts_hooks.mjs tests/mapping_forms.test.mjs` 在仓库根运行，确认 committed 基线即失败（执行者未修未藏）。

抽测反例（防"报告只挑好的写"）：任选 2–3 条其标"通过"的 Q05 契约断言（如 409 带 currentRevision、2MB→413、未登录遍历 401）独立重验；任选 1 条其标"未测/环境阻塞"的项确认确实未冒充通过。

## 4. 边界红线

不改业务代码、不改既有测试与金样、不降低/删除任何断言、不改正式需求与接口文档、不 merge、不 push、不新建 worktree、不重启 18765、不停 18931、不删 `.runtime` 下他人数据、不连真实数据库/外部服务、不使用真实凭据。发现问题**记录**，不顺手修复。磁盘故障注入仅对你自建的临时副本。

## 5. 交付与停止点

1. 验收结论按项给出：**实锤 / 报告失实 / 证据不足 / 无法复现（含原因）**，并给出总体结论（验收通过/不通过）；单列"执行者越界项"（若有：改了不该改的文件、把未测写成通过、把延期写成缺陷、把已知当新回归等）。
2. 分类核对：报告是否分清 新发现 / 基线已知 / 明确延期 / 未合入分支（acceptance-fixes、ontology-build 的修复不在本树，两码事）。
3. 结论写入本目录新文件 `验收记录.md`（验收人：你，注明验收 HEAD 与实际复现命令/证据位置），通过 `.collaboration/context.py record` 追加交接（status=verified/blocked 按实际；只提交自己的验收文档与 entry，不代提交他人内容）。
4. 停在交回用户：不安排修复、不合并 main、不清理执行环境；你自起的专属实例与临时副本自行停止/删除（按 PID+cwd 确认后），报告保留。
