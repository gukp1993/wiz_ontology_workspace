# 系统全方位深度测试 · 计划与环境登记

2026-09-21。**当前状态：Q00–Q08 已交付，S1–S3 继续验证已完成，交回 Codex 独立复验。** 目标：独立发现并复现问题、并通过可核算证据支撑结论，不是实施功能。
执行入口：[执行指令.md](执行指令.md)；继续验证依据 [继续验证指令.md](继续验证指令.md)；结果见 [测试报告.md](测试报告.md)、[缺陷清单.md](缺陷清单.md)、[结果索引.md](结果索引.md)；复验入口 [独立复验指令_20260921_S1S3.md](独立复验指令_20260921_S1S3.md)。

## 环境

| 项目 | 值 |
|---|---|
| 主协调仓库 | /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2 |
| 实际测试cwd | /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test |
| 分支 | codex/test |
| 创建基线 | d6c73c29a19d97ad8da22ad3d8a8f3c0615e3b40，创建时最新已提交main |
| 端口 | 18931，创建时bind检查空闲，未启动；开始前再次核实 |
| URL | http://127.0.0.1:18931，登记地址不代表服务已运行 |
| 数据根 | 本树.runtime/test-data；仅合成数据，不复制真实库 |
| Python | 本树.runtime/venv，尚未创建 |
| 前端 | 本树frontend/node_modules和dist，尚未安装或构建 |
| 资源登记 | 主仓库公共Git目录workbench-tasks/system-deep-test.json |
| 实施/测试负责人 | 用户委托harness为单一协调者，开始时填写实际身份 |
| 集成负责人 | Codex；本轮未授权集成或合并 |

main未提交的文档和上下文没有带入。acceptance-fixes与ontology-build分支未合入的内容不属于本次测试基线；不得自行合并来消除缺陷。主服务18765不动。

## 任务矩阵

每项开始前明确输入和具体证据；以下全部待执行，不预写完成。

| ID | 目标/范围 | 前置与协议输入 | 交付物及文件owner | 验收方法/命令 | 并行与状态 |
|---|---|---|---|---|---|
| Q00 | 冻结SHA、能力盘点、风险与需求状态 | AGENTS、需求/接口/历史验收 | 协调者：本计划、报告、环境登记 | git status/log/worktree；查运行进程与真实DB地址 | 串行，待执行 |
| Q01 | 独立环境与现有回归基线 | Q00 | 协调者：安装/启动记录；禁止生产文件修改 | 专属venv、前端构建；runner用法核对后跑quick/http/unit；固定端口串行 | 串行，待执行 |
| Q02 | 本体建设完整业务链 | Q01；本体接口与已实施需求 | 本体测试角色：tests/deep_ontology_*、脱敏证据 | 新建→编辑→共享/关联→校验发布→导出副本读回，合法与非法对照 | 可与Q03独立账号/数据并行，待执行 |
| Q03 | 项目配置和编排完整链 | Q01；项目/编排协议 | 项目测试角色：tests/deep_project_* | 接入→身份→取值/动作→编排→校验发布→升级；不用真实外部服务 | 可与Q02并行，待执行 |
| Q04 | UI与交互一致性、可达性 | Q01；当前产品风格/明确需求 | UI角色：截图/浏览器脚本，独占其测试文件 | 1440/1024/768、缩放、键盘、长文本、空态、错误/取消/返回/刷新 | 独立浏览器账号可并行，待执行 |
| Q05 | 数据可靠性与账号/接口边界 | Q01；CAS/权限/存储协议 | 可靠性角色：tests/deep_integrity_* | 双标签冲突、故障注入、重复发布、导入原子性、跨账号拒绝、重启读回 | 固定端口/故障注入串行，待执行 |
| Q06 | 分层性能与资源测试 | Q02/Q03功能基线；测量方法先定 | 性能角色：tests/deep_performance_*及原始指标 | 独占服务；分规模、冷/暖、重复采样、吞吐/延迟/内存 | 禁止与其他负载测试并行，待执行 |
| Q07 | 独立复现、严重性和覆盖收口 | Q02～Q06 | 协调者：测试报告.md、缺陷清单.md、证据索引 | 逐项可复现、误报排除、未测清楚；不篡改旧测试 | 串行，待执行 |
| Q08 | 交接并停止 | Q07 | 协调者：自己的测试/文档commit和context记录 | 准确SHA/URL/进程/端口/结论；不合并不清理其他环境 | 串行，待执行 |

默认一个agent协调完成；harness支持时可把无写冲突任务分给子agent。生产代码只读，共享文档、Git、依赖安装、构建、启停由协调者单写者管理；额外agent不授权额外worktree。新测试文件按任务分属，先登记后写，不并发修改同文件。

## 授权停止点与交付

用户将执行指令交给harness后，允许在本树准备依赖、启动隔离实例、写合成数据、运行测试、增加测试和记录。不允许修复业务代码、改需求/金样/原断言、合并其他分支或main、操作真实库、重启主服务。发现阻断继续完成独立可测部分；不得悄悄修复或切分支后称原基线通过。

结果输出在本目录测试报告.md、缺陷清单.md。大日志/性能原始数据/截图放本树.runtime/test-evidence，提交精选脱敏证据或足够重现的脚本与摘要，不提交数据库/凭据/依赖。记录每项通过、失败、阻塞、未覆盖，不宣称穷尽所有问题。

若以后用户授权集成，由Codex串行建临时集成树、核对要合入的测试/文档提交并组合重验，普通冲突按双方语义处理，需求冲突再询问；main脏则等待，不stash/reset。合并成功后才按已授权范围、归属和PID清理测试树与登记的可丢弃数据；未合并保留环境。回退使用revert，代码回退不还原数据。真实主服务更新另需授权。

## 实际执行记录

协调者 owner：Qoder harness（用户委托的测试执行者）；子代理：Q02/Q03/Q05 各一名（Q02 一次断连后重派成功，Q03/Q05 首轮断连后按复用策略重派完成）。执行日期 2026-09-21（当日跨多个小时段，末段 08:30–09:15 有精确时间戳证据）。

**SHA**：开始时与结束时业务代码均为 `d6c73c2`（零修改）；本树 HEAD 在 d6c73c2 之上仅有测试文档/登记提交（执行期间 981603e），本轮测试记录提交即本次 commit（文档+tests/deep_*+交接条目），两类 SHA 已在报告区分登记。

**环境与进程**：专属 venv 与前端 build 于 Q01 完成；18931（PID 56914，数据根 .runtime/test-data）承载功能/UI/性能实例，**结束时仍在运行**；18932 Q05 专属（test-data-q05）及 18933/18939 故障子实例均按 PID+cwd 双确认后停止、端口释放；18932 又被 Q06 临时复用后同样停止。主工作台 18765 与 8765 全程未触碰。假账号 qa_deep_a/b/c、qa_int_a/b/c、qa_perf，口令仅存测试脚本/证据约定内，未用于任何真实资产。

**执行与结果**（全部经真实 HTTP/浏览器生产入口，未改断言/金样/业务代码/正式需求）：

| 项 | 状态 | 关键数字 |
|---|---|---|
| Q00 | 完成 | SHA/端口/数据根/registry 实核；能力清单五态建立 |
| Q01 | 完成 | venv+build；runner quick/http/unit 通过；`mapping_forms.test.mjs` committed main 即失败（基线已知，单列未修） |
| Q02 | 完成 | 34 断言：31 通过；A01 基线复现（HTTP 全链路+发布快照读回）；新发现 D-Q02-01（P1）；O4-07a 阻塞归因 D-Q02-01；O4-06 无 API 入口记未验证 |
| Q03 | 完成 | 74 断言：73 通过；R02 基线复现并定位根因（project_validation.py:512）；无新增缺陷 |
| Q04 | 大部分完成 | 删除确认/撤销恢复/重做/登录退出往返/导入异常反馈等通过；新发现撤销按钮 label 陈旧 P3；A02 重新定性为"本树 UI 生产入口不可达（死代码）"；R01 真实浏览器复现；截图/宽度/缩放/画布/键盘类**环境阻塞未测** |
| Q05 | 完成 | ~75 断言 I1–I9 全符合契约；新发现 D1（Origin 缺省放行 vs 文档，P2 待裁定）、D2（损坏回传内部异常文本，P2）、D3（跨账号错误码口径，P3）；无越权/无半写/无明文密钥 |
| Q06 | **用户指令取消** | 仅保留有限样本（冷启动 0.479s；保存 10/100/500=38ms/391ms/5000ms，n=1），无统计结论 |
| Q07 | 完成 | 缺陷全部可复现并有 JSONL/脚本证据；已知/新发现/延期/未合入分支四类分清（见两份报告） |
| Q08 | 完成 | 提交自己文件、context record、保留环境交回用户 |

**未覆盖/限制**：完整性能矩阵（取消）；浏览器视觉/宽度/缩放/截图/画布/纯键盘/断网（应用内浏览器隐藏，NATIVE 指针不可用）；发布不可变无入口未验证；TTL 30min 未等待；未测 MySQL 路径。未宣称穷尽所有问题。

**保留证据**：`.runtime/test-evidence/{q02,q03,q04,q05,q06,server-18931.log}`；数据根 `.runtime/test-data`（18931 在用）、`test-data-q05`、`test-data-q06`（合成、可丢弃，未删待集成时处置）；`tests/deep_*` 14 个新脚本入库。共享上下文交接：本任务 ticket 与各子任务 ticket（000133/000134 等 entries）。

**停止点**：未合并 main、未创建修复分支、未清理 worktree；18931 服务保留运行，交回用户安排修复。

---

# 继续验证（2026-09-21 下午，按《继续验证指令》）

依据 [继续验证指令.md](继续验证指令.md) 处理独立验收的 S1–S3：修订测试判定、整理可追溯证据、完成必要复验。**不修改业务代码，不全面重测，不恢复性能压测，不合并 main。** 结果与统计见 [测试报告.md](测试报告.md) §8 与 [结果索引.md](结果索引.md)；缺陷口径见 [缺陷清单.md](缺陷清单.md)。

## 与本轮的基线核对（V00）

| 项目 | 实际核实 |
|---|---|
| 执行 cwd | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test`（唯一 cwd） |
| 分支 | `codex/test`；开始 HEAD `42987ce`（指令说明不能机械要求等于 53ddab0） |
| 被验业务 SHA | `d6c73c2`；`git diff --name-status d6c73c2..HEAD` 仅新增 `tests/deep_*`、本需求文档、交接条目与自动摘要，**业务代码与既有测试零修改** |
| worktree 列表 | 4 个：主仓（main）、ontology-build、acceptance-fixes、test（本树）；未新建、未切换 |
| 原测试实例 | 18931（PID 56914）只读观察，**未写入、未停止、未复用其账号** |
| 18941（Codex 验收实例） | 实测空闲（已释放），本轮未使用 |

## 环境登记（本轮唯一临时实例）

| 项目 | 值 |
|---|---|
| 端口 | **18951**（选前实测空闲；未使用 18765/8765） |
| URL | http://127.0.0.1:18951 |
| 数据根 | `.runtime/reverify-data`（本工作树内，全新合成根） |
| 证据目录 | `.runtime/reverify-evidence/`（不入库） |
| Python | `.runtime/venv`（Python 3.9.6，复用本树既有依赖，未升级） |
| 前端 | 复用本树既有 `frontend/dist`（index-CB119CO_.js），本轮未重新构建，未与其它 agent 并发构建 |
| 账号 | `qa_reverify_a` / `qa_reverify_b`（仅合成） |
| 存储隔离 | 启动前 `env -u WIZ_DATABASE_URL`；实测进程环境 `WIZ_WORKBENCH_ROOT`/`WIZ_WORKBENCH_PORT` 生效，库落在 `.runtime/reverify-data/data/workbench.sqlite3` |
| 资源登记 | 公共 Git 目录 `workbench-tasks/system-deep-test-reverify.json`（加锁目录内新增，未改动他人登记） |

## 任务矩阵与结果

owner 为本轮执行者（zcode，协调者/单一写者）。Git、启停、依赖准备、汇总文档单人串行；判定修订与证据盘点由同一执行者顺序完成（本轮未使用多 agent，如实记录）。

| ID | 目标/交付 | 交付物与文件 | 验证要求 | 状态与结果 |
|---|---|---|---|---|
| V00 | 冻结基线、登记环境与证据协议 | 本计划；`workbench-tasks/system-deep-test-reverify.json` | cwd/HEAD/业务差异/端口/数据根核实 | 完成：业务 `d6c73c2` 零差异；18951 实测空闲；数据根/证据目录新建 |
| V01 | 修正 A01/R02/O4-06 判定与结果分类 | `tests/deep_verdicts.py`、`tests/deep_verdicts_test.py`、`tests/deep_reverify_scenarios.py`；修订 `deep_project_chain.py` P8、`deep_ontology_o3o4o5.py` O3-04/O4-06 | 正确阻断、缺陷复现、无关错误三类不混淆；前置失败显式 blocked | 完成：判定单测 **23/23** 通过；三类分类各有反例 |
| V02 | 盘点历史证据，定义有效批次与替代关系 | `tests/deep_results_index.py`、[结果索引.md](结果索引.md)（脱敏） | 汇总可重算，无法归属项标未确认 | 完成：14 个有效批次 + 替代原因 + 逐行重分类；被替代 94 行保留不计 |
| V03 | 用新环境复跑必要场景 | 18951 实例；`tests/deep_reverify_r02_a01.py`；runId 目录 | HTTP 真实链路，前置成功，保存准确结果 | 完成：见下方运行日志与结果 |
| V04 | 统一统计、缺陷范围与分级 | 测试报告/缺陷清单/本计划 | S1–S3 逐项有回应，数字一致 | 完成：三份文档数字统一为索引口径 |
| V05 | 自检、提交、交接与清理 | Git 提交、共享交接、进程与数据根清理 | 精确 SHA/结果/限制 | 完成：见 §继续验证收尾 |

## 本轮运行记录（命令、结果、限制）

```bash
cd /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test
# 判定逻辑自测（纯逻辑，不连服务）
.runtime/venv/bin/python tests/deep_verdicts_test.py                 # 23 项，通过 23，退出码 0

# 启动本轮唯一隔离实例（18951）
env -u WIZ_DATABASE_URL WIZ_WORKBENCH_ROOT="$PWD/.runtime/reverify-data" WIZ_WORKBENCH_PORT=18951 \
  nohup .runtime/venv/bin/python -m workbench.server >> .runtime/reverify-evidence/server-18951.log 2>&1 &

# 定向复跑（R02/A01 + 负对照）
DEEP_RUN_ID=reverify-20260921T0958Z Q03_BASE=http://127.0.0.1:18951 Q03_USER=qa_reverify_b \
  .runtime/venv/bin/python tests/deep_reverify_r02_a01.py

# 修订后两套脚本全链路回归
DEEP_RUN_ID=regress-20260921T1005Z Q02_BASE=http://127.0.0.1:18951 Q02_USER=qa_reverify_a \
  .runtime/venv/bin/python tests/deep_ontology_o3o4o5.py
DEEP_RUN_ID=regress-20260921T1008Z Q03_BASE=http://127.0.0.1:18951 Q03_USER=qa_reverify_b \
  .runtime/venv/bin/python tests/deep_project_chain.py

# 结果索引重算（只读原始日志）
.runtime/venv/bin/python tests/deep_results_index.py --write-doc
```

| runId | 记录 | 分类统计 | 关键结论 |
|---|---|---|---|
| `reverify-20260921T0958Z`（有效） | 15 | product_pass 11、known_defect_reproduced 3、info 1 | 前置全成功；A01 与 R02 两条观察均判**已知缺陷复现**（不是产品通过）；负对照（引用不存在编排）通过 |
| `reverify-20260921T0947Z`（被替代） | 15 | — | 工具构造错误（连接缺 `engine`、记录器序列化 Path）→ R02 发布被无关 422 拦截，判为 test_error；证据保留 |
| `reverify-20260921T0952Z`（丢弃） | 15 | — | 工具构造错误（负对照复用已推进 revision → 409 无法归因）；中间运行未保留 |
| `regress-20260921T1005Z`（有效） | 22 | product_pass 17、known 1、new 1、static_check_pass 1、blocked 1、not_tested 1 | 修订后 `deep_ontology_o3o4o5.py` 全链路可运行；O4-06 拆为静态核对 + O4-06b 未测 |
| `regress-20260921T1008Z`（有效） | 54 | product_pass 51、known 2、info 1 | 修订后 `deep_project_chain.py` 全链路可运行；P8 前置与负对照均通过 |

证据路径：`.runtime/reverify-evidence/runs/<runId>/`（含 SUMMARY.json）；两套回归脚本的 runId 证据写在同一套件目录的 `runs/<runId>/` 下，**旧的追加式 JSONL 未被覆盖**。原始大日志留 `.runtime/`，入库仅脱敏索引与脚本。

## 限制与未做（本轮）

- 只跑受判定修订影响的场景 + 两套修订后脚本，**未全面重测**；未重复 kill -9、未全端点扫描、未全密钥字节扫描。
- 未重新构建前端（沿用既有 dist 的 SHA 记录）；未做浏览器复测（本轮范围不含）。
- Q06 性能**按指令不恢复**；视觉/宽度/缩放/画布/键盘、MySQL 路径、TTL 等待仍未测。
- `mapping_forms` 失败保留原状。

## 继续验证收尾（V05）

按 PID + cwd 双确认后停止本轮 18951 实例并删除本轮合成数据根 `.runtime/reverify-data`；保留脱敏证据 `.runtime/reverify-evidence/`（含 server 日志、runId 证据、索引 JSON）。原 18931 实例、`test-data*`、原日志与 Codex 验收证据**未触碰**。分支仍为 `codex/test`，未合并 main、未 push、未重启主工作台。
