# 系统全方位深度测试 · F01–F05 整改独立验收执行指令（交 Codex / 独立 harness）

> **用途**：可直接整份复制给 Codex 或其它执行者，对「按 S1–S3 复验整改（F01–F05）+ 对抗加固」这一轮的测试交付做**独立验收**。
> 本指令自包含，不依赖聊天历史；被验对象是**测试判定器与证据核对器**的可靠性，不是要求先修业务缺陷。
>
> **本版要点**：实施方在交付前另做了一轮对抗性自验（主动尝试"骗过判定器"），发现并修复了若干残余通道，
> 因此 §4 的期望包含**加固后的更严行为**（例如：诊断必须由**同一条诊断消息**同时满足"定位"与"原因"；
> 配置类参数类型不符一律 test_error）。§5 给出"出现即判不通过"的判据，请按此复核，不要接受任何"应该可以"的说法。

---

## 0. 本轮验收的标准提示词（用户可直接复制）

```
在【system-deep-test 的 codex/test 工作树】中，按【文档/需求/20260921_系统全方位深度测试/独立验收执行指令_20260921_F01F05.md】
对【F01–F05 整改轮】做独立验收，不合并 main。
授权：只读复验、自起自关隔离实例、在 /tmp 或新目录写临时证据；不改业务代码/既有测试/金样/依赖、不改验收记录与他人交接、
不触碰 18931 与 test-data*、不重启 18765。验收完成后输出逐项结论与不通过判据，停在「待用户授权集成」。
```

## 1. 被验对象与基线（已实测核对，请自行复核）

| 项目 | 值 |
|---|---|
| 主协调仓库 | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` |
| **验收 cwd（唯一）** | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test` |
| 分支 | `codex/test` |
| **被验交付范围** | **包含对抗加固**：`53a7ab2`（F01–F05 整改主体）→ `91494f0`（对抗加固 H1–H7，修改 `tests/deep_*` 实现）→ `eb82ffe`（交接）→ 按 G01–G03 整改的提交 → **G 整改后的第四轮对抗复验（R1–R4）与覆盖结论修正**。以 `git log --oneline` 实际结果为准，**不要**把被验范围当成只有 `53a7ab2`。 |
| 允许的 HEAD 差异 | 交付后只应再出现**文档/交接类**提交（`文档/`、`.collaboration/`、根级 `session_context.md`）与**本轮 G01–G03 整改**（`tests/deep_verdicts*.py`、`tests/deep_results_index*.py`）。若出现**业务代码**提交或其他 `tests/` 文件改动，先报告再决定是否继续。 |
| 被验业务 SHA | `d6c73c29a19d97ad8da22ad3d8a8f3c0615e3b40`；`git diff d6c73c2..HEAD -- workbench frontend tests/fixtures` 必须为空 |
| 本轮改动范围 | `tests/deep_*.py`（6 个文件）+ 本需求 `文档/需求/20260921_系统全方位深度测试/*.md` + 共享交接；见 §2 |
| 前序结论（只读参考，勿修改） | [验收记录.md](验收记录.md)（S1–S3）、[S1S3独立复验记录_20260921.md](S1S3独立复验记录_20260921.md)（F01–F05 提出方） |
| 实施方自述结果 | [缺陷清单.md](缺陷清单.md) 的「复验整改记录（第二轮 · F01–F05）」与「独立验证发现的追加加固」 |

先执行并贴出结果：`git log --oneline -5`、`git status --short`、`git diff --name-status d6c73c2..HEAD`、`git worktree list`。

**被验文件指纹**（可用于确认你验的就是这一版；`sha256` 前 16 位）：

| 文件 | 说明 |
|---|---|
| `tests/deep_verdicts.py` | 判定器（含对抗加固） |
| `tests/deep_verdicts_test.py` | 判定单测：期望 **107/107** 通过 |
| `tests/deep_reverify_scenarios.py` | R02/A01 场景（诊断分组已锚定具体身份） |
| `tests/deep_results_index.py` | 索引与覆盖核对 |
| `tests/deep_results_index_test.py` | 核对器单测：期望 **51/51** 通过 |
| `tests/deep_reverify_r02_a01.py` | 定向复跑入口 |

> 复验时请以 `git show <被验HEAD>:tests/<文件> | shasum -a 256` 自行记录指纹；**不要**照抄旧指纹（加固后已变）。

## 2. 被验改动清单（相对 `47d5a8f`）

新增：`tests/deep_results_index_test.py`。
修改：`tests/deep_verdicts.py`（判定器）、`tests/deep_verdicts_test.py`（单测）、`tests/deep_reverify_scenarios.py`（R02/A01 场景与版本计数）、`tests/deep_results_index.py`（覆盖核对）、以及 §5 列出的四份文档 + 复验指令。

复核：`git diff 47d5a8f..HEAD --stat` 是否只含上述文件；有无夹带业务改动。
文档侧本批次涉及六份：`测试报告.md`、`测试计划.md`、`缺陷清单.md`、`结果索引.md`（更新）、
`S1S3独立复验记录_20260921.md`（新增，第三方结论）、`独立复验指令_20260921_S1S3.md`（更新），
以及根级 `session_context.md` 与 `.collaboration/entries/` 交接条目。

## 3. 只读边界与资源约束

- **不得**修改业务代码、既有非 deep 回归测试、`tests/fixtures/validation_golden.json`、依赖声明/锁文件、真实数据；
  **不得**修改 `验收记录.md` 与 `S1S3独立复验记录_20260921.md`（要补记结论请另存你自己的报告文件）。
- **不得**合并/推送 main、不得创建修复分支、不得重启主工作台 **18765**、不得使用 **8765**。
- **不得**触碰前轮实例与本任务以外环境：`18931`（仍在监听）、`.runtime/test-data*`、`.runtime/test-evidence/`（**既有文件**）、`.runtime/accept-evidence/`、`.runtime/s1s3-accept-evidence/`（前轮验收证据）。
- 例外：K11 运行 `deep_ontology_o3o4o5.py` / `deep_project_chain.py` 时，其 Recorder 会在 `test-evidence/q02|q03/runs/<你的 runId>/` 下**新建**子目录写证据（脚本无环境变量可改该路径）。**允许新增 runId 子目录；严禁修改或删除既有文件与既有 runId 目录。**
- 需要真实 HTTP 时**自起自关**新隔离实例：端口自行核对空闲；数据根建在 `/tmp` 或本树 `.runtime/` 下的**新目录**；
  启动前 `env -u WIZ_DATABASE_URL`，并实测进程 `WIZ_WORKBENCH_ROOT`/`WIZ_WORKBENCH_PORT` 生效、SQLite 落在隔离根。
  仅用合成账号/数据；不连真实 MySQL/Redis/模型。收尾时按 PID+cwd 双确认停止并删除自建合成数据根。
- 脚本支持环境变量覆盖：`Q02_BASE` / `Q03_BASE` / `Q02_USER` / `Q03_USER` / `DEEP_RUN_ID`。**不要指向 18931 或真实根。**

## 4. 必做验收项（K01–K14）

**总原则：每条都要自己构造输入或自己跑命令，并给出命令、退出码与关键输出；不要只复述实施方自述。**
反例脚本请写在 `/tmp`，用 `.runtime/venv/bin/python`（Python 3.9.6）执行。

### K01 单测基线
```bash
.runtime/venv/bin/python tests/deep_verdicts_test.py        # 期望 107/107 通过、退出码 0
.runtime/venv/bin/python tests/deep_results_index_test.py   # 期望 51/51 通过、退出码 0
```

### K02 F01 · 拒绝分支必须同样证明「版本零新增」
自行构造输入调用 `deep_verdicts.classify_guard_attempt(True, response, expect)`，**至少 5 组**：

| 输入 | 期望 `result` |
|---|---|
| 422 + 诊断命中 + `version_before=1, version_after=2` | `new_defect_reproduced`（拒绝却新增=拦截不生效；**绝不是 product_pass**） |
| 422 + 诊断命中 + `version_after=None` | `test_error`（版本不可读=证据不足） |
| 422 + 诊断命中 + `version_before=None` | `test_error` |
| 422 + 诊断命中 + `version_before=3, version_after=3` | `product_pass`（对照） |
| 422 + 诊断命中 + 不传 `require_no_version_increase` | `product_pass`（确认版本门是可选开关，未被做成无条件） |

### K03 F01 · 版本计数读取失败不得当 0
自行写一个只有 `get(path)` 的假 API 对象，注入 `deep_reverify_scenarios._count_versions`（或经 `_versions_of`/`_releases_of`）：

| 假响应 | 期望 |
|---|---|
| HTTP 500 | 返回 `None` |
| 200 但 `items` 不是数组（如字符串/字典/缺键） | 返回 `None` |
| 200 且 `items=[]` | 返回 `0`（区别于上一条） |
| 无状态码（网络失败） | 返回 `None` |

### K04 F02 · 校验接口必须先过状态码与结构
调用 `deep_verdicts.classify_validate_observation(True, response, expect)`：

| 输入 | 期望 `result` |
|---|---|
| **500 但 `errors` 文案完全正确且命中诊断词**（伪造响应） | `test_error`（**绝不能 product_pass**） |
| 200 但 `errors` 是字符串 | `test_error` |
| 200 但响应非对象（如纯文本） | `test_error` |
| `status=None`（网络失败） | `test_error` |
| 200 + `errors=[]` | 缺陷复现（`known_defect_reproduced` 或 `new_defect_reproduced`，取决于 `known_defect`） |
| 200 + `errors` 非空且诊断命中 | `product_pass`（对照） |
| 200 + `errors` 非空但诊断与目标无关 | `test_error` |

### K05 F02 · R02 负对照两段都必须满足期望
读 `tests/deep_reverify_scenarios.py` 的负对照段（第 7 段），确认：只有 **validate 拦到「编排不存在」类诊断** *且* **publish 被 422 拦截且版本零新增**，整条负对照才记 `product_pass`；任一为 `test_error` 时整条记 `test_error`（不允许"记录了却不用"）。若你的判断与实现不同，给出反例。

### K06 F03 · 诊断匹配的范围（4 组反例 + 2 组对照）
自行调用 `deep_verdicts.diag_text(response)` 与 `classify_guard_attempt`：

| 输入 | 期望 |
|---|---|
| `{"errors":["属性A 未绑定"]}` | `diag_text` **必须包含**该文案（诊断键下的字符串列表要被收集） |
| `{"report":{"errors":["未绑定"],"items":[{"issues":["未绑定"]}]}}` | 必须取到嵌套文案 |
| `{"error":"名称重复","state":{"kind":"flow"}}` 配 `diagnostic_terms=['flow']` | `test_error`（回显载荷不得命中） |
| `{"error":"x","payload":{"type":"flow"}}` 配 `['flow']` | `test_error` |
| 真实冲突载荷形态 `{"error":"部分名称已被占用","code":"DUPLICATE_NAME","conflicts":[{"kind":"flow",...}]}` 配 `['flow']` | `test_error`（内层判别值键 `kind` 值不得当诊断） |
| 无任何约定诊断字段（如 `{"scene":"x","state":{"kind":"flow"}}`）配 `['flow']` | `test_error`（**不得回退为序列化整份响应来搜索**） |
| `{"errors":[{"name":"activePower","message":"未绑定"}]}` 配两组诊断 `[['activePower'],['未绑定']]` | `product_pass`（两字段属**同一条目**，必须能在同一条消息内命中两组） |
| `{"errors":["消息甲(含定位)","消息乙(含原因)"]}` 配两组诊断 | `test_error`（**不得**由两条不同消息各贡献一半拼出假命中 —— 加固后的关键行为） |
| 分组诊断只命中原因组 | `test_error`（组间 AND） |

**对抗用例（加固后必须被挡住，全部取自真实产品文案）**：

| 输入 | 期望 |
|---|---|
| `动作绑定 X/启动：实现方式未选择（项目接口或函数编排）` 配 R02 两组 | `test_error`（泛词"编排/未选择"不得命中） |
| `属性来源 X.p：引用的函数编排 fl-x：读取失败（StorageUnavailable），暂不能校验该绑定；请稍后重试` 配 R02 两组 | `test_error`（基础设施故障 fail-closed 不得算正确拦截） |
| `额外编排不存在或不属于当前账号：fl-x` 配负对照两组 | `test_error`（无关归属错误不得让负对照成立） |
| 顶层 `field`/`text` 回显目标词 | `test_error` |
| `items[].name` 回显（真实接口为每个条目回显） | 不得让校验观察判为"已拦截" |

### K06b G01–G02 · 独立验收提出的假通过（必测）
| 输入 | 期望 |
|---|---|
| `{"report":{"errors":["activePower","输出未绑定"]}}` 配两组 `[['activePower'],['输出未绑定']]` | `test_error`（**嵌套容器不得把两条不同诊断拼成一条**） |
| `{"report":{"errors":["属性 X.activePower 引用编排输出未绑定"]}}` 配同样两组 | `product_pass`（对照：同一条诊断仍须能命中） |
| `{"check":{"diagnostics":[{"message":"activePower"},{"message":"输出未绑定"}]}}` 配两组 | `test_error`（条目边界须贯穿嵌套） |
| `{"errors":[{"name":"activePower","message":"未绑定"}]}` 配两组 | `product_pass`（对照：同一条目内多字段可联合） |
| validate：`{"errors":["名称重复"],"warnings":["activePower 输出未绑定"]}` 配两组 | `test_error`（**warning 不得替无关 error 证明阻断**） |
| validate：`{"errors":["属性 X.activePower 引用编排输出未绑定"]}` 配两组 | `product_pass`（对照） |
| validate：`{"errors":[],"warnings":["activePower 输出未绑定"]}` 配两组 | 缺陷复现（不是 product_pass） |
| guard：拒绝原因只出现在 `warnings` 里 | `test_error` |

### K06c R1–R4 · 第四轮对抗复验提出的残余通道（必测）
| 输入 | 期望 |
|---|---|
| `{"errors":{"e1":"activePower","e2":"输出未绑定"}}` 配两组诊断 | `test_error`（信封值是 dict 不得合并） |
| `{"report":{"wrapper":{"errors":["activePower","输出未绑定"]}}}` 配两组 | `test_error`（**未登记包装键**不得让边界失效） |
| `{"errors":[[{"a":"activePower","b":"输出未绑定"}]]}` 配两组 | `test_error`（双层信封列表） |
| `{"errors":[{"a_list":["activePower"],"b_list":["输出未绑定"]}]}` 配两组 | `test_error`（同一 dict 内两个 list） |
| `{"report":{"title":"activePower","reason_text":"输出未绑定"}}` 配两组 | `test_error`（容器自身两标量） |
| validate：`{"errors":["数据连接名称重复：主数据库"],"items":[{"status":"unconfigured","issues":["activePower 输出未绑定"]}]}` | `test_error`（**真实字段 `status=unconfigured` 是待补全、不阻断**） |
| validate：同上但 `status:"invalid"` | `product_pass`（对照：真阻断仍算） |
| guard/validate：`{"errors":["名称重复"],"note":"activePower 输出未绑定"}` | `test_error`（`note/detail` 提示键不得当阻断证据） |
| guard/validate：`{"errors":["名称重复"],"report":{"detail":{"level":"warning"},"message":"activePower 输出未绑定"}}` | `test_error`（level 嵌在内层也算） |
| 对照：`{"errors":[{"name":"activePower","message":"未绑定"}]}` 配两组 | `product_pass`（同条目内多字段可联合） |
| 对照：`{"errors":["属性 X.activePower 引用编排输出未绑定"]}` 配两组 | `product_pass` |

### K06d S3 · 覆盖核对的「已覆盖 vs 未确认」（必测，含一处结论修正）
| 检查 | 期望 |
|---|---|
| 拆分覆盖被 `blocked`/`test_error`/`not_tested` 行顶替 | **不算已覆盖**：必须在覆盖表里显式列为「未确认」并写明原因 |
| 有效行缺 `result`、`kind` 为 `' info'`/`'INFO '`/`'information'` | 不算业务证据 |
| `classify_row({'verdict':'fail','kind':'info'})` | 不得返回 `info`（真实失败不得被降级） |
| `COVERAGE_UNCONFIRMED` 登记表 | 必须在源码内可审计；未登记的真实缺口仍报错退出 |
| **真实数据核对** | 结果索引 §2.2 中 `q02-o3o4o5-1` 的 `O4-07` 应显示 **O4-07b 已覆盖、O4-07a 未确认**（原因是该行 `blocked`：restore 成功路径因 D-Q02-01 无法成立）。请自行核实该结论是否正确；**不得**出现「全部已覆盖」 |

### K07 F03 · 未提供诊断依据不放行
`classify_guard_attempt(True, resp(422,{"errors":["任意"]}), {"block_status":422})` → 期望 `test_error`；
显式传 `diagnostic_not_required=True` → 期望 `product_pass`（对照）。

### K08 F04 · 一拆多 vs 任选其一
用内存构造的批次调用 `deep_results_index.validate_superseded_coverage`：

| 输入 | 期望 |
|---|---|
| 旧编号 `O4-07`，有效批次**只有** `O4-07a` | **必须报错**（AssertionError，且报错写明缺 `O4-07b`） |
| 旧编号 `O4-07`，有效批次**只有** `O4-07b` | **必须报错**（写明缺 `O4-07a`） |
| `O4-07a` + `O4-07b` 都在 | 通过，`splitCoverage.missing` 为空 |
| 自设一个 `CASE_ID_ALTERNATIVES` 项，只命中其一 | 通过（任选其一语义仍在） |

### K09 F04 · 残余豁免通道（追加加固）
| 输入 | 期望 |
|---|---|
| 被替代场景名含 `crash` 但 `kind` 非工具错误（如 `biz`） | 报错（不得仅凭名字豁免） |
| 名字含 `crash` 且 `kind` 为工具错误（如「异常中断」） | 允许无覆盖，但必须出现在 `toolErrorCrashRows` |
| 有效批次用 **info 说明行** 顶替业务场景 | 报错（info 不得充当业务覆盖） |
| 有效批次是真实业务判定行 | 通过（对照） |

### K09b G03 · 覆盖核对的「有效业务证据」过滤（必测）
| 输入 | 期望 |
|---|---|
| 拆分：`O4-07a` + `O4-07b` **两行都是 info** | 报错 |
| 拆分：`O4-07a` 真实业务 + `O4-07b` 仅 info | 报错，且写明缺 `O4-07b` |
| 别名（`CASE_ID_ALTERNATIVES`）：只有 info 行命中 | 报错 |
| 对照：拆分 a+b 都是真实业务行 | 通过 |
| 对照：同编号真实业务行；以及同编号既有 info 又有业务行 | 通过（过滤不得过度拒绝） |
| 对照：被替代批次自身的 info 行 | 列入 `infoOnlyRows` 且不导致报错 |

### K10 配置自检（追加加固：类型不符一律 test_error）
| 输入 | 期望 |
|---|---|
| `block_status=200` 与 `allow_status=(200,201)` 重叠 | `test_error` 并提示用例配置有误 |
| `require_no_version_increase=True` 但版本计数传字符串 `'9'/'10'` | `test_error` |
| 同样但传整数 `9` / `10` | `new_defect_reproduced`（对照） |
| `status` 传字符串 `'422'` / 浮点 / 布尔 | `test_error`（不得按业务状态判定） |
| `diagnostic_terms` 传字符串（非列表） | `test_error`（不得退化为逐字符匹配） |
| `diagnostic_terms=['']` | `test_error`（空串不得恒真通过） |
| `require_no_version_increase` 传 `0`/`None`/`'false'` | `test_error`（不得静默关掉证据门） |
| `version_before=2, version_after=1`（回退） | `test_error`（证据自相矛盾） |
| `precondition_ok` 传真值字符串 `'false'` | `test_error`（**不得**当成前置成功） |
| `known_defect` 传字符串 `'false'` | `test_error`（不得把新缺陷降级为已知） |
| `block_status` 传字符串 `'422'` | `test_error`（**不得**因类型不符把真拦截判死或放行） |
| 接受态（2xx）同时传 `require_no_version_increase=True` | 仍按"未被拦截"记缺陷复现，**不得**因"版本未增加"改判通过 |

### K10b 证据索引的兜底行为
| 输入 | 期望 |
|---|---|
| 证据行 `verdict` 是未知枚举（如 `''`/`None`/拼写错误） | **报错退出**（不得默认算 product_pass） |
| 证据行缺 `kind` 字段 | 报错退出 |
| 工具错误豁免 | 只依据记录自身声明（mode:kind/title/evidence 含"中断/异常/Traceback"），**与 case 名字无关**：业务场景名含 `crash` 不得豁免；名字平常但有堆栈应豁免 |

### K11 真实 HTTP 端到端（自起隔离实例）
```bash
cd /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/test
# 自行选空闲端口与全新数据根；示例端口仅占位
env -u WIZ_DATABASE_URL WIZ_WORKBENCH_ROOT=<你的新数据根> WIZ_WORKBENCH_PORT=<你的端口> \
  nohup .runtime/venv/bin/python -m workbench.server >> <你的日志> 2>&1 &

DEEP_RUN_ID=<你的编号> Q03_BASE=http://127.0.0.1:<端口> Q03_USER=<合成账号> \
  .runtime/venv/bin/python tests/deep_reverify_r02_a01.py
DEEP_RUN_ID=<你的编号>-onto  Q02_BASE=http://127.0.0.1:<端口> Q02_USER=<合成账号> \
  .runtime/venv/bin/python tests/deep_ontology_o3o4o5.py
DEEP_RUN_ID=<你的编号>-proj  Q03_BASE=http://127.0.0.1:<端口> Q03_USER=<合成账号> \
  .runtime/venv/bin/python tests/deep_project_chain.py
```

期望（**在当前未修业务基线上，A01/R02 仍应归为已知缺陷复现，不追求业务全绿**）：

| 脚本 | 期望分类 |
|---|---|
| `deep_reverify_r02_a01.py` | 15 条 = `product_pass` 11 / `known_defect_reproduced` 3 / `info` 1；**负对照（引用不存在编排）两段均满足期望**；A01 与 R02 两条观察均记 known。加固后重跑已实测同样的分类（runId `hard-20260921-r02a01b`） |
| `deep_ontology_o3o4o5.py` | 22 条 = 17 pass / 1 known / 1 new / 1 static / 1 not_tested / 1 blocked |
| `deep_project_chain.py` | 54 条 = 51 pass / 2 known / 1 info |

要点核对：R02 的 project-validate 观察若返回非 200 或结构不符必须记 `test_error`（不是 pass）；发布观察必须同时核对"版本零新增"。

### K12 索引可重算与文档一致
```bash
.runtime/venv/bin/python tests/deep_results_index.py        # 只读重算，退出码 0
```
期望：14 个有效批次、被替代 94 行、断言记录 187、唯一场景 187、缺陷根因 9、
`product_pass=178 known_defect_reproduced=3 new_defect_reproduced=2 static_check_pass=1 blocked=1 info=2`。

- **请自行把生成结果与已提交的 `结果索引.md` 对照**（建议把输出重定向到 /tmp 或临时副本，**不要覆盖仓库文件**）：除生成时间外应一致。
- 抽查**至少 4 行**（含被重分类行，如 `q03/project_chain.jsonl#46` 应为 `known_defect_reproduced`）与 `.runtime/test-evidence/` 原始 JSONL 比对。
- 核对 §2.2 覆盖表：8 条 `*-crash` 行是否确为工具异常中断（打开原始行看堆栈）；`O7-00` 这类「本身即说明行」是否如实单列而非当作业务覆盖。

### K13 文档口径与分级（S1/S3 不得回退）
核对 `测试报告.md`、`缺陷清单.md`、`结果索引.md`、`测试计划.md` 是否互相一致，并确认：

- 老数字（34 / 74 / 约 75 断言、73 通过）**只出现在**「原表述 → 修订」对照行或标了失效提示的历史区；
- D-Q02-01 仍是「新发布本体无法通过现有历史快照入口恢复」，且明确**发布/版本列表/版本内容读取正常**，未被写成发布整体失效或数据丢失；
- R02 仍是 **P1** 且写明保持理由与降级条件；R01 未被写成整页瘫痪；A02 未被用来宣布全图谱校验通过；
  Q01 为「回归完成，存在已知失败 mapping_forms」；跨账号结论含 D3 例外；
- 单测数量按轮次标注（第一轮 23 → 第二轮 49），不出现自相矛盾的两个数字。

### K14 F05 与未测边界
- `测试计划.md` 的历史「实际执行记录」段开头有**显著失效提示**（旧数字仅供追溯、并链接最新索引），且历史数字**仍在**（不是删掉冒充修复）。
- 报告仍显式保留未测项：视觉/宽度/缩放/截图/画布/纯键盘/断网、MySQL 路径、TTL 30min 等待、发布不可变性破坏路径；
  Q06 性能取消且未恢复、取消来源标注为未独立核实；`mapping_forms` 失败保留未修。

## 5. 不通过的判据（出现任一条即判不通过并给最小复现）

1. 任一"错误响应被判成产品通过"的输入仍记为 `product_pass`（尤其 500 带正确文案、拒绝却新增版本、版本不可读）。
1b. 无关错误/基础设施故障能借**泛词**在同一条消息内凑齐"定位+原因"两组（K06 对抗用例）；或两条**不同**消息各贡献一半被判命中。
1c. 配置类参数类型不符（字符串状态码、字符串诊断词、`0` 版门、真值字符串前置等）未记 `test_error`，或因此把**真拦截判死/把拒绝放行**。
1d. 证据索引对未知 `verdict` 或缺失字段**默认算通过**，或工具错误豁免仍依据 case 名字。
2. 诊断匹配仍可从回显载荷或内层判别值键（`kind/type/status`）命中，或反过来把 `errors[].name` 承载的真拦截判死。
3. 未提供诊断依据时自动放行。
4. 一拆多场景只覆盖一个子用例仍被判定覆盖完整。
5. 索引重算与已提交 `结果索引.md` 或原始 JSONL 不一致（数字或逐行分类）。
6. 文档把已知缺陷、静态核对或未测项计入产品通过，或分级/标题按 S3 要求回退。
7. 发现本轮改动夹带业务代码、既有测试、金样或依赖变更。

## 6. 输出格式与停止点

输出：**逐项 K01–K14 结论**（通过 / 不通过）＋ 每项的命令、退出码与关键输出 ＋ 缺陷清单（含最小复现）＋ 仍存限制 ＋ 明确「是否可交回用户」。
结论用一句话总述（通过 / 有条件通过 / 不通过）。

**停止点**：验收完成后即止。**不合并 main、不 push、不修业务代码、不清理他人环境、不重启主工作台。**
验收结论写入你自己的报告文件或共享交接记录；不要修改 `验收记录.md`、`S1S3独立复验记录_20260921.md` 与实施者的交接条目。

## 7. 已知限制（复验时不应视为新问题）

- 本轮是**针对性复验**，不是全面重测：未重跑 kill -9 注入、未做全端点扫描、未做全量密钥字节扫描、未做浏览器复测（沿用既有 dist，未重新构建）。
- 业务缺陷（A01、R02、D-Q02-01、D1、D2、D3、R01、Q04-01、`mapping_forms` 共 9 个根因）**本轮未修**，属另行安排范围。
- `18931` 与前轮 `test-data*`、前轮证据均保留未动，**不是本轮应在意的残留**。
- 本指令若与更早的 `独立复验指令_20260921_S1S3.md`（第二版）不一致，**以本指令为准**；两者的 S1–S3 部分口径一致，本指令补充了 F01–F05 与追加加固的完整反例清单。
