# 整表自动填写：开发计划 v1

日期：2026-09-22。状态：待实施，所有下列任务尚未开始；不得将现有辅助填写开发状态记作本次改版完成。

## 1. 基线与顺序

需求唯一新基准：本目录《需求说明.md》；交互参考同目录两页HTML。旧辅助填写能力可复用，新需求覆盖旧版逐卡勾选交互。

已核对现有开发环境：

| 项目 | 记录/实际观察 | 执行前要求 |
|---|---|---|
| 任务 | assist-fill-production，原owner zcode | 同一需求继续使用，先确认没有其他执行者写入 |
| 分支/cwd | codex/assist-fill-production；主仓库/worktree/assist-fill-production | 所有业务命令在此cwd，禁止main实施 |
| 当前HEAD | 544f6c8；检查时工作树干净 | 重核SHA与后续提交；记录新改版起点 |
| 登记原base | 039a4f0 | 历史起点，不要求倒回此版本 |
| 端口 | 登记18951 | 本轮未验证运行/占用，不声称服务可用 |
| 数据根 | 旧登记 `<worktree>/.runtime/assist-data` | 与最新AGENTS要求不同，按§6核对迁移，不自行覆盖已有数据 |
| 登记交付SHA | 4c9e5fb | 与HEAD不同；按实际提交取证，不把登记SHA当最新 |
| Python | 登记system python3 3.9.6 | 实际确认；依赖变更用任务独立环境 |
| 创建/集成 | 同任务worktree已存在 | 无需新建；不隐含合并main或主服务更新 |

主仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。

先做协议与字段迁移清单，再并行实现：不能只加“生成后调用adopt”。现有后端注册表、前端适配白名单、UI枚举必须收敛到同一契约，保留必要codec。

## 2. 技术落地与迁移决策

1. 建议 `contracts/forms/*.json` 为开发期单一表单定义；T0确认目录后冻结。后端装载，前端生成/消费元数据，schema版本与摘要可重现；生成物只由命令生成，不手改。接口文档描述结构、错误码、兼容方案。
2. 普通字段的读取/写入/标签/枚举使用契约与声明式映射。JSON-LD、类型清理、Redis编码、参数列表采用已有业务setter/受限codec，不通用深路径赋值。
3. 抽取 `useFormAutofill` 类公共宿主逻辑，复用请求取消、指纹、快照。保留无AI表单的原始保存链路。
4. fill协议明确版本，输出受限operation；旧check/explain继续使用或明确兼容路由。T0登记接口01/04/05/README，再改服务和客户端。不能让旧客户端收到新响应却按suggestions处理。
5. 补答会改变draft，原contextToken绑定旧draft：每次续填必须重新取得当前上下文及token，携带稳定sessionId/roundId/questionId，回答只作用于仍有效的问题。两轮补问与手改后继续必测。
6. 统一时间序列规范表示。当前字段注册枚举与适配器的timeSeries表达需T0核对并明确转换，禁止在提示词里解释两套不一致编码蒙混过关。
7. 在草稿副本上计算依赖清理和校验，业务setter的副作用须包含在修改摘要中。失败不留下半组；未知字段和历史扩展保留。相同字段多条冲突操作拒绝并重试/补问，不后项覆盖前项。
8. 模型不负责变更权限或持久化；返回操作只有经过后端验证、前端版本核验与业务预检才可应用。初次生成不要求成功保存整个表单；已有无关必填缺失不阻断独立合法填写组。
9. 不新增数据库业务表，提示词/草稿会话默认内存，短期token沿用或升级现有机制；如实现认为必须迁移数据库，先列原因与兼容方案再决定，不静默增加。

## 3. 任务拆分与并行归属

下面均为待开始。协调者派发时填实际owner、分支SHA、实际测试命令与状态；角色名不是实际完成记录。

| ID | 目标与范围 | 前置/输入 | 文件归属及交付物 | 验收与验证命令 | Owner | 并行组 |
|---|---|---|---|---|---|---|
| T0 | 11场景真实字段、权限、语义、旧协议迁移冻结 | 需求A01-A18、当前代码 | 接口01/04/05/README；本计划补充字段/codec矩阵、限额和兼容表 | 枚举、必填、可空、clear、source.kind、question/token统一；逐表核对 | 协调者 | 串行 |
| T1 | 单一契约及生成/加载/一致性检查 | T0 | contracts/forms；生成脚本；后端契约loader；生成TS文件 | 增删字段/枚举能检出漂移；产物幂等；`python3 tests/test_autofill_contracts.py`（计划新增） | 契约owner | G1 |
| T2 | 操作协议、patch验证与模型输出约束 | T0 | assist_schema及受限operation模块、专属测试 | 非法路径/类型/重复操作/清空/依据校验；`python3 tests/test_autofill_patch.py`（计划新增） | 后端A | G1 |
| T3 | 默认表单、按需侧栏与自动填入状态机 | T0 | AssistPanel、useAssistPanel或新状态模块；前端单测 | 打开/关闭/迟到/无变化/撤销，使用冻结协议mock；`node --import ./tests/ts_hooks.mjs --test tests/test_autofill_state.mjs`（计划新增） | 前端A | G1 |
| T4 | 上下文及多轮补答、鉴权和接口串接 | T1/T2 | assist_context/service/routes，api客户端；公共路由由协调者写 | token轮换、问题归属、跨用户目标、无业务写入；`python3 tests/test_autofill_http.py`（计划新增） | 后端B | G2 |
| T5 | 本体对象/链接/规则/动作薄适配 | T1/T3 | ontologyBindings/workflowBindings及ObjectWorkspace/规则/动作组件 | O1/O3/O4/O5，普通字段不再重复白名单；A04/A10 | 本体A | G2 |
| T6 | 属性/共享属性薄适配及复杂类型 | T1/T3 | propertyBinding/PropertyManager；必要格式适配 | O2类型→观测→格式顺序；共享影响与只读；A03/A08/A12 | 本体B | G2 |
| T7 | 数据库/Redis/编排来源适配 | T1/T3/T4 | propertySourceBinding/PropertySources | P2/P3/P4，连接/表/字段所属验证；A05/A06/A08/A13 | 项目前端A | G3 |
| T8 | 实例识别/链接/动作接口适配 | T1/T3/T4 | identityLinkBindings/actionBindingAdapter；相应页面 | P1/P5/P6；行ID/未知字段保留、不执行接口 | 项目前端B | G3 |
| T9 | 全场景集成与模型桩对抗 | T4-T8 | 专属测试及合成fixtures，不改生产数据 | A01-A17、旧D1-D7语义、非储能实例；`python3 tests/run.py all` + 本期前端套件 | 测试owner | G4 |
| T10 | 浏览器及分支交付 | T9 | 测试记录、实际SHA/URL、计划状态表 | A18三档、console、异步乱序、保存revision；`cd frontend && npm run build` | 协调者 | 串行 |

计划新增命令不是已存在入口；实施者需实际建立并验证，若仓库测试组织不同，T0记录等价实际路径/命令。交付时不能保留不存在的命令。

契约和接口文档由唯一owner维护。共同修改types、App.vue、server.py、依赖锁文件、注册索引、context摘要时先向协调者提交变更请求，不多人并写。实际并行agent数按工具能力最大化利用；强依赖保持串行。同目录安装、构建、Git操作串行；每个测试进程独立临时库和端口。

## 4. 强制回归证据

- 逐场景：页面入口、输入要求、当前值、实际字段变化、未变字段、保存前后revision。不得仅截图按钮。
- 自动填入后等待超过既有自动保存窗口，服务端数据不变；确认共享影响取消/409都不写入。
- 两轮补答：先填独立字段→补答→重取context→再填；期间手改应保护用户修改，不能重复应用旧计划。
- 字段契约演进：临时测试fixture加普通字段/新枚举，改布局不改引擎；删字段/改类型使旧响应拒绝；普通字段契约缺适配的构建校验应失败。
- 复杂类型：时间序列与数值互切、格式清理、编排输出类型、依赖连接/表字段、参数列表局部更新与未知行保留。
- 提供方异常/模型无效响应/提示注入/假evidence/伪造token/权限边界。只声明真正测到的安全结果。
- 模型桩完整协议联调必做；真实提供方用合成资料按已有授权测试，报告是否真实调用。复制了main配置不等于允许把用户业务数据自动发送模型。
- 浏览器实际操作三档视口；运行时console/异常必须真实采集，未采集记未知。被策略阻塞不绕过，保留未覆盖项待独立验收。

## 5. 验收与停止点

开发者提交后交独立验收，报告实际分支HEAD及本次改版前后差异。原分支中旧辅助填写和main合入内容不能自动视为通过，组合测试覆盖其受影响范围。

失败回同一worktree修复，报告新SHA；旧验收结论不覆盖新增提交。通过停在待用户授权集成，不自行合并/推送/重启main。协议已升级但页面未全部接入不算完成，不能只交两页示例作为11场景实现。

## 6. 环境、数据与完整授权约定

### 标准提示词与停止点

- “在整表自动填写已登记的assist-fill-production worktree中，按本目录执行指令开发、自测并提交，不合并main。”授权已有环境实施，无需重复创建。
- 如已有环境不存在：“为整表自动填写创建worktree，从最新已提交main建分支与目录，登记环境，先不要开发。”只授权创建；也可明确“创建并开发”合并两个阶段。
- “验收该分支最新提交，不合并main。”独立验收。
- “将验收通过的指定提交集成并合并main，普通冲突自行处理并重验；不重启主工作台。”允许临时集成worktree及串行集成，停止于已合并未更新主服务。
- “将已合并main构建并更新主工作台；真实数据库迁移先说明操作。”单独授权运行更新。

本文件只是执行说明，不是自动创建/合并指令。已获同任务授权的环境可持续复用。

### 工作树与资源

已有开发cwd：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/assist-fill-production`。创建其他必要工作树仍需明确授权。新树从最新已提交本地main派生并登记base SHA，不盲目pull、不复制未提交代码，路径必须在主仓库/worktree/下，禁止覆盖目录。主仓库只作协调，不在其直接改业务代码。

资源登记使用git-common-dir/workbench-tasks并加同一跨进程锁，协调者独占Git、安装和启动操作。已有18951先核PID/cwd和归属；不得抢18765、8765或他人端口。显式指定WIZ_WORKBENCH_PORT、WIZ_WORKBENCH_ROOT、独立依赖；核对WIZ_DATABASE_URL实际指向，防止继承main数据库覆盖项。

### 最新数据规则与旧环境迁移

最新AGENTS要求新建开发/临时集成树复制main完整配置：WIZ_WORKBENCH_ROOT设工作树根，使用main有效数据根运行 `python3 -m workbench.storage.transfer backup --output <worktree>/data/workbench.sqlite3` 生成WAL一致快照，禁止cp活库；复制root key与运行资产（如data/ontology-build-blobs），设key目录0700/文件0600。不得输出密钥、入Git、复制到不受控位置。main行为一致指可用配置，不保证外部系统状态相同。

现有登记是旧`.runtime/assist-data`模式：先核实真实运行根、数据库、资产和新编辑数据，制作可恢复备份，再在本任务环境内迁入工作树根并更新启动命令及登记；保留旧数据直到确认迁移完整，禁止用main快照覆盖已有任务数据。真实重加密、覆盖或无法无损合并的冲突须报告并等待用户决定。此步骤不得停止main或他人服务。

有真实数据/密钥副本的树登记“含真实数据，不可自动丢弃”。自动化回归必须另用空库/合成数据的临时根，不在用户数据副本上跑破坏性测试；默认不运行真实业务接口。浏览器隔离上下文，前端dist由分支后端托管，防Vite代理误连main。

### 集成、回退、清理

集成需用户明确授权与准确提交的独立验收依据；单一集成负责人串行处理队列。获授权后从最新main建立临时集成树，按最新数据规则配置，合入已验SHA，普通冲突按双方契约解决，业务冲突交用户选择，重跑组合回归/构建/适用浏览器用例。main脏则等待，不stash/reset；main前进则重新组合。基线一致且干净才能ff更新main。

代码合入不等于主服务更新；数据库迁移、远程push、主环境重启不在隐含授权内。代码回退默认revert，数据库与资产另行恢复，不用Git回退冒充数据恢复。

合并授权包含成功后清理可丢弃的任务产物：保留SHA/验收证据/交接后，核对PID/cwd、分支均已合入、无未提交内容，停止本任务服务，释放端口，Git移除工作树及已合并分支。不删除整个worktree父目录。**含真实数据和key副本的开发/集成树不能自动删除**；先请用户明确数据处置并保留，禁止force绕过未跟踪数据保护。未知/未合并/他人资产保留并报告。

最终分别列代码合并SHA、已清理项、保留路径及原因、数据处置和主服务更新状态；未授权合并则报告“未合并、环境保留”。

## 7. 共享交接

开工/实施/验收前按AGENTS在所在树read，并额外核对主树交接；结束record，记录需求版本、实际改动、测试和限制。不直接编辑session_context。不同树日志不按sequence猜绝对先后，合并后render重建。仅提交自己文件，遇他人交接重读，不自动改变范围。

## 8. T0 冻结记录（2026-09-22，zcode 协调者）

状态：已冻结并登记接口文档（04 §6 新增、README 变更行、05 速查 53b 备注）。以下为实施基准；与需求冲突时以需求说明为准并先改文档再改码。

### 8.1 协议要点（详见 04 §6）

- fill 请求必带 `protocol:2`；响应 `protocol:"autofill/1"`；check/explain 兼容不变。
- operations 五种：set／clear／row.append／row.update／row.remove；field 为契约内点路径；rowId 本地稳定标识；basis 程序核验（intent 引文或本会话 questionId）。
- 同字段冲突→整组转 unresolved（不后项覆盖）；原子组不完整→整组 unresolved；引用失效→单条 unresolved；整体结构违规 502（既有分层）。
- 会话内存态：TTL 30min、LRU 200/进程，绑定 user+target+formId+schemaDigest；questionId 归属 session+round，跨轮答复 400。
- 限额冻结：operations≤12、unresolved≤12，其余沿用 §5（intent 4000/answers 8×2000/questions 3/draft 200KB/max_tokens 4000）。
- 续轮握手：应用操作→重取 contextToken→generate 带 sessionId+answers+draft。

### 8.2 契约与模块归属（单一 owner）

| 文件/模块 | Owner | 说明 |
|---|---|---|
| `contracts/forms/*.json`（10 个 formId=targetKind） | T1 | 单一来源；propertySource 按 draft.kind 子契约 |
| `workbench/assist_forms.py`（loader+digest+一致性检查） | T1 | loader 重算 SHA-256 |
| `workbench/assist_forms_gen.py`（`python3 -m` 生成器） | T1 | 产出 `frontend/src/assist/formContracts.gen.ts`（生成物入库禁手改） |
| `workbench/assist_ops.py`（operations 校验）+ `assist_schema.py` fill 解析分支 | T2 | parse_model_output 增加 autofill/1 解析；check/explain 路径不动 |
| `frontend/src/assist/formAutofill.ts`（宿主引擎/状态机，新文件）＋ `useAssistPanel.ts`＋`AssistPanel.vue`＋`types.ts` | T3 | 按冻结协议 mock 联调；组件旧勾选交互移除 |
| `workbench/assist_context.py`＋`assist_service.py`＋`assist_routes.py`（会话/多轮/串接） | T4 | server.py 白名单不变，协调者例外修改 |
| `ontologyBindings.ts`＋ObjectWorkspace.vue（对象/链接编辑器）＋规则/动作组件与 `workflowBindings.ts` | T5 | O1/O3/O4/O5 |
| `propertyBinding.ts`＋PropertyManager.vue | T6 | O2；宿主 ObjectWorkspace 若需改由 T5 唯一 owner 代改或协调者处理 |
| `propertySourceBinding.ts`＋PropertySources.vue | T7 | P2/P3/P4 |
| `identityLinkBindings.ts`＋`actionBindingAdapter.ts`＋对应宿主 | T8 | P1/P5/P6 |
| App.vue | 本轮不改 | 入口在各表单页头，不经 App |

### 8.3 实际测试路径（T0 登记，交付不得留占位）

| 测试 | Owner | 命令（仓库根） |
|---|---|---|
| 契约一致性与漂移 | T1 | `python3 tests/test_autofill_contracts.py` |
| 操作校验与模型输出对抗 | T2 | `python3 tests/test_autofill_patch.py` |
| 前端状态机 | T3 | `node --import ./tests/ts_hooks.mjs --test tests/test_autofill_state.mjs` |
| HTTP 会话/多轮/鉴权 | T4 | `python3 tests/test_autofill_http.py` |
| 组件套件更新 | T5-T8 | 各 `tests/assist_*.test.mjs`（旧勾选断言重写为回填断言） |
| 集成对抗 | T9 | `python3 tests/run.py all` + 本期前端套件全量 |
| 交付 | T10 | `cd frontend && npm run build`；三档视口浏览器取证 |

已知既有失败（main 侧债务，非本轮范围）：`tests/flow_test_workspace.test.mjs`、`tests/legacy_graph_bridge.test.mjs`。

### 8.4 环境与数据迁移

- 分支已同步 main（349fa20：ui-fix 26 提交＋本需求文档），vue-tsc/build 通过；本轮开发基线 349fa20。
- 数据根按最新 AGENTS 迁移：`.runtime/assist-data` → 工作树根（`data/workbench.sqlite3`＋`keys/`），先备份旧根再切换并更新启动命令与登记；迁移完成前旧根保留。

## 9. 实施结果（2026-09-22，zcode）

### 9.1 状态与提交

**已实施、自测与浏览器复验完成，待独立验收。** 分支 `codex/assist-fill-production`（起点 349fa20，即 main 578acd7 合入后 + 本需求文档），本次改版 12 笔提交：

| SHA | 内容 |
|---|---|
| 60037b8 | T0 协议冻结（04 §6 autofill/1、README、05、本计划 §8） |
| 34ba83a | T1 契约单一来源：contracts/forms/*.json（10 表单）+ assist_forms loader + 生成器 + 契约守护 423 项 |
| e5b2ab5 | T2 受限 operations 校验器 assist_ops + parse_fill_output（问题 id 服务端换发）；40 项 |
| 78dfc38 | T3 前端引擎 formAutofill + AssistPanel 420px 抽屉重写；状态机 68 项 |
| 1e303dc | T4 后端串接：进程内会话（TTL30m/LRU200）+ 契约 digest 入令牌 + 引用核验；HTTP 13 组 |
| b0442d2 | T5 本体四场景（对象/链接/规则/动作）接入；14+12 项 |
| 4511f7a | T6 属性接入（dataTypeTransform/formattingCodec + typeCore 原子组）；74 项 |
| f5f5d27 | ui-fix 合入后的 SSR 适配（modalFocus 守卫、connection_manager 测试桩） |
| 34f3b4b | T7 属性来源三类（database/redis/flow）接入 + kind 边界；18 项 |
| 5f0c6e3 | T8 实例/链接/动作接口接入（rowId 局部落行、auth.* 双保险）；16+16 项 |
| 380f29e | T9 对抗集成 + P1 修复（list 契约摘要 KeyError→13 类全通、等值回显转 empty、auth.* 行字段拦截）；12 组 |
| 08d2215 | T10 浏览器三项修复（F1 焦点、F2 关闭保留输入、F3 empty 带 unresolved 如实展示） |

### 9.2 验证证据

- 后端：`tests/run.py unit` 52/52、`quick` 3/3、`http` 11/11；autofill 专项 4 套件（contracts 423 断言 / patch 40 项 / http 13 组 / integration 12 组）全过。
- 前端：47 套件 45 过（`flow_test_workspace`、`legacy_graph_bridge` 为 main 既有债务，本分支未引入）；`vue-tsc` 0 错；`npm run build` 过。
- 浏览器（headless Chrome + CDP 真实事件，assist_dev 账号 + autofill/1 桩）：A01/A03/A09/A10/A11/A18 + console 0 error 通过；F1/F2/F3 修复后复验全过（含 A01 未被破坏、二次宿主抽查）。
- 真实模型（GLM-5.3-Flash，合成数据）：对象表单 2/2 字段、basis 逐字合规、14.7s；属性表单 timeSeries 原子组成对给出（15.6s），信息不足时正确转为提问/待补，未出现半组或契约外值。6 次调用全部 200。

### 9.3 已知限制与未覆盖

- **项目区 connection/table/result.valueField 真实回填未验**：本机 MySQL 3306 不可达 + `bindings.catalogs` 为空 → 后端 fail-closed 转 unresolved（正确行为）；该组字段的目录候选链路需可用目录才能端到端验证。
- 真实模型未触达后端兜底分支（半组/冲突整组拒绝）——真实 GLM 自我约束强，这些防线由模型桩覆盖（T9 集成套件）。
- 未覆盖：`row.update/row.remove` 行操作浏览器路径、redis/flow 变体浏览器、check/explain 次要入口、`MODEL_NOT_CONFIGURED`/`MODEL_TIMEOUT`/`CONTEXT_STALE` 浏览器呈现、会话 TTL/LRU 实测。
- `context.editableFields` 的 dataType 枚举（xsd:*）与契约枚举（业务名 + timeSeries）形态并存（低影响，前端暂只用其展示名）——记为债务。
- glm 单次 15–52s 波动；提供方 timeout 已在 18951 配 300s。

### 9.4 环境

- 数据根已按最新 AGENTS 迁到工作树根：`data/workbench.sqlite3` + `keys/wb-root.key`（0700/0600）；旧根 `.runtime/assist-data` 与迁移前快照 `.runtime/assist-data-backup-20260922/` 保留未删。**含真实数据副本，不可自动丢弃。**
- 18951 运行中（分支 dist 托管）；assist_dev 默认提供方指向本机 autofill/1 桩（18933），admin 指向真实 GLM。
- 验收合成的对象 `mg:t10_accept_obj` 已从草稿移除；项目区夹具已还原。
