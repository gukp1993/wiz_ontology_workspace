# zcode执行指令：本体与项目辅助填写正式实现

日期：2026-09-21。此文件用于正式功能开发，不是再次整改离线原型。

## 1. 开工必读

主仓库：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`。

依次阅读根AGENTS.md、本目录《需求说明.md》§1～8、《开发计划_正式实现.md》全文、《交互原型_v1.html》、《复验报告_20260921_F01F02.md》及现行接口文档。读取共享上下文，核对需求是否已更新。

原型仅是交互参考，不拷贝其SAVED/BUFFERS、固定候选和预置响应到生产。原型哈希4fd7539fd00fcf3984bd1bd07b9b8bbb2288fd39e97f13a792ebaf962eafb735；不能把开发方自测说成独立验收全部通过。

## 2. 必须交付的能力

在真实本体与项目编辑表单接入统一辅助面板，覆盖O1～O5、P1～P6；三种用途为帮我填写、检查当前内容、解释怎么填。复用现有默认模型配置和通用模型调用层，真实生成结构化建议。

- 业务含义不清先问；答案按questionId结构化传输，分别确认参数，不要求按斜杠拼接。
- 来源目录/对象/编排从服务端按权限取，不能相信前端任意候选或模型幻觉ID；项目只用固定引用版本。
- 模型仅提供受限字段建议。后端白名单、类型、引用及依赖校验；前端原子采纳再检查；正常保存继续走原业务校验。
- 原值来自真实当前draft；空值可默认勾选，已有值不默认替换，相同值无需变更。
- 采纳不触发changed/touch自动保存，不直接操作全局state或数据库；必须验证超过900ms后仍未持久化。用户显式保存才落盘，取消恢复，保留共享影响确认和CAS。
- 缺信息/不兼容待补充或阻断，不伪造已支持的运行时绑定、业务唯一性、SOC比例和参数等价关系。
- 目标、版本、参考目录变化与表单手改令旧建议失效；取消/关闭生成时丢弃迟到结果；撤销不得覆盖后续手改。
- 解释无patch。所有文本安全渲染。凭据明文、认证头不入上下文，llm_client原始trace不返前端、不写日志。
- 无模型、超时、无效响应、空建议有不同提示与恢复路径；不错误200假成功，不长时间持写锁。

不增加AI菜单、聊天历史、自动执行、自动建编排、动作本体参数、MCP口径或延期页面改版。

## 3. 派发与实施

使用开发计划T0～T12任务表，开工先把负责人、负责文件、依赖、状态落实。T0核对实际字段/权限/保存事件，并先更新接口文档01/04/05/README，冻结协议与上限，再实施API和前端。

T1上下文、T2输出验证、T3公共面板可在协议稳定后并行；后续按任务表依赖分批。每个文件单owner，共用App.vue/server.py/公共类型/接口文档由协调者统一修改。允许已有授权worktree内多agent，不自动创建额外worktree；工具无多agent则串行。依赖安装、构建、暂存提交由协调者串行完成。

实现采用当前项目模块边界，建议新assist模块，避免把11场景全部堆进App.vue。模型适配复用公共chat能力，不借用编排执行器。具体新增路径和接口命名在T0冻结，不得把计划中的建议当成现存能力。

每一批完成报告实际文件、验证和剩余问题，不能以子agent完成代替组合验收。更新任务表实际新增测试文件及可执行命令，不留下占位命令作为完成证据。

## 4. 必测与交付

按开发计划§6完整执行：
- D1～D7、A01～A17，11场景各正常和缺信息路径；增加非储能对象与不同字段，防硬编码。
- 模型桩覆盖非法JSON/截断/越界字段/不存在引用/类型不兼容/暂不确定/失败与超时。
- HTTP层鉴权、跨用户/项目/contextToken复用、引用过期、无业务写入，密钥不泄漏。
- 采纳/取消/撤销/显式保存/409/共享确认/自动保存窗口保护。
- 三档视口与console、XSS、异步竞态。浏览器策略阻塞不能绕过，标未覆盖。
- `npm run build`、本轮新增与受影响回归通过；默认合成数据和模型桩，真实提供方联调需授权配置，不得用真实业务资料试验。

提交时只包含本任务文件。交付实际分支、SHA、环境URL、数据根、构建/测试/浏览器证据、已覆盖与未覆盖、真实模型是否联调、合并状态。

交付状态为“已实施、自测完成、待独立验收”，不得自称Codex验收通过。发现需求矛盾明确报告具体选择，不静默删需求；常规实现问题自行解决。

## 5. 环境与授权停止点（完整约定）


主仓库固定为 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`；开发cwd必须是另行授权并登记的需求worktree，不是主目录。

| 项 | 值 |
|---|---|
| 任务ID | assist-fill-production |
| 执行/集成负责人 | zcode开发；Codex获授权后集成 |
| 需求版本 | 本计划2026-09-21 + 原型需求§1～8 |
| 分支/实际worktree/base SHA | 待授权创建后登记，建议codex/assist-fill-production及主仓库/worktree/assist-fill-production |
| 端口/数据根/数据库URL/Python环境/运行命令 | 待实际分配、核验并登记，不预填可用结论 |
| 状态 | 未创建环境、未开发；不是已授权集成 |

标准提示词与停止点：
1. “为本体与项目辅助填写正式实现创建worktree，从最新已提交main建立独立分支及目录，登记端口和数据目录，先不要开发。”只授权创建。
2. “在已登记worktree按执行指令_正式实现.md开发、自测并提交，不合并main。”只授权该环境开发。
3. 可一次明确“创建worktree并按指令开发、自测提交，不合并main”。普通“开发/验收”不替代未给出的创建授权。
4. “验收本需求最新提交，不合并main。”独立验收；通过停在待用户授权集成。
5. “将本需求验收通过提交集成并合并main，处理冲突并重验，成功后清理对应环境；不重启主工作台。”授权串行集成及正常清理。
6. “将已合并main构建并更新主工作台；真实数据库迁移先说明操作。”单独授权运行更新。

创建前查git status/worktree list/main与remote；从最新已提交本地main派生，不复制未提交内容，不盲目pull。worktree置于主仓库根worktree/，确认已忽略；分支已存在先核归属，禁止覆盖。共享登记使用git-common-dir/workbench-tasks并加锁。

分支使用自己的依赖、.runtime、dist，独立Python环境；所有命令显式cwd。独立空闲端口禁18765/8765，显式WIZ_WORKBENCH_PORT和WIZ_WORKBENCH_ROOT；清理或覆盖继承WIZ_DATABASE_URL，确认实际数据库独立，凭据/附件/密钥也隔离。只用合成数据，不复制真实数据。浏览器独立上下文，优先分支后端托管dist，防dev代理误连18765。

分支验证后报告SHA、URL、数据根和实际状态。不合并、不更新主服务。集成由获授权的单一负责人串行排队，最新main建临时集成worktree，合入准确已验SHA，处理普通冲突并重跑组合检查。需求矛盾交用户决定；main脏或前进不得强行覆盖，排队或重新组合。仅已验证组合且main基线不变、工作树干净才ff更新。

代码与数据回退分开，代码优先revert，不认为回退Git能回退数据库。真实迁移、远程push不在开发/合并隐含授权内。本功能预期无需数据库迁移，若需要先说明原因。

授权合并成功后保存证据与交接，核实分支全部合入、无未提交和需保留数据，再停止本任务服务，Git移除对应开发/临时集成worktree及已合并分支，清理登记可丢弃的依赖、数据与端口；禁止删worktree父目录或他人环境。异常/归属不明则保留报告，不强删。主服务更新另行授权。


## 6. 共享交接

每轮按AGENTS用context.py read；分支实施/验收前复读并核对主目录最新交接。结束用record记录需求版本、实际提交与证据；不手改session_context.md。没有明确创建授权且无已登记环境时，只做只读开工核对，不在main实施业务代码。已有同任务授权环境可复用，无需重复申请。
