# 本体与项目辅助填写 F01/F02 复验报告

日期：2026-09-21。复验方：Codex。

## 1. 结论

**总判定：部分覆盖。已独立复跑的检查通过；他方补验的关键原始证据与修复目标一致，本轮没有发现需要继续修改原型的新缺陷。**

F01 的位置解析已修复，F02 的文案已修正。不建议再下达笼统整改任务。本轮不能签署“Codex 已完成全部浏览器独立验收”：同一原型的浏览器访问策略禁令未解除，没有通过其他浏览器、CDP或复制路径绕过。

本报告区分两种证据：① 本轮亲自执行的静态与纯函数测试；② 读取其他 harness 留下的浏览器原始记录。第二类仅为证据复核，不冒充本轮真实点击，也不将开发方自测直接转换为独立通过结论。

## 2. 基线

- 被验修复提交：`4c2dd9e`，当前 HEAD：`388d8aa`。
- 原型：`交互原型_v1.html`，2078 行。
- SHA256：`4fd7539fd00fcf3984bd1bd07b9b8bbb2288fd39e97f13a792ebaf962eafb735`。
- 当前原型与被验提交逐字节一致；最新补验未修改原型。
- 他方 VA、VB、VC、VD、VE 五份 `proto.html` 的哈希均与该基线一致。
- 修复提交仅修改原型与需求说明；需求说明纯追加。
- 无关未跟踪文档未操作。

## 3. V1～V9 判定

| 项目 | 本轮独立判定 | 实际检查及他方证据复核 |
|---|---|---|
| V1 交付完整性 | 通过 | 核对哈希、行数、提交文件一致性、需求纯追加；五份他方测试副本同哈希 |
| V2 静态检查 | 通过 | 17项禁用词及“标注归属”均0；唯一内联脚本 `node --check` 退出0；3处https为示例/占位文案，无外部资源属性与CSS url |
| V3 解析函数 | 通过 | 从当前HTML提取原函数，独立运行21例全部通过，未使用他方测试结果代替 |
| V4 浏览器输入矩阵 | 未覆盖 | 本轮未点击；他方VA记录17例成功，反例 `/ SOC` 全页“固定值 SOC”仅出现在attr_name卡，model_name卡不存在该值 |
| V5 采纳与原值 | 未覆盖 | 本轮未点击；核对VB原始diff：单项采纳只改对应bind/bindv与内部提示标志，SAVED无变化；已有model_name属性绑定保留 |
| V6 保存与取消 | 未覆盖 | 本轮未点击；他方提供V6/V7状态记录，不能替代本轮保存、取消实测 |
| V7 文案与行为 | 未覆盖 | 静态文案符合；他方记录单token两项禁选、等号输入禁选；实际页面联动未由本轮执行 |
| V8 保留项 | 未覆盖 | 核对他方关面板证据：生成中关闭、等待4.5秒、重开后0张卡，状态idle，迟到结果忽略提示存在；其他保留项仍属他方执行 |
| V9 XSS与console | 未覆盖 | 核对他方注入记录：img/svg增量0、注入变量undefined；本轮未进行浏览器注入或日志采集 |

## 4. 关键值证据

### 4.1 本轮独立解析结果

- `/ SOC` → `{state:'two', mn:'', an:'SOC'}`。
- `my_model /` → `{state:'two', mn:'my_model', an:''}`。
- `my_model`、`my_model SOC` → `one`，不按空格拆分。
- `a / b / c` → `many`，count=3、seps=2。
- `a // b` → `many`、consec=true；连续分隔符不合并。
- `a=b`、`a＝b`、`a=b / c` → `kv`。
- 空白、两侧为空、中文分隔符、Tab/全角空格/换行包裹输入、片段内空白均符合指定结果。

### 4.2 他方原始证据抽核（非本轮浏览器实测）

| 场景 | 原始可观察值 | 文件 |
|---|---|---|
| `/ SOC` 不错归属 | totalOccurrencesInBody=1；appearsInModelNameCard=false；appearsInAttrNameCard=true | `va/evidence/counterexample_case3.json` |
| 仅采纳attr_name | 变化仅 `bind.attr_name: ''→const`、`bindv.attr_name: SOC`、`__filledTip`；savedDiffs=[]；延迟后结果相同 | `vb/evidence/V5.1_diff.json` |
| 已有attr=SOC，只补model_name | 变化仅model_name对应两项和提示标志；DOM中attr仍const/SOC | `vb/evidence/V5.2_diff.json` |
| 已有簇编号绑定不被覆盖 | DOM中model_name仍attr/cluster_code；仅attr_name两项变化；savedDiffs=[] | `vb/evidence/V5.3_diff.json` |
| 生成中关闭 | 重开sugCardCount=0；state=idle；“面板已关闭时取消了生成；迟到结果将被忽略。” | `vd/evidence/V8-2_close_during_generate.json` |

他方证据根：`/tmp/kg_f01f02_accept_20260921/`。

## 5. 问题与处理方案

### 本轮新增产品缺陷

未发现新的P0/P1/P2。此结论限于独立检查和证据抽核范围，不等于未覆盖链路不存在缺陷。

### F01/F02处置

| 原问题 | 当前判断 | 是否继续修代码 |
|---|---|---|
| F01丢失空项位置、把SOC归为model_name | 独立纯函数反例已通过，他方采纳diff与预期一致 | 不需要基于当前证据继续修改 |
| F02单token提示缺失、宣称支持等号归属 | 静态修复确认；指定句在attr_name pending中，无须两卡重复 | 不需要继续修改 |

### 验收材料注意事项（不是产品缺陷）

1. VB目录保留了一份 `V5.1_error.json`，内容是验收脚本的 `ReferenceError: checkedDefaults is not defined`，发生在/tmp脚本，不是原型浏览器异常。后续run_log、results_summary及前后快照已有成功结果。交付方宜明确标记失败尝试与最终重跑记录，避免把脚本错误算作产品错误或静默隐藏。
2. VA/VC的Log探针未证明通道有效，不能把全套Log统计统一宣称为已验证0；沿用交接中的“未知”说明。VE有独立通道探针记录，不代表其他组所有时段都已获证。
3. 证据位于/tmp，可能被清理；正式验收交付前应将必要的脱敏操作步骤、字段差异、日志口径及对应哈希归档，不能只保留“通过”汇总。

### 剩余闭环方案

若要求严格满足“独立验收方亲自点击”的标准，应由未参与本轮修复且获允许访问原型的验收环境，按现有执行指令完成V4～V9；不需要另改需求或继续修原型。发现实际缺陷后再按复现步骤最小修复并复验。该步骤是补验，不是代码整改。

既有P2（未保存提示、改答路径、默认勾选体验等）保持原登记，本轮没有授权重做交互，不重新包装成整改阻断项。

## 6. 日志与限制

本轮console error/warning、未捕获异常、Log、原生弹窗：均未知（未启动浏览器采集）。他方记录的0属于他方测试结果，不登记为Codex实测0。

未覆盖：本轮真实浏览器点击、XSS运行时、视口布局、多浏览器兼容、完整无障碍走查。

本轮独立脚本及结果：
- `/tmp/codex_f01f02_review_20260921/check.py`
- `/tmp/codex_f01f02_review_20260921/parse_test.js`
- `/tmp/codex_f01f02_review_20260921/parse_results.json`
- `/tmp/codex_f01f02_review_20260921/static_results.json`

仅按用户请求新增此Markdown报告，并按协作约定记录交接；未改原型/需求正文/业务文件，未连接业务系统、未启动或重启服务、未合并。
