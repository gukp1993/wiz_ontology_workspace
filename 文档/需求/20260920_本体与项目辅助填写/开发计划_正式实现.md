# 本体与项目辅助填写：正式实现开发计划

日期：2026-09-21。执行者：zcode；独立验收：Codex或用户指定的未参与修复者。
状态：计划交付，未实施。用户本轮要求从原型进入正式开发安排；创建开发worktree和合并仍分别按明确授权执行。

## 1. 范围与基准

交互与字段基准为同目录《需求说明.md》§1～8和《交互原型_v1.html》。原型修复提交4c2dd9e，2078行，SHA256 `4fd7539fd00fcf3984bd1bd07b9b8bbb2288fd39e97f13a792ebaf962eafb735`。开发方浏览器补验已有记录，Codex独立检查仍为部分覆盖；不得称原型已获完整独立通过。

**正式实现相对于原型的必要变化**：用真实配置的模型生成建议，移除生产页面的演示控制和硬编码储能响应，使用真实表单缓冲区、当前用户可访问的定义及目录。原型的SAVED/BUFFERS、固定SOC标识、parseConstPair不是生产协议；正式补充问题的答案按questionId和字段分别传输，不让用户必须用斜杠拼两个参数。

原型需求中的“不接真实LLM、不改业务代码”适用于原型制作；本计划在用户本轮正式开发要求下改为实现辅助填写服务。除此以外不扩大业务范围。测试默认模型桩，真实模型联调只使用授权配置与合成资料。

### 本期覆盖

| 分组 | 接入位置 | 建议范围与约束 |
|---|---|---|
| O1 | 对象编辑 | 名称、业务定义；不自动创建层级 |
| O2 | 私有属性、共享属性编辑 | 定义、数据类型、观测类型、现有格式化结构；沿用共享修改影响确认 |
| O3 | 链接编辑 | 现有两端对象、正反向名、数量关系、定义；须有用户依据 |
| O4 | 业务规则 | 名称、定义、选填规则内容；不添加输出结果字段 |
| O5 | 动作定义 | 名称、定义、选填预期效果；不新增动作参数或提交条件 |
| P1 | 实例识别 | 单textarea说明、已有表与身份字段候选；支持登记实例说明 |
| P2 | 数据库字段取值 | 说明、现有目录字段与匹配建议，不主动刷新目录或查询业务记录 |
| P3 | Redis取值 | 用户提供的命令、Key模板、已确认参数来源；不猜Key或比例 |
| P4 | 已有编排绑定 | 说明、已有兼容编排及输入绑定；运行时输入若现有表单不支持则待配置 |
| P5 | 链接映射 | 说明与有依据的两端字段，不用同名推断等价 |
| P6 | 动作接口映射 | 用户提供的方法、完整URL和现有接口参数映射；不执行请求，不生成凭据 |

同一面板支持“帮我填写／检查当前内容／解释怎么填”。生产不能把11个固定样例当成覆盖；必须测试不同对象名、字段名、项目和空配置。

不做：自动执行SQL/Python/动作、业务数据查询、MCP口径生成、自动新建编排、知识库、聊天历史、新AI菜单、模型管理重做、延期本体管理/发布页改版。

## 2. 已核对基础与职责

- 后端 `workbench/llm_client.py` 已有OpenAI兼容chat公共调用、超时和响应限制；应复用通用调用，不借用函数编排执行流程。
- 该公共层trace包含请求/响应摘要。辅助填写不可把其原始trace返回、持久化或写日志；只返回耗时、请求标识等允许元数据。
- `frontend/src/app/http.ts` 是统一请求出口；新增辅助api模块必须通过它。
- `PropertyManager.vue` 已有局部draft和共享影响确认；`PropertySources.vue` 已有表单保存流程。采纳只改各表单draft，不emit触发自动保存的changed，不调用commit-now/form-save。
- 路由权限、模型配置可见性、项目固定引用、字段编码以开工时实现及接口文档为准，T0逐一核对。路径名不是接口契约，不能照搬原型结构。

## 3. 生产交互与生命周期

1. 表单页头一处辅助入口，沿用现有风格。上下文标明本体草稿／项目固定引用版本、对象和字段；可折叠查看本次参考信息。
2. 默认使用当前已有默认模型；未配置时保留输入，提示前往模型设置。无需新增提供方选择管理菜单。
3. 发送前以字段白名单整理最小上下文：当前编辑表单、必要业务定义、已有目录元数据、候选编排签名及用户说明。提示所选内容将发送给配置的模型。业务记录、密码、API Key、认证头、凭据明文永不进入prompt。
4. 缺信息提出1～3个问题，支持分别回答与“暂不确定”。互不依赖的文本建议可先采纳，依赖未确认信息的项待补充、禁选。
5. 每项显示字段、真实旧值、建议值和来源。旧值由当前本地draft读取，不信任模型返回的old。空字段有效建议默认勾选；真实旧值替换默认不勾；等值无需变更。旧值是否为空用类型/值判断，不靠“未/空”等中文前缀。
6. 类型和观测类型作为原子建议组：数值转时间序列必须配合法观测类型；改离时间序列必须同步清空。来源绑定中相互依赖的字段也按原子组处理，禁止半组落入非法结构。
7. 填入所选建议先在draft副本合并并校验，再一次更新；待补充、禁选、未知字段不能修改。只改勾选组，保留其他值。显示“已填入表单，尚未保存”。
8. 保存沿用原表单校验、共享影响确认、CAS与权限；取消恢复已保存内容；撤销仅在采纳后未再手改时可用。刷新不得把未保存建议写回。
9. 目标切换、版本切换、表单手改、参考目录/编排/提供方变化使建议过期；再生成。关闭生成中的面板取消本地请求并作废迟到结果；已完成结果关闭后可重开，前提是目标和指纹仍一致。
10. 请求失败保留输入和表单，可重试；取消只承诺不采纳结果，不声称上游模型已经停止或不会计费。本期非流式，沿用调用超时上限；网络等待不得持全局写锁。
11. 解释模式只生成帮助信息，无可执行patch；检查模式定位具体字段。所有用户/模型文本按文本或受限安全渲染，不用v-html直接插入。

## 4. 协议与安全校验设计（T0先落实接口文档）

建议新增只读语义的 `POST /api/assist-context`、`POST /api/assist-generate`；路径是本计划建议，T0统一确认后登记到接口文档、速查表和server白名单，不代表当前已实现。

- context请求：space、当前本体/项目id、targetKind、targetId或新建对象的父级、用途、允许的编辑快照。后端按当前用户可见范围重取权威定义/项目引用/目录/签名，提供经过裁剪的只读上下文及contextToken。不要接受客户端任意整个项目对象作为可信参考。
- generate请求：requestId、contextToken、目标标识、mode、编辑快照、意图、answers、editFingerprint。token绑定用户/目标/权威修订和规范化快照摘要，不能允许跨目标或修改候选后复用；实现可采用签名token或有界短期存储，T0选定，不新增业务持久化表。
- response建议结构：requestId、contextFingerprint、questions、suggestions、issues、explanation。suggestion含id、受限fieldKey或原子组、typed proposedValues、ready/pending/blocked、reason、evidenceRefs；不接收任意JSON路径、可执行apply或模型生成的资产ID。
- evidenceRefs只能引用实际发送给模型的候选或用户回答，后端核验存在性和类型；引用文本不能当成执行授权。自然语言语义无法确定时明确“建议，需确认”，不能假称业务口径已验证。
- 后端按场景注册字段及类型白名单，拒绝越界/未知键；技术候选不在当前目录、对象不存在、编排输出不兼容、参数来源未确认时阻断相关建议。客户端采纳再次校验，保存仍走原业务后端校验。
- 不把提示词当唯一保护：模型输出即不可信数据。供应商响应无JSON、截断、未知枚举、过多条目、超大输出应返回明确错误，不变成空成功或部分越界写入。
- T0在接口文档冻结请求体、单字段、候选数、输出条目、token有效期、超时等上限；不得放宽server既有2MB限制。需裁剪时明确候选范围，不能把被裁掉的候选误判为不存在。
- HTTP按现有错误封装：400输入非法，401/403身份或权限失败，404目标不可见/不存在按现有统一策略，409上下文已变需重取，422有效请求但模型配置/建议语义不满足，502模型无效响应，504超时。无建议但有效响应可以200并显式empty状态；禁止错误200+空数据。最终定义以T0先行更新的文档为准。
- 不新增持久聊天、prompt日志、建议缓存资产；服务响应不改任何本体/项目修订。日志只留脱敏错误码、时长和请求id。检查意图/自由文本误粘认证信息并拒绝或脱敏，明确不能保证识别所有用户主动提供的敏感文字。

## 5. 细粒度任务与归属

全表状态均为待开始。实际负责人由zcode派发时填写；角色占位不是完成记录。

| ID | 目标/范围 | 依赖/输入基线 | 交付物与负责文件 | 验收/命令 | Owner | 并行组 |
|---|---|---|---|---|---|---|
| T0 | 核对11场景真实字段、权限、draft事件与协议 | 本计划、现行API、原型 | 本计划补字段映射表；接口04/01/05/README；共享协议冻结 | 每场景可编辑字段/禁止字段/保存路径明确 | 协调者 | 串行 |
| T1 | 上下文构建及脱敏 | T0 | 建议新workbench/assist_context.py；对应后端测试 | 越权、固定引用、目录缺失、快照过期、密钥排除 | 后端A | G1 |
| T2 | 模型输出schema和建议验证 | T0 | 建议新workbench/assist_schema.py；恶意输出fixtures/tests | 未知字段、幻觉ID、类型组合、不完整参数拒绝 | 后端B | G1 |
| T3 | 面板与状态机 | T0 | frontend/src/assist/面板、types、状态逻辑及测试 | 取消/迟到/失效/撤销/原子采纳单测 | 前端A | G1 |
| T4 | API与模型生成串接 | T1/T2 | assist_routes/service/api；server白名单由协调者唯一修改 | 隔离HTTP、模型桩、错误码、无业务写入 | 后端A+协调者 | G2 |
| T5 | 本体对象与链接接入 | T3 | ObjectWorkspace.vue及适配测试 | O1/O3完成生成至原有保存 | 前端B | G2 |
| T6 | 属性/共享属性接入 | T3 | PropertyManager.vue及必要格式组件 | O2类型联动、共享确认取消、CAS | 前端C | G2 |
| T7 | 业务规则与动作定义接入 | T3 | BusinessRuleDialog/Library、ActionLibrary及测试 | O4/O5无新增本体参数、字段不越界 | 前端B | G3（T5后） |
| T8 | 实例识别与链接映射接入 | T3 | ObjectSources与开工定位的链接组件 | P1/P5登记对象、关联依据、无自动探测 | 前端C | G3（T6后） |
| T9 | 属性取值三类接入 | T3/T4 | PropertySources及来源适配测试 | P2/P3/P4动态候选、逐参数答案、F01/F02回归 | 前端A | G3 |
| T10 | 动作接口映射接入 | T3/T4 | ActionBindings及测试 | P6完整URL校验、参数依据、绝不执行 | 前端B | G4（T7后） |
| T11 | 对抗与生命周期集成 | T4～T10 | tests/专属集成及浏览器脚本 | A01～A17、D1～D7，11场景、跨用户/目标/版本/保存保护 | 测试owner | 串行集成 |
| T12 | 分支交付与独立验收 | T11 | 实际SHA/URL/证据/未覆盖清单 | build+相关回归+浏览器实测 | 协调者 | 串行 |

每个文件同时仅一个owner。发现共用文件时协调者先登记拆分写入归属；API、App.vue、server.py、公共类型、依赖锁文件由协调者处理。独立任务可多agent并行；无多agent能力按同表串行，不为并行创建额外worktree。安装依赖、同目录构建、Git操作串行。

## 6. 验证与完成门槛

- 保留现有原型不改为生产演示；生产另以合成模型桩覆盖D1～D7及A01～A17，记录按钮操作、真实表单值与持久化前后差异。
- 每类场景至少一条正常链路、一条缺信息链路；另使用非储能对象及不同技术字段，证明非硬编码。
- 后端：上下文越权/过期、模型幻觉引用、错误类型、恶意HTML、伪造evidence、响应超限、超时、目录缺失；返回建议前后revision与数据库内容不变。
- 前端：取消生成、关闭、切项目/对象/版本、手改字段、答案暂不确定、目录改变、旧响应晚到、重复请求、只勾部分字段、撤销保护。
- 保存：采纳后不触发900ms自动保存；等待超过自动保存窗口验证数据库未变；取消恢复，显式保存一次，409不假成功；共享影响确认取消不写。
- 执行命令：`python3 tests/run.py --test tests/<本轮实际新增文件>.py`、相关现有回归；前端使用仓库现有测试入口；`cd frontend && npm run build`。T0必须把实际新增测试文件和可复制命令填回此计划，交付不得保留占位命令。
- 浏览器1440×900、1280×800、768×1024实测，采集console与异常；不能以代码存在判通过。环境受阻标未覆盖并保留待验收。
- 隔离模型桩验证网络调用链是必做；经授权的真实提供方用合成资料可做连通冒烟，未做真实提供方联调须明示，不能把桩说成真实LLM验证。

## 7. 开发环境、授权与集成（自包含）

主仓库固定为 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`；开发cwd必须是另行授权并登记的需求worktree，不是主目录。

| 项 | 值 |
|---|---|
| 任务ID | assist-fill-production |
| 执行/集成负责人 | zcode开发；Codex获授权后集成 |
| 需求版本 | 本计划2026-09-21 + 原型需求§1～8 |
| 分支/实际worktree/base SHA | 待授权创建后登记，建议codex/assist-fill-production及主仓库/worktree/assist-fill-production |
| 端口/数据根/数据库URL/Python环境/运行命令 | 待实际分配、核验并登记，不预填可用结论 |
| 状态 | 未创建环境、未开发；不是已授权集成 |

标准提示词与停止点：
1. “为本体与项目辅助填写正式实现创建worktree，从最新已提交main建立独立分支及目录，登记端口和数据目录，先不要开发。”只授权创建。
2. “在已登记worktree按执行指令_正式实现.md开发、自测并提交，不合并main。”只授权该环境开发。
3. 可一次明确“创建worktree并按指令开发、自测提交，不合并main”。普通“开发/验收”不替代未给出的创建授权。
4. “验收本需求最新提交，不合并main。”独立验收；通过停在待用户授权集成。
5. “将本需求验收通过提交集成并合并main，处理冲突并重验，成功后清理对应环境；不重启主工作台。”授权串行集成及正常清理。
6. “将已合并main构建并更新主工作台；真实数据库迁移先说明操作。”单独授权运行更新。

创建前查git status/worktree list/main与remote；从最新已提交本地main派生，不复制未提交内容，不盲目pull。worktree置于主仓库根worktree/，确认已忽略；分支已存在先核归属，禁止覆盖。共享登记使用git-common-dir/workbench-tasks并加锁。

分支使用自己的依赖、.runtime、dist，独立Python环境；所有命令显式cwd。独立空闲端口禁18765/8765，显式WIZ_WORKBENCH_PORT和WIZ_WORKBENCH_ROOT；清理或覆盖继承WIZ_DATABASE_URL，确认实际数据库独立，凭据/附件/密钥也隔离。只用合成数据，不复制真实数据。浏览器独立上下文，优先分支后端托管dist，防dev代理误连18765。

分支验证后报告SHA、URL、数据根和实际状态。不合并、不更新主服务。集成由获授权的单一负责人串行排队，最新main建临时集成worktree，合入准确已验SHA，处理普通冲突并重跑组合检查。需求矛盾交用户决定；main脏或前进不得强行覆盖，排队或重新组合。仅已验证组合且main基线不变、工作树干净才ff更新。

代码与数据回退分开，代码优先revert，不认为回退Git能回退数据库。真实迁移、远程push不在开发/合并隐含授权内。本功能预期无需数据库迁移，若需要先说明原因。

授权合并成功后保存证据与交接，核实分支全部合入、无未提交和需保留数据，再停止本任务服务，Git移除对应开发/临时集成worktree及已合并分支，清理登记可丢弃的依赖、数据与端口；禁止删worktree父目录或他人环境。异常/归属不明则保留报告，不强删。主服务更新另行授权。

---

## 8. T0 落地记录（2026-09-21，zcode）

### 8.1 环境登记（实际核验值）

| 项 | 值 |
|---|---|
| 分支 | `codex/assist-fill-production` |
| base SHA | `039a4f0`（创建时最新已提交本地 main，工作树干净） |
| worktree | `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/assist-fill-production` |
| 端口 | **18951**（空闲已核验；禁用 18765/8765） |
| 数据根 | `<worktree>/.runtime/assist-data`（独立 SQLite，惰性建库；未设 WIZ_DATABASE_URL，无真实库继承） |
| 浏览器验收账号 | `assist_dev`（仅存在于隔离数据根） |
| Python 环境 | 系统 python3 3.9.6 + 后端依赖（PyYAML/rdflib/SQLAlchemy/Alembic/cryptography 已装） |
| 启动命令 | `cd <worktree> && WIZ_WORKBENCH_PORT=18951 WIZ_WORKBENCH_ROOT="$PWD/.runtime/assist-data" ./start.sh start` |
| 状态 | 服务已启动并登录验证（HTTP 200）；待开发 |

### 8.2 字段映射表（11 场景 → 真实表单字段；权威来源 workbench/assist_fields.py）

| 场景 | 组件（draft 机制） | 可辅助字段键 | 不在范围/禁改 |
|---|---|---|---|
| O1 object | ObjectWorkspace.vue `editor.draft`（显式保存 submitForm，无自动保存） | label、comment | 不自动建层级 |
| O2 property/sharedProperty | PropertyManager.vue `draft`（MANAGED 键集；共享影响确认 impact 指纹） | label、comment、dataType（select）、obsType（与 dataType 原子组）、formatting（复合组，受限样式子集） | 共享引用只读态不改；引用影响走既有确认 |
| O3 link | ObjectWorkspace.vue LinkDraft | label、from、to（ref:object）、cardinality（4 枚举）、reverseLabel、comment | 两端对象仅限图内既有对象 |
| O4 rule | BusinessRuleLibrary.vue 内嵌表单 | name、description、content（选填） | 不添加 output 输出字段 |
| O5 action | ActionLibrary.vue draft | name、description、effect（选填） | 不新增参数/提交条件 |
| P1 identity | ObjectSources.vue identityDraft + noteDraft | mode、connection、table、primaryKey、note | 登记实例清单不由模型改写 |
| P2 propertySource(field/database) | PropertySources.vue draft（field 直取 / database 直选表） | field；connection、table、result.valueField、result.timestampField、lookup.match（复合组）、note | 不刷新目录、不查业务记录 |
| P3 propertySource(redis) | 同上 switchKind('redis') | connection、command、key、hashField、params（复合组）、conversion、missing、note | 不猜 Key/比例；timeSeries 属性禁 redis 由表单约束 |
| P4 propertySource(flow) | 同上 switchKind('flow') | flow、output、inputs（复合组）、result.valueField/timestampField、note | 运行时时间输入保持待配置；不自动建编排 |
| P5 linkMapping | LinkMappings.vue draft + noteDraft | sourceId、field、targetSourceId、targetField、note | 同名不构成等价依据 |
| P6 actionBinding | ActionBindings.vue draft（actionHttpModel 结构） | method、path（完整 URL）、bodyFormat、description、parameters（复合组）、note | **auth.\* 永不在白名单**；不执行请求、不生成凭据 |

说明（draft 事实，来自 T0 代码核对）：
- 全部 11 个表单走 `inject('form-save')/('form-guard')` 显式保存契约（App.vue submitForm），**不经 touch 900ms 自动保存**；采纳建议只合并进各表单本地 draft，不调用 submitForm/commit-now/touch，即天然满足「采纳不触发自动保存」。
- 复合组结构冻结：`lookup.match` 行 `{field,operator:'eq',value:{kind:identityKey|identityField|property|constant|parameter,…}}`；redis `params[token]` `{from:primary}|{from:identityField,field}|{from:property,property}`；flow `inputs[inputId]` `{from:property,property}|{from:constant,value}|{from:instanceId}`；动作 `parameters` 行 `{name,in,value:{from:actionInput|instanceId|property|constant,…}}`。
- 项目说明（note）按 mappingDescriptions 既有分键存储（objects/properties/links/actions），采纳经各表单既有 commitDesc 链路。
- 默认模型：`llm_providers.default_provider()`（按账号）；前端经 `GET /api/llm-providers` 的 `isDefault` 感知，assist-context 响应携带 `modelReady`。

### 8.3 协议冻结（详见 接口文档 04 §5、05 速查表 53a/53b、README 变更记录）

- 新增 `POST /api/assist-context`、`POST /api/assist-generate`（均不持写锁、不写业务修订）。
- contextToken：HMAC 签名（进程内密钥，不落库），绑定用户/目标/权威指纹/draft 摘要，TTL 600s；失配一律 409 CONTEXT_STALE。
- 错误码：400 INVALID_ARGUMENT / 404 NOT_FOUND / 409 CONTEXT_STALE / 422 MODEL_NOT_CONFIGURED・ASSIST_INVALID_STATE / 502 MODEL_BAD_RESPONSE / 504 MODEL_TIMEOUT；有效无建议 200+`status:"empty"`。
- 上限：intent≤4000、答案≤2000/条、questions≤3、suggestions≤12、issues≤30、draft≤200KB、max_tokens 4000、超时沿用提供方配置（≤300s）。

### 8.4 任务表落款（派发记录，随实施更新）

| ID | Owner | 状态 |
|---|---|---|
| T0 | zcode（协调者） | 完成（本节） |
| T1/T2/T3 | 待派发（G1 并行） | 未开始 |
| T4 | zcode（server.py 白名单协调者唯一修改） | 未开始 |
| T5/T6 | 待派发（G2） | 未开始 |
| T7/T8/T9 | 待派发（G3） | 未开始 |
| T10 | 待派发（G4） | 未开始 |
| T11/T12 | zcode | 未开始 |

---

## 9. 交付记录（2026-09-21，zcode；待 Codex 独立验收）

### 9.1 交付标识

| 项 | 值 |
|---|---|
| 分支 / worktree | `codex/assist-fill-production` / `worktree/assist-fill-production`（base `039a4f0`） |
| 交付 SHA | `f355ca0`（功能与修复完结）＋ `60c65c0`（测试对齐）＋ 本提交（文档落款） |
| 隔离环境 | http://127.0.0.1:18951 · 数据根 `<worktree>/.runtime/assist-data`（合成数据，可丢弃）· 账号 assist_dev |
| 服务状态 | 运行中（供独立验收复用；含本地模型桩 18912 与已登记默认提供方「本地模型桩」） |

### 9.2 实际完成范围（T0～T12 全表）

- **后端**：`assist_fields.py`（场景/字段白名单注册表）、`assist_context.py`（上下文构建＋HMAC contextToken＋权威指纹＋未配置目标容忍）、`assist_schema.py`（模型输出分层校验：502 整体违规／单条丢弃／blocked 可见、原子组、等值丢弃、evidenceRefs 核验）、`assist_service.py`＋`assist_routes.py`（生成编排与错误映射）、`server.py` 白名单＋2 路由。
- **前端**：`assist/types.ts`、`useAssistPanel.ts`（状态机：代际丢弃迟到响应/失效双通道/原子采纳/一次性撤销/CONTEXT_STALE 重取）、`AssistPanel.vue`、6 个表单 binding 适配器（ontologyBindings/propertyBinding/workflowBindings/identityLinkBindings/propertySourceBinding/actionBindingAdapter），11 场景（O1～O5、P1～P6）全部接入：入口按钮＋面板＋采纳只改本地草稿零自动保存＋手改失效＋撤销保护。
- **接口文档**：04 §5 全量契约＋增量登记（截断标记/分层违规口径/answers 上限）、05 速查表 99 条、README 变更记录两条。

### 9.3 实际新增测试文件与可复制命令（替代 §6 占位）

| 文件 | 覆盖 | 命令（cwd=worktree） |
|---|---|---|
| tests/test_assist_context.py | 上下文/越权/令牌/指纹/脱敏/截断/未配置容忍/只读不变式（14 项） | `python3 tests/test_assist_context.py` |
| tests/test_assist_schema.py | 模型输出分层校验/复合组/原子组/恶意输出（57 项） | `python3 tests/test_assist_schema.py` |
| tests/test_assist_api.py | HTTP 链路：401/400/404/409/422/502/504/200 empty＋模型桩真实网络调用＋只读不变式（10 组） | `python3 tests/test_assist_api.py`（端口 18911/桩 18912） |
| tests/assist_panel.test.mjs | 面板状态机 51 项 | `node --import ./tests/ts_hooks.mjs tests/assist_panel.test.mjs` |
| tests/assist_object_workspace.test.mjs | O1/O3 接入 9 项 | 同上模式 |
| tests/assist_property_manager.test.mjs | O2 接入 54 项 | 同上模式 |
| tests/assist_workflow.test.mjs | O4/O5 接入 11 项 | 同上模式 |
| tests/assist_identity_link.test.mjs | P1/P5 接入 16 项 | 同上模式 |
| tests/assist_property_sources.test.mjs | P2/P3/P4 接入 12 项 | 同上模式 |
| tests/assist_action_bindings.test.mjs | P6 接入 54 项 | 同上模式 |
| tests/mapping_forms.test.mjs | 修复基线既有失败并按当前组件行为重写（覆盖点保留） | 同上模式 |

全量回归：前端 46/46 套件通过；`python3 tests/run.py all` 51/52→安装 pypdf（环境缺口，非本功能）后 parsers 95/95 全绿；`cd frontend && npm run typecheck` 0 错误；`npm run build` 通过。

### 9.4 浏览器验收（隔离实例真实点击，两轮）

第一轮 15 条矩阵：O1/O2/O2b/O3/O4/O5/P2/S14/XSS 通过；发现 6 项 P1。第二轮聚焦复验＋修复：
- 已修复并复验：P1 实例识别 projectId 断链、P3 未配置属性 404、登记实例显示、P6 动作绑定入口（含凭据零出网断言）、S13 缺信息问题卡与 pending→ready、P5 目标格式（API 级 200＋ready 建议；面板接线第二轮已验证）。
- revision 取证：各场景采纳后等待 ≥2.5s revision 不变，表单保存后前进（O1/P1/P2/P4/P6 均取证）。
- 三档视口 1440×900/1280×800/768×1024 面板可达。console：第二轮正式窗口 error 0/warning 0/未捕获 0。
- 证据：/tmp/kg_t11_browser/agent/（58 截图＋JSON）、/tmp/kg_t11_browser/agent2/（37 截图＋JSON/console.jsonl）。

### 9.5 未覆盖项与限制（如实）

1. **真实模型联调未做**：全部生成验证走本地模型桩（合成建议）；经授权的真实提供方联调待用户配置后另行执行，不得把桩验证说成真实 LLM 验证。
2. 目录候选：隔离环境无真实 MySQL，P1 的表/字段候选为空（fail-closed 正确行为）；目录候选链路由单元测试覆盖。
3. 原生 `<select>` 弹层键序驱动（headless 限制）、真机多浏览器矩阵、完整无障碍走查未做。
4. S14 非空 issues 渲染、P4 桩空建议场景未在浏览器演练（逻辑层已单测覆盖）。
5. 隔离数据根中的演示数据（辅助验收本体/项目/编排/模型桩配置）为合成数据，集成清理时可整体丢弃。

### 9.6 任务表终态

T0～T12 全部完成；owner 落款：T0/T4/T11/T12=zcode（协调者，server.py/接口文档唯一修改人）；T1/T2/T3=并行 agent A/B/C；T5/T6=并行 agent D/E；T7/T8/T9/T10=并行 agent F/G/H/I。全部提交由协调者串行执行。

### 9.7 子代理验收循环（2026-09-22，用户指令：子代理验收直至通过）

- **R1（判定：不通过）**：15 条矩阵 + 全量测试 + 真实模型抽查。9/11 场景全链路通过；3 项 P1：DEF-01 本体区上下文固定读默认工作区（协议缺 ontologyId）、DEF-02 新建动作辅助 404、DEF-03 替换真实旧值的建议默认勾选（违背需求 §3.5）；P2：检查页签无字段定位动作（不阻断，登记待办）。
- **修复（fe1179f）**：协议补 ontologyId 端到端贯通（App provide → 5 宿主 binding → 令牌签入 → check_generate 一致性 → 跨账号 404）；新建动作 targetId 传空；默认勾选门控改为「ready 且宿主旧值为空」，9 个场景套件适配并各增「旧值非空默认不勾」断言。
- **R2（判定：不通过，唯一 P1）**：三项 P1 运行时修复全部复验通过（代码/API/浏览器三层直证），唯一遗留=ontologyId 未登记接口文档。
- **补登记（53ef04a）**：04 §5.1 请求表 + README 变更记录（纯文档 +2 行）。
- **R3（判定：通过）**：4 项复验全过（变更范围/三方一致/上下文与 api 套件复跑 14+10/工作区零写入）。
- 证据：/tmp/kg_accept_final/（R1，48 截图）、/tmp/kg_accept_r2/（R2）、/tmp/kg_accept_r3/（R3）。真实模型（minimax）admin 侧冒烟通过（含 ontologyId 贯通后）。
- 登记待办（不阻断）：DEF-04 检查页签字段定位（P2，需逐宿主 locate 接线）；OBS-2 登录过渡态一次性 TypeError（疑存量全局问题，建议另立治理项）。
