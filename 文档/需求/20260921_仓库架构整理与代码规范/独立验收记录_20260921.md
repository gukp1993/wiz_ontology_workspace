# 仓库架构整理与代码规范 · 独立验收记录（2026-09-21）

- 验收人：zcode（用户于 2026-09-21 直接指令本工具执行验收，按 AGENTS「用户明确指定其他执行者优先」）
- 验收 cwd：`worktree/repo-cleanup`，分支 `codex/repo-cleanup`
- 实际验收 HEAD：`6f61f6c`（= 业务末提交 `eeb7097` + 2 条纯文档提交 `c1c8d28`/`6f61f6c`，`git diff eeb7097..HEAD --stat` 确认增量仅验收指令 96 行 + 协作条目 + session_context 渲染，业务内容与 `eeb7097` 一致，符合指令 §0 前进规则）
- 验收方式：只读核对 + 隔离测试（未合并 main、未 push、未启停 18765、未触碰 assist-fill-production 与 build-governance 工作树；测试均为脚本自管临时根）

## 总判定：**不通过**（B 项核心交付实际未发生；A/C/D 通过）

| 项 | 判定 | 摘要 |
| --- | --- | --- |
| A 文档重组 | **通过** | 目录结构/归位/rename 历史全部符合；指令一处期望值误记（见 A-6） |
| B 仓库精简 / ontology 退跟踪 | **不通过** | **ontology 991 个、.idea 5 个文件仍在 HEAD 树内被跟踪，退出跟踪从未发生**；tracked 1858 ≠ 期望 ~855 |
| C 代码规范静态检查 | **通过** | ruff 全过、eslint 0 error、build 通过、quick 3/3；配置豁免理由齐全 |
| D 回归与语义抽查 | **通过** | all 48/49（唯一失败为 pypdf 环境缺口，main 对照成立）、TS 6/6、语义抽查 6/6 |

---

## A. 文档重组（b8627db）——通过

| 检查 | 期望 | 实测 | 判定 |
| --- | --- | --- | --- |
| `ls -1 文档/` 顶层 | 需求/交付物/接口文档/prototypes/导出/历史归档_20260916前/评审与纪要/概念与说明 + 3 根级 md | 完全一致 | ✓ |
| `文档/imp` | 不存在 | 不存在（exit 1） | ✓ |
| `评审与纪要` 数量 | 6 | 6 | ✓ |
| `概念与说明` 数量 | 4 | 4 | ✓ |
| `交付物/*.md` 数量 | ≥12 | **8**（见 A-6） | ✓（按基线事实） |
| 归档内 V3实施方案说明.md | 0 | 0 | ✓ |
| 设计方案_v3 rename 历史 | 连续 | `git log --follow` 识别 rename（b8627db ← ddc4d31 init） | ✓ |

**A-6 期望值误记说明（非实现缺陷）**：基线 `b4a56a9` 的 `文档/交付物/` 顶层只有 4 个**子目录**、0 份顶层 md；本分支顶层 md 恰为归位的 8 份（8 份逐一与开发计划 §A1 清单吻合，归档内同名残留 0）。指令「≥12」系把「原 4 个子目录」误当顶层 md 计入；实现与开发计划一致，判通过。

**断链核对**：AGENTS.md 全部 `文档/...` 引用逐条 `[ -e ]` 断言存在（唯一 MISS 为归档规则节的 `YYYYMMDD_本次需求名称` 模板占位符，非实际路径）；README 引用存在（grep 输出中 2 条 MISS 为 `[文本](路径)` 链接语法被连带捕获的假阳性，文件已单独验证存在）。11 份归位文件全部命中。

## B. 仓库精简 / ontology 退出跟踪（d7f79ce）——**不通过**

| 检查 | 期望 | 实测 | 判定 |
| --- | --- | --- | --- |
| `git ls-files \| grep -c '^ontology/'` | 0 | **991** | ✗ |
| `git ls-files \| grep -c '^\.idea/'` | 0 | **5** | ✗ |
| `.gitignore` 含 `/ontology/` 与 `.idea/` | 在 | 在（第 10/11 行） | ✓ |
| `find ontology -type f \| wc -l` | 991（=基线） | 991 | ✓ |
| vault 原样 | 存在 | 主仓库存在；验收工作树本无 vault（该目录一直被忽略、从未 checkout，属正常） | ✓ |
| `git diff b4a56a9..HEAD --stat -- data/ keys/` | 0 | 0 | ✓ |
| `git ls-files \| wc -l` | ~855 | **1858** | ✗ |

**核心缺陷（证据链）**：

1. `git ls-tree -r d7f79ce --name-only | grep -c '^ontology/'` = **991**，`git show d7f79ce --name-status | grep -c '^D.*ontology'` = **0**——该提交树内没有任何 ontology 删除记录，「退出跟踪」未发生。HEAD（6f61f6c）同。
2. 基线 `b4a56a9` 树实测 tracked = **1852**（非指令与提交信息所称 1758）；HEAD = **1858**。提交信息「tracked 文件总数 1758 → 855」与仓库事实不符（1852−991−5=856 ≈ 855，两个声称数字均与实测对不上，疑为计划期口径未在提交前实测）。
3. `.gitignore` 规则已加但**只对未跟踪文件生效**，对仍在索引中的 991+5 个文件无效——现状合并 main 后仓库仍带全部 ontology 数据，用户「文件太多太臃肿，优化精简」的核心目标未达成。

**通过子项**：磁盘 991 文件完好、data/keys 零接触、AGENTS（目录节/§2/§13 补注）与 README 同步、主仓库 vault 未受影响。

## C. 代码规范静态检查（ec0c0aa、eeb7097）——通过

| 检查 | 期望 | 实测 |
| --- | --- | --- |
| `~/Library/Python/3.9/bin/ruff check workbench tests` | All checks passed | All checks passed! |
| `npm run lint` | 0 error | 0 error（无输出，exit 0） |
| `npm run build` | vue-tsc 0 错 + vite 成功 | ✓ built in 3.77s |
| `python3 tests/run.py quick` | 3/3 | 3/3（8 步骤全过） |

配置核对：`ruff.toml` 每条 ignore（E701/E702/E703/E731/E741/E402/B023）均注明理由；`eslint.config.js` 每条 off 均分组注明（Prettier 域不重排/既有约定/渐进类型/已知债务）；AGENTS「代码规范（2026-09-21 起）」节存在且与两份配置一致。✓

## D. 回归与语义抽查——通过

- `python3 tests/run.py all`：**48/49**，唯一失败 `test_ontology_build_parsers.py`（2 项 D15，pypdf 缺失）。
- **pypdf main 对照（§2 要求）**：main 工作树跑同文件 **83/85，失败同为 2 项 D15**（'未安装 PDF 解析库'）——确认为既有环境缺口，不计本次缺陷。
- TS 测试：save_queue / http_layer / flow_model / business_rule_model / action_model / undo_history **6/6 OK**。
- 语义抽查 6 条：
  1. `model_routes.py` `_rebuild_index`：`from workbench.storage import engine as store, indexing`——补未定义名 `store`（原仅 import indexing 即调用 `store.read_snapshot`，真 bug），逻辑不变 ✓（注意：落在 d7f79ce，非指令所述 ec0c0aa）
  2. `test_mapping_descriptions.py`：`import_seed_into_db()` 返回版本号、`main()` 接收——修 `SEED_VERSION` 全局未定义，行为等价；顺带删除未用 import ✓（同上在 d7f79ce）
  3. `FlowEditor.vue`：影子初始化从 `linkShadowOwner` computed 移入 `watch(() => linkDialog.value && linkDialog.value.chosenInputId)`（弹窗打开与切换输入均触发，预置/克隆逻辑原样搬移），computed 变纯、未同步时显式返回 null 并注释 ✓（同上在 d7f79ce）
  4. `LinkMappings.vue`：`title="成员预览 · "+linkLabel(...)` → `:title="'成员预览 · ' + linkLabel(...)"`——修模板解析错误，拼接语义保持 ✓（同上在 d7f79ce）
  5. `eeb7097`：`{{ state.graphName }}` → `{{ bridgeState.graphName }}`，顶栏副标题「有图名显图名，否则未选择本体」语义正确，该提交独立干净（1 文件 1 行）✓
  6. B904 抽 5 处（SystemExit/_EvalError×2/PackageFormatError×2）均为显式 `from None` 抑制，转换型异常链对象正确；`_` 前缀抽 3 处（`_flowDirty`/`_areaLabel`/`_title`）确认作用域内无原名残留引用、无遮蔽 ✓

### 附带交付质量问题（不影响判定，须修复 B 时一并处理）

**提交组织与提交信息严重错位**：`d7f79ce` 信息声称只做退跟踪，实际包含 lint 基线绝大部分内容（ruff.toml、requirements-dev.txt、package.json/lock +1701 行、前后端机械修复、AGENTS/README 规范节）；`ec0c0aa` 信息声称「建立 Python/Vue 代码规范基线并完成存量机械修复」，实际仅 1 个文件 `frontend/eslint.config.js`；指令 §D 所列 4 个语义修复点全部在 d7f79ce 而非 ec0c0aa。自验记录（「git diff --cached 无 data/keys 误入」「磁盘 ontology/ 完好」）未覆盖关键断言 `git ls-files | grep -c '^ontology/'` = 0，导致核心未交付未被发现。

---

## 修复要求（原分支继续，不合并 main）

1. **补做真正的退出跟踪**：在原 worktree 执行 `git rm -r --cached ontology/ .idea/`（磁盘文件保留），**单独成提交**（不混入其他改动），提交后实测断言：`git ls-files | grep -c '^ontology/'` = 0、`grep -c '^\.idea/'` = 0、`git ls-files | wc -l` ≈ 862（=1858−991−5）、`find ontology -type f | wc -l` 仍 991、`git diff --stat -- data/ keys/` 仍 0。
2. 修正后续提交信息使与内容一致（本地未合并分支可整理 d7f79ce 起历史，或在新提交信息中如实更正记录；验收以最终树为准，不做强制要求）。
3. 修复后交付新 SHA，重新走独立验收；本轮「不通过」结论针对 `6f61f6c`，不覆盖新提交。

## 停止点

按指令 §4 与 AGENTS：本轮**不合并 main、不清理工作树、不更新主服务**，状态停在「验收不通过，待原分支修复后复验」。集成/合并仍待用户明确指令。

---

# 复验记录（第 2 轮，2026-09-21）——**通过**

- 复验人：zcode（用户指令「继续验收」）；复验 cwd 与红线同首轮。
- 整改提交：`df941aa`（补做退出跟踪）、`09ae5e2`（整改记录 + 指令期望值修正）、`abab867`（交接登记）；复验 HEAD `abab867`。
- 业务提交 b8627db..eeb7097 内容未变（df941aa 仅索引删除），首轮 A/C/D 结论继续有效，本轮复验聚焦 B 项整改。

## B 项整改逐条复验

| 检查 | 修复要求 | 实测 | 判定 |
| --- | --- | --- | --- |
| `git ls-files \| grep -c '^ontology/'` | 0 | **0** | ✓ |
| `git ls-files \| grep -c '^\.idea/'` | 0 | **0** | ✓ |
| HEAD 树级（`git ls-tree -r HEAD`） | 0 / 0 | **0 / 0**（提交对象级证据，非索引态） | ✓ |
| `git ls-files \| wc -l` | ≈862（后按整改方口径 ≈864） | **865**（=864+abab867 新增交接条目 1 文件，构成吻合） | ✓ |
| `find ontology -type f \| wc -l` | 仍 991 | **991**；`.idea/` 磁盘 5 文件（含 `.idea/.gitignore`）均保留 | ✓ |
| `git diff b4a56a9..HEAD --stat -- data/ keys/` | 0 | **0** | ✓ |
| 整改提交纯度 | 单独成提交、不混入其他改动 | df941aa **996 行改动 = 991 ontology 删除 + 5 .idea 删除 + 0 其他** | ✓ |
| .gitignore 规则 | 保留 | `/ontology/`（第 10 行）、`.idea/`（第 11 行） | ✓ |

## 整改记录与更正核对（09ae5e2）

- 开发计划 §7 如实记录根因：**拆分提交前的 `git reset` 撤销了 `git rm --cached` 暂存**（`git add -u` 无法恢复删除暂存，.gitignore 只对未跟踪文件生效）——与首轮验收推断一致；自验在 reset 后未复测 `git ls-files` 被明确登记为教训。
- 数字更正确认：基线 1852（非 1758）；tracked 目标 ≈864（非 855，855 系计划期估算值）。
- 验收指令两处期望值同步修正：`交付物/*.md ≥12 → 8`、`git ls-files \| wc -l ≈855 → ≈864`——与本轮复验实测全部吻合。
- 历史提交不改写（d7f79ce/ec0c0aa 信息错位以整改记录为准），处理方式可接受。

## 回归哨兵

- `python3 tests/run.py quick`：3/3（整改为纯 git 索引操作，无代码变更；首轮 all 48/49 + pypdf 环境缺口对照、TS 6/6、语义抽查 6/6 结论不变）。
- 工作树 clean，无未预期 untracked 文件。

## 最终判定：**通过（B 项整改复验通过，A/C/D 沿用首轮通过结论）**

状态转为**「待用户授权集成」**：不合并 main、不清理工作树、不更新主服务，均待用户明确指令。

