# 仓库架构整理与代码规范 · 独立验收指令

交付时间：2026-09-21；交付人：zcode；本指令自包含，验收人（默认 Codex）无需聊天上下文即可执行。

## 0. 被验对象与环境

- 主仓库（协调目录）：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`（main 在此，**验收不动 main**）。
- 验收 cwd（工作树）：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/repo-cleanup`，分支 `codex/repo-cleanup`。
- 基线：`b4a56a9`（创建工作树时的最新已提交 main）。被验提交（按序）：
  1. `b8627db` refactor(docs): 文档目录重组
  2. `d7f79ce` chore(repo): ontology/ 与 .idea/ 退出 git 跟踪
  3. `ec0c0aa` chore(lint): Python/Vue 代码规范基线与存量机械修复
  4. `eeb7097` fix(frontend): EditorView 模板 brand 副标题 state 引用修复
  （另有 `d262914`/`4dd121f` 两条协作交接记录提交，不属于业务验收面，只需确认存在。）
- **最终验收 HEAD 以验收时 `git log --oneline -8` 实际输出为准**；若 HEAD 因交接提交前进，以「业务提交范围 b8627db..eeb7097 内容不变」为准核对。

## 1. 必读顺序

1. 本文件。
2. `文档/需求/20260921_仓库架构整理与代码规范/开发计划.md`（盘点结论、方案、验证记录；验收判定的对照基准）。
3. `AGENTS.md` 的「代码规范」节与「架构边界」第 2/13 条（含 20260921 退出跟踪补注）。
4. `ruff.toml` 与 `frontend/eslint.config.js`（规则与每条豁免的理由）。

## 2. 验收边界与红线

- **只读验收 + 隔离测试**：允许跑测试/构建/lint；禁止合并 main、禁止 push、禁止改业务代码、禁止启停 18765 主服务、禁止触碰 `worktree/assist-fill-production` 与 `worktree/build-governance` 两个他人工作树。
- **真实数据零写入**：`ontology/`（已退出 git 跟踪的迁移备份 + vault 密钥）、`data/`（在线 SQLite）、`keys/` 一律只读。自动化测试一律用临时根（测试脚本自管 `WIZ_WORKBENCH_ROOT`/端口，无需手工设置）。
- **已知环境缺口（不计为本次缺陷）**：本机系统 python3 无 pypdf，`tests/test_ontology_build_parsers.py` 的 2 项 D15 用例会失败。判定前先在 main 工作树跑同文件做对照——main 同样失败即为既有环境缺口（requirements-dev/requirements 已登记，安装方式由用户决定）；若 main 通过而本分支失败才是本次缺陷。
- 依赖安装：验收需要 `~/Library/Python/3.9/bin/ruff`（已存在；或 `pip3 install --user -r requirements-dev.txt`）与 `frontend/node_modules`（可用 `npm install` 在**验收工作树内**安装，不得复用/改动主仓库 node_modules）。

## 3. 验收项

### A. 文档重组（对应提交 b8627db）

```bash
cd /Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/worktree/repo-cleanup
ls -1 文档/                 # 期望顶层：需求/ 交付物/ 接口文档/ prototypes/ 导出/ 历史归档_20260916前/ 评审与纪要/ 概念与说明/ + 3 个根级md（设计方案v3/迁移清单_20260915/架构优化实施说明_20260915）
ls 文档/imp 2>/dev/null      # 期望不存在
ls -1 文档/评审与纪要 | wc -l # 期望 6
ls -1 文档/概念与说明 | wc -l # 期望 4
ls -1 文档/交付物/*.md | wc -l # 期望 8（基线交付物顶层为 4 个子目录 0 份 md，8 份全系归位）
# 归位文件与归档内不再有同名重复（以 交付物/新增8份 为例）：
find 文档/历史归档_20260916前 -name "V3实施方案说明.md" | wc -l   # 期望 0（已移出）
git log --follow --oneline -- 文档/通用储能本体工作台设计方案_v3.md | head -3  # 期望历史连续（rename 被识别）
```

引用断链核对：`grep -o '文档/[^）)」，】 ]*' AGENTS.md | sort -u`，输出的每个路径逐一 `[ -f ... ]` 断言存在（开发计划 §A1 列出的 11 份归位文件必须全部命中存在）。

### B. 仓库精简 / ontology 退出跟踪（对应提交 d7f79ce）

```bash
git ls-files | grep -c '^ontology/'        # 期望 0
git ls-files | grep -c '^\.idea/'          # 期望 0
grep -n '/ontology/' .gitignore && grep -n '.idea/' .gitignore   # 两条忽略规则在
find ontology -type f | wc -l              # 期望 = git ls-tree -r b4a56a9 --name-only ontology | wc -l（991，磁盘文件一个不少）
ls ontology/vault >/dev/null && echo vault-存在                              # 密钥目录原样
git diff b4a56a9..HEAD --stat -- data/ keys/ | wc -l                        # 期望 0（真实数据零接触）
git ls-files | wc -l                       # 期望 ≈864（整改提交后实测；基线 1852）
```

文档同步核对：README.md「数据与保存」「目录」两节与 AGENTS.md「目录」节/架构边界 §2/§13 含退出跟踪表述；README 不再出现 `发布包/`、`文档/归档/` 等已删除引用。

### C. 代码规范与静态检查（对应提交 ec0c0aa、eeb7097）

```bash
~/Library/Python/3.9/bin/ruff check workbench tests     # 期望 All checks passed!
cd frontend && npm run lint                              # 期望 0 error
npm run build                                            # 期望 vue-tsc 0 错误 + vite 构建成功
cd .. && python3 tests/run.py quick                      # 期望 3/3
```

规范配置核对：`ruff.toml`/`eslint.config.js` 每条关闭的规则都有注明理由；AGENTS.md「代码规范」节存在且与两份配置一致。

### D. 回归与语义抽查（对应提交 ec0c0aa、eeb7097）

```bash
python3 tests/run.py all    # 期望 48/49：唯一失败 test_ontology_build_parsers.py 的 2 项 D15（pypdf 缺口，先按 §2 做main对照）
for t in save_queue http_layer flow_model business_rule_model action_model undo_history; do
  node --import ./tests/ts_hooks.mjs tests/$t.test.mjs >/dev/null 2>&1 && echo "OK $t" || echo "FAIL $t"; done   # 期望 6 个 OK
```

语义抽查（逐条读 diff，确认「修复了什么、是否语义保持」）：
1. `git show ec0c0aa -- workbench/model_routes.py`：`_rebuild_index` 补 `from workbench.storage import engine as store`（修未定义名真 bug，逻辑不变）。
2. `git show ec0c0aa -- tests/test_mapping_descriptions.py`：`import_seed_into_db` 改为返回版本号、`main()` 接收（修 SEED_VERSION 未定义，行为等价）。
3. `git show ec0c0aa -- frontend/src/flow/FlowEditor.vue`：`linkShadowOwner` computed 内的影子初始化移入 `watch(chosenInputId)`，computed 变纯（核对弹窗打开/切换输入仍会同步 shadow）。
4. `git show ec0c0aa -- frontend/src/project/LinkMappings.vue`：`title="成员预览 · "+linkLabel(...)` → `:title="'成员预览 · ' + linkLabel(...)"`（修模板解析错误）。
5. `git show eeb7097`：模板 `{{ state.graphName }}` → `{{ bridgeState.graphName }}`（修重命名引发的模板解析回退；此条是 ec0c0aa 自查发现的回归修复，验收时确认顶栏副标题逻辑=「有图名显图名，否则未选择本体」）。
6. 抽 3 个 B904 `raise ... from` 修复点与 2 个 `_` 前缀未用变量点，确认异常链对象正确、无遮蔽作用域内原名。

## 4. 判定与交付

- 三态：**通过 / 不通过 / 部分覆盖**（某项无法执行时如实在该项记 blocked，不得写通过）。
- 逐项记录 A/B/C/D 每条命令的实际输出摘要与判定；记录实际验收 HEAD SHA。
- 任何「期望值不符」即该项不通过；pypdf 两项按 §2 对照实验归为环境缺口单独标注。
- 通过后状态为「待用户授权集成」：不合并 main、不清理工作树、不更新主服务——这三项均待用户明确指令。
- 交付：验收记录写入 `文档/需求/20260921_仓库架构整理与代码规范/独立验收记录_YYYYMMDD.md` 并提交到本分支；按共享上下文约定 record 交接。
