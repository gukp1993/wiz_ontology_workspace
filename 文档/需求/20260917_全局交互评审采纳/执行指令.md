# 其他 harness 执行指令：全局交互评审采纳

请实施本目录需求，完成代码、验证与提交；不要只输出分析或另一份方案。

## 项目与必读

根目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

按顺序阅读：

1. `AGENTS.md`，查看 `git status --short` 与当前代码；不覆盖用户或其他执行者修改。
2. `文档/需求/20260917_全局交互评审采纳/需求说明.md`：本期G1–G5、B1–B11是范围与验收清单。
3. `文档/需求/20260917_对象建模交互评审采纳/需求说明.md`：只作为前一批兼容基线，已完成的不重写。
4. 当前页面和既有 `文档/需求/20260917_微软列表详情布局/交互原型_v1.html`：遵循现有后台风格与列表／详情布局，不复刻旧原型中的模拟数据和模拟接口。

本次按用户要求只有需求和指令，没有新原型。旧原型与本次条款冲突时以本次明确要求为准。外部报告 `/Users/gukepeng/WorkBuddy/2026-09-17-21-47-06/本体工作台交互评审报告.html` 只作背景，不得全量照搬；访问不到它不阻碍实施。

## 第一步：确认现状与必要回归

先列出G1–G5各项“已有／缺失／需复验”，直接继续实现缺失项，不把已有部分重做。重点确认上一批已引入的项目懒加载、画布共用编辑表单和返回上下文。

主要代码入口：

- `frontend/src/app/http.ts`：唯一请求／错误解析层。
- `frontend/src/App.vue`：初始化、项目读取、导航、顶栏状态和全局反馈。
- `frontend/src/app/formGuard.ts`、`app/saveCoordinator.ts`：先理解，再决定是否需要改动；优先复用。
- `frontend/src/ontology/OntologyRelease.vue`、`project/ProjectValidation.vue`、`project/ProjectHome.vue`：校验／发布及项目错误。
- `frontend/src/ontology/SharedLibrary.vue`、`project/ConnectionManager.vue`：操作收纳与删除语义。
- `frontend/src/ontology/ObjectCanvas.vue`、`frontend/src/style.css`：模式提示与局部响应式布局。
- `workbench/server.py`、开发代理配置：只在Origin复现链路中检查，禁止绕过校验。

预览入口及文件可能随近期改动迁移，按当前import定位，不因路径不同新建重复页面。

## 第二步：G1／G2读取兜底与错误表达

1. 在现有HTTP层增加可选的有界读取能力，默认必要读取15秒，测试可注入短时间；不在页面散写fetch。覆盖响应体读取，清理timer／监听器，保留外部取消信号、SaveRequestError.status/currentRevision/data及409行为。
2. 本体初始化明确loading/error/ready，失败不误判为空本体；超时给重试，重试不重复注册监听器。保留项目局部加载与当前深链逻辑，取消旧请求不弹错、不覆盖新状态。
3. 建立最小共用错误呈现能力，接入需求指定入口。完整错误局部展示，顶栏仅摘要；不能把保存／发布关键错误变成会自动消失的Toast。
4. 写操作不套用读取自动超时与重试。等待慢时仍等待结果；结果未知保留输入与现有revision冲突流程。先核对修订／发布版本，不盲目重复发布，不创建新幂等或执行框架。
5. 实测Origin问题：记录浏览器地址、请求Origin和目标端口的脱敏证据。标准地址无问题就写明是评审环境因素；确认配置不匹配才最小修复。不能移除白名单、增加通配、伪造Origin或降低请求安全要求。

如修改保存协调器行为，先补对应测试并运行现有保存队列测试。共享HTTP层改动应回归正常成功、非JSON异常响应、网络异常、取消、超时与409，避免把不合法的成功响应静默当成有效数据。

## 第三步：G3保存状态

用真实guard dirty状态和当前区域Saver状态派生顶栏文案。注意打开表单不等于dirty，guard注册数量不能代替未保存修改判断；dirty计算需响应输入、取消、还原、保存成功等变化。

有未提交输入时不展示无范围绿色“已保存”。已有保存失败／冲突不得被更轻的表单提示覆盖。保留原有离开保护和表单提交链，不增加动画、字段差异表或状态中心。对共享表单入口做代表性验证，而非只验证对象编辑页。

## 第四步：G4／G5操作和布局

- 共享属性保留维护定义、查看引用；引用到对象、复制为私有、删除进更多。连接保留配置、测试；删除进更多，表结构目录入口保持可达。
- 菜单复用现有组件或最小局部实现，键盘／Escape／焦点正常；确认文案遵循实际删除行为，不修改数据清理或转换逻辑。
- 修复发布按钮局部收缩与390px顶栏裁剪；桌面布局不退化。窄屏工作区切换、上下文选择、创建入口、保存反馈和撤销重做可访问，保留项目引用版本。
- 画布只加随连线阶段变化的短提示，不新增导览、模式系统、字段或图编辑功能。

## 验证与交付

1. 自动化测试使用 `WIZ_WORKBENCH_ROOT=<临时数据目录>` 与 `WIZ_WORKBENCH_PORT=<独立空闲端口>`；不要向真实18765或真实ontology写测试数据，也不要调用真实设备控制API。
2. 为G1–G3的状态变化、取消／旧响应、写入结果未知补必要行为测试。B6使用mock或隔离服务模拟“服务端完成、客户端丢响应”，不在真实本体上反复发布。
3. 按B1–B11逐项浏览器验收。检查390／768／1280／1440px，提供必要截图；不能用静态截图代替保存失败、重试和菜单键盘流程。
4. `cd frontend` 后执行 `npm run build`，运行改动相关回归；若改了HTTP层／保存协调器必须验证调用方兼容，不能只跑类型检查。工具限制导致未验收则明确列出，不声称通过。
5. 查看git diff/status，仅提交本任务文件；不提交ontology数据、vault、runtime、dist、依赖目录及别人的修改，不使用reset清理现场。
6. 最终报告G1–G5完成情况、B1–B11验收结果、主要文件、实际commit哈希、Origin排查结论和未验证限制。把简短实施记录追加到本指令末尾，不另生成多份重复文档。

## 禁止扩大范围

不改本体／项目协议、已发布版本、业务字段与删除语义；不把接口／契约上移主导航；不隐藏引用版本；不将表单改抽屉；不增加脉冲动画、首次导览、差异摘要、最近使用统计、隐藏H1、favicon或函数编排改造任务。

不为局部错误处理引入UI库、路由库、状态库或后台服务。允许为明确的Origin配置错误作最小修复；其他超范围改动须报告具体必要性，不自行发散。本需求已明确部分直接执行，不重复询问已确定选择。

文档交付本身不代表已实施。执行者须在实际构建与验证后如实交付，真实本体与项目数据保持独立且不用于验收。
