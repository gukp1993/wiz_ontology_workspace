# 执行指令 · 本体工作概览优化 v1

项目：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260919_本体工作概览优化`

这是供用户交接实施的指令；当前任务仅产出原型。收到用户实施要求后按下面执行。

## 阅读顺序

1. AGENTS.md、共享上下文 read、Git 状态。
2. 需求说明.md。
3. **打开交互原型_v1.html，切换空本体/建设中/已发布及更多状态。实际点击创建、取消、问题填写、校验过期、引用项目。严格遵循当前白色侧栏与浅色卡片风格，不沿用历史深色原型。**
4. 开发计划.md、相关接口文档、OntologyHome.vue 与现有导航/保存流程。

## 固定任务

- T0 核对版本、项目、校验、导航能力。
- T1 实现分模块异步状态和当前草稿结果有效性；处理旧响应覆盖、新建空态与已有资产状态。
- T2 按原型更新工作概览，不改整个工作台导航/样式。
- T3 复用新建对象、Excel、问题定位、发布及项目切换；概览不另写编辑器。
- T4 验收 H01–H12、相关测试、build 和隔离浏览器。

## 不可偏离

空本体：一个主按钮“创建第一个对象”；导入次要、下载文字入口。删除重复去处理与大块项目映射入口。

有内容：对象/共享属性/链接/规则/动作按定义统计，不显示完成百分比，不把零项可选内容当错误。问题取真实校验并精确定位。已发布：各项目显示自己的固定版本，不自动升级。

尚未校验、校验中、失败、通过、过期严格区分。不能 errors=[] 就假定通过，也不能 suggestion=null 就显示已就绪。草稿差异未知时不显示一致。版本读取失败不等于未发布，项目读取失败不等于无项目。

原型顶部场景选择器、固定日期/数量和模拟弹窗不进入正式产品。正式版下载真实模板、复用真实编辑器，不能复制一个只能假保存的说明框当完成功能。导入维持现有四类定义范围。

## 技术与保护

最小组件改造，UI token 复用 frontend/src/style.css。请求使用 app/http + 区域 api。导航使用现有 focus 与项目切换，不能仅改显示文字却未选中对应项目。

接口不够时先更新接口文档、README 变更记录；无变更则不改后端和数据协议。校验规则、保存协调器、不可变发布边界不放松。

不修改用户真实数据和其他执行者代码。自动化测试隔离 WIZ_WORKBENCH_ROOT、端口和 WIZ_DATABASE_URL；不在真实资产上试创建、导入或删除。

## 交付

实施记录追加开发计划.md，包含命令/结果、H01–H12、视觉证据和限制；通过共享脚本记录实际状态；只提交自己负责的文件。回复用户访问地址、改动、验收、未测项、commit。不得宣称原型已完成正式实施。
