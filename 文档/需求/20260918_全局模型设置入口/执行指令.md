# 全局设置中心与用户菜单：执行指令（v2）

请在 `/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2` 实施本需求。

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260918_全局模型设置入口`。

## 阅读顺序

1. 根 AGENTS.md，git status，识别用户/其他harness已有改动。
2. 本目录 `需求说明.md`。
3. **打开并操作 `交互原型_v1.html`，按文件内v2版本，严格遵循左下用户名/齿轮、用户弹出菜单、设置独立分类侧栏、返回编辑现场的交互；不得仍放在本体更多工具下，也不得自行新增第三个业务Tab。**
4. `开发计划.md`。
5. 当前导航、App.vue、LlmProviders.vue、ToolsPage.vue、FlowEditor/NodeConfig。函数编排优化正在实施，按最新代码整合，不用旧文件覆盖。

## 具体任务

1. 底部固定用户区：头像+用户名触发用户菜单，旁边独立齿轮，hover/focus提示“设置”。菜单头部展示身份，操作项固定为“设置”和“退出登录”；齿轮和菜单进入同一全局设置中心。无身份API时显示本地用户，不从数据库连接/系统账号读取用户名。移除更多工具的LLM入口。
2. 设置页整块侧栏替换为“返回工作区 / 全局设置 / 基础设置→模型设置”，隐藏业务Tab与资产选择器；首期只注册模型设置，其他能力实际实现后再增加。入口settings默认到settings-models，llm旧hash和旧内部调用兼容。处理 initialSpace/navigate/watch/lastOntologyView：不能默认归本体或发生回跳。
3. 全局页独立于本体/项目选中与加载结果。页眉写“全局设置 / 模型设置”；不显示业务版本、校验发布、撤销重做、本体/项目Saver状态。模型列表自身四态清晰。
4. 复用已有提供方配置组件/API/稳定ID。用户侧称“模型配置”，表单必填优先、高级超时/温度折叠；API Key编辑留空沿用且不回显，endpoint按现状正常回显。配置保存留在当前页面，结果与连通性区别明确。
5. 各来源前往设置时走现有离开保护；保留当前业务空间和编辑上下文，提供返回原页/编排。恢复节点、页签、画布；不能只跳回hash丢失全部本地状态。
6. 返回编排刷新模型列表，保持providerId不变，不自动选择新建模型，不自动执行；变更后旧检查结论待重检。加载失败不冒充无配置。
7. 旧“LLM配置”设置入口文案同步；Python的LLM推演说明保留。其他技术字段名不做无意义全仓替换。
8. 用户菜单向上弹出、视口避让、外部点击/Esc/Tab离开关闭、焦点与aria同步。已在设置页点击设置不得覆盖最初返回来源。尊重业务侧栏折叠偏好，设置侧栏独立完整显示，返回恢复原样。
9. 核对认证能力：退出登录的启用/禁用、确认、失败反馈、成功转登录页和编辑保护按需求实现。当前没有认证时记录依赖，不宣称完成真实退出。
10. 保存/取消/失败/脏输入离开/删除默认影响等行为按需求落实；连通测试仅用户点击。旧后端默认选择规则不变，不新增跨全局引用扫描。

## 边界

这是入口和设置页面交互优化，不是模型平台或账户系统重建。用户已确认设置/退出登录两项，不加其他操作。退出选项必须存在；有真实登录态则接认证API并走未保存保护，无认证本地模式则禁用并显示未登录原因，不能用伪造session或清空localStorage模拟注销。不新增资料编辑/认证系统；不得清空模型配置、vault或业务数据。禁止增加厂商市场、自动发现模型、项目级覆盖、权限体系、调用计费、提示词管理；禁止迁移vault、改执行器或改本体模型协议。

真实 ontology 与vault不要读密钥、手改、删、提交。测试使用临时WIZ_WORKBENCH_ROOT、独立端口、模拟模型API；不得向真实模型发起测试请求。API仍走 app/http，不绕过安全和离开保护。

原型仅本地模拟：示例模型/地址/连通结果不能搬入产品或作为默认配置。正式版用现有请求、组件、路由和保存机制，无需引入新UI框架或路由库。

## 验收与交付

执行需求G01–G16及开发计划的定向测试，完成 npm run build 与隔离浏览器验收。不要只检查按钮位置；必须覆盖“本体加载失败仍能进入”与“返回编排恢复现场”。

将实际修改、最终导航/返回协议、测试记录、未验证事项追加本目录开发计划。按AGENTS提交git，仅包含本次文件。最终回复提供完成项、测试结果、剩余事项、commit与页面地址。不得把未验证项写成通过。
