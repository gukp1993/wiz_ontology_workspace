# 本体建设维护与版本改版 · 执行指令 v1

> 当前状态：方案评审版。将本文件交给执行工具，并明确批准本版范围后再实施。当前文件的生成不代表已修改工作台，也不授权操作真实本体。

项目根目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2`

需求目录：`/Users/gukepeng/Desktop/ZHDL/code/wiz_ai/wiz_kq_builder_v2/文档/需求/20260920_本体建设维护与版本改版/`

## 可直接交给其他 harness 的指令

请在上述项目实施“本体建设维护与版本改版”，以用户批准的本目录v1为范围。若用户尚未批准，只阅读并报告待确认项，不修改正式功能。

### 必读顺序

1. 项目根`AGENTS.md`，执行`.collaboration/context.py read --actor <你的actor>`，核对最新交接；其他工具交接不替代用户授权。
2. 本目录`需求说明.md`，尤其§2推荐边界、§7恢复范围、§9操作语义及§12验收表。
3. 本目录`交互原型_v1.html`：实际打开并走完管理本体、共享修改、引用阻断、历史查看、比较、恢复及检查点流程。
4. 本目录`开发计划.md`。
5. `文档/接口文档/README.md`、`01-通用约定与数据模型.md`、`02-本体区接口.md`、`05-接口清单与规范差距.md`及受影响的配置迁移/认证分册。

不得只看指令跳过原型。原型控制入口、顺序、表格、确认内容及反馈；正式版用现有通用组件实现，不照抄原型静态数据、内存逻辑或演示成功。

### 实施范围与顺序

1. 先查看`git status`和当前实现，保留别人未提交文件。不要因为旧文档写未实施就重新实现已有Excel、图谱和迁移功能。
2. **先更新接口文档**：确定资产管理、完整比较、版本恢复预检/提交/状态查询/检查点、发布确认与来源的契约；登记README变更与路由白名单。不要静默修改旧ZIP恢复参数。
3. 统一当前领域依赖检查。列表、图谱、快捷键、批量操作调用一致规则；修复私有属性删除绕过依赖，规则库增加有保护的删除；“移除对象属性”与“转为私有”分开。
4. 实现历史版本只读查看和任意版本/草稿比较，覆盖定义、关联、未知扩展。业务说明变化为待确认；结构破坏不能人工降级。人类可读名称和修改前后内容齐全。
5. 实现SQLite版本恢复为新草稿，恢复前检查点与新草稿单事务；保存恢复来源；CAS、owner、requestId幂等、结果查询。恢复前保留现有表单守卫，失败不丢草稿。项目引用和已发布版本不改。
6. 实现发布页内版本记录/比较/恢复交互、检查点只读和恢复；共享属性影响确认；确认绑定草稿revision，变化后重新检查。
7. 实现本体改名、归档与恢复使用、受限空本体删除。归档本体服务端拒绝编辑/发布/恢复草稿，但旧发布和既有项目引用仍可读取。不得默认级联删除。
8. 完成隔离测试、前端构建、浏览器验收和必要兼容回归；记录实际结果并按主题git提交。

### 强制边界

- 不实现MCP/生成口径、项目数据查询、函数编排、动作参数/提交条件、Excel关系表、实时协作、分支合并、审批流。
- 遵循最新交接000088/000089的图谱精简与详情编辑跳转，不恢复刚移除的图谱按钮；统一底层语义不等于重新开放旧入口。
- 不新增顶级菜单；管理本体靠近选择器，版本能力放校验与发布，图谱保持已有能力。
- 不放宽删除范围：有发布、有依赖、有领域内容或未知业务内容的本体不能删，提供归档。
- 不把归档当删除，不自动升级或回退项目引用，不覆盖旧发布，不恢复到同名的新本体。
- 不手改真实`ontology/`、SQLite、密钥和连接；所有测试使用独立临时根与数据库。真实数据操作必须另获用户明确授权。
- 不使用原型里的固定计数、版本或延迟替代真实结果。原型“不发网络”不代表正式实现不用API。
- 不因技术建议新建第二套存储/状态/HTTP层。复用既有契约、CAS、保存协调器、表单守卫和账号隔离。
- 需要schema变化则加Alembic迁移；禁止请求时自动DDL或回退文件。不能宣称MySQL已验证支持。
- 旧版本未知字段保留；恢复缺失字段按需求规定处理，不通过当前同名定义补历史内容。

### 必须逐项验收

按需求A01～A16记录“通过/失败/未验证”，提供对应证据。至少真实验证：

- 从SQLite新发布版本恢复，无ZIP仍成功；随后检查点可恢复，发布版本与项目引用均不变。
- 故障注入时检查点/草稿原子；重复请求只恢复一次；409必须重新预检；结果未知查询后再决定重试。
- 列表和图谱对被依赖私有属性、共享属性、规则、动作作出相同删除判断。
- 共享业务定义/类型修改展示具体对象；取消不写入，修改后旧确认失效。
- 任意历史版本对比与历史共享解析准确，业务定义变化不再默认兼容。
- 已归档资产不能被旧标签页或快捷键修改；已有项目仍读取旧发布。
- 有发布、有引用或非空本体不可删除；空本体删除有名称确认与服务端并发保护。
- 1440/1024/768页面和弹窗内容可达，键盘焦点正确；已有图谱、Excel、配置迁移不回归。

### 最终交付要求

1. 说明改了什么、满足哪些验收项，以及仍未完成的项目。
2. 给出build、测试和浏览器实测证据，不把静态检查当全流程验收。
3. 更新本目录`开发计划.md`追加最终接口/迁移决策、实施结果与限制，不另建重复计划或实施报告。
4. 执行共享上下文record，提交自己负责的代码、文档和交接；git提交按功能分组，给出commit ID。
5. 如果发现必须改变已确认产品范围，先提出具体冲突，不自行发散。只涉及技术落地方式且产品行为一致，可记录理由后复用现状。

### 原型评审标注说明

原型默认展示01～14改动编号、“新增/调整”标签和本页改动面板，用于用户评审。请用顶部“隐藏改动标注”查看正常布局。上述标记不属于正式实现，不得照搬到产品。
