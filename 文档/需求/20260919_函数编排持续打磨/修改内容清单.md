# 函数编排持续打磨 · 修改内容清单

日期：2026-09-19 晚 — 09-20 晨。范围：函数编排前后端（`frontend/src/flow/`、`workbench/flows.py` 等）。
过程：三路代码审查 + 两轮独立验收代理交替；命名轮 R01–R20；审查发现 45 条、修复 38 条、不采纳 1 条。
配套：[打磨日志.md](打磨日志.md)（逐轮记录与证据）。所有改动均通过 build（vue-tsc）与全量回归（run.py all 33/33），并经两轮子代理独立验收通过。

## 1. 提交总览（13 个）

| 提交 | 类型 | 内容 |
| --- | --- | --- |
| `38806d5` | fix | 列表状态区分问题/提示并按更新时间排序；空编排给指引提示；运行项目标签折行修复 |
| `8e5b7d2` | fix | 编排列表配置检查携带 LLM 提供方上下文（Python 编排不再假「检查通过」） |
| `75bc160` | fix | 后端批次 A：链测试环拦截、重复节点 ID、Redis 缺命令、凭据冲突、故障如实上报 |
| `4341a7a` | fix | 测试视图批次 B：可选输入、忙闸释放、范围回退、空文本限定、错误即时清除等 8 项 |
| `1e6aecc` | fix | 节点配置批次 C：改名引用全类型提示、公式键随迁、换行独立、定位高亮全覆盖 |
| `5c3235b` | fix | 连接空态区分未选项目/项目无连接；LLM 配置补真实示例占位 |
| `4799bcb` | fix | 问题定位高亮按参数限定（field+parameterId） |
| `b7bc31b` | refactor | NodeConfig 遗留节点缺 implementation/execution 改显式 effect 初始化 |
| `581b80c` | perf | 列表 LLM 元数据每账号读一次（提出循环外） |
| `c039746` | fix | 二轮审查修复：画布白屏 P1、类型残留、LLM 异常路径等 11 项 |
| `f0c76a9` | fix | LLM JSON 候选块解析失败回退后续块 |
| `a5401a2` | docs | 打磨日志（R01–R20 全记录） |
| `5353652` | docs | 交接与两轮验收记录（000062–000064） |

## 2. 后端修改明细（workbench/）

| # | 严重度 | 问题 | 修改 | 文件 | 提交 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | P2 | 链测试 422 门只看节点级错误，成环（FLOW_CYCLE 为流程级）漏拦，退化成通用 400 | 门改为「流程级错误 OR 被测节点错误」都拦截 | flow_routes.py | `75bc160` | 隔离实例 API：成环 flow-run 返回 422；范围外节点错误不拦片段（200） |
| B2 | P2 | 节点稳定 ID 重复无校验，执行期静默互相覆盖 | check 报 `NODE_ID_DUPLICATE` | flows.py | `75bc160` | API 实证 flow-check 报出；test_flows 新用例 |
| B3 | P2 | Redis 节点缺 command 时 check 通过、执行器静默按 GET 执行 | 缺命令报 `REDIS_COMMAND_MISSING` | flows.py | `75bc160` | API 实证；test_flows 新用例 |
| B4 | P2 | Redis args 标识符不对照已声明输入，未声明名被当字面量发给 Redis | 标识符须 ∈ 已声明输入名，否则 `REDIS_ARGS_NAME_UNDECLARED` | flows.py | `75bc160` | test_flows 新用例（正/反两向） |
| B5 | P2 | HTTP 节点同时选凭据又自填 Authorization 头时，凭据被静默忽略 | check 报 `HTTP_AUTH_CONFLICT`；执行器兜底凭据优先并记日志 | flow_http.py、flow_executor.py | `75bc160` | test_flows 新用例 |
| B6 | P3 | 密钥库/凭据存储故障被裸 except 伪装成「未保存密码/凭据不存在」 | 存储故障如实转 NodeFailure（缺失仍按原语义返回空） | flow_executor.py | `75bc160` | 既有 executor 回归 33 项全过 |
| B7 | P3 | Redis 客户端从不 close，长驻服务 fd 滞留 | 执行完 try/finally close | flow_executor.py | `75bc160` | executor 回归 |
| B8 | P2 | LLM 响应为合法 JSON 但非对象（数组/字符串）时 `data.get` 抛 AttributeError 裸 500 | 解析后判 isinstance，转为「响应格式不符合」可读错误 | llm_client.py | `c039746` | 二轮验收代理审查通过 |
| B9 | P2 | except 元组漏 `http.client.HTTPException`（读中断类异常裸逃逸） | 并入捕获元组 | llm_client.py | `c039746` | 同上 |
| B10 | P3 | 响应体超 1MB 被静默截断后误报「无法解析」 | 达上限明确报「响应过大」 | llm_client.py | `c039746` | 验收确认恰好 1MB 不误判 |
| B11 | P3 | JSON 平衡块提取不跳过引号内字符，值含不成对括号截错块 | 提取改引号感知扫描 | llm_client.py | `c039746` | 直测 `{"a": "}"}` 等用例 |
| B12 | P3 | 首个候选块解析失败后不回退后续块 | 从块尾继续找下一候选块，再换括号类型 | llm_client.py | `f0c76a9` | 直测回退用例 4 例 |
| B13 | P2 | endpoint 不拒换行/控制字符，保存后调用必失败且难排查 | endpoint 含控制字符拒绝保存 | llm_providers.py | `c039746` | 隔离实例 API：带 \n/\t 均 400 |
| B14 | P3 | 列表逐编排读一次 LLM 元数据（N 次 DB 读） | 提到循环外每账号一次 | flows.py | `581b80c` | test_flows 94 项全过 |
| B15 | P2 | 列表 check 不感知 LLM 配置，未配模型时 Python 编排假「检查通过」 | listing 携带 llm_meta | flows.py | `8e5b7d2` | test_flows 新用例；浏览器实测「1 个问题」 |
| B16 | P2 | 列表 check_flow 把 warning 也置 pending 但只数 error，「待完善」无计数 | listing 输出 warningCount；按更新时间降序 | flows.py | `38806d5` | test_flows 新用例；浏览器四态复核 |
| B17 | P3 | 空编排（零处理节点）显示「检查通过」 | check 给 `FLOW_EMPTY` 指引 warning | flows.py | `38806d5` | test_flows 新用例；浏览器复核「1 项提示」 |

## 3. 前端修改明细（frontend/src/）

| # | 严重度 | 问题 | 修改 | 文件 | 提交 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | P1 | 测试视图「可选」（unbound）布尔/对象/列表输入留空被必填校验阻断，被迫填值歪曲测试语义 | run() 在类型分派前跳过未提供的可选输入（任何类型） | FlowTestWorkspace.vue | `4341a7a` | 组件测试 A15a（留空可运行/填 true 按布尔提交） |
| F2 | P1 | 画布遇悬挂输出绑定（导入/旧草稿）时 cy.add 缺端点抛错且画布已清空 → 整块白屏；原坏边过滤器是死代码 | 坏边在 edgeElements 内按节点存在性过滤；删除死过滤器 | FlowCanvas.vue | `c039746` | 二轮验收审查通过（nodeIds 与渲染节点集闭合） |
| F3 | P2 | 运行中切项目，在途请求不释放 running，按钮最长禁用 300s | 切上下文 watch 立即释放；孤儿 finally 加代次守卫 | FlowTestWorkspace.vue | `4341a7a` | 组件测试 A15b |
| F4 | P2 | 被测节点被删除后范围残留，报错显示裸 UUID | 失效范围回退整条并提示 | FlowTestWorkspace.vue | `4341a7a` | 组件测试 A15e |
| F5 | P2 | 重命名弹窗「取消/点遮罩」绕过脏确认（与 Esc 行为矛盾） | 统一走 closeDialog（脏确认+焦点恢复） | FlowEditor.vue | `4341a7a` | 浏览器实测：取消→脏确认→放弃关闭 |
| F6 | P2 | renameTechnical 对 http/redis 完全跳过引用检查，URL/Key 模板占位改名后静默失效 | 五类节点统一按实现体检查并给出对应提示 | NodeConfig.vue | `1e6aecc` | build+回归；审查复核 |
| F7 | P3 | calc 输出改名后公式键残留旧名（孤儿公式） | 改名随迁公式键（撞名保护） | NodeConfig.vue | `1e6aecc`/`c039746` | 验收复核（撤销一步可还原） |
| F8 | P3 | 自动换行状态五个编辑框共享，部分框无法还原 | 按编辑器独立（python/sql/http体/redis key），redis/http 补开关 | NodeConfig.vue | `1e6aecc` | build+回归 |
| F9 | P2 | 问题定位「去处理」对 sql/url/connectionId 等字段无滚动无高亮（flash 只有技术名/来源绑定有落点） | flash 高亮覆盖全部实现字段 | NodeConfig.vue | `1e6aecc` | build+回归 |
| F10 | P3 | 输入/输出区同名「技术名」同时闪烁 | flash 按 field+parameterId 复合限定 | NodeConfig.vue | `4799bcb` | 验收确认前后端稳定 ID 同源 |
| F11 | P3 | 多标量输出时整份 outputs JSON 每个标量重复渲染一遍 | 移到循环外渲染一次 | FlowTestWorkspace.vue | `4341a7a` | 组件回归 |
| F12 | P3 | 表单错误常驻，修正输入后不消失 | 输入/范围变化即清除（sync watcher 防竞态） | FlowTestWorkspace.vue | `4341a7a` | 组件测试 A15d |
| F13 | P3 | 数值勾「使用空文本」提交空串，到后端才被驳回 | 非文本类型即时报错并隐藏勾选 | FlowTestWorkspace.vue | `4341a7a` | 组件测试 A15c |
| F14 | P3 | 「旧项目结果」徽标不可达（切项目即清结果，死分支） | 移除死分支，语义与行为一致 | FlowTestWorkspace.vue | `4341a7a` | 组件回归 |
| F15 | P2 | 画布拓扑签名含 classes：勾选测试集/运行状态翻转即全量销毁重建（打断拖拽） | 签名只含 id/type；动态类走 refreshData 轻量更新 | FlowCanvas.vue | `c039746` | 二轮验收确认四动态类全覆盖 |
| F16 | P2 | TypeEditor 类型切换按当前类型清理，object↔list 残留矛盾键（fields/elementType 并存） | 按目标类型清理；同类型重选不丢已配结构 | TypeEditor.vue | `c039746` | 验收确认 list→list 保留 elementType |
| F17 | P3 | 无入口参数时选「编排输入」生成空 inputId 绑定（静默断链） | 来源置空等待先声明入口参数 | BindingEditor.vue | `c039746` | build+回归 |
| F18 | P3 | 画布 notice 定时器卸载未清理 | unmount clearTimeout | FlowCanvas.vue | `c039746` | build |
| F19 | P3 | NodeConfig 在 computed getter 里写 props（渲染期副作用，遗留节点有 TypeError 风险） | watchEffect 显式初始化 + 只读 computed | NodeConfig.vue | `b7bc31b` | 验收确认 setup 期收敛无循环 |
| F20 | P3 | SQL/Redis 连接空态只有「项目还没有连接」一种文案（连接状态恒 ready，loading/failed 为死分支） | has-project 区分「未选项目」与「项目无连接」 | NodeConfig.vue、FlowEditor.vue | `5c3235b` | build+回归 |
| F21 | P3 | 重命名撤销快照标签为通用「编辑编排」 | 补 actionLabel | NodeConfig.vue | `c039746` | 验收复核 |
| F22 | P3 | LLM 配置弹窗缺真实示例（用户曾不知道填什么） | 接口地址/模型标识补 MiniMax、DeepSeek 示例 | LlmProviders.vue | `5c3235b` | 浏览器确认占位生效 |
| F23 | P3 | 编辑器页头「运行项目」窄屏折行成「运行项/目」 | white-space:nowrap | FlowEditor.vue | `38806d5` | 浏览器复核 |
| F24 | P3 | 列表「待完善」不显示计数 | 显示「·N 个问题」或「·N 项提示」 | FlowList.vue | `38806d5` | 浏览器四态复核 |

## 4. 测试与回归增量

| 项 | 之前 | 之后 | 说明 |
| --- | --- | --- | --- |
| tests/test_flows.py | 85 项 | 94 项 | 空编排提示、warningCount、排序、重复 ID、Redis 缺命令/args、认证冲突、列表 LLM 感知 |
| tests/flow_test_workspace.test.mjs | 21 项 | 36 项 | A15a–e：可选输入、忙闸释放、空文本限定、错误清除、删节点回退 |
| tests/flow_model.test.mjs | 29 项级断言 | 46 项 | 随镜像行为扩展 |
| 最终全量回归 | — | run.py all 33/33 全过 | quick/http/unit 全组；build（vue-tsc）通过 |

## 5. 审查发现处置统计（45 条）

| 去向 | 数量 | 说明 |
| --- | --- | --- |
| 已修复 | 38 | 含 P1×2（画布白屏、可选输入阻断）、P2×13，其余 P3 |
| 不采纳 | 1 | LLM 内网地址黑名单：工作台仅绑定 127.0.0.1，且会禁掉 Ollama 等本地模型合法场景；已堵控制字符注入面，留多租户阶段再评估 |
| 确认非缺陷/重复 | 6 | 审查代理误报或与既有行为重合，逐条核实后关闭 |

## 6. 验收与未验证事项

- **两轮独立验收代理全部通过**：第一轮 6 提交（回归全绿 + 隔离实例 API 实证 NODE_ID_DUPLICATE / REDIS_COMMAND_MISSING / 成环 422 / 认证冲突）；第二轮 4 提交（重点 diff 审查 + endpoint 控制字符 400 实证）。记录见 `.collaboration/entries/000062/000063`。
- 浏览器实测（隔离实例）：列表四态、重命名脏守卫、画布加删节点、SQL 模板呈现、单节点测试错误路径、LLM 弹窗占位、HTTP/SQL 编排真实执行与失败传播。
- **未做**（边界外，非本轮缺陷）：属性取值预览支持 flow 来源（属取值执行能力）；SSRF 黑名单（见不采纳）；`mapping_forms` 既有基线失败（他人负责）。

## 附录：同日更早的两个相关提交（打磨轮之前）

| 提交 | 内容 |
| --- | --- |
| `a29aa2a` | 编排列表操作平铺（去掉「更多」下拉）、列宽固定布局、说明省略号+title |
| `1854e5d` | 函数编排列表输出可绑定时间序列属性（result:{valueField,timestampField} 字段映射，前后端+契约+测试） |
