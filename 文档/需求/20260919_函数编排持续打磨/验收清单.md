# 函数编排打磨 · 验收清单（按功能维度）

日期：2026-09-20。验收环境：http://127.0.0.1:18765 （已重启为最新代码）。
用法：逐项按「验收操作」执行，对照「预期结果」勾选；每项标注了对应提交，可回溯 [修改内容清单.md](修改内容清单.md)。
标注 ⚠ 的项依赖本机不具备的外部环境（MySQL/Redis 驱动），只能验收错误提示与配置面，不能验收真实执行。

## A. 编排列表页（函数编排菜单）

| 编号 | 功能点 | 验收操作 | 预期结果 | 提交 |
| --- | --- | --- | --- | --- |
| A1 | 列表排序 | 编辑任意一个编排后返回列表 | 最近编辑的编排排在第一行（更新时间降序） | `38806d5` |
| A2 | 配置状态计数 | 观察列表状态列 | 「检查通过」/「待完善 · N 个问题」/「待完善 · N 项提示」三种；不再出现无计数的「待完善」 | `38806d5` |
| A3 | 空编排提示 | 新建一个空编排（只填名称）不添加节点 | 列表状态为「待完善 · 1 项提示」，不再是「检查通过」 | `38806d5` |
| A4 | Python 编排未配模型 | 查看含 Python 节点的编排（未配置模型时） | 状态「待完善 · 1 个问题」，问题为「尚未配置 LLM 提供方」 | `8e5b7d2` |
| A5 | 操作平铺 | 观察操作列 | 编辑/复制/删除三按钮平铺，删除为红色；点复制列表即时多一行；删除有确认弹窗 | `a29aa2a` |
| A6 | 说明截断 | 找一条长说明编排 | 说明单行省略号，鼠标悬停 title 显示全文；名称列不挤压其他列 | `a29aa2a` |

## B. 编排编辑器 · 设计视图

| 编号 | 功能点 | 验收操作 | 预期结果 | 提交 |
| --- | --- | --- | --- | --- |
| B1 | 页头布局 | 打开任意编排编辑器 | 「运行项目」标签单行不折行 | `38806d5` |
| B2 | 重命名脏守卫 | 点名称旁 ✎ → 改内容 → 点「取消」（或点遮罩/Esc） | 弹「弹窗中有未提交的修改」确认；放弃后关闭且名称未变；未修改时取消直接关闭 | `4341a7a` |
| B3 | 连接空态区分 | 不选运行项目时打开 SQL 或 Redis 节点配置 | 提示「尚未选择运行项目：SQL/Redis 数据连接来自项目…」；选了项目但无连接时才提示「当前项目还没有…」 | `5c3235b` |
| B4 | 添加/删除节点 | 「＋ 添加节点」选类型 → 再删除该节点 | 五类节点（SQL/计算/Python/Redis/HTTP）齐全；添加/删除画布即时同步，无白屏 | `c039746` |
| B5 | 画布轻量更新 | 勾选测试复选框 / 点击节点选中时 | 画布不整体重绘（已拖开的节点位置保持、不打断拖拽） | `c039746` |
| B6 | 改名引用提示 | 把 SQL 节点某输入技术名改掉（SQL 里仍在用旧名） | 出现「实现仍引用旧名；请检查 SQL :参数」提示；HTTP/Redis 节点改名同样有对应提示 | `1e6aecc` |
| B7 | calc 公式随迁 | 计算节点输出改名 | 公式保留（键跟随新名），不丢公式 | `1e6aecc` |
| B8 | 问题定位高亮 | 制造一个 SQL/连接类配置问题 → 检查配置 → 点问题「去处理」 | 滚动到对应区块且该字段高亮；连续点同一问题也有反馈；输入/输出同名技术名只亮对应一个 | `1e6aecc`/`4799bcb` |
| B9 | 类型编辑器 | 类型编辑器里 object↔list 来回切换后保存重开 | 无残留矛盾键，正常打开；list→list 重选不丢已配元素类型 | `c039746` |
| B10 | 悬挂引用不白屏 | （如有旧草稿/导入的编排，输出绑定的节点已被删）打开画布 | 正常渲染，缺端点的边不画，配置检查里可定位问题；不出现整块白屏 | `c039746` |

## C. 测试视图（页头「▷ 测试编排」）

| 编号 | 功能点 | 验收操作 | 预期结果 | 提交 |
| --- | --- | --- | --- | --- |
| C1 | 可选输入放行 | 对含未绑定来源输入（布尔或 JSON 类型）的节点做单节点测试，该输入留空 | 可直接运行，不再被「请选择是/否」「请填写 JSON」阻断；填了值仍按类型校验 | `4341a7a` |
| C2 | 空文本限定 | 文本入口勾「使用空文本」运行 | 提交空字符串有效；数值/日期时间输入不再显示该勾选，强填报「该类型不支持提交空文本」 | `4341a7a` |
| C3 | 忙闸释放 | 运行中（请求未回）切到别的编排或项目再回测试视图 | 运行按钮立即恢复可用；旧请求的晚回包不会写到新上下文结果里 | `4341a7a` |
| C4 | 删节点范围回退 | 单节点测试某节点 → 返回设计删除该节点 → 再进测试视图 | 范围自动回退为整条编排并提示；不出现「被测节点不存在：<ID>」裸 ID 报错 | `4341a7a` |
| C5 | 错误即时清除 | 制造一个校验错误（如必填入口留空点运行）→ 再填上值 | 错误提示随输入修正立即消失，不用再点运行 | `4341a7a` |
| C6 | 标量 JSON 去重 | 运行一个有 2 个以上标量输出的节点 | 数值突出显示每个输出各一份，原始 JSON 只显示一份 | `4341a7a` |
| C7 | 错误路径可读 | ⚠ 对 SQL 节点做单节点测试（本机无 MySQL/项目上下文） | 失败原因可读（如「SQL节点需要数据连接与所属项目上下文」），失败沿范围传递的下游标「未执行」 | `75bc160` |
| C8 | 片段执行回归 | SOC 链做整条/片段测试（如输入 8） | 逐节点输出正确（8→16→19→57→28.5→2850），范围外节点不执行 | 既有能力回归 |

## D. LLM 配置（设置 → 模型设置）

| 编号 | 功能点 | 验收操作 | 预期结果 | 提交 |
| --- | --- | --- | --- | --- |
| D1 | 示例占位 | 点「新增模型配置」 | 接口地址占位含 MiniMax/DeepSeek 完整 URL 示例；模型标识含 MiniMax-M2 示例 | `5c3235b` |
| D2 | endpoint 校验 | 尝试在接口地址里粘贴带换行/控制字符的内容保存 | 保存被拒：「接口地址不能包含换行或控制字符」 | `c039746` |
| D3 | 密钥回显红线 | 编辑已有配置 | 接口地址回显、API Key 不回显（留空沿用）；列表不出现密钥 | 既有行为复核 |

## E. 项目映射 · 函数编排来源（对象映射 → 属性取值）

| 编号 | 功能点 | 验收操作 | 预期结果 | 提交 |
| --- | --- | --- | --- | --- |
| E1 | 列表输出绑时间序列 | 时间序列属性（如 采样soc）选「函数编排」→ 选「采样值取值规则」 | 取值输出里「采样序列 · 列表」**可选中**；出现「取值字段/时间字段」两个下拉（采样值·数值 / 时间戳·日期时间） | `1854e5d` |
| E2 | 保护规则不变 | 标量属性尝试绑列表输出；任意属性选对象输出 | 仍被拒绝（选项禁用/校验阻断），不放开 | `1854e5d` |
| E3 | 摘要与状态 | 保存绑定后看属性清单 | 摘要形如「函数编排 · 采样值取值规则 · 采样序列 · 采样值→值 · 时间戳→时间」，状态「已配置」 | `1854e5d` |

## F. 校验与稳定性（保存/检查时触发，无界面路径的走 API）

| 编号 | 功能点 | 验收操作 | 预期结果 | 提交 |
| --- | --- | --- | --- | --- |
| F1 | 重复节点 ID | ⚠ API 提交两节点同 ID 的草稿后检查 | 报「节点 ID 重复：执行时会互相覆盖」 | `75bc160` |
| F2 | Redis 缺命令 | Redis 节点只选连接不选命令，检查配置 | 报「请选择 Redis 命令」，不再静默按 GET 执行 | `75bc160` |
| F3 | 成环链测试 | ⚠ API 对成环两节点发 flow-run targets | 返回 422「被测节点配置有误：存在循环依赖」，而非 400/500 | `75bc160` |
| F4 | 认证冲突 | HTTP 节点同时选凭据又自填 Authorization 头 | 检查报「两者冲突，凭据会被忽略」 | `75bc160` |
| F5 | 列表性能 | 打开编排列表 | 无可感知变慢（LLM 元数据每账号仅读一次） | `581b80c` |

## 验收说明

- ⚠ 项依赖外部驱动（本机未装 PyMySQL/redis 或未选项目），只验收配置面与错误提示；真实 SQL/Redis 执行需在连有真实数据源的环境验证。
- F1/F3 无界面入口（正常操作画不出重复 ID/成环），已由两轮独立验收代理在隔离实例 API 实证，可抽查 `git show 75bc160` 与验收记录 `.collaboration/entries/000062`。
- 全部改动均通过：build（vue-tsc）、run.py all 33/33、test_flows 94、flow_test_workspace 36、flow_model 46 项。
